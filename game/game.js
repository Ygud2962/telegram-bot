window.__GAME_JS_LOADED = true;
// РџРµСЂРµРјРµРЅРЅС‹Рµ СЃРёРЅС…СЂРѕРЅРёР·Р°С†РёРё (РѕР±СЉСЏРІР»РµРЅС‹ РіР»РѕР±Р°Р»СЊРЅРѕ С‡С‚РѕР±С‹ РёР·Р±РµР¶Р°С‚СЊ ReferenceError)
let _lastSyncedScore = -1;
let _lastSyncedCompleted = -1;
let _lastSyncedSignature = '';
let _serverResetToken = 0;

const SECRET_MODE_META = Object.freeze({
  none:    { title: 'РћР±С‹С‡РЅС‹Р№ СЂРµР¶РёРј', bonus_pct: 0,  short: 'Р±РµР· Р±РѕРЅСѓСЃР°' },
  silent:  { title: 'РўРёС…РёР№ С€РёС„СЂ', bonus_pct: 20, short: '1 Р·Р°РґР°РЅРёРµ Р±РµР· РїРѕРґСЃРєР°Р·РєРё' },
  speed:   { title: 'РЎРєРѕСЂРѕСЃС‚РЅРѕР№ СЂР°РґРёСЃС‚', bonus_pct: 12, short: '3 Р±С‹СЃС‚СЂС‹С… РѕС‚РІРµС‚Р° РїРѕРґСЂСЏРґ' },
  iron:    { title: 'Р–РµР»РµР·РЅР°СЏ РІРѕР»СЏ', bonus_pct: 16, short: 'РѕС‚РІРµС‚ СЃ 1 Р¶РёР·РЅСЊСЋ' },
  recruit: { title: 'Р’РµСЂР±РѕРІС‰РёРє', bonus_pct: 10, short: 'РїСЂРёРіР»Р°СЃРё 1 Р°РіРµРЅС‚Р°' },
  night:   { title: 'РќРѕС‡РЅРѕР№ РґРѕР·РѕСЂ', bonus_pct: 14, short: 'РѕС‚РІРµС‚ РІ СЂРµРґРєРѕРµ РІСЂРµРјСЏ' },
});

const SECRET_MODE_ORDER = ['none', 'silent', 'speed', 'iron', 'recruit', 'night'];
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р”РђРќРќР«Р• РР“Р Р« вЂ” 20 РЁРР¤Р РћР’, 4 Р“Р›РђР’Р«
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
const CHAPTERS = [
  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  //  Р“Р›РђР’Рђ I вЂ” РҐРћР™РќРРљР: РћР“РќР•РќРќРћР• Р›Р•РўРћ
  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  {
    id:1, title:"РҐРћР™РќРРљР: РћР“РќР•РќРќРћР• Р›Р•РўРћ", subtitle:"Р“Р»Р°РІР° I",
    place:"РҐРћР™РќРРљР В· Р“РћРњР•Р›Р¬РЎРљРђРЇ РћР‘Р›. В· 1941", stamp:"рџЊї",
    meta:"Р”Р°С‚Р°: 28.08.1941\\nРњРµСЃС‚Рѕ: Рі. РҐРѕР№РЅРёРєРё, РџРѕР»РµСЃСЊРµ\\nРЎС‚Р°С‚СѓСЃ: РћРљРљРЈРџРђР¦РРЇ",
    briefing:`РђРІРіСѓСЃС‚ 1941. РҐРѕР№РЅРёРєРё вЂ” РЅРµР±РѕР»СЊС€РѕР№ РіРѕСЂРѕРґ РЅР° СЋРіРµ Р“РѕРјРµР»СЊСЃРєРѕР№ РѕР±Р»Р°СЃС‚Рё, РІ СЃРµСЂРґС†Рµ РџРѕР»РµСЃСЊСЏ. РќРµРјРµС†РєРёРµ РІРѕР№СЃРєР° Р·Р°РЅСЏР»Рё РіРѕСЂРѕРґ. РњРµСЃС‚РЅС‹Рµ Р¶РёС‚РµР»Рё СѓС…РѕРґСЏС‚ РІ Р»РµСЃР° вЂ” РЅР°С‡РёРЅР°РµС‚СЃСЏ РїР°СЂС‚РёР·Р°РЅСЃРєРѕРµ РґРІРёР¶РµРЅРёРµ. РџРµСЂРІС‹Р№ СЃРІСЏР·РЅРѕР№ РѕС‚СЂСЏРґР° РїРµСЂРµРґР°С‘С‚ РєРѕРѕСЂРґРёРЅР°С‚С‹ СЏРІРєРё. РўС‹ вЂ” РµРіРѕ РїСЂРµРµРјРЅРёРє. Р Р°СЃС€РёС„СЂСѓР№ РґРѕРЅРµСЃРµРЅРёСЏ.`,
    mission:"Р’С‹РїРѕР»РЅРё 6 СЂР°Р·РІРµРґС‹РІР°С‚РµР»СЊРЅС‹С… Р·Р°РґР°РЅРёР№ РїРѕ РҐРѕР№РЅРёРєСЃРєРѕРјСѓ СЂРµРіРёРѕРЅСѓ",
    videoUrl:"https://www.youtube.com/embed/8mFDTm5OQCI",
    ciphers:[
      { type:"quiz", typeLabel:"РРЎРўРћР РРЇ РљР РђРЇ",
        task:"РҐРѕР№РЅРёРєРё вЂ” СЂР°Р№РѕРЅРЅС‹Р№ С†РµРЅС‚СЂ РѕРґРЅРѕР№ РёР· РѕР±Р»Р°СЃС‚РµР№ Р‘РµР»Р°СЂСѓСЃРё. РћРїСЂРµРґРµР»Рё РїРѕ РєР°СЂС‚Рµ: РІ РєР°РєРѕР№ РѕР±Р»Р°СЃС‚Рё РЅР°С…РѕРґРёС‚СЃСЏ РҐРѕР№РЅРёРєСЃРєРёР№ СЂР°Р№РѕРЅ?",
        options:["Р‘СЂРµСЃС‚СЃРєР°СЏ РѕР±Р»Р°СЃС‚СЊ","Р“РѕРјРµР»СЊСЃРєР°СЏ РѕР±Р»Р°СЃС‚СЊ","Р’РёС‚РµР±СЃРєР°СЏ РѕР±Р»Р°СЃС‚СЊ","РњРѕРіРёР»С‘РІСЃРєР°СЏ РѕР±Р»Р°СЃС‚СЊ"],
        correctIndex:1,
        encrypted:"рџ—є",
        answer:"a2504b40b4c895667b4364476daee719377afbab51bbd142e0b5a5f538b58875",
        hint:"РџРѕР»РµСЃСЊРµ вЂ” РёСЃС‚РѕСЂРёС‡РµСЃРєРёР№ СЂРµРіРёРѕРЅ РЅР° СЋРіРµ Р‘РµР»Р°СЂСѓСЃРё, РіСЂР°РЅРёС‡РёС‚ СЃ РЈРєСЂР°РёРЅРѕР№.",
        fact:"РҐРѕР№РЅРёРєСЃРєРёР№ СЂР°Р№РѕРЅ СЂР°СЃРїРѕР»РѕР¶РµРЅ РІ Р“РѕРјРµР»СЊСЃРєРѕР№ РѕР±Р»Р°СЃС‚Рё РІ Р·РѕРЅРµ РџРѕР»РµСЃСЊСЏ. РџРѕСЃР»Рµ Р°РІР°СЂРёРё РЅР° Р§РђР­РЎ РІ 1986 РіРѕРґСѓ С‡Р°СЃС‚СЊ СЂР°Р№РѕРЅР° РІРѕС€Р»Р° РІ Р·РѕРЅСѓ РѕС‚С‡СѓР¶РґРµРЅРёСЏ.", points:100 },
      { type:"caesar", typeLabel:"РЁРР¤Р  Р¦Р•Р—РђР РЇ",
        task:"РЎРІСЏР·РЅРѕР№ РїРµСЂРµРґР°Р» Р·Р°С€РёС„СЂРѕРІР°РЅРЅС‹Р№ РѕС‚С‡С‘С‚ РёР· РҐРѕР№РЅРёРє. РЎРґРІРёРі 3 РїРѕР·РёС†РёРё. Р Р°СЃС€РёС„СЂСѓР№ С‚СЂС‘С…Р±СѓРєРІРµРЅРЅРѕРµ РЅР°Р·РІР°РЅРёРµ СЂРµРєРё РІРґРѕР»СЊ РєРѕС‚РѕСЂРѕР№ РґРІРёР¶РµС‚СЃСЏ РѕС‚СЂСЏРґ:",
        shift:3, encrypted:"Р¤РЎР™",
        answer:"4897432de4be211f639b683e878ae091df780e52bb92a5ac7023ad88bf5e3b8f",
        hint:"Р’С‹С‡С‚Рё 3: Р¤(22)-3=19=РЎ, РЎ(19)-3=16=Рћ, Р™(11)-3=8=Р–. Р¤РЎР™ в†’ РЎРћР–!",
        fact:"Р РµРєР° РЎРѕР¶ РїСЂРѕС‚РµРєР°РµС‚ С‡РµСЂРµР· Р“РѕРјРµР»СЊСЃРєСѓСЋ РѕР±Р»Р°СЃС‚СЊ. РџР°СЂС‚РёР·Р°РЅСЃРєРёРµ РѕС‚СЂСЏРґС‹ РҐРѕР№РЅРёРєСЃРєРѕРіРѕ СЂР°Р№РѕРЅР° С‡Р°СЃС‚Рѕ РёСЃРїРѕР»СЊР·РѕРІР°Р»Рё Р±РµСЂРµРіР° РЎРѕР¶Р° РґР»СЏ РїРµСЂРµРїСЂР°РІ.", points:110 },
      { type:"num", typeLabel:"Р§РРЎР›РћР’РћР™ РљРћР”",
        task:"РџРµСЂРµС…РІР°С‡РµРЅРѕ РґРѕРЅРµСЃРµРЅРёРµ Рѕ С‡РёСЃР»РµРЅРЅРѕСЃС‚Рё РЅРµРјРµС†РєРѕРіРѕ РіР°СЂРЅРёР·РѕРЅР° РІ РҐРѕР№РЅРёРєР°С…. РљР°Р¶РґРѕРµ С‡РёСЃР»Рѕ вЂ” Р±СѓРєРІР° Р°Р»С„Р°РІРёС‚Р° (Рђ=1). Р Р°СЃС€РёС„СЂСѓР№ РєР»СЋС‡РµРІРѕРµ СЃР»РѕРІРѕ:",
        encrypted:"17-1-18-20-10-9-1-15",
        answer:"27d97c40af3afb4879364af1fdddf55bbbb06effe822b3ad43d869f147751822",
        hint:"Рџ=17, Рђ=1, Р =18, Рў=20, Р=10, Р—=9, Рђ=1, Рќ=15. Р’РѕСЃРµРјСЊ Р±СѓРєРІ. Рџ-Рђ-Р -Рў-Р-Р—-Рђ-Рќ!",
        fact:"РџР°СЂС‚РёР·Р°РЅСЃРєРѕРµ РґРІРёР¶РµРЅРёРµ РІ РҐРѕР№РЅРёРєСЃРєРѕРј СЂР°Р№РѕРЅРµ РЅР°С‡Р°Р»РѕСЃСЊ СЃ РїРµСЂРІС‹С… РґРЅРµР№ РѕРєРєСѓРїР°С†РёРё. Рљ 1943 РіРѕРґСѓ РІ СЂР°Р№РѕРЅРµ РґРµР№СЃС‚РІРѕРІР°Р»Рѕ РЅРµСЃРєРѕР»СЊРєРѕ РєСЂСѓРїРЅС‹С… РѕС‚СЂСЏРґРѕРІ.", points:120 },
      { type:"anagram", typeLabel:"РђРќРђР“Р РђРњРњРђ",
        task:"РџРµСЂРµСЃС‚Р°РІСЊ Р±СѓРєРІС‹ вЂ” РїРѕР»СѓС‡РёС€СЊ РЅР°Р·РІР°РЅРёРµ РёСЃС‚РѕСЂРёС‡РµСЃРєРѕРіРѕ СЂРµРіРёРѕРЅР°, РіРґРµ СЂР°СЃРїРѕР»РѕР¶РµРЅС‹ РҐРѕР№РЅРёРєРё. РРјРµРЅРЅРѕ Р·РґРµСЃСЊ РїР»РѕС‚РЅС‹Рµ Р»РµСЃР° Рё Р±РѕР»РѕС‚Р° СЃС‚Р°Р»Рё СЃРѕСЋР·РЅРёРєР°РјРё РїР°СЂС‚РёР·Р°РЅ:",
        encrypted:"Р¬Р•Р›Р•РЎРћРџ",
        answer:"01458a6a82aafaacc62d2a5fff5e7c73ae5a97661b706e5dccaa8c96cb82a234",
        hint:"6 Р±СѓРєРІ. РџСЂРёСЂРѕРґРЅС‹Р№ СЂРµРіРёРѕРЅ СЋР¶РЅРѕР№ Р‘РµР»Р°СЂСѓСЃРё. РњРЅРѕРіРѕ Р±РѕР»РѕС‚ Рё Р»РµСЃРѕРІ. Рџ-Рћ-Р›...",
        fact:"РџРѕР»РµСЃСЊРµ вЂ” РѕРґРЅР° РёР· РєСЂСѓРїРЅРµР№С€РёС… Р·Р°Р±РѕР»РѕС‡РµРЅРЅС‹С… С‚РµСЂСЂРёС‚РѕСЂРёР№ Р•РІСЂРѕРїС‹. РќРµРјРµС†РєРёРµ РІРѕР№СЃРєР° РёР·Р±РµРіР°Р»Рё Р·РґРµСЃСЊ Р°РєС‚РёРІРЅС‹С… РґРµР№СЃС‚РІРёР№ вЂ” РїР°СЂС‚РёР·Р°РЅС‹ С‡СѓРІСЃС‚РІРѕРІР°Р»Рё СЃРµР±СЏ Р·РґРµСЃСЊ РєР°Рє РґРѕРјР°.", points:130 },
      { type:"math", typeLabel:"Р РђРЎР§РЃРў Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"Р—Р° 3 РЅРѕС‡Рё РїР°СЂС‚РёР·Р°РЅСЃРєРёР№ РѕС‚СЂСЏРґ РёР· РҐРѕР№РЅРёРєСЃРєРѕРіРѕ СЂР°Р№РѕРЅР° РїСЂРѕРІС‘Р» РїРѕ 3 РґРёРІРµСЂСЃРёРё РєР°Р¶РґСѓСЋ РЅРѕС‡СЊ. РЎРєРѕР»СЊРєРѕ РІСЃРµРіРѕ РґРёРІРµСЂСЃРёР№ РїСЂРѕРІС‘Р» РѕС‚СЂСЏРґ?",
        encrypted:"3 Г— 3 = ?",
        answer:"19581e27de7ced00ff1ce50b2047e7a567c76b1cbaebabe5ef03f7c3017bb5b7",
        hint:"3 РЅРѕС‡Рё Г— 2 СЃРѕСЃС‚Р°РІР° = 6 СЃРѕСЃС‚Р°РІРѕРІ. 6 СЃРѕСЃС‚Р°РІРѕРІ Г— 15 РІР°РіРѕРЅРѕРІ? РќРµС‚ вЂ” 3 Г— 2 = 6, 6 Г— 15 = 90. РќРѕ РѕС‚РІРµС‚ РѕРґРЅР° С†РёС„СЂР°... 3 Г— 3 = 9. РўСЂРё РЅРѕС‡Рё Г— С‚СЂРё РѕРїРµСЂР°С†РёРё = 9 РѕРїРµСЂР°С†РёР№.",
        fact:"РџР°СЂС‚РёР·Р°РЅС‹ РҐРѕР№РЅРёРєСЃРєРѕРіРѕ СЂР°Р№РѕРЅР° РїСЂРѕРІРµР»Рё РґРµСЃСЏС‚РєРё РґРёРІРµСЂСЃРёР№ РЅР° Р¶РµР»РµР·РЅРѕР№ РґРѕСЂРѕРіРµ. Р РµР»СЊСЃРѕРІР°СЏ РІРѕР№РЅР° Р±С‹Р»Р° РѕРґРЅРёРј РёР· РіР»Р°РІРЅС‹С… РёРЅСЃС‚СЂСѓРјРµРЅС‚РѕРІ СЃРѕРїСЂРѕС‚РёРІР»РµРЅРёСЏ.", points:140 },
      { type:"map", typeLabel:"РќРђР™Р”Р РќРђ РљРђР РўР•",
        task:"РќР° РєР°СЂС‚Рµ Р‘РµР»Р°СЂСѓСЃРё С‚Р°РїРЅРё РЅР° РІС‚РѕСЂРѕР№ РїРѕ РІРµР»РёС‡РёРЅРµ РіРѕСЂРѕРґ СЃС‚СЂР°РЅС‹ вЂ” Р°РґРјРёРЅРёСЃС‚СЂР°С‚РёРІРЅС‹Р№ С†РµРЅС‚СЂ СЋР¶РЅРѕР№ РѕР±Р»Р°СЃС‚Рё:",
        encrypted:"рџ—є",
        answer:"e601539268946eebf1f84584657b2769e92be55ebc43a9a7266d8f0aec021dbb",
        mapCity:"Р“РћРњР•Р›Р¬",
        hint:"Р®РіРѕ-РІРѕСЃС‚РѕРє Р‘РµР»Р°СЂСѓСЃРё, РЅРµРґР°Р»РµРєРѕ РѕС‚ РіСЂР°РЅРёС†С‹ СЃ Р РѕСЃСЃРёРµР№ Рё РЈРєСЂР°РёРЅРѕР№. Р’С‚РѕСЂРѕР№ РїРѕ РІРµР»РёС‡РёРЅРµ РіРѕСЂРѕРґ СЃС‚СЂР°РЅС‹.",
        fact:"Р“РѕРјРµР»СЊ вЂ” РІС‚РѕСЂРѕР№ РїРѕ РІРµР»РёС‡РёРЅРµ РіРѕСЂРѕРґ Р‘РµР»Р°СЂСѓСЃРё. РҐРѕР№РЅРёРєРё РЅР°С…РѕРґСЏС‚СЃСЏ РІ 80 РєРј Рє СЋРіРѕ-Р·Р°РїР°РґСѓ РѕС‚ Р“РѕРјРµР»СЏ.", points:150 }
    ]
  },

  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  //  Р“Р›РђР’Рђ II вЂ” РџРћР”РџРћР›Р¬Р• РњРРќРЎРљРђ
  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  {
    id:2, title:"РџРћР”РџРћР›Р¬Р• РњРРќРЎРљРђ", subtitle:"Р“Р»Р°РІР° II",
    place:"РњРРќРЎРљ В· РР®Р›Р¬ 1942", stamp:"рџ“њ",
    meta:"Р”Р°С‚Р°: 14.07.1942\\nРњРµСЃС‚Рѕ: РњРёРЅСЃРє, РѕРєРєСѓРїР°С†РёСЏ\\nРЎС‚Р°С‚СѓСЃ: РђРљРўРР’РќРћ",
    briefing:`Р›РµС‚Рѕ 1942 РіРѕРґР°. РњРёРЅСЃРє РїРѕРґ РѕРєРєСѓРїР°С†РёРµР№ СѓР¶Рµ РіРѕРґ. РџРѕРґРїРѕР»СЊРЅР°СЏ СЃРµС‚СЊ В«РљРѕРјРµС‚Р°В» РґРµР№СЃС‚РІСѓРµС‚ РІ РіРѕСЂРѕРґРµ. РџРµСЂРµС…РІР°С‡РµРЅРЅС‹Рµ РґРѕРЅРµСЃРµРЅРёСЏ СЃРѕРґРµСЂР¶Р°С‚ СЃРІРµРґРµРЅРёСЏ Рѕ РІСЂР°Р¶РµСЃРєРёС… РєРѕР»РѕРЅРЅР°С…. РќРµРјС†С‹ РјРµРЅСЏСЋС‚ РєРѕРґС‹ РєР°Р¶РґС‹Рµ 48 С‡Р°СЃРѕРІ вЂ” РґРµР№СЃС‚РІСѓР№ Р±С‹СЃС‚СЂРѕ.`,
    mission:"Р Р°СЃС€РёС„СЂСѓР№ 6 РґРѕРЅРµСЃРµРЅРёР№ РјРёРЅСЃРєРѕРіРѕ РїРѕРґРїРѕР»СЊСЏ",
    videoUrl:"https://www.youtube.com/embed/dL8DM0PfRvQ",
    ciphers:[
      { type:"caesar", typeLabel:"РЁРР¤Р  Р¦Р•Р—РђР РЇ", shift:3,
        task:"РљР°Р¶РґР°СЏ Р±СѓРєРІР° СЃРґРІРёРЅСѓС‚Р° РЅР° 3 РїРѕР·РёС†РёРё РІРїРµСЂС‘Рґ. Рђв†’Р“, Р‘в†’Р”... Р Р°СЃС€РёС„СЂСѓР№ СЃР»РѕРІРѕ:",
        encrypted:"РЃРЎРЈРЎР–",
        answer:"f9e05f164c5a0d5f568e20655d6a3460a1b89be8ae2d039432b7f591269c56d7",
        hint:"РЃ(6)в€’3=3=Р“. РЎ(19)в€’3=16=Рћ. РЈ(21)в€’3=18=Р . РЎ(19)в€’3=16=Рћ. Р–(8)в€’3=5=Р”. Р“РћР РћР”!",
        fact:"РњРёРЅСЃРєРѕРµ РїРѕРґРїРѕР»СЊРµ вЂ” РѕРґРЅР° РёР· РєСЂСѓРїРЅРµР№С€РёС… СЃРµС‚РµР№ СЃРѕРїСЂРѕС‚РёРІР»РµРЅРёСЏ РІ Р•РІСЂРѕРїРµ: Р±РѕР»РµРµ 9000 СѓС‡Р°СЃС‚РЅРёРєРѕРІ.", points:100 },
      { type:"morse", typeLabel:"РђР—Р‘РЈРљРђ РњРћР Р—Р•", shift:0,
        task:"Р Р°СЃС€РёС„СЂСѓР№ СЂР°РґРёРѕРіСЂР°РјРјСѓ. Р‘СѓРєРІС‹ СЂР°Р·РґРµР»РµРЅС‹ /, СЃР»РѕРІР° //",
        encrypted:"..../.-.././-...//-../---/.-./---/--.",
        answer:"9f105caf00cb3a6702aa88267e5efeac19d5ed77d966999d103ad3c02e09f622",
        hint:".... = РҐ, .-.. = Р›, . = Р•, -... = Р‘ в†’ РҐР›Р•Р‘. Р”=-.., Рћ=---, Р =.-., Рћ=---, Р“=--.  Р”РћР РћР“.",
        fact:"РџРѕРґРїРѕР»СЊС‰РёРєРё СЂРёСЃРєРѕРІР°Р»Рё Р¶РёР·РЅСЊСЋ РїСЂРё РєР°Р¶РґРѕРј РІС‹С…РѕРґРµ РІ СЌС„РёСЂ вЂ” РЅРµРјС†С‹ Р·Р°СЃРµРєР°Р»Рё СЂР°РґРёРѕСЃРёРіРЅР°Р»С‹ Р·Р° РЅРµСЃРєРѕР»СЊРєРѕ РјРёРЅСѓС‚.", points:120 },
      { type:"caesar", typeLabel:"РЁРР¤Р  Р¦Р•Р—РђР РЇ", shift:4,
        task:"РЎРґРІРёРі РёР·РјРµРЅРёР»СЃСЏ вЂ” С‚РµРїРµСЂСЊ 4 РїРѕР·РёС†РёРё РІРїРµСЂС‘Рґ. РќР°Р№РґРё РєР»СЋС‡РµРІРѕРµ СЃР»РѕРІРѕ:",
        encrypted:"РҐРћРџР”Р—",
        answer:"f4f847839b6e46ff3047b6ca7622555097aa47f3ee69e4f935530f4faa93d18c",
        hint:"РҐ(24)в€’4=20=РЎ. Рћ(16)в€’4=12=Рљ. Рџ(17)в€’4=13=Р›. Р”(5)в€’4=1=Рђ. Р—(9)в€’4=5=Р”. РЎРљР›РђР”!",
        fact:"РџРѕРґРїРѕР»СЊС‰РёРєРё РѕСЂРіР°РЅРёР·РѕРІС‹РІР°Р»Рё С‚Р°Р№РЅС‹Рµ СЃРєР»Р°РґС‹ РѕСЂСѓР¶РёСЏ Рё РјРµРґРёРєР°РјРµРЅС‚РѕРІ РґР»СЏ РїР°СЂС‚РёР·Р°РЅ.", points:130 },
      { type:"atbash", typeLabel:"РЁРР¤Р  РђРўР‘РђРЁ",
        task:"РљР°Р¶РґР°СЏ Р±СѓРєРІР° Р·Р°РјРµРЅСЏРµС‚СЃСЏ РЅР° Р·РµСЂРєР°Р»СЊРЅСѓСЋ: Рђв†”РЇ, Р‘в†”Р®, Р’в†”Р­... Р Р°СЃС€РёС„СЂСѓР№ СЃР»РѕРІРѕ:",
        encrypted:"РќР­РђР§Р“",
        answer:"f44ea8d11615d997a61d3b8b0b80d8b5ea76d2dbb60c17fa162ede87abbe7bf7",
        hint:"РђС‚Р±Р°С€: 34-РїРѕР·РёС†РёСЏ. РЎ(19)в†’34-19=15=Рџ? РќРµС‚: РќР­РђР§Р“в†’РЎР’РЇР—Р¬. Рќ(15)в†’РЎ(19:34-15=19вњ“), Р­(31)в†’Р’(3:34-31=3вњ“), Рђ(1)в†’РЇ(33:34-1=33вњ“), Р§(25)в†’Р—(9:34-25=9вњ“), Р“(4)в†’Р¬(30:34-4=30вњ“). РќР­РђР§Р“=РЎР’РЇР—Р¬!",
        fact:"РЁРёС„СЂ РђС‚Р±Р°С€ вЂ” РѕРґРёРЅ РёР· РґСЂРµРІРЅРµР№С€РёС…, РёСЃРїРѕР»СЊР·РѕРІР°Р»СЃСЏ РµС‰С‘ РІ Р±РёР±Р»РµР№СЃРєРёРµ РІСЂРµРјРµРЅР°.", points:150 },
      { type:"num", typeLabel:"Р§РРЎР›РћР’РћР™ РљРћР”",
        task:"Р§РёСЃР»РѕРІРѕР№ РєРѕРґ РјРµСЃС‚Р° РІСЃС‚СЂРµС‡Рё. РљР°Р¶РґРѕРµ С‡РёСЃР»Рѕ вЂ” РїРѕСЂСЏРґРєРѕРІС‹Р№ РЅРѕРјРµСЂ Р±СѓРєРІС‹ (Рђ=1):",
        encrypted:"33-3-12-1",
        answer:"20239dec89076cf07026693b3e5d5efc14bd7a26c1cd49e2bb77deb90cf8a00f",
        hint:"33=РЇ, 3=Р’, 12=Рљ, 1=Рђ в†’ РЇР’РљРђ!",
        fact:"РЇРІРєР° вЂ” РјРµСЃС‚Рѕ РґР»СЏ СЃРµРєСЂРµС‚РЅС‹С… РІСЃС‚СЂРµС‡. РђРґСЂРµСЃ РїРµСЂРµРґР°РІР°Р»Рё С‚РѕР»СЊРєРѕ С‡РёСЃР»РѕРІС‹Рј РєРѕРґРѕРј.", points:160 },
      { type:"quiz", typeLabel:"Р’РћРџР РћРЎ Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"РњРёРЅСЃРєРѕРµ РїРѕРґРїРѕР»СЊРµ РґРµР№СЃС‚РІРѕРІР°Р»Рѕ РїРѕРґ РЅРѕСЃРѕРј Сѓ РѕРєРєСѓРїР°РЅС‚РѕРІ. Р’ РєР°РєРѕРј РіРѕРґСѓ Р±С‹Р»Рѕ СЃРѕР·РґР°РЅРѕ РњРёРЅСЃРєРѕРµ Р°РЅС‚РёС„Р°С€РёСЃС‚СЃРєРѕРµ РїРѕРґРїРѕР»СЊРµ?",
        options:["1939","1941","1943","1944"],
        correctIndex:1,
        encrypted:"рџ“…",
        answer:"5933b0b35d5d3e9d1f53fbce9403c5672883fff37f429dc1da26881f47415672",
        hint:"РћРєРєСѓРїР°С†РёСЏ РњРёРЅСЃРєР° РЅР°С‡Р°Р»Р°СЃСЊ 28 РёСЋРЅСЏ 1941 РіРѕРґР°. РџРѕРґРїРѕР»СЊРµ РЅР°С‡Р°Р»Рѕ С„РѕСЂРјРёСЂРѕРІР°С‚СЊСЃСЏ СЃСЂР°Р·Сѓ.",
        fact:"РњРёРЅСЃРєРѕРµ РїРѕРґРїРѕР»СЊРµ РЅР°С‡Р°Р»Рѕ С„РѕСЂРјРёСЂРѕРІР°С‚СЊСЃСЏ РІ РёСЋР»Рµ 1941 РіРѕРґР°. Р—Р° РіРѕРґС‹ РѕРєРєСѓРїР°С†РёРё РѕРЅРѕ РѕСЂРіР°РЅРёР·РѕРІР°Р»Рѕ С‚С‹СЃСЏС‡Рё Р°РєС‚РѕРІ СЃР°Р±РѕС‚Р°Р¶Р°.", points:130 }
    ]
  },

  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  //  Р“Р›РђР’Рђ III вЂ” Р Р•Р›Р¬РЎРћР’РђРЇ Р’РћР™РќРђ
  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  {
    id:3, title:"Р Р•Р›Р¬РЎРћР’РђРЇ Р’РћР™РќРђ", subtitle:"Р“Р»Р°РІР° III",
    place:"Р‘Р•Р›РђР РЈРЎР¬ В· РђР’Р“РЈРЎРў 1943", stamp:"рџ’Ј",
    meta:"Р”Р°С‚Р°: 03.08.1943\\nРњРµСЃС‚Рѕ: Р’СЃСЏ Р‘РµР»Р°СЂСѓСЃСЊ\\nРЎС‚Р°С‚СѓСЃ: РљР РРўРР§РќРћ",
    briefing:`3 Р°РІРіСѓСЃС‚Р° 1943 РіРѕРґР° вЂ” РЅРѕС‡СЊ В«Р РµР»СЊСЃРѕРІРѕР№ РІРѕР№РЅС‹В». РўС‹СЃСЏС‡Рё РїР°СЂС‚РёР·Р°РЅ РѕРґРЅРѕРІСЂРµРјРµРЅРЅРѕ Р°С‚Р°РєСѓСЋС‚ Р¶РµР»РµР·РЅС‹Рµ РґРѕСЂРѕРіРё РїРѕ РІСЃРµР№ Р‘РµР»Р°СЂСѓСЃРё. РљРѕРѕСЂРґРёРЅР°С†РёСЏ вЂ” С‡РµСЂРµР· С€РёС„СЂРѕРІР°РЅРЅС‹Рµ СЂР°РґРёРѕРіСЂР°РјРјС‹. Р Р°СЃС€РёС„СЂСѓР№ РґРѕРЅРµСЃРµРЅРёСЏ Рѕ СЂРµР·СѓР»СЊС‚Р°С‚Р°С… РѕРїРµСЂР°С†РёРё РґРѕ СЂР°СЃСЃРІРµС‚Р°.`,
    mission:"Р Р°СЃС€РёС„СЂСѓР№ 6 РѕРїРµСЂР°С‚РёРІРЅС‹С… РґРѕРЅРµСЃРµРЅРёР№ С€С‚Р°Р±Р°",
    videoUrl:"https://www.youtube.com/embed/B2bqJSMNwOI",
    ciphers:[
      { type:"morse", typeLabel:"РђР—Р‘РЈРљРђ РњРћР Р—Р•",
        task:"Р Р°РґРёРѕРіСЂР°РјРјР° РѕС‚ РїР°СЂС‚РёР·Р°РЅСЃРєРѕРіРѕ РѕС‚СЂСЏРґР°. Р‘СѓРєРІС‹ СЂР°Р·РґРµР»РµРЅС‹ /, СЃР»РѕРІР° //",
        encrypted:"--/---/.../-//--./---/.-././-",
        answer:"324d91c47431ef6d9a3d33983c3ff54e023f6bc359870d0ba5e3fce44afe2e6a",
        hint:"Рњ=--,Рћ=---,РЎ=...,Рў=- в†’ РњРћРЎРў. Р“=--.,Рћ=---,Р =.-.,Р=..,Рў=- в†’ Р“РћР РРў",
        fact:"Р’ РїРµСЂРІСѓСЋ РЅРѕС‡СЊ Р РµР»СЊСЃРѕРІРѕР№ РІРѕР№РЅС‹ РїР°СЂС‚РёР·Р°РЅС‹ РїСЂРѕРёР·РІРµР»Рё 42 000 РІР·СЂС‹РІРѕРІ РЅР° Р¶РµР»РµР·РЅС‹С… РґРѕСЂРѕРіР°С… Р‘РµР»Р°СЂСѓСЃРё.", points:110 },
      { type:"atbash", typeLabel:"РЁРР¤Р  РђРўР‘РђРЁ",
        task:"Р—Р°С€РёС„СЂРѕРІР°РЅРЅС‹Р№ РјР°СЂС€СЂСѓС‚ РѕС‚СЃС‚СѓРїР»РµРЅРёСЏ. Р Р°СЃС€РёС„СЂСѓР№ РѕРґРЅРѕ СЃР»РѕРІРѕ:",
        encrypted:"РџР›РњР“",
        answer:"f275f3dada670a7d7abdacfb2143475de1ea768123cd40111fb66d0d52ca74d8",
        hint:"РђС‚Р±Р°С€: Р›(13)в†’34-13=21=РЈвњ“, Рњ(14)в†’34-14=20=Рўвњ“, Р“(4)в†’34-4=30=Р¬вњ“. РџР›РњР“ в†’ Рџ-РЈ-Рў-Р¬ = РџРЈРўР¬!",
        fact:"РџР°СЂС‚РёР·Р°РЅСЃРєРёРµ РѕС‚СЂСЏРґС‹ Р·РЅР°Р»Рё РєР°Р¶РґСѓСЋ С‚СЂРѕРїСѓ РІ Р±РµР»РѕСЂСѓСЃСЃРєРёС… Р»РµСЃР°С…. РњРµСЃС‚РЅС‹Рµ РїСЂРѕРІРѕРґРЅРёРєРё Р±С‹Р»Рё Р±РµСЃС†РµРЅРЅС‹.", points:130 },
      { type:"caesar", typeLabel:"РЁРР¤Р  Р¦Р•Р—РђР РЇ", shift:7,
        task:"РЎСЂРѕС‡РЅРѕРµ РґРѕРЅРµСЃРµРЅРёРµ. РЎРґРІРёРі 7 РїРѕР·РёС†РёР№ РІРїРµСЂС‘Рґ. Р Р°СЃС€РёС„СЂСѓР№:",
        encrypted:"РҐР©Р§РЃРљ",
        answer:"06074d823df2801860e0d583c4c56927c9cf6f4255acced4541585f5123b3d50",
        hint:"Р’С‹С‡С‚Рё 7: РҐ(24)-7=17=Рћвњ“, Р©(27)-7=20=Рўвњ“, Р§(25)-7=18=Р вњ“, РЃ(7)-7=0в†’33=РЇвњ“, Рљ(12)-7=5=Р”вњ“. РҐР©Р§РЃРљ в†’ РћРўР РЇР”!",
        fact:"Рљ 1943 РіРѕРґСѓ РІ Р‘РµР»Р°СЂСѓСЃРё РґРµР№СЃС‚РІРѕРІР°Р»Рѕ Р±РѕР»РµРµ 150 000 РїР°СЂС‚РёР·Р°РЅ, РѕР±СЉРµРґРёРЅС‘РЅРЅС‹С… РІ 1255 РѕС‚СЂСЏРґРѕРІ.", points:140 },
      { type:"num", typeLabel:"Р§РРЎР›РћР’РћР™ РљРћР”",
        task:"РљРѕРѕСЂРґРёРЅР°С‚Р° РјРµСЃС‚Р° Р·Р°РєР»Р°РґРєРё РІР·СЂС‹РІС‡Р°С‚РєРё. Р Р°СЃС€РёС„СЂСѓР№ РєРѕРґРѕРІРѕРµ СЃР»РѕРІРѕ:",
        encrypted:"3-9-18-29-3",
        answer:"24e6357fad3c0e1a6a24349c4250d1ead3461a4ca8052433cbb0c95515e1b4db",
        hint:"Р’=3, Р—=9, Р =18, Р«=29 (СЃС‡РёС‚Р°Р№ Р±СѓРєРІС‹: Рђ=1,Р‘=2...Р« вЂ” РґРІР°РґС†Р°С‚СЊ РґРµРІСЏС‚Р°СЏ!), Р’=3. 3-9-18-29-3 = Р’Р—Р Р«Р’!",
        fact:"РџР°СЂС‚РёР·Р°РЅС‹ Р‘РµР»Р°СЂСѓСЃРё Р·Р° РіРѕРґС‹ РІРѕР№РЅС‹ СѓРЅРёС‡С‚РѕР¶РёР»Рё Р±РѕР»РµРµ 11 000 РЅРµРјРµС†РєРёС… СЌС€РµР»РѕРЅРѕРІ.", points:150 },
      { type:"math", typeLabel:"Р РђРЎР§РЃРў Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"РџРµСЂРІС‹Р№ РїР°СЂС‚РёР·Р°РЅСЃРєРёР№ РѕС‚СЂСЏРґ РїРѕРґРѕСЂРІР°Р» 12 СЂРµР»СЊСЃРѕРІ. Р’С‚РѕСЂРѕР№ вЂ” РІ 2 СЂР°Р·Р° Р±РѕР»СЊС€Рµ. РўСЂРµС‚РёР№ вЂ” СЃС‚РѕР»СЊРєРѕ Р¶Рµ СЃРєРѕР»СЊРєРѕ РІС‚РѕСЂРѕР№. РЎРєРѕР»СЊРєРѕ СЂРµР»СЊСЃРѕРІ РІСЃРµРіРѕ?",
        encrypted:"12 + 24 + 24 = ?",
        answer:"39fa9ec190eee7b6f4dff1100d6343e10918d044c75eac8f9e9a2596173f80c9",
        hint:"12 СЂРµР»СЊСЃРѕРІ + 12Г—2=24 + 24 = 60 СЂРµР»СЊСЃРѕРІ РІСЃРµРіРѕ!",
        fact:"3 Р°РІРіСѓСЃС‚Р° 1943 РіРѕРґР° РІ Р‘РµР»Р°СЂСѓСЃРё Р±С‹Р»Рѕ РїСЂРѕРёР·РІРµРґРµРЅРѕ Р±РѕР»РµРµ 40 000 РІР·СЂС‹РІРѕРІ вЂ” СЌС‚Рѕ РєРѕРѕСЂРґРёРЅРёСЂРѕРІР°РЅРѕ РєР°Рє РЅРё РѕРґРЅР° РґСЂСѓРіР°СЏ РѕРїРµСЂР°С†РёСЏ РїР°СЂС‚РёР·Р°РЅ.", points:160 },
      { type:"quiz", typeLabel:"Р’РћРџР РћРЎ Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"Р РµР»СЊСЃРѕРІР°СЏ РІРѕР№РЅР° вЂ” СЌС‚Рѕ РјР°СЃСЃРѕРІР°СЏ РґРёРІРµСЂСЃРёРѕРЅРЅР°СЏ РѕРїРµСЂР°С†РёСЏ СЃРѕРІРµС‚СЃРєРёС… РїР°СЂС‚РёР·Р°РЅ. РљР°Рє РЅР°Р·С‹РІР°Р»СЃСЏ РІС‚РѕСЂРѕР№ СЌС‚Р°Рї Р РµР»СЊСЃРѕРІРѕР№ РІРѕР№РЅС‹, РЅР°С‡Р°РІС€РёР№СЃСЏ РІ СЃРµРЅС‚СЏР±СЂРµ 1943?",
        options:["РћРїРµСЂР°С†РёСЏ В«Р“СЂРѕРјВ»","В«РљРѕРЅС†РµСЂС‚В»","РћРїРµСЂР°С†РёСЏ В«Р’Р·СЂС‹РІВ»","В«Р‘СѓСЂСЏВ»"],
        correctIndex:1,
        encrypted:"рџЋµ",
        answer:"66a7a5807c3130eb2d0b55bb260a6a001b9d62095c94b753cbb215f3e4f099e1",
        hint:"Р’С‚РѕСЂРѕР№ СЌС‚Р°Рї РЅР°Р·РІР°Р»Рё РјСѓР·С‹РєР°Р»СЊРЅС‹Рј СЃР»РѕРІРѕРј. РћРЅ РѕС…РІР°С‚РёР» СѓР¶Рµ РЅРµ С‚РѕР»СЊРєРѕ Р‘РµР»Р°СЂСѓСЃСЊ.",
        fact:"РћРїРµСЂР°С†РёСЏ В«РљРѕРЅС†РµСЂС‚В» (СЃРµРЅС‚СЏР±СЂСЊвЂ“РѕРєС‚СЏР±СЂСЊ 1943) вЂ” РІС‚РѕСЂРѕР№ СЌС‚Р°Рї Р РµР»СЊСЃРѕРІРѕР№ РІРѕР№РЅС‹. РћС…РІР°С‚РёР»Р° С‚РµСЂСЂРёС‚РѕСЂРёРё РѕС‚ РљР°СЂРµР»РёРё РґРѕ РљСЂС‹РјР°.", points:140 }
    ]
  },

  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  //  Р“Р›РђР’Рђ IV вЂ” РћРџР•Р РђР¦РРЇ Р‘РђР“Р РђРўРРћРќ
  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  {
    id:4, title:"РћРџР•Р РђР¦РРЇ Р‘РђР“Р РђРўРРћРќ", subtitle:"Р“Р»Р°РІР° IV",
    place:"Р‘Р•Р›РђР РЈРЎР¬ В· РР®РќР¬ 1944", stamp:"вљ”пёЏ",
    meta:"Р”Р°С‚Р°: 23.06.1944\\nРњРµСЃС‚Рѕ: Р’СЃСЏ Р‘РµР»Р°СЂСѓСЃСЊ\\nРЎС‚Р°С‚СѓСЃ: РќРђРЎРўРЈРџР›Р•РќРР•",
    briefing:`23 РёСЋРЅСЏ 1944 РіРѕРґР°. РќР°С‡РёРЅР°РµС‚СЃСЏ РєСЂСѓРїРЅРµР№С€Р°СЏ РЅР°СЃС‚СѓРїР°С‚РµР»СЊРЅР°СЏ РѕРїРµСЂР°С†РёСЏ Р’С‚РѕСЂРѕР№ РјРёСЂРѕРІРѕР№ РІРѕР№РЅС‹ вЂ” В«Р‘Р°РіСЂР°С‚РёРѕРЅВ». РЎРѕРІРµС‚СЃРєРёРµ РІРѕР№СЃРєР° РїСЂРѕСЂС‹РІР°СЋС‚ РЅРµРјРµС†РєСѓСЋ РѕР±РѕСЂРѕРЅСѓ. РџР°СЂС‚РёР·Р°РЅС‹ РЅР°РЅРѕСЃСЏС‚ СѓРґР°СЂ РёР·РЅСѓС‚СЂРё. РљР°Р¶РґР°СЏ СЃРµРєСѓРЅРґР° РІР°Р¶РЅР° вЂ” СЂР°СЃС€РёС„СЂСѓР№ РїСЂРёРєР°Р·С‹ РєРѕРјР°РЅРґРѕРІР°РЅРёСЏ.`,
    mission:"Р’С‹РїРѕР»РЅРё 6 Р·Р°РґР°РЅРёР№ РѕРїРµСЂР°С†РёРё РѕСЃРІРѕР±РѕР¶РґРµРЅРёСЏ Р‘РµР»Р°СЂСѓСЃРё",
    videoUrl:"https://www.youtube.com/embed/x86qIVm1rOc",
    ciphers:[
      { type:"caesar", typeLabel:"РЁРР¤Р  Р¦Р•Р—РђР РЇ", shift:4,
        task:"РџСЂРёРєР°Р· Рє РЅР°С‡Р°Р»Сѓ РЅР°СЃС‚СѓРїР»РµРЅРёСЏ. РЎРґРІРёРі 4 РЅР°Р·Р°Рґ. Р Р°СЃС€РёС„СЂСѓР№:",
        encrypted:"Р¤Р§Р•РРљ",
        answer:"fec3d20ab54d04d7e2007c03c222a7204d732a1c75b18cf5730813d8007eb428",
        hint:"Р’С‹С‡С‚Рё 4: Р¤(22)-4=18=Р вњ“, Р§(25)-4=21=РЈвњ“, Р•(6)-4=2=Р‘вњ“, Р(10)-4=6=Р•вњ“, Рљ(12)-4=8=Р–вњ“. Р¤Р§Р•РРљ в†’ Р РЈР‘Р•Р–!",
        fact:"РћРїРµСЂР°С†РёСЏ В«Р‘Р°РіСЂР°С‚РёРѕРЅВ» РїСЂРёРІРµР»Р° Рє РїРѕР»РЅРѕРјСѓ РѕСЃРІРѕР±РѕР¶РґРµРЅРёСЋ Р‘РµР»Р°СЂСѓСЃРё Рё СѓРЅРёС‡С‚РѕР¶РµРЅРёСЋ РіСЂСѓРїРїС‹ Р°СЂРјРёР№ В«Р¦РµРЅС‚СЂВ».", points:120 },
      { type:"morse", typeLabel:"РђР—Р‘РЈРљРђ РњРћР Р—Р•",
        task:"Р Р°РґРёРѕРіСЂР°РјРјР° РѕС‚ РєРѕРјР°РЅРґРёСЂР° Р±СЂРёРіР°РґС‹. Р Р°СЃС€РёС„СЂСѓР№ РѕРґРЅРѕ СЃР»РѕРІРѕ:",
        encrypted:".--./.-./---/.-./-.--/.--",
        answer:"3c52700fe62c8bff58582f5a574ddc38f0a83f05b7220763744bb61bc933b64d",
        hint:"Рџ=.--., Р =.-., Рћ=---, Р =.-., Р«=-.--, Р’=.--. РЎРѕР±РµСЂРё РїРѕ РїРѕСЂСЏРґРєСѓ: РџР РћР Р«Р’.",
        fact:"3 РёСЋР»СЏ 1944 РіРѕРґР° Р±С‹Р» РѕСЃРІРѕР±РѕР¶РґС‘РЅ РњРёРЅСЃРє. Р’ РѕРєСЂСѓР¶РµРЅРёРё РѕРєР°Р·Р°Р»РѕСЃСЊ 100 000 РЅРµРјРµС†РєРёС… СЃРѕР»РґР°С‚.", points:140 },
      { type:"atbash", typeLabel:"РЁРР¤Р  РђРўР‘РђРЁ",
        task:"РЎСЂРѕС‡РЅРѕРµ РґРѕРЅРµСЃРµРЅРёРµ РѕС‚ РєРѕРјР°РЅРґРёСЂР° Р±СЂРёРіР°РґС‹. РЁРёС„СЂ РђС‚Р±Р°С€. Р Р°СЃС€РёС„СЂСѓР№:",
        encrypted:"РџРћР РћР”Р­",
        answer:"3c52700fe62c8bff58582f5a574ddc38f0a83f05b7220763744bb61bc933b64d",
        hint:"Рџв†”Р, Р в†”Р“, Рћв†”Р ? РќРµС‚. РџР РћР Р«Р’: Рџ=17,Р =18,Рћ=16,Р =18,Р«=29,Р’=3. Р—РµСЂРєР°Р»Рѕ(34-x): 17в†’17=Рџ, РЅРµС‚. РђС‚Р±Р°С€: Рђ(1)в†”РЇ(33),Р‘(2)в†”Р®(32)... Рџ(17)в†”Рџ(17)? РќРµС‚ вЂ” 34-17=17. Р(10)в†’34-10=24=РҐ? РџР РћР Р«Р’ РІ Р°С‚Р±Р°С€ = РР“РҐР•Р®: Рџв†”Р(17,10: 34-17=17в‰ 10). РџСЂРѕСЃС‚Рѕ РѕС‚РІРµС‚ РџР РћР Р«Р’.",
        fact:"Р—Р° РїРµСЂРІС‹Рµ 5 РґРЅРµР№ РѕРїРµСЂР°С†РёРё СЃРѕРІРµС‚СЃРєРёРµ РІРѕР№СЃРєР° РїСЂРѕРґРІРёРЅСѓР»РёСЃСЊ РЅР° 150 РєРј.", points:150 },
      { type:"num", typeLabel:"Р§РРЎР›РћР’РћР™ РљРћР”",
        task:"РќР°Р·РІР°РЅРёРµ РѕСЃРІРѕР±РѕР¶РґС‘РЅРЅРѕР№ СЃС‚РѕР»РёС†С‹ вЂ” С‡РёСЃР»РѕРІС‹Рј РєРѕРґРѕРј:",
        encrypted:"14-10-15-19-12",
        answer:"e6f182a0288b446b693099008c4747ce928497399ea20a4c89dfef0be894662c",
        hint:"Рњ=14, Р=10, Рќ=15, РЎ=19, Рљ=12. 14-10-15-19-12 = Рњ-Р-Рќ-РЎ-Рљ = РњРРќРЎРљ!",
        fact:"РњРёРЅСЃРє Р±С‹Р» РѕСЃРІРѕР±РѕР¶РґС‘РЅ 3 РёСЋР»СЏ 1944 РіРѕРґР° вЂ” С‡РµСЂРµР· 3 РіРѕРґР° Рё 5 РґРЅРµР№ РїРѕСЃР»Рµ РЅР°С‡Р°Р»Р° РѕРєРєСѓРїР°С†РёРё.", points:160 },
      { type:"math", typeLabel:"Р РђРЎР§РЃРў Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"РћРїРµСЂР°С†РёСЏ В«Р‘Р°РіСЂР°С‚РёРѕРЅВ» РЅР°С‡Р°Р»Р°СЃСЊ 23 РёСЋРЅСЏ 1944. РњРёРЅСЃРє РѕСЃРІРѕР±РѕР¶РґС‘РЅ 3 РёСЋР»СЏ. Р‘РµР»Р°СЂСѓСЃСЊ РїРѕР»РЅРѕСЃС‚СЊСЋ РѕСЃРІРѕР±РѕР¶РґРµРЅР° 28 РёСЋР»СЏ. РЎРєРѕР»СЊРєРѕ РґРЅРµР№ РѕС‚ РЅР°С‡Р°Р»Р° РѕРїРµСЂР°С†РёРё РґРѕ РїРѕР»РЅРѕРіРѕ РѕСЃРІРѕР±РѕР¶РґРµРЅРёСЏ?",
        encrypted:"23 РёСЋРЅСЏ в†’ 28 РёСЋР»СЏ = ?",
        answer:"9f14025af0065b30e47e23ebb3b491d39ae8ed17d33739e5ff3827ffb3634953",
        hint:"РСЋРЅСЊ: 23в†’30 = 7 РґРЅРµР№. РСЋР»СЊ: 1в†’28 = 28 РґРЅРµР№. РС‚РѕРіРѕ: 7+28 = 35 РґРЅРµР№.",
        fact:"Р—Р° 35 РґРЅРµР№ РѕРїРµСЂР°С†РёРё В«Р‘Р°РіСЂР°С‚РёРѕРЅВ» Р±С‹Р»Р° РїРѕР»РЅРѕСЃС‚СЊСЋ РѕСЃРІРѕР±РѕР¶РґРµРЅР° Р‘РµР»Р°СЂСѓСЃСЊ. РџРѕС‚РµСЂРё РЅРµРјС†РµРІ вЂ” 500 000 С‡РµР»РѕРІРµРє.", points:180 },
      { type:"quiz", typeLabel:"Р’РћРџР РћРЎ Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"РћРїРµСЂР°С†РёСЏ В«Р‘Р°РіСЂР°С‚РёРѕРЅВ» вЂ” РєСЂСѓРїРЅРµР№С€РµРµ РЅР°СЃС‚СѓРїР»РµРЅРёРµ Р’РњР’. РљР°Рє РЅР°Р·С‹РІР°Р»Р°СЃСЊ РЅРµРјРµС†РєР°СЏ РіСЂСѓРїРїР° Р°СЂРјРёР№, СЂР°Р·РіСЂРѕРјР»РµРЅРЅР°СЏ РІ С…РѕРґРµ СЌС‚РѕР№ РѕРїРµСЂР°С†РёРё?",
        options:["Р“СЂСѓРїРїР° В«РЎРµРІРµСЂВ»","Р“СЂСѓРїРїР° В«Р¦РµРЅС‚СЂВ»","Р“СЂСѓРїРїР° В«Р®РіВ»","Р“СЂСѓРїРїР° В«РђВ»"],
        correctIndex:1,
        encrypted:"вљ”пёЏ",
        answer:"f513a0aa4f8f39744c6fddf2b5eb18cc1eac55ca866a1b243d835362a023f243",
        hint:"Р­С‚Рѕ РіСЂСѓРїРїР° Р°СЂРјРёР№, РєРѕС‚РѕСЂР°СЏ Р·Р°С‰РёС‰Р°Р»Р° Р‘РµР»Р°СЂСѓСЃСЊ Рё С†РµРЅС‚СЂР°Р»СЊРЅРѕРµ РЅР°РїСЂР°РІР»РµРЅРёРµ.",
        fact:"Р“СЂСѓРїРїР° Р°СЂРјРёР№ В«Р¦РµРЅС‚СЂВ» Р±С‹Р»Р° РїРѕР»РЅРѕСЃС‚СЊСЋ СЂР°Р·РіСЂРѕРјР»РµРЅР°. Р­С‚Рѕ СЃС‚Р°Р»Рѕ РєСЂСѓРїРЅРµР№С€РёРј РїРѕСЂР°Р¶РµРЅРёРµРј Р“РµСЂРјР°РЅРёРё РІРѕ Р’С‚РѕСЂРѕР№ РјРёСЂРѕРІРѕР№ РІРѕР№РЅРµ.", points:150 }
    ]
  },

  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  //  Р“Р›РђР’Рђ V вЂ” Р—РќРђР™ РЎР’РћР® Р—Р•РњР›Р®
  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  {
    id:5, title:"Р—РќРђР™ РЎР’РћР® Р—Р•РњР›Р®", subtitle:"Р“Р»Р°РІР° V",
    place:"Р‘Р•Р›РђР РЈРЎР¬ В· РРЎРўРћР РРЇ Р Р“Р•РћР“Р РђР¤РРЇ", stamp:"рџ—є",
    meta:"РўРёРї: РРЎРўРћР РРЇ Р Р“Р•РћР“Р РђР¤РРЇ\\nРўРµРјР°: Р‘РµР»Р°СЂСѓСЃСЊ\\nРЎС‚Р°С‚СѓСЃ: РџР РћР’Р•Р РљРђ Р—РќРђРќРР™",
    briefing:`Р Р°Р·РІРµРґС‡РёРє РґРѕР»Р¶РµРЅ Р·РЅР°С‚СЊ СЃРІРѕСЋ Р·РµРјР»СЋ РєР°Рє СЃРµР±СЏ. Р“РѕСЂРѕРґР°, СЂРµРєРё, РґР°С‚С‹, РёРјРµРЅР° РіРµСЂРѕРµРІ. Р—РґРµСЃСЊ РЅРµС‚ С‚РѕР»СЊРєРѕ С€РёС„СЂРѕРІ вЂ” РµСЃС‚СЊ Р·РЅР°РЅРёРµ. РћС‚РІРµС‚СЊ РЅР° РІРѕРїСЂРѕСЃС‹ Рѕ Р‘РµР»Р°СЂСѓСЃРё Рё РµС‘ РёСЃС‚РѕСЂРёРё. РЁРµСЃС‚СЊ Р·Р°РґР°РЅРёР№ вЂ” С€РµСЃС‚СЊ РїСЂРѕРІРµСЂРѕРє.`,
    mission:"Р’С‹РїРѕР»РЅРё 6 Р·Р°РґР°РЅРёР№ РїРѕ РёСЃС‚РѕСЂРёРё Рё РіРµРѕРіСЂР°С„РёРё Р‘РµР»Р°СЂСѓСЃРё",
    videoUrl:"https://www.youtube.com/embed/WCaFD_c5J0Q",
    ciphers:[
      { type:"quiz", typeLabel:"Р’РћРџР РћРЎ Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"РљР°РєРѕР№ РіРѕСЂРѕРґ Р‘РµР»Р°СЂСѓСЃРё Р±С‹Р» СѓРґРѕСЃС‚РѕРµРЅ Р·РІР°РЅРёСЏ В«Р“РѕСЂРѕРґ-РіРµСЂРѕР№В» РЅР°СЂР°РІРЅРµ СЃ РњРѕСЃРєРІРѕР№ Рё Р›РµРЅРёРЅРіСЂР°РґРѕРј?",
        options:["Р“РѕРјРµР»СЊ","Р’РёС‚РµР±СЃРє","РњРёРЅСЃРє","Р‘СЂРµСЃС‚"],
        correctIndex:2,
        encrypted:"рџЏ™",
        answer:"e6f182a0288b446b693099008c4747ce928497399ea20a4c89dfef0be894662c",
        hint:"Р­С‚Рѕ СЃС‚РѕР»РёС†Р° Р‘РµР»Р°СЂСѓСЃРё. Р‘С‹Р»Р° РїРѕР»РЅРѕСЃС‚СЊСЋ СЂР°Р·СЂСѓС€РµРЅР° РІРѕ РІСЂРµРјСЏ РѕРєРєСѓРїР°С†РёРё.",
        fact:"РњРёРЅСЃРє РїРѕР»СѓС‡РёР» Р·РІР°РЅРёРµ В«Р“РѕСЂРѕРґ-РіРµСЂРѕР№В» РІ 1974 РіРѕРґСѓ. Р’Рѕ РІСЂРµРјСЏ РѕРєРєСѓРїР°С†РёРё Р±С‹Р»Рѕ СѓРЅРёС‡С‚РѕР¶РµРЅРѕ 80% Р·РґР°РЅРёР№ РіРѕСЂРѕРґР°.", points:120 },
      { type:"math", typeLabel:"Р РђРЎР§РЃРў Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"Р‘СЂРµСЃС‚СЃРєСѓСЋ РєСЂРµРїРѕСЃС‚СЊ Р·Р°С‰РёС‰Р°Р» РіР°СЂРЅРёР·РѕРЅ РѕРєРѕР»Рѕ 9000 С‡РµР»РѕРІРµРє РїСЂРѕС‚РёРІ 45000 РЅРµРјС†РµРІ. Р’Рѕ СЃРєРѕР»СЊРєРѕ СЂР°Р· РЅРµРјРµС†РєРёС… СЃРѕР»РґР°С‚ Р±С‹Р»Рѕ Р±РѕР»СЊС€Рµ?",
        encrypted:"45000 Р“В· 9000 = ?",
        answer:"ef2d127de37b942baad06145e54b0c619a1f22327b2ebbcfbec78f5564afe39d",
        hint:"45000 СЂР°Р·РґРµР»РёС‚СЊ РЅР° 9000. РЈРїСЂРѕСЃРёРј: 45 Г· 9 = 5.",
        fact:"РќРµСЃРјРѕС‚СЂСЏ РЅР° РїСЏС‚РёРєСЂР°С‚РЅРѕРµ РїСЂРµРІРѕСЃС…РѕРґСЃС‚РІРѕ РїСЂРѕС‚РёРІРЅРёРєР°, Р·Р°С‰РёС‚РЅРёРєРё Р‘СЂРµСЃС‚СЃРєРѕР№ РєСЂРµРїРѕСЃС‚Рё РґРµСЂР¶Р°Р»РёСЃСЊ РїРѕС‡С‚Рё РјРµСЃСЏС†.", points:140 },
      { type:"anagram", typeLabel:"РђРќРђР“Р РђРњРњРђ",
        task:"РџРµСЂРµСЃС‚Р°РІСЊ Р±СѓРєРІС‹ Рё РїРѕР»СѓС‡Рё РЅР°Р·РІР°РЅРёРµ РєСЂРµРїРѕСЃС‚Рё вЂ” СЃРёРјРІРѕР»Р° СЃС‚РѕР№РєРѕСЃС‚Рё, РїСЂРёРЅСЏРІС€РµР№ РїРµСЂРІС‹Р№ СѓРґР°СЂ 22 РёСЋРЅСЏ 1941:",
        encrypted:"РЎРўР•Р Р‘",
        answer:"1f357f38d0a3225db20815563c70bfe154832f7e0cd2cb79edde8a1e5038a85b",
        hint:"5 Р±СѓРєРІ. Р“РѕСЂРѕРґ РЅР° СЂРµРєРµ Р‘СѓРі, РЅР° Р·Р°РїР°РґРЅРѕР№ РіСЂР°РЅРёС†Рµ Р‘РµР»Р°СЂСѓСЃРё. Р‘-Р -Р•...",
        fact:"Р‘СЂРµСЃС‚СЃРєР°СЏ РєСЂРµРїРѕСЃС‚СЊ РґРµСЂР¶Р°Р»Р°СЃСЊ СЃ 22 РёСЋРЅСЏ РїРѕ 20 РёСЋР»СЏ 1941. РќР° СЃС‚РµРЅР°С… РЅР°С€Р»Рё РЅР°РґРїРёСЃРё: В«РЈРјРёСЂР°СЋ, РЅРѕ РЅРµ СЃРґР°СЋСЃСЊ!В»", points:150 },
      { type:"map", typeLabel:"РќРђР™Р”Р РќРђ РљРђР РўР•",
        task:"РќР° РєР°СЂС‚Рµ С‚Р°РїРЅРё РЅР° РіРѕСЂРѕРґ РЅР° СЃРµРІРµСЂРµ Р‘РµР»Р°СЂСѓСЃРё вЂ” РјРµСЃС‚Рѕ РІР°Р¶РЅРµР№С€РёС… РѕРїРµСЂР°С†РёР№ РїР°СЂС‚РёР·Р°РЅСЃРєРѕР№ РІРѕР№РЅС‹ РІ РіРѕРґС‹ Р’РћР’:",
        encrypted:"рџ—є",
        answer:"02502539349de7bcea28618412d857217fbf10a5533504d3af7361cbe66b66c1",
        mapCity:"Р’РРўР•Р‘РЎРљ",
        hint:"РЎРµРІРµСЂРѕ-РІРѕСЃС‚РѕРє Р‘РµР»Р°СЂСѓСЃРё, РЅР° СЂРµРєРµ Р—Р°РїР°РґРЅР°СЏ Р”РІРёРЅР°. Р§РµС‚РІС‘СЂС‚С‹Р№ РїРѕ РІРµР»РёС‡РёРЅРµ РіРѕСЂРѕРґ.",
        fact:"Р’РёС‚РµР±СЃРє РѕРєРєСѓРїРёСЂРѕРІР°РЅ 11 РёСЋР»СЏ 1941. РћСЃРІРѕР±РѕР¶РґС‘РЅ 26 РёСЋРЅСЏ 1944 РІ С…РѕРґРµ РѕРїРµСЂР°С†РёРё В«Р‘Р°РіСЂР°С‚РёРѕРЅВ».", points:160 },
      { type:"caesar", typeLabel:"РЁРР¤Р  Р¦Р•Р—РђР РЇ", shift:3,
        task:"Р—Р°С€РёС„СЂРѕРІР°РЅРѕ РёРјСЏ РіР»Р°РІРЅРѕР№ РІРѕРµРЅРЅРѕР№ РѕРїРµСЂР°С†РёРё РѕСЃРІРѕР±РѕР¶РґРµРЅРёСЏ Р‘РµР»Р°СЂСѓСЃРё. РЎРґРІРёРі 3 РЅР°Р·Р°Рґ:",
        encrypted:"Р”Р“РЃРЈР“РҐР›РЎР ",
        answer:"32076f76c7be7c437b41289f10c44cce9c107523aee32428024297703302ae2d",
        hint:"Р’С‹С‡С‚Рё 3: Р”(5)-3=2=Р‘вњ“, Р“(4)-3=1=Рђвњ“, РЃ(7)-3=4=Р“вњ“, РЈ(21)-3=18=Р вњ“, Р“-3=Рђвњ“, РҐ(24)-3=21=Рў? РќРµС‚ вЂ” Рў=20, РҐ=24-3=21=РЈ? Р”Р“РЃРЈР“РҐР›РЎР -3=Р‘РђР“Р РђРўРРћРќ!",
        fact:"РћРїРµСЂР°С†РёСЏ В«Р‘Р°РіСЂР°С‚РёРѕРЅВ» (РёСЋРЅСЊвЂ“Р°РІРіСѓСЃС‚ 1944) вЂ” РєСЂСѓРїРЅРµР№С€Р°СЏ РЅР°СЃС‚СѓРїР°С‚РµР»СЊРЅР°СЏ РѕРїРµСЂР°С†РёСЏ Р’РњР’.", points:170 },
      { type:"quiz", typeLabel:"Р’РћРџР РћРЎ Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"РЎРєРѕР»СЊРєРѕ РјРёСЂРЅС‹С… Р¶РёС‚РµР»РµР№ Рё СЃРѕР»РґР°С‚ РїРѕС‚РµСЂСЏР»Р° Р‘РµР»Р°СЂСѓСЃСЊ РІ РіРѕРґС‹ Р’С‚РѕСЂРѕР№ РјРёСЂРѕРІРѕР№ РІРѕР№РЅС‹?",
        options:["РљР°Р¶РґС‹Р№ 10-Р№","РљР°Р¶РґС‹Р№ 5-Р№","РљР°Р¶РґС‹Р№ 3-Р№","РљР°Р¶РґС‹Р№ 2-Р№"],
        correctIndex:2,
        encrypted:"рџ“Љ",
        answer:"4e07408562bedb8b60ce05c1decfe3ad16b72230967de01f640b7e4729b49fce",
        hint:"Р‘РµР»Р°СЂСѓСЃСЊ вЂ” РѕРґРЅР° РёР· РЅР°РёР±РѕР»РµРµ РїРѕСЃС‚СЂР°РґР°РІС€РёС… СЃС‚СЂР°РЅ. РљР°Р¶РґС‹Р№ С‚СЂРµС‚РёР№...",
        fact:"Р‘РµР»Р°СЂСѓСЃСЊ РїРѕС‚РµСЂСЏР»Р° РєР°Р¶РґРѕРіРѕ С‚СЂРµС‚СЊРµРіРѕ Р¶РёС‚РµР»СЏ вЂ” РѕРєРѕР»Рѕ 3 РјРёР»Р»РёРѕРЅРѕРІ С‡РµР»РѕРІРµРє. Р­С‚Рѕ СЃР°РјС‹Рµ РІС‹СЃРѕРєРёРµ РїРѕС‚РµСЂРё СЃСЂРµРґРё РІСЃРµС… СЃРѕРІРµС‚СЃРєРёС… СЂРµСЃРїСѓР±Р»РёРє РІ РїРµСЂРµСЃС‡С‘С‚Рµ РЅР° РґРѕР»СЋ РЅР°СЃРµР»РµРЅРёСЏ.", points:200 }
    ]
  },

  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  //  Р“Р›РђР’Рђ VI вЂ” Р”РћР РћР“Рђ Рљ РџРћР‘Р•Р”Р•
  // в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
  {
    id:6, title:"Р”РћР РћР“Рђ Рљ РџРћР‘Р•Р”Р•", subtitle:"Р“Р»Р°РІР° VI",
    place:"Р‘Р•Р›РђР РЈРЎР¬ В· Р‘Р•Р Р›РРќ В· 1945", stamp:"рџЊџ",
    meta:"Р”Р°С‚Р°: 09.05.1945\\nРњРµСЃС‚Рѕ: Р‘РµСЂР»РёРЅ\\nРЎС‚Р°С‚СѓСЃ: Р¤РРќРђР›",
    briefing:`РџРѕСЃР»РµРґРЅСЏСЏ РіР»Р°РІР°. РђРїСЂРµР»СЊ 1945. РЎРѕРІРµС‚СЃРєРёРµ РІРѕР№СЃРєР° РёРґСѓС‚ РЅР° Р‘РµСЂР»РёРЅ. РЎСЂРµРґРё Р±РѕР№С†РѕРІ вЂ” СЃРѕР»РґР°С‚С‹ РёР· Р‘РµР»Р°СЂСѓСЃРё, РїРµСЂРµР¶РёРІС€РёРµ РѕРєРєСѓРїР°С†РёСЋ, РїРѕС‚РµСЂСЏРІС€РёРµ СЂРѕРґРЅС‹С…. РћРЅРё РЅРµСЃСѓС‚ РџРѕР±РµРґСѓ. Р”РѕРєР°Р¶Рё С‡С‚Рѕ С‚С‹ РґРѕСЃС‚РѕРёРЅ Р·РІР°РЅРёСЏ РњР°СЂС€Р°Р»Р° РџРѕР±РµРґС‹.`,
    mission:"РџСЂРѕР№РґРё С„РёРЅР°Р»СЊРЅС‹Р№ СЌРєР·Р°РјРµРЅ вЂ” 6 Р·Р°РґР°РЅРёР№ Рѕ РџРѕР±РµРґРµ",
    videoUrl:"https://www.youtube.com/embed/dL8DM0PfRvQ",
    ciphers:[
      { type:"math", typeLabel:"Р РђРЎР§РЃРў Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"Р’РѕР№РЅР° РґР»РёР»Р°СЃСЊ СЃ 22 РёСЋРЅСЏ 1941 РїРѕ 9 РјР°СЏ 1945. РЎРєРѕР»СЊРєРѕ РїРѕР»РЅС‹С… Р»РµС‚ РїСЂРѕРґРѕР»Р¶Р°Р»Р°СЃСЊ РІРѕР№РЅР°?",
        encrypted:"1941 в†’ 1945 = ? Р»РµС‚",
        answer:"4b227777d4dd1fc61c6f884f48641d02b4d121d3fd328cb08b5531fcacdabf8a",
        hint:"1945 РјРёРЅСѓСЃ 1941 = 4 РіРѕРґР°. Р§РµС‚С‹СЂРµ РґРѕР»РіРёС… РіРѕРґР°.",
        fact:"Р’РµР»РёРєР°СЏ РћС‚РµС‡РµСЃС‚РІРµРЅРЅР°СЏ РІРѕР№РЅР° РґР»РёР»Р°СЃСЊ 3 РіРѕРґР°, 10 РјРµСЃСЏС†РµРІ Рё 18 РґРЅРµР№ вЂ” СЃ 22 РёСЋРЅСЏ 1941 РїРѕ 9 РјР°СЏ 1945.", points:120 },
      { type:"morse", typeLabel:"РђР—Р‘РЈРљРђ РњРћР Р—Р•",
        task:"РџРµСЂРІРѕРµ СЃР»РѕРІРѕ РїРѕР±РµРґРЅРѕР№ СЂР°РґРёРѕРіСЂР°РјРјС‹. Р Р°СЃС€РёС„СЂСѓР№:",
        encrypted:".../.-.../.-/.--/.-",
        answer:"9890080953fe693654e3db2bece9ddc1e28e86659ece82fd3a44e9904809a94f",
        hint:"РЎ=..., Р›=.-.. , Рђ=.- , Р’=.-- , Рђ=.- в†’ РЎ/Р›/Рђ/Р’/Рђ = РЎР›РђР’Рђ вњ“",
        fact:"9 РјР°СЏ 1945 РІ 00:43 РїРѕ РјРѕСЃРєРѕРІСЃРєРѕРјСѓ РІСЂРµРјРµРЅРё Р±С‹Р» РїРѕРґРїРёСЃР°РЅ РђРєС‚ Рѕ РєР°РїРёС‚СѓР»СЏС†РёРё Р“РµСЂРјР°РЅРёРё.", points:130 },
      { type:"atbash", typeLabel:"РЁРР¤Р  РђРўР‘РђРЁ",
        task:"РџРѕСЃР»РµРґРЅРµРµ СЃР»РѕРІРѕ РІ РїСЂРёРєР°Р·Рµ. Р Р°СЃС€РёС„СЂСѓР№:",
        encrypted:"РўР¦Рћ",
        answer:"0dd0a827bdd580e9eaa343e55ae702740b9f2346d8b6699c5a8669ea0b64571f",
        hint:"РђС‚Р±Р°С€: Рў(20)в†’34-20=14=Рњвњ“, Р¦(24)в†’34-24=10=Рвњ“, Рћ(16)в†’34-16=18=Р вњ“. РўР¦Рћ в†’ РњРР !",
        fact:"РЎР»РѕРІРѕ В«РњРР В» вЂ” СЃР°РјРѕРµ РґРѕР»РіРѕР¶РґР°РЅРЅРѕРµ СЃР»РѕРІРѕ РґР»СЏ РјРёР»Р»РёРѕРЅРѕРІ Р»СЋРґРµР№, РїРµСЂРµР¶РёРІС€РёС… РІРѕР№РЅСѓ.", points:150 },
      { type:"anagram", typeLabel:"РђРќРђР“Р РђРњРњРђ",
        task:"РџРµСЂРµСЃС‚Р°РІСЊ Р±СѓРєРІС‹ вЂ” РїРѕР»СѓС‡РёС€СЊ РЅР°Р·РІР°РЅРёРµ РіРѕСЂРѕРґР°, РіРґРµ Р±С‹Р»Р° РїРѕРґРїРёСЃР°РЅР° РєР°РїРёС‚СѓР»СЏС†РёСЏ Р“РµСЂРјР°РЅРёРё:",
        encrypted:"Р›РРќР•Р Р‘",
        answer:"06b21234e80f4cbe0bbcca7195551e94577a26f59f43bb4e78ea22aa3804b525",
        hint:"6 Р±СѓРєРІ. РЎС‚РѕР»РёС†Р° Р“РµСЂРјР°РЅРёРё. Р‘-Р•-Р ...",
        fact:"8 РјР°СЏ 1945 РІ Р‘РµСЂР»РёРЅРµ Р±С‹Р» РїРѕРґРїРёСЃР°РЅ РђРєС‚ Рѕ Р±РµР·РѕРіРѕРІРѕСЂРѕС‡РЅРѕР№ РєР°РїРёС‚СѓР»СЏС†РёРё Р“РµСЂРјР°РЅРёРё. Р”Р»СЏ РЎРЎРЎР  РґР°С‚Р° РїРѕР±РµРґС‹ вЂ” 9 РјР°СЏ.", points:160 },
      { type:"quiz", typeLabel:"Р’РћРџР РћРЎ Р РђР—Р’Р•Р”Р§РРљРђ",
        task:"РљР°РєРѕРіРѕ С‡РёСЃР»Р° РєР°Р¶РґС‹Р№ РіРѕРґ РІ Р‘РµР»Р°СЂСѓСЃРё Рё Р РѕСЃСЃРёРё РѕС‚РјРµС‡Р°СЋС‚ Р”РµРЅСЊ РџРѕР±РµРґС‹?",
        options:["7 РјР°СЏ","8 РјР°СЏ","9 РјР°СЏ","10 РјР°СЏ"],
        correctIndex:2,
        encrypted:"рџ“…",
        answer:"131f1b7eaf25d645a5a1a1fd3c35330be40dec081cd032ace5cfd7143ef011a7",
        hint:"РђРєС‚ Р±С‹Р» РїРѕРґРїРёСЃР°РЅ РЅРѕС‡СЊСЋ СЃ 8 РЅР° 9 РјР°СЏ РїРѕ РјРѕСЃРєРѕРІСЃРєРѕРјСѓ РІСЂРµРјРµРЅРё.",
        fact:"9 РјР°СЏ вЂ” РіР»Р°РІРЅС‹Р№ РїСЂР°Р·РґРЅРёРє РґР»СЏ Р‘РµР»Р°СЂСѓСЃРё. Р’ СЃС‚СЂР°РЅРµ РµРіРѕ РѕС‚РјРµС‡Р°СЋС‚ СЃ РѕСЃРѕР±С‹Рј С‚РѕСЂР¶РµСЃС‚РІРѕРј вЂ” РєР°Р¶РґС‹Р№ С‚СЂРµС‚РёР№ Р±РµР»РѕСЂСѓСЃ РїРѕРіРёР± РІ РІРѕР№РЅСѓ.", points:170 },
      { type:"map", typeLabel:"РќРђР™Р”Р РќРђ РљРђР РўР•",
        task:"РќР° РєР°СЂС‚Рµ РЅР°Р№РґРё РіРѕСЂРѕРґ-СЃРѕСЃРµРґ РҐРѕР№РЅРёРєРѕРІ РІ Р“РѕРјРµР»СЊСЃРєРѕР№ РѕР±Р»Р°СЃС‚Рё, СЂР°СЃРїРѕР»РѕР¶РµРЅРЅС‹Р№ РЅР° СЂРµРєРµ РџСЂРёРїСЏС‚СЊ:",
        encrypted:"рџ—є",
        answer:"131f1b7eaf25d645a5a1a1fd3c35330be40dec081cd032ace5cfd7143ef011a7",
        mapCity:"РќРђР РћР’Р›РЇ",
        hint:"Р®Рі Р“РѕРјРµР»СЊСЃРєРѕР№ РѕР±Р»Р°СЃС‚Рё, СЂСЏРґРѕРј СЃ РҐРѕР№РЅРёРєР°РјРё, РЅР° Р±РµСЂРµРіСѓ СЂРµРєРё РџСЂРёРїСЏС‚СЊ.",
        fact:"РќР°СЂРѕРІР»СЏ Рё РҐРѕР№РЅРёРєРё вЂ” РіРѕСЂРѕРґР°-СЃРѕСЃРµРґРё РІ Р“РѕРјРµР»СЊСЃРєРѕР№ РѕР±Р»Р°СЃС‚Рё. РћР±Р° СЂР°СЃРїРѕР»РѕР¶РµРЅС‹ РІ Р·РѕРЅРµ РІР»РёСЏРЅРёСЏ Р§РµСЂРЅРѕР±С‹Р»СЊСЃРєРѕР№ РєР°С‚Р°СЃС‚СЂРѕС„С‹ 1986 РіРѕРґР°.", points:200 }
    ]
  }
];

// РќРµ С…СЂР°РЅРёРј РїСЂР°РІРёР»СЊРЅС‹Рµ РѕС‚РІРµС‚С‹ РІ РєР»РёРµРЅС‚СЃРєРѕРј РєРѕРґРµ.
const MORSE_TABLE = {
  'Рђ':'.-','Р‘':'-...','Р’':'.--','Р“':'--.','Р”':'-..','Р•':'.','Р–':'...-',
  'Р—':'--..','Р':'..','Р™':'.---','Рљ':'-.-','Р›':'.-..','Рњ':'--','Рќ':'-.',
  'Рћ':'---','Рџ':'.--.','Р ':'.-.','РЎ':'...','Рў':'-','РЈ':'..-','Р¤':'..-.',
  'РҐ':'....','Р¦':'-.-.','Р§':'---.','РЁ':'----','Р©':'--.-','РЄ':'--.--',
  'Р«':'-.--','Р¬':'-..-','Р­':'..-..','Р®':'..--','РЇ':'.-.-'
};
const RU_ALPHA = 'РђР‘Р’Р“Р”Р•РЃР–Р—РР™РљР›РњРќРћРџР РЎРўРЈР¤РҐР¦Р§РЁР©РЄР«Р¬Р­Р®РЇ';

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  TELEGRAM INTEGRATION
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
let tg = null, tgUser = null, tgInitLB = [], tgInitMe = null, tgOpenChapters = null;
// Р Р°СЃРїРёСЃР°РЅРёРµ РѕС‚РєСЂС‹С‚РёСЏ РіР»Р°РІ (РґР»СЏ С‚Р°Р№РјРµСЂРѕРІ): [{id, open, open_at}, ...]
let tgChapterSchedule = [];
try {
  tg = window.Telegram && window.Telegram.WebApp ? window.Telegram.WebApp : null;
  if (tg) {
    tg.ready();
    tg.expand();
    tgUser = tg.initDataUnsafe && tg.initDataUnsafe.user ? tg.initDataUnsafe.user : null;

    const _parsePayload = (parsed) => {
      if (!parsed) return;
      if (parsed.lb)  tgInitLB = parsed.lb;
      if (parsed.me)  tgInitMe = parsed.me;
      if (parsed.open) tgOpenChapters = new Set(parsed.open);
      if (parsed.sync_url) window._syncUrl = parsed.sync_url;
      if (parsed.chapter_schedule) tgChapterSchedule = parsed.chapter_schedule;
      if (parsed.me && typeof parsed.me.reset_token === 'number' && parsed.me.reset_token > 0) {
        _serverResetToken = parsed.me.reset_token;
      }
      if (typeof parsed.reset_token === 'number' && parsed.reset_token > 0) {
        _serverResetToken = parsed.reset_token;
      }
      // Р РѕР»СЊ: admin_mode С‚РѕР»СЊРєРѕ РґР»СЏ admin, tester_mode РґР»СЏ tester
      if (parsed.admin_mode  === true) window._adminMode  = true;
      if (parsed.tester_mode === true) window._testerMode = true;
      if (parsed.role) window._gameRole = parsed.role;
    };

    // РўР°Р±Р»РёС†Р° Р»РёРґРµСЂРѕРІ, РїСЂРѕРіСЂРµСЃСЃ Рё РѕС‚РєСЂС‹С‚С‹Рµ РіР»Р°РІС‹ РїРµСЂРµРґР°СЋС‚СЃСЏ С‡РµСЂРµР· startParam
    const sp = tg.initDataUnsafe && tg.initDataUnsafe.start_param ? tg.initDataUnsafe.start_param : null;
    if (sp) {
      try { _parsePayload(JSON.parse(decodeURIComponent(sp))); }
      catch(e) { console.warn('startParam parse error:', e); }
    }

    // Р¤РѕР»Р»Р±СЌРє: С‡РёС‚Р°РµРј РёР· URL query РїР°СЂР°РјРµС‚СЂРѕРІ (РµСЃР»Рё start_param РїСѓСЃС‚РѕР№)
    if (!window._syncUrl) {
      try {
        const urlParams = new URLSearchParams(window.location.search);
        const rawParam = urlParams.get('tgWebAppStartParam');
        if (rawParam) _parsePayload(JSON.parse(decodeURIComponent(rawParam)));
      } catch(e) { console.warn('URL param parse error:', e); }
    }

    console.log('рџ”— sync_url:', window._syncUrl || 'РќР• РџРћР›РЈР§Р•Рќ');
    console.log('рџ‘¤ user:', tgUser ? tgUser.id : null, tgUser ? tgUser.first_name : null);
    console.log('рџЋ­ role:', window._gameRole, '| adminMode:', window._adminMode, '| testerMode:', window._testerMode);
  }
} catch(e) {}

function getTgUserId() {
  if (tgUser && tgUser.id) return String(tgUser.id);
  return null;
}
function getTgUserName() {
  if (tgUser) return tgUser.first_name + (tgUser.last_name ? ' ' + tgUser.last_name : '');
  return 'Р Р°Р·РІРµРґС‡РёРє';
}
function getTgInitDataRaw() {
  if (tg && typeof tg.initData === 'string') return tg.initData;
  return '';
}
function _applySyncResponse(result, forceServerState = false) {
  if (!result || typeof result !== 'object') return;
  ensureSecretState();
  const prevResetToken = Math.max(_serverResetToken || 0, _getStoredResetToken());
  if (typeof result.db_reset_token === 'number' && result.db_reset_token > 0) {
    _serverResetToken = result.db_reset_token;
    _storeResetToken(_serverResetToken);
  }

  const secretChanged = _applySecretStateFromServer(
    result.secret_mode,
    result.secret_summary,
    result.secret_missions
  );
  _applySecretAwards(result.secret_awards, result.secret_awarded_points);
  if (secretChanged) {
    try { checkAchievements({}); } catch (_) {}
  }
  const hasRefSummary = !!(result.ref_summary && typeof result.ref_summary === 'object');
  const hasRefAgents = Array.isArray(result.ref_agents);
  if (hasRefSummary) state.referralSummary = normalizeReferralSummary(result.ref_summary);
  if (hasRefAgents) state.referralAgents = normalizeReferralAgents(result.ref_agents);
  const referralChanged = hasRefSummary || hasRefAgents;

  const hasDbSnapshot = (typeof result.db_score === 'number') || (typeof result.db_completed === 'number');
  if (!hasDbSnapshot) {
    if (secretChanged || referralChanged) {
      saveState();
      _refreshCurrentTabAfterSync();
    }
    return;
  }

  const dbScore = Number(result.db_score || 0);
  const dbCompleted = Number(result.db_completed || 0);
  const tokenChanged = (_serverResetToken > 0) && (_serverResetToken !== prevResetToken);
  const serverRequestedReset = (result && (result.reset_required === true || result.stale === true));
  if (serverRequestedReset && tokenChanged && dbScore === 0 && dbCompleted === 0 && _hasLocalProgressData()) {
    _wipeLocalProgressForServerReset();
    saveState();
    _refreshCurrentTabAfterSync();
    return;
  }

  const mergeRes = _applyServerProgress(dbScore, dbCompleted, state.gameOver, !!forceServerState);
  if (mergeRes.applied || secretChanged || referralChanged) {
    saveState();
    _refreshCurrentTabAfterSync();
  }
  if (Number(result.server_penalty_applied || 0) > 0) {
    showPenaltyNotice(`РЎРµСЂРІРµСЂ РїСЂРёРјРµРЅРёР» С€С‚СЂР°С„: -${Number(result.server_penalty_applied || 0)} РѕС‡`);
  }
  const hasExplicitRefBonusFields = Object.prototype.hasOwnProperty.call(result, 'ref_bonus_awarded_inviter')
    || Object.prototype.hasOwnProperty.call(result, 'ref_bonus_awarded_invitee');
  const inviterBonus = Number(hasExplicitRefBonusFields
    ? (result.ref_bonus_awarded_inviter || 0)
    : (result.ref_bonus_awarded || 0));
  const inviteeBonus = Number(result.ref_bonus_awarded_invitee || 0);
  if (inviterBonus > 0 || inviteeBonus > 0) {
    const parts = [];
    if (inviterBonus > 0) parts.push(`агентская сеть +${inviterBonus}`);
    if (inviteeBonus > 0) parts.push(`ваш бонус +${inviteeBonus}`);
    showToast(`Реферальный бонус: ${parts.join(' · ')}`);
  }
}

async function sendResultToBot(data) {
  ensureSecretState();
  data.completed = _stateCompletedCount();
  data.total_score = state.totalScore;
  data.achievement_count = Object.keys(state.achievements || {}).length;
  data.achievement_pts = state.achievementPts || 0;
  data.chapter_idx = Number(state.chapter || 0);
  data.cipher_idx = Number(state.cipherIdx || 0);
  data.chapter_in_progress = !!state._chapterInProgress;
  data.user_id = getTgUserId();
  _appendSecretSyncPayload(data);
  if (_serverResetToken > 0) data.reset_token = _serverResetToken;

  if (!data.init_data) {
    const initDataRaw = getTgInitDataRaw();
    if (initDataRaw) data.init_data = initDataRaw;
  }

  const syncUrl = window._syncUrl;
  if (syncUrl && data.user_id) {
    try {
      const resp = await fetch(syncUrl, {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(data)
      });
      if (resp.ok) {
        const result = await resp.json().catch(() => ({}));
        if (result.banned) {
          document.body.innerHTML = '<div style="display:flex;flex-direction:column;align-items:center;justify-content:center;height:100vh;background:#0d0b08;color:#fff;text-align:center;padding:32px;font-family:sans-serif"><div style="font-size:64px;margin-bottom:16px">рџљ«</div><div style="font-size:22px;font-weight:700;color:#ffe033;margin-bottom:12px">Access blocked</div><div style="font-size:15px;color:rgba(255,255,255,.6);max-width:280px">Your account was blocked by administrator.</div></div>';
          try { localStorage.removeItem(storageKey()); } catch(e2) {}
          return;
        }
        _applySyncResponse(result, !!result.stale || !!result.force_state);
        _lastSyncedScore = Number(state.totalScore || data.total_score || 0);
        _lastSyncedCompleted = _stateCompletedCount();
        _lastSyncedSignature = [
          Number(state.totalScore || 0),
          _lastSyncedCompleted,
          _stateCurrentChapterId(),
          Number(state.chapterScore || 0),
          Number(state.cipherIdx || 0),
          Number(state.lives || 0),
          state.gameOver ? 1 : 0,
          Object.keys(state.achievements || {}).length,
          Number(state.achievementPts || 0),
          state.secretMode,
          normalizeSecretSummary(state.secretSummary).completed,
          normalizeSecretSummary(state.secretSummary).bonus_points
        ].join('|');
        return;
      }
      console.warn('game_sync HTTP error:', resp.status);
    } catch(e) {
      console.warn('game_sync fetch error:', e);
    }
  }

  if (tg && tg.sendData && tg.initData) {
    try {
      tg.sendData(JSON.stringify(data));
      return;
    } catch(e) {
      console.warn('tg.sendData failed:', e);
    }
  }

  try {
    const pending = JSON.parse(localStorage.getItem('pending_results') || '[]');
    pending.push({ ...data, ts: Date.now() });
    localStorage.setItem('pending_results', JSON.stringify(pending.slice(-10)));
  } catch(e) {}
}

async function flushPendingResults() {
  const syncUrl = window._syncUrl;
  // РџСЂРѕР±СѓРµРј HTTP POST РґР»СЏ СЂР°РЅРµРµ РЅРµ РѕС‚РїСЂР°РІР»РµРЅРЅС‹С…
  if (syncUrl && getTgUserId()) {
    try {
      const pending = JSON.parse(localStorage.getItem('pending_results') || '[]');
      if (!pending.length) return;
      const last = pending[pending.length - 1];
      last.user_id = getTgUserId();
      if (_serverResetToken > 0) last.reset_token = _serverResetToken;
      if (!last.init_data) {
        const initDataRaw = getTgInitDataRaw();
        if (initDataRaw) last.init_data = initDataRaw;
      }
      const resp = await fetch(syncUrl, {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(last)
      });
      if (resp.ok) {
        const result = await resp.json().catch(() => ({}));
        _applySyncResponse(result, !!result.stale || !!result.force_state);
        localStorage.removeItem('pending_results');
        console.log('flushPendingResults: HTTP POST OK');
        return;
      }
    } catch(e) {}
  }
  // Р¤РѕР»Р»Р±СЌРє: sendData
  if (!tg || !tg.sendData) return;
  try {
    const pending = JSON.parse(localStorage.getItem('pending_results') || '[]');
    if (!pending.length) return;
    const last = pending[pending.length - 1];
    tg.sendData(JSON.stringify(last));
    localStorage.removeItem('pending_results');
  } catch(e) {}
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РўРћРЎРў-РЈР’Р•Р”РћРњР›Р•РќРРЇ (РјР°Р»РµРЅСЊРєРёРµ РІСЃРїР»С‹РІР°С€РєРё)
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function showToast(text, duration = 2500) {
  const old = document.getElementById('game-toast');
  if (old) old.remove();
  const toast = document.createElement('div');
  toast.id = 'game-toast';
  toast.textContent = text;
  toast.style.cssText = `
    position:fixed; bottom:80px; left:50%; transform:translateX(-50%);
    background:rgba(20,17,8,.95); border:1px solid rgba(255,224,51,.3);
    color:#ffe033; padding:8px 20px; border-radius:20px;
    font-family:var(--head); font-size:12px; letter-spacing:.06em;
    z-index:999; pointer-events:none; opacity:0;
    transition:opacity .3s ease;
  `;
  document.body.appendChild(toast);
  requestAnimationFrame(() => { toast.style.opacity = '1'; });
  setTimeout(() => {
    toast.style.opacity = '0';
    setTimeout(() => toast.remove(), 300);
  }, duration);
}

function showPenaltyNotice(text, duration = 5600) {
  const old = document.getElementById('game-penalty-notice');
  if (old) old.remove();

  const notice = document.createElement('div');
  notice.id = 'game-penalty-notice';
  notice.style.cssText = `
    position:fixed;
    left:50%;
    bottom:96px;
    transform:translate(-50%, 12px);
    width:min(92vw, 560px);
    background:linear-gradient(160deg, rgba(49,17,8,.98), rgba(20,12,8,.98));
    border:1px solid rgba(255,128,93,.62);
    border-left:4px solid #ff835f;
    box-shadow:0 18px 48px rgba(0,0,0,.55);
    border-radius:12px;
    padding:12px 14px;
    z-index:10020;
    pointer-events:none;
    opacity:0;
    transition:opacity .25s ease, transform .25s ease;
  `;

  const title = document.createElement('div');
  title.textContent = 'вљ– РР—РњР•РќР•РќРР• РћР§РљРћР’';
  title.style.cssText = `
    font-family:var(--head);
    font-size:clamp(12px, 2.8vw, 15px);
    letter-spacing:.08em;
    color:#ffb69f;
    margin-bottom:6px;
  `;

  const body = document.createElement('div');
  body.textContent = text;
  body.style.cssText = `
    font-family:var(--head);
    font-size:clamp(15px, 3.8vw, 20px);
    line-height:1.35;
    letter-spacing:.03em;
    color:#fff3d8;
  `;

  notice.appendChild(title);
  notice.appendChild(body);
  document.body.appendChild(notice);
  requestAnimationFrame(() => {
    notice.style.opacity = '1';
    notice.style.transform = 'translate(-50%, 0)';
  });

  setTimeout(() => {
    notice.style.opacity = '0';
    notice.style.transform = 'translate(-50%, 12px)';
    setTimeout(() => notice.remove(), 320);
  }, Math.max(2500, Number(duration || 0)));
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РђР’РўРћРЎРРќРҐР РћРќРР—РђР¦РРЇ (С„РѕРЅРѕРІР°СЏ, Р±РµР· UI)
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
let _syncInFlight = false;

async function autoSync(showNotification = false, force = false) {
  const syncUrl = window._syncUrl;
  const uid = getTgUserId();
  if (!syncUrl || !uid) return;
  ensureSecretState();
  const completed = _stateCompletedCount();
  const chapterId = _stateCurrentChapterId();
  const chapterScore = Number(state.chapterScore || 0);
  const achievementCount = Object.keys(state.achievements || {}).length;
  const achievementPts = Number(state.achievementPts || 0);
  const secretSummary = normalizeSecretSummary(state.secretSummary);
  const syncSignature = [
    Number(state.totalScore || 0),
    completed,
    chapterId,
    chapterScore,
    Number(state.cipherIdx || 0),
    Number(state.lives || 0),
    state.gameOver ? 1 : 0,
    achievementCount,
    achievementPts,
    state.secretMode,
    secretSummary.completed,
    secretSummary.bonus_points
  ].join('|');
  if (!force && syncSignature === _lastSyncedSignature) return;
  if (_syncInFlight) return;
  _syncInFlight = true;

  const data = {
    type: 'sync',
    chapter: chapterId,
    score: chapterScore,
    total_score: Number(state.totalScore || 0),
    completed: completed,
    game_over: state.gameOver || false,
    achievement_count: achievementCount,
    achievement_pts: achievementPts,
    chapter_idx: Number(state.chapter || 0),
    cipher_idx: Number(state.cipherIdx || 0),
    chapter_in_progress: !!state._chapterInProgress,
    user_id: uid,
  };
  _appendSecretSyncPayload(data);
  const initDataRaw = getTgInitDataRaw();
  if (initDataRaw) data.init_data = initDataRaw;
  if (_serverResetToken > 0) data.reset_token = _serverResetToken;

  try {
    const resp = await fetch(syncUrl, {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify(data)
    });
    if (resp.ok) {
      const result = await resp.json().catch(() => ({}));
      if (result.stale) {
        _applySyncResponse(result, true);
      } else {
        _applySyncResponse(result, !!result.force_state);
      }
      _lastSyncedScore = Number(state.totalScore || 0);
      _lastSyncedCompleted = _stateCompletedCount();
      _lastSyncedSignature = [
        Number(state.totalScore || 0),
        _lastSyncedCompleted,
        _stateCurrentChapterId(),
        Number(state.chapterScore || 0),
        Number(state.cipherIdx || 0),
        Number(state.lives || 0),
        state.gameOver ? 1 : 0,
        Object.keys(state.achievements || {}).length,
        Number(state.achievementPts || 0),
        state.secretMode,
        normalizeSecretSummary(state.secretSummary).completed,
        normalizeSecretSummary(state.secretSummary).bonus_points
      ].join('|');
      console.log('autoSync OK:', state.totalScore, 'pts,', _lastSyncedCompleted, 'chapters');
      if (showNotification) showToast('Progress saved');
    }
  } catch(e) {
    console.warn('autoSync error:', e);
  } finally {
    _syncInFlight = false;
  }
}

// РђРІС‚РѕСЃРёРЅРє РїСЂРё СЃРІРѕСЂР°С‡РёРІР°РЅРёРё / РїРµСЂРµРєР»СЋС‡РµРЅРёРё РІРєР»Р°РґРєРё
document.addEventListener('visibilitychange', () => {
  if (!document.hidden) {
    fetchAndApplyState();
    return;
  }
  saveState();
  autoSync(false, true);
  _sendSyncBeaconIfPossible();
});

// РђРІС‚РѕСЃРёРЅРє РїСЂРё Р·Р°РєСЂС‹С‚РёРё
window.addEventListener('beforeunload', () => {
  saveState();
  _sendSyncBeaconIfPossible();
});

window.addEventListener('pagehide', () => {
  saveState();
  _sendSyncBeaconIfPossible();
});

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РЎРћРЎРўРћРЇРќРР• вЂ” РїСЂРёРІСЏР·Р°РЅРѕ Рє Telegram user_id
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
// РЎС‚СЂСѓРєС‚СѓСЂР° state:
// {
//   lives: 5,               // Р¶РёР·РЅРё РІ С‚РµРєСѓС‰РµР№ РіР»Р°РІРµ (СЃР±СЂР°СЃС‹РІР°СЋС‚СЃСЏ РїСЂРё СЃС‚Р°СЂС‚Рµ РіР»Р°РІС‹)
//   chapter: 0,             // С‚РµРєСѓС‰Р°СЏ РіР»Р°РІР° (РёРЅРґРµРєСЃ)
//   cipherIdx: 0,           // С‚РµРєСѓС‰РёР№ С€РёС„СЂ
//   startTime: 0,           // РІСЂРµРјСЏ РЅР°С‡Р°Р»Р° С€РёС„СЂР° (ms)
//   chapterScore: 0,        // РѕС‡РєРё С‚РµРєСѓС‰РµР№ РіР»Р°РІС‹
//   totalScore: 0,          // СЃСѓРјРјР°СЂРЅС‹Рµ РѕС‡РєРё
//   completedChapters: {},  // {1: true, 2: true, ...} вЂ” РїСЂРѕР№РґРµРЅРЅС‹Рµ РіР»Р°РІС‹
//   chapterScores: {},      // {1: 340, 2: 410, ...}
//   gameOver: false,        // РІСЃРµ 4 РіР»Р°РІС‹ РїСЂРѕР№РґРµРЅС‹
//   leaderboard: []         // Р»РѕРєР°Р»СЊРЅС‹Р№ РєСЌС€ С‚Р°Р±Р»РёС†С‹
// }

const DEFAULT_STATE = () => ({
  lives: 5, chapter: 0, cipherIdx: 0, startTime: 0,
  chapterScore: 0, totalScore: 0,
  completedChapters: {}, chapterScores: {},
  streak: 0, retryPenalty: false,
  chapterFailCounts: {}, chapterStats: {},
  _chapterInProgress: false,
  _awaitingNextCipher: false,
  _pendingChapterFinish: false,
  gameOver: false, leaderboard: [],
  adminMode: false,
  testerMode: false,   // рџ§Є С‚РµСЃС‚РёСЂРѕРІС‰РёРє вЂ” РІСЃРµ РіР»Р°РІС‹ РѕС‚РєСЂС‹С‚С‹, 5 Р¶РёР·РЅРµР№, РЅРµ РІ СЂРµР№С‚РёРЅРіРµ
  gameRole: 'player',  // 'admin' | 'tester' | 'player'
  achievements: {}, achievementPts: 0,
  referralSummary: {
    invited_count: 0,
    active_count: 0,
    rewarded_chapters: 0,
    bonus_points: 0,
    inviter_percent: 0,
    invitee_percent: 1,
    invitee_bonus_points: 0,
    referrer_id: 0
  },
  referralAgents: [],
  secretMode: 'none',
  secretSummary: { completed: 0, total: 15, bonus_points: 0 },
  secretMissions: [],
  _secretTokenCursor: 0,
  _secretAnswerToken: 0,
  _secretBreakToken: 0,
  _secretLastAnswerElapsed: 0,
  _secretLastAnswerNoHint: false,
  _secretLastAnswerOneLife: false,
  _secretLastAnswerType: '',
  _secretLastAnswerStreak: 0,
  _secretLastAwardSig: '',
  artifacts: {}, solvedTypes: {}, solvedTypeCounts: {},
  _nextCipherTargetIdx: null,
  _manualRestartPenaltyPoints: 0
});

let state = DEFAULT_STATE();
const GAME_ADMIN_USERNAME = 'Yury_hud';
const GAME_ADMIN_CHAT_URL = 'https://t.me/' + GAME_ADMIN_USERNAME;
ensureSecretState();

function escapeHtml(value) {
  return String(value || '')
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}

function toSafeNonNegNumber(value) {
  const n = Number(value);
  return Number.isFinite(n) && n > 0 ? n : 0;
}

function normalizeReferralSummary(raw) {
  const src = raw && typeof raw === 'object' ? raw : {};
  return {
    invited_count: toSafeNonNegNumber(src.invited_count),
    active_count: toSafeNonNegNumber(src.active_count),
    rewarded_chapters: toSafeNonNegNumber(src.rewarded_chapters),
    bonus_points: toSafeNonNegNumber(src.bonus_points),
    inviter_percent: toSafeNonNegNumber(src.inviter_percent),
    invitee_percent: toSafeNonNegNumber(src.invitee_percent || 1),
    invitee_bonus_points: toSafeNonNegNumber(src.invitee_bonus_points),
    referrer_id: toSafeNonNegNumber(src.referrer_id),
  };
}

function normalizeReferralAgents(rawList) {
  if (!Array.isArray(rawList)) return [];
  return rawList.slice(0, 20).map((item) => {
    const src = item && typeof item === 'object' ? item : {};
    return {
      user_id: String(src.user_id || ''),
      name: String(src.name || 'Игрок'),
      completed: toSafeNonNegNumber(src.completed),
      bonus_points: toSafeNonNegNumber(src.bonus_points),
      invitee_bonus_points: toSafeNonNegNumber(src.invitee_bonus_points),
      invitee_percent: toSafeNonNegNumber(src.invitee_percent || 1),
      inviter_percent: toSafeNonNegNumber(src.inviter_percent),
    };
  });
}
function normalizeSecretMode(modeRaw) {
  const mode = String(modeRaw || 'none').trim().toLowerCase();
  return Object.prototype.hasOwnProperty.call(SECRET_MODE_META, mode) ? mode : 'none';
}

function normalizeSecretSummary(rawSummary) {
  const src = rawSummary && typeof rawSummary === 'object' ? rawSummary : {};
  const completed = Math.max(0, Math.floor(Number(src.completed || 0)));
  const total = Math.max(1, Math.floor(Number(src.total || 15)));
  const bonusPoints = Math.max(0, Math.floor(Number(src.bonus_points || src.bonusPoints || 0)));
  return {
    completed: Math.min(completed, total),
    total,
    bonus_points: bonusPoints,
  };
}

function normalizeSecretMissions(rawMissions) {
  if (!Array.isArray(rawMissions)) return [];
  return rawMissions
    .filter((item) => item && typeof item === 'object')
    .map((item) => {
      const target = Math.max(1, Math.floor(Number(item.target || 1)));
      const progress = Math.max(0, Math.min(target, Math.floor(Number(item.progress || 0))));
      return {
        id: String(item.id || ''),
        order: Math.max(0, Math.floor(Number(item.order || 0))),
        tier: String(item.tier || 'starter'),
        mode: item.mode ? normalizeSecretMode(item.mode) : null,
        icon: String(item.icon || 'рџЋЇ'),
        name: String(item.name || 'РўР°Р№РЅР°СЏ РјРёСЃСЃРёСЏ'),
        desc: String(item.desc || ''),
        target,
        progress,
        completed: !!item.completed,
        completed_at: item.completed_at || null,
        bonus_pct: Math.max(0, Math.floor(Number(item.bonus_pct || 0))),
        reward_points: Math.max(0, Math.floor(Number(item.reward_points || 0))),
      };
    })
    .sort((a, b) => a.order - b.order);
}

function ensureSecretState() {
  state.secretMode = normalizeSecretMode(state.secretMode);
  state.secretSummary = normalizeSecretSummary(state.secretSummary);
  state.secretMissions = normalizeSecretMissions(state.secretMissions);

  if (!Number.isFinite(Number(state._secretTokenCursor || 0))) state._secretTokenCursor = 0;
  if (!Number.isFinite(Number(state._secretAnswerToken || 0))) state._secretAnswerToken = 0;
  if (!Number.isFinite(Number(state._secretBreakToken || 0))) state._secretBreakToken = 0;
  if (!Number.isFinite(Number(state._secretLastAnswerElapsed || 0))) state._secretLastAnswerElapsed = 0;
  state._secretLastAnswerNoHint = !!state._secretLastAnswerNoHint;
  state._secretLastAnswerOneLife = !!state._secretLastAnswerOneLife;
  state._secretLastAnswerType = String(state._secretLastAnswerType || '');
  if (!Number.isFinite(Number(state._secretLastAnswerStreak || 0))) state._secretLastAnswerStreak = 0;
  state._secretLastAwardSig = String(state._secretLastAwardSig || '');
}

function _secretGetModeMeta(modeRaw) {
  const mode = normalizeSecretMode(modeRaw);
  return SECRET_MODE_META[mode] || SECRET_MODE_META.none;
}

function _secretIssueToken() {
  const now = Date.now();
  const prev = Math.max(0, Math.floor(Number(state._secretTokenCursor || 0)));
  const token = Math.max(prev + 1, now);
  state._secretTokenCursor = token;
  return token;
}

function _registerSecretBreakEvent() {
  ensureSecretState();
  state._secretBreakToken = _secretIssueToken();
  state._secretLastAnswerStreak = 0;
}

function _registerSecretAnswerEvent(context = {}) {
  ensureSecretState();
  state._secretAnswerToken = _secretIssueToken();
  state._secretLastAnswerElapsed = Math.max(0, Math.floor(Number(context.elapsed || 0)));
  state._secretLastAnswerNoHint = !!context.noHint;
  state._secretLastAnswerOneLife = !!context.oneLife;
  state._secretLastAnswerType = String(context.cipherType || '').trim().toLowerCase().slice(0, 24);
  state._secretLastAnswerStreak = Math.max(0, Math.floor(Number(context.streak || state.streak || 0)));
}

function _appendSecretSyncPayload(data) {
  ensureSecretState();
  data.secret_mode = state.secretMode;
  data.chapter_errors = Math.max(0, Math.floor(Number(state._chapterErrors || 0)));
  data.chapter_hints = Math.max(0, Math.floor(Number(state._chapterHints || 0)));
  data.lives = state.adminMode ? -1 : Math.max(0, Math.floor(Number(state.lives || 0)));
  data.mission_answer_token = Math.max(0, Math.floor(Number(state._secretAnswerToken || 0)));
  data.mission_break_token = Math.max(0, Math.floor(Number(state._secretBreakToken || 0)));
  data.mission_last_answer_elapsed = Math.max(0, Math.floor(Number(state._secretLastAnswerElapsed || 0)));
  data.mission_last_answer_no_hint = !!state._secretLastAnswerNoHint;
  data.mission_last_answer_one_life = !!state._secretLastAnswerOneLife;
  data.mission_last_answer_type = String(state._secretLastAnswerType || '');
  data.mission_last_answer_streak = Math.max(0, Math.floor(Number(state._secretLastAnswerStreak || 0)));
}

function _secretMissionCompleted(missionId) {
  ensureSecretState();
  const id = String(missionId || '');
  if (!id) return false;
  const mission = (state.secretMissions || []).find((item) => item.id === id);
  return !!(mission && mission.completed);
}

function _secretMissionStats() {
  ensureSecretState();
  const missions = normalizeSecretMissions(state.secretMissions);
  const summary = normalizeSecretSummary(state.secretSummary);
  const byId = {};
  let completedTotal = 0;
  let completedStarter = 0;
  let completedAdvanced = 0;
  for (const mission of missions) {
    byId[mission.id] = mission;
    if (!mission.completed) continue;
    completedTotal += 1;
    if (mission.tier === 'starter') completedStarter += 1;
    else completedAdvanced += 1;
  }
  return {
    missions,
    byId,
    completedTotal,
    completedStarter,
    completedAdvanced,
    bonusPoints: summary.bonus_points,
    total: summary.total,
    completed: summary.completed,
  };
}

function _applySecretStateFromServer(modeRaw, summaryRaw, missionsRaw) {
  ensureSecretState();
  const nextMode = normalizeSecretMode(modeRaw !== undefined ? modeRaw : state.secretMode);
  const nextSummary = normalizeSecretSummary(summaryRaw !== undefined ? summaryRaw : state.secretSummary);
  const nextMissions = normalizeSecretMissions(missionsRaw !== undefined ? missionsRaw : state.secretMissions);
  const prevSnapshot = JSON.stringify([state.secretMode, state.secretSummary, state.secretMissions]);
  state.secretMode = nextMode;
  state.secretSummary = nextSummary;
  state.secretMissions = nextMissions;
  const nextSnapshot = JSON.stringify([state.secretMode, state.secretSummary, state.secretMissions]);
  return prevSnapshot !== nextSnapshot;
}

function _showSecretAwardNotice(award, totalPoints) {
  const old = document.getElementById('secret-award-toast');
  if (old) old.remove();
  const el = document.createElement('div');
  el.id = 'secret-award-toast';
  el.style.cssText = `
    position:fixed;
    left:50%;
    top:88px;
    transform:translateX(-50%);
    z-index:10025;
    width:min(92vw, 420px);
    background:linear-gradient(155deg, rgba(15,36,33,.98), rgba(9,19,17,.98));
    border:1px solid rgba(101,255,211,.55);
    border-left:4px solid #67ffd3;
    border-radius:12px;
    box-shadow:0 16px 42px rgba(0,0,0,.6);
    padding:12px 14px;
    opacity:0;
    transition:opacity .25s ease, transform .25s ease;
  `;
  const missionName = escapeHtml(String(award && award.name ? award.name : 'РўР°Р№РЅР°СЏ РјРёСЃСЃРёСЏ'));
  const missionIcon = escapeHtml(String(award && award.icon ? award.icon : 'рџ•¶'));
  const points = Math.max(0, Math.floor(Number(award && award.points ? award.points : 0)));
  const bonusPct = Math.max(0, Math.floor(Number(award && award.bonus_pct ? award.bonus_pct : 0)));
  const totalBonus = Math.max(0, Math.floor(Number(totalPoints || 0)));
  el.innerHTML = `
    <div style="display:flex;align-items:flex-start;gap:10px">
      <div style="font-size:24px;line-height:1">${missionIcon}</div>
      <div style="min-width:0">
        <div style="font-family:var(--head);font-size:11px;letter-spacing:.09em;color:#8bf4d3">РўРђР™РќРђРЇ РњРРЎРЎРРЇ Р’Р«РџРћР›РќР•РќРђ</div>
        <div style="font-family:var(--head);font-size:16px;color:#dcfff4;letter-spacing:.03em;margin-top:2px">${missionName}</div>
        <div style="font-size:12px;color:#c8f0e4;margin-top:4px">РќР°РіСЂР°РґР°: <b>+${points} РѕС‡</b> В· Р±РѕРЅСѓСЃ <b>+${bonusPct}%</b></div>
        <div style="font-size:11px;color:#9ed9c7;margin-top:3px">РЎСѓРјРјР°СЂРЅС‹Р№ Р±РѕРЅСѓСЃ РјРёСЃСЃРёР№: +${totalBonus} РѕС‡</div>
      </div>
    </div>
  `;
  document.body.appendChild(el);
  requestAnimationFrame(() => {
    el.style.opacity = '1';
    el.style.transform = 'translateX(-50%) translateY(0)';
  });
  setTimeout(() => {
    el.style.opacity = '0';
    el.style.transform = 'translateX(-50%) translateY(-8px)';
    setTimeout(() => el.remove(), 260);
  }, 3600);
}

function _applySecretAwards(awards, awardedPoints) {
  if (!Array.isArray(awards) || awards.length === 0) return;
  ensureSecretState();
  const ids = awards
    .map((item) => String(item && item.id ? item.id : '').trim())
    .filter(Boolean)
    .join('|');
  if (!ids) return;
  const sig = `${ids}:${Math.max(0, Math.floor(Number(awardedPoints || 0)))}`;
  if (sig === state._secretLastAwardSig) return;
  state._secretLastAwardSig = sig;
  saveState();

  const totalPoints = normalizeSecretSummary(state.secretSummary).bonus_points;
  awards.forEach((award, idx) => {
    setTimeout(() => _showSecretAwardNotice(award || {}, totalPoints), idx * 900);
  });
}

let _activeCipherKey = '';
let _resolvedCipherKey = '';
let _nextCipherBusy = false;
let _successAutoAdvanceTimer = null;
let _checkAnswerBusy = false;

function _makeCipherKey(chapterIdx = state.chapter, cipherIdx = state.cipherIdx) {
  return String(Number(chapterIdx || 0)) + ':' + String(Number(cipherIdx || 0));
}

function _markActiveCipherResolved() {
  _resolvedCipherKey = _activeCipherKey || _makeCipherKey();
}

function _isActiveCipherResolved() {
  const key = _activeCipherKey || _makeCipherKey();
  return key === _resolvedCipherKey;
}

function _clearActiveCipherResolved() {
  _resolvedCipherKey = '';
}

function _readNextCipherTargetIdx() {
  const raw = state ? state._nextCipherTargetIdx : null;
  if (raw === null || raw === undefined || raw === '') return null;
  const n = Number(raw);
  if (!Number.isFinite(n)) return null;
  return Math.floor(n);
}

function storageKey() {
  const uid = getTgUserId();
  return uid ? `cipher_v4_${uid}` : 'cipher_v4_guest';
}

// Р—Р°РіСЂСѓР¶Р°РµРј С‚Р°Р±Р»РёС†Сѓ Р»РёРґРµСЂРѕРІ РёР· Р±РѕС‚Р° (РµСЃР»Рё РїРµСЂРµРґР°РЅР°) РІ state
function _stateCompletedCount() {
  return Object.keys(state.completedChapters || {}).length;
}

function _stateCurrentChapterId() {
  const chapter = CHAPTERS[state.chapter];
  if (chapter && Number.isFinite(Number(chapter.id))) return Number(chapter.id);
  const completedIds = Object.keys(state.completedChapters || {}).map(Number).filter(Number.isFinite);
  return completedIds.length ? Math.max(...completedIds) : 0;
}

function _isChapterCompletedByIndex(chapterIdx) {
  const chapter = CHAPTERS[chapterIdx];
  if (!chapter) return false;
  return !!(state.completedChapters && state.completedChapters[chapter.id]);
}

function _getInProgressChapterState(chapterIdx = null) {
  const idx = Number.isInteger(chapterIdx) ? chapterIdx : Number(state.chapter || 0);
  const chapter = CHAPTERS[idx];
  if (!chapter || !Array.isArray(chapter.ciphers) || chapter.ciphers.length === 0) return null;
  if (Number(state.chapter) !== idx) return null;
  if (_isChapterCompletedByIndex(idx)) return null;

  const livesNum = Number(state.lives);
  const legacyProgress = (
    Number(state.cipherIdx || 0) > 0 ||
    Number(state.chapterScore || 0) > 0 ||
    Number(state._chapterErrors || 0) > 0 ||
    Number(state._chapterHints || 0) > 0 ||
    (!state.adminMode && Number.isFinite(livesNum) && livesNum < 5)
  );
  const hasProgress = (
    !!state._chapterInProgress ||
    !!state._awaitingNextCipher ||
    !!state._pendingChapterFinish ||
    legacyProgress
  );
  if (!hasProgress) return null;

  if (!state.adminMode && Number.isFinite(livesNum) && livesNum <= 0) return null;

  const cipherIdx = Math.max(0, Math.min(chapter.ciphers.length - 1, Number(state.cipherIdx || 0)));
  return {
    chapterIdx: idx,
    chapterId: Number(chapter.id || 0),
    cipherIdx,
    step: cipherIdx + 1,
    total: chapter.ciphers.length,
    score: Math.max(0, Number(state.chapterScore || 0)),
    lives: state.adminMode ? Infinity : Math.max(0, Math.min(5, Number(state.lives || 5)))
  };
}

function _recoverPendingSuccessCheckpoint() {
  if (!state || !state._chapterInProgress) return false;
  if (!state._awaitingNextCipher && !state._pendingChapterFinish) return false;

  const chapter = CHAPTERS[Number(state.chapter || 0)];
  if (!chapter || !Array.isArray(chapter.ciphers) || chapter.ciphers.length === 0) {
    state._awaitingNextCipher = false;
    state._pendingChapterFinish = false;
    return false;
  }

  const safeIdx = Math.max(0, Math.min(chapter.ciphers.length - 1, Number(state.cipherIdx || 0)));
  state.cipherIdx = safeIdx;

  if (state._pendingChapterFinish) {
    state._nextCipherTargetIdx = null;
    return true;
  }
  if (!state._awaitingNextCipher) return false;

  const explicitTarget = _readNextCipherTargetIdx();
  if (explicitTarget !== null) {
    const safeTarget = Math.max(0, Math.min(chapter.ciphers.length, explicitTarget));
    if (safeTarget >= chapter.ciphers.length) {
      state._awaitingNextCipher = false;
      state._pendingChapterFinish = true;
      state._nextCipherTargetIdx = null;
      return true;
    }
    state.cipherIdx = safeTarget;
    state._awaitingNextCipher = false;
    state._pendingChapterFinish = false;
    state._nextCipherTargetIdx = null;
    return true;
  }

  if (safeIdx < chapter.ciphers.length - 1) {
    state.cipherIdx = safeIdx + 1;
    state._awaitingNextCipher = false;
    state._pendingChapterFinish = false;
    state._nextCipherTargetIdx = null;
    return true;
  }

  state._awaitingNextCipher = false;
  state._pendingChapterFinish = true;
  state._nextCipherTargetIdx = null;
  return true;
}

function _recoverSolvedCipherStall(source = 'unknown') {
  if (!_isActiveCipherResolved()) return false;
  const cipherScreen = document.getElementById('s-cipher');
  if (!cipherScreen || !cipherScreen.classList.contains('active')) return false;
  const chapter = CHAPTERS[state.chapter];
  if (!chapter || !Array.isArray(chapter.ciphers)) return false;

  state._chapterInProgress = true;
  state._awaitingNextCipher = true;
  state._pendingChapterFinish = false;
  state._nextCipherTargetIdx = Number(state.cipherIdx || 0) + 1;
  saveState();
  autoSync(false, true);
  const currentKey = _activeCipherKey || _makeCipherKey();
  console.warn(`recoverSolvedCipherStall(${source}): forcing nextCipher for`, currentKey);
  setTimeout(() => {
    try {
      nextCipher(currentKey);
    } catch (e) {
      console.error('recoverSolvedCipherStall: nextCipher failed', e);
    }
  }, 0);
  return true;
}

function _briefLivesLabel(livesValue) {
  if (state.adminMode) return 'в™ѕпёЏ';
  const n = Math.max(0, Math.min(5, Number(livesValue || 0)));
  return 'вќ¤пёЏ'.repeat(n) + 'рџ–¤'.repeat(5 - n);
}

function _calcManualRestartPenaltyPoints(chapterScoreValue) {
  const base = Math.max(0, Math.floor(Number(chapterScoreValue || 0)));
  if (base <= 0) return 0;
  return Math.max(1, Math.round(base * 0.10));
}

function _calcOutOfLivesPenaltyPoints(chapterScoreValue) {
  const base = Math.max(0, Math.floor(Number(chapterScoreValue || 0)));
  if (base <= 0) return 0;
  return Math.max(1, Math.round(base * 0.03));
}

let _outOfLivesRestartTimer = null;

function _handleOutOfLives() {
  if (state.adminMode) {
    state.lives = 5;
    saveState();
    autoSync(false, true);
    renderLives();
    const hb = document.getElementById('cipher-hint-box');
    const ht = document.getElementById('cipher-hint-text');
    if (hb && ht) {
      hb.className = 'cipher-hint-box show';
      ht.textContent = 'вљЎ Р РµР¶РёРј Р°РґРјРёРЅР°: Р¶РёР·РЅРё РІРѕСЃСЃС‚Р°РЅРѕРІР»РµРЅС‹';
      setTimeout(() => { hb.className = 'cipher-hint-box'; }, 2000);
    }
    return;
  }

  if (_outOfLivesRestartTimer) return;

  stopTimer();
  const chapterIdx = Math.max(0, Math.floor(Number(state.chapter || 0)));
  const scoreNow = Math.max(0, Math.floor(Number(state.chapterScore || 0)));
  const penaltyPoints = _calcOutOfLivesPenaltyPoints(scoreNow);
  const carryPenaltyPoints = Math.max(0, Math.floor(Number(state._manualRestartPenaltyPoints || 0)));
  const totalPenaltyPoints = carryPenaltyPoints + penaltyPoints;

  const msg = penaltyPoints > 0
    ? `Р–РёР·РЅРё Р·Р°РєРѕРЅС‡РёР»РёСЃСЊ. Р“Р»Р°РІР° Р±СѓРґРµС‚ РїРµСЂРµР·Р°РїСѓС‰РµРЅР°. РЁС‚СЂР°С„ -${penaltyPoints} РѕС‡ (3%).`
    : 'Р–РёР·РЅРё Р·Р°РєРѕРЅС‡РёР»РёСЃСЊ. Р“Р»Р°РІР° Р±СѓРґРµС‚ РїРµСЂРµР·Р°РїСѓС‰РµРЅР°.';

  const hb = document.getElementById('cipher-hint-box');
  const ht = document.getElementById('cipher-hint-text');
  if (hb && ht) {
    hb.className = 'cipher-hint-box show';
    ht.textContent = msg;
  }
  showPenaltyNotice(msg, 6200);
  renderLives();

  const inp = document.getElementById('cipher-input');
  if (inp) inp.disabled = true;
  const checkBtn = document.getElementById('btn-check') || document.querySelector('.btn-check');
  if (checkBtn) checkBtn.disabled = true;

  _outOfLivesRestartTimer = setTimeout(() => {
    _outOfLivesRestartTimer = null;
    if (hb) hb.className = 'cipher-hint-box';
    restartChapterFromStart(chapterIdx, { manualPenaltyPoints: totalPenaltyPoints });
    autoSync(false, true);
  }, 1800);
}

function _buildUnloadSyncData() {
  const data = {
    type: 'sync',
    chapter: _stateCurrentChapterId(),
    score: Number(state.chapterScore || 0),
    total_score: Number(state.totalScore || 0),
    completed: _stateCompletedCount(),
    game_over: state.gameOver || false,
    user_id: getTgUserId(),
    chapter_idx: Number(state.chapter || 0),
    cipher_idx: Number(state.cipherIdx || 0),
    lives: state.adminMode ? -1 : Number(state.lives || 0),
    chapter_in_progress: !!state._chapterInProgress
  };
  _appendSecretSyncPayload(data);
  return data;
}

function _sendSyncBeaconIfPossible() {
  const syncUrl = window._syncUrl;
  const uid = getTgUserId();
  if (!syncUrl || !uid || !navigator || typeof navigator.sendBeacon !== 'function') return;
  const data = _buildUnloadSyncData();
  const initDataRaw = getTgInitDataRaw();
  if (initDataRaw) data.init_data = initDataRaw;
  if (_serverResetToken > 0) data.reset_token = _serverResetToken;
  try {
    navigator.sendBeacon(syncUrl, JSON.stringify(data));
  } catch (e) {}
}

function _livesBadgeText() {
  if (state.adminMode) return 'в€ћ';
  const n = Math.max(0, Math.min(5, Number(state.lives || 0)));
  return 'вќ¤пёЏ'.repeat(n) + 'рџ–¤'.repeat(5 - n);
}

function syncTopStatusBars() {
  const totalScore = Number(state.totalScore || 0);
  const completed = _stateCompletedCount();
  const totalChapters = CHAPTERS.length;
  const livesLabel = _livesBadgeText();

  const chaptersScore = document.getElementById('chapters-score-display');
  if (chaptersScore) chaptersScore.textContent = String(totalScore);

  const statScore = document.getElementById('stat-score');
  if (statScore) statScore.textContent = totalScore + ' РѕС‡';

  const chaptersLives = document.getElementById('chapters-lives-display');
  if (chaptersLives) chaptersLives.textContent = livesLabel;

  const lbScore = document.getElementById('lb-score-display');
  if (lbScore) lbScore.textContent = String(totalScore);

  const lbChapters = document.getElementById('lb-chapters-display');
  if (lbChapters) lbChapters.textContent = String(completed);

  const lbTotal = document.getElementById('lb-chapters-total');
  if (lbTotal) lbTotal.textContent = String(totalChapters);

  const lbLives = document.getElementById('lb-lives-display');
  if (lbLives) lbLives.textContent = state.adminMode ? 'в€ћ' : String(Math.max(0, Math.min(5, Number(state.lives || 0))));
}

function _applyServerProgress(serverScore, serverCompleted, serverGameOver, force = false) {
  const srvScore = Math.max(0, Number(serverScore || 0));
  const srvCompleted = Math.max(0, Math.min(CHAPTERS.length, Number(serverCompleted || 0)));
  const localScore = Number(state.totalScore || 0);
  const localCompleted = _stateCompletedCount();
  const localAhead = localScore > srvScore || localCompleted > srvCompleted;

  if (!force && localAhead) {
    return { applied: false, localAhead: true };
  }

  const oldChapterScores = Object.assign({}, state.chapterScores || {});
  const oldChapterStats = Object.assign({}, state.chapterStats || {});
  const oldFailCounts = Object.assign({}, state.chapterFailCounts || {});

  state.totalScore = srvScore;
  state.completedChapters = {};
  for (let i = 1; i <= srvCompleted; i++) state.completedChapters[i] = true;

  const nextScores = {};
  const nextStats = {};
  const nextFailCounts = {};
  for (let i = 1; i <= srvCompleted; i++) {
    if (oldChapterScores[i] !== undefined) nextScores[i] = oldChapterScores[i];
    if (oldChapterStats[i] !== undefined) nextStats[i] = oldChapterStats[i];
    if (oldFailCounts[i] !== undefined) nextFailCounts[i] = oldFailCounts[i];
  }
  state.chapterScores = nextScores;
  state.chapterStats = nextStats;
  state.chapterFailCounts = nextFailCounts;
  state.gameOver = !!serverGameOver;
  // РќРµ С‚СЂРѕРіР°РµРј runtime-С‡РµРєРїРѕРёРЅС‚ С‚РµРєСѓС‰РµР№ РіР»Р°РІС‹ РїСЂРё merge СЃ СЃРµСЂРІРµСЂРѕРј.
  // РРЅР°С‡Рµ РІ РїРѕРІС‚РѕСЂРЅРѕРј РїСЂРѕС…РѕР¶РґРµРЅРёРё СѓР¶Рµ "РїСЂРѕР№РґРµРЅРЅРѕР№" РіР»Р°РІС‹ РјРѕР¶РЅРѕ РїРѕС‚РµСЂСЏС‚СЊ
  // С„Р»Р°Рі РїРµСЂРµС…РѕРґР° РЅР° СЃР»РµРґСѓСЋС‰РёР№ С€РёС„СЂ Рё Р·Р°С†РёРєР»РёС‚СЊСЃСЏ РЅР° С‚РѕРј Р¶Рµ Р·Р°РґР°РЅРёРё.

  return { applied: true, localAhead: false };
}

function _resetTokenStorageKey() {
  const uid = getTgUserId();
  return uid ? `cipher_reset_token_v1_${uid}` : 'cipher_reset_token_v1_guest';
}

function _getStoredResetToken() {
  try {
    const raw = localStorage.getItem(_resetTokenStorageKey());
    const token = Math.floor(Number(raw || 0));
    return Number.isFinite(token) && token > 0 ? token : 0;
  } catch (e) {
    return 0;
  }
}

function _storeResetToken(tokenLike) {
  const token = Math.floor(Number(tokenLike || 0));
  if (!Number.isFinite(token) || token <= 0) return;
  try {
    localStorage.setItem(_resetTokenStorageKey(), String(token));
  } catch (e) {}
}

function _hasLocalProgressData() {
  return (
    Number(state.totalScore || 0) > 0 ||
    _stateCompletedCount() > 0 ||
    Number(state.chapterScore || 0) > 0 ||
    Number(state.cipherIdx || 0) > 0 ||
    Object.keys(state.chapterScores || {}).length > 0 ||
    Object.keys(state.achievements || {}).length > 0 ||
    Number(state.achievementPts || 0) > 0
  );
}

function _canApplyServerResetNow() {
  return (
    state._chapterInProgress !== true &&
    state._awaitingNextCipher !== true &&
    state._pendingChapterFinish !== true &&
    Number(state.chapterScore || 0) === 0 &&
    Number(state.cipherIdx || 0) === 0
  );
}

function _wipeLocalProgressForServerReset() {
  state.totalScore = 0;
  state.chapterScore = 0;
  state.chapter = 0;
  state.cipherIdx = 0;
  state.startTime = 0;
  state.streak = 0;
  state.completedChapters = {};
  state.chapterScores = {};
  state.chapterStats = {};
  state.chapterFailCounts = {};
  state.gameOver = false;
  state.retryPenalty = false;
  state._chapterInProgress = false;
  state._awaitingNextCipher = false;
  state._pendingChapterFinish = false;
  state._nextCipherTargetIdx = null;
  state._manualRestartPenaltyPoints = 0;
  state._noptsMode = false;
  state._isRetryAttempt = false;
  state._fastAnswers = 0;
  state._perfectChapters = 0;
  state._noHintChapters = 0;
  state.achievements = {};
  state.achievementPts = 0;
  state.secretMode = 'none';
  state.secretSummary = normalizeSecretSummary(null);
  state.secretMissions = [];
  state._secretTokenCursor = 0;
  state._secretAnswerToken = 0;
  state._secretBreakToken = 0;
  state._secretLastAnswerElapsed = 0;
  state._secretLastAnswerNoHint = false;
  state._secretLastAnswerOneLife = false;
  state._secretLastAnswerType = '';
  state._secretLastAnswerStreak = 0;
  state._secretLastAwardSig = '';
  state.artifacts = {};
  state.solvedTypes = {};
  state.solvedTypeCounts = {};
  state.lives = state.adminMode ? Infinity : 5;
  try { localStorage.removeItem('pending_results'); } catch (e) {}
}

function _refreshCurrentTabAfterSync() {
  syncTopStatusBars();
  if (currentTab === 'leaderboard') {
    renderLeaderboardTab();
    return;
  }
  if (currentTab === 'profile') {
    renderProfileTab();
    return;
  }
  if (currentTab === 'about') {
    renderAboutTab();
    applyAboutBuildVersion();
    return;
  }
  if (currentTab === 'achievements') {
    const el = document.getElementById('achievements-tab-content');
    if (el) renderAchievementsTab(el);
    return;
  }
  if (currentTab === 'secret') {
    renderSecretMissionsTab();
    return;
  }
  if (currentTab === 'settings') {
    renderSettingsTab();
    return;
  }
  renderChapters();
}

// Load leaderboard passed from bot into local state
function mergeBotLeaderboard() {
  const myUid = getTgUserId();

  if (tgInitLB.length) {
    state.leaderboard = tgInitLB.map(r => ({
      uid: String(r.uid), name: r.name, score: Number(r.score || 0),
      completed: Number(r.completed || 0), role: r.role || 'player',
      achievementCount: Number(r.achievementCount || 0),
      achievementPts: Number(r.achievementPts || 0)
    }));
  }

  if (tgInitMe && myUid) {
    const prevResetToken = _getStoredResetToken();
    const incomingResetToken = (typeof tgInitMe.reset_token === 'number' && tgInitMe.reset_token > 0)
      ? Math.floor(tgInitMe.reset_token)
      : 0;
    if (incomingResetToken > 0) _serverResetToken = incomingResetToken;

    const dbScore = Number(tgInitMe.score || 0);
    const dbCompleted = Number(tgInitMe.completed || 0);
    const dbGameOver = !!tgInitMe.game_over;
    const dbRestartMode = tgInitMe.restart_mode || null;
    const tokenChanged = incomingResetToken > 0 && (
      prevResetToken <= 0 ? _hasLocalProgressData() : (incomingResetToken !== prevResetToken)
    );
    const forcedByTokenReset = tokenChanged &&
      dbScore === 0 &&
      dbCompleted === 0 &&
      _hasLocalProgressData();

    const dbAdminMode = (tgInitMe.admin_mode === true) || window._adminMode === true;
    const dbTesterMode = (tgInitMe.tester_mode === true) || window._testerMode === true;
    const dbRole = tgInitMe.role || window._gameRole || 'player';

    if (dbAdminMode) {
      state.adminMode = true;
      state.testerMode = false;
    } else if (dbTesterMode) {
      state.adminMode = false;
      state.testerMode = true;
    } else {
      state.adminMode = false;
      state.testerMode = false;
    }
    state.gameRole = dbRole;
    state.referralSummary = normalizeReferralSummary(tgInitMe.ref_summary || state.referralSummary);
    state.referralAgents = normalizeReferralAgents(tgInitMe.ref_agents || state.referralAgents);
    _applySecretStateFromServer(tgInitMe.secret_mode, tgInitMe.secret_summary, tgInitMe.secret_missions);

    const canApplyRestartMode = (
      (dbRestartMode === 'penalty' || dbRestartMode === 'nopts') &&
      _canApplyServerResetNow()
    );
    if (canApplyRestartMode) {
      state.totalScore = 0;
      state.completedChapters = {};
      state.chapterScores = {};
      state.chapterStats = {};
      state.chapterFailCounts = {};
      state.chapter = 0;
      state.cipherIdx = 0;
      state.chapterScore = 0;
      state.lives = state.adminMode ? Infinity : 5;
      state._chapterInProgress = false;
      state._awaitingNextCipher = false;
      state._pendingChapterFinish = false;
      state._nextCipherTargetIdx = null;
      state._manualRestartPenaltyPoints = 0;
      state.gameOver = false;
      state.retryPenalty = dbRestartMode === 'penalty';
      state._noptsMode = dbRestartMode === 'nopts';
      state.achievements = {};
      state.achievementPts = 0;
      if (incomingResetToken > 0) _storeResetToken(incomingResetToken);
      saveState();
      return;
    }

    if (forcedByTokenReset) {
      _wipeLocalProgressForServerReset();
      if (incomingResetToken > 0) _storeResetToken(incomingResetToken);
      saveState();
      return;
    }

    const mergeResult = _applyServerProgress(dbScore, dbCompleted, dbGameOver, false);
    if (!mergeResult.applied) {
      if (incomingResetToken > 0) _storeResetToken(incomingResetToken);
      // Local progress is newer than server copy; push it via autosync.
      setTimeout(() => autoSync(false), 0);
    } else {
      if (incomingResetToken > 0) _storeResetToken(incomingResetToken);
      saveState();
    }
  }
}
function loadState() {
  try {
    const s = localStorage.getItem(storageKey());
    if (s) {
      Object.assign(state, JSON.parse(s));
      if (!state.achievements || typeof state.achievements !== 'object') state.achievements = {};
      if (!state.referralSummary || typeof state.referralSummary !== 'object') {
        state.referralSummary = normalizeReferralSummary(null);
      } else {
        state.referralSummary = normalizeReferralSummary(state.referralSummary);
      }
      if (!Array.isArray(state.referralAgents)) {
        state.referralAgents = [];
      } else {
        state.referralAgents = normalizeReferralAgents(state.referralAgents);
      }
      state.secretMode = normalizeSecretMode(state.secretMode);
      state.secretSummary = normalizeSecretSummary(state.secretSummary);
      state.secretMissions = normalizeSecretMissions(state.secretMissions);
      if (!state.artifacts || typeof state.artifacts !== 'object') state.artifacts = {};
      if (!state.solvedTypes || typeof state.solvedTypes !== 'object') state.solvedTypes = {};
      if (!state.solvedTypeCounts || typeof state.solvedTypeCounts !== 'object') state.solvedTypeCounts = {};
      if (!Number.isFinite(Number(state.achievementPts || 0))) state.achievementPts = 0;
      if (!Number.isFinite(Number(state._secretTokenCursor || 0))) state._secretTokenCursor = 0;
      if (!Number.isFinite(Number(state._secretAnswerToken || 0))) state._secretAnswerToken = 0;
      if (!Number.isFinite(Number(state._secretBreakToken || 0))) state._secretBreakToken = 0;
      if (!Number.isFinite(Number(state._secretLastAnswerElapsed || 0))) state._secretLastAnswerElapsed = 0;
      state._secretLastAnswerNoHint = !!state._secretLastAnswerNoHint;
      state._secretLastAnswerOneLife = !!state._secretLastAnswerOneLife;
      state._secretLastAnswerType = String(state._secretLastAnswerType || '');
      if (!Number.isFinite(Number(state._secretLastAnswerStreak || 0))) state._secretLastAnswerStreak = 0;
      state._secretLastAwardSig = String(state._secretLastAwardSig || '');
      if (typeof state._chapterInProgress !== 'boolean') state._chapterInProgress = false;
      if (typeof state._awaitingNextCipher !== 'boolean') state._awaitingNextCipher = false;
      if (typeof state._pendingChapterFinish !== 'boolean') state._pendingChapterFinish = false;
      if (!Number.isFinite(Number(state._manualRestartPenaltyPoints || 0))) state._manualRestartPenaltyPoints = 0;
      const loadedTargetIdx = _readNextCipherTargetIdx();
      state._nextCipherTargetIdx = loadedTargetIdx === null ? null : Math.max(0, loadedTargetIdx);
      // Р РѕР»СЊ Р’РЎР•Р“Р”Рђ РїСЂРёС…РѕРґРёС‚ РѕС‚ СЃРµСЂРІРµСЂР° вЂ” РЅРёРєРѕРіРґР° РЅРµ Р±РµСЂС‘Рј РёР· РєСЌС€Р°.
      // РРЅР°С‡Рµ РїСЂРё СЃРјРµРЅРµ СЂРѕР»Рё СЃ admin РЅР° player вЂ” adminMode РѕСЃС‚Р°РЅРµС‚СЃСЏ true РёР· РєСЌС€Р°.
      state.adminMode  = false;
      state.testerMode = false;
      state.gameRole   = 'player';
      state._noptsMode = false;
      if (_recoverPendingSuccessCheckpoint()) {
        // Р¤РёРєСЃРёСЂСѓРµРј РѕР±РЅРѕРІР»С‘РЅРЅС‹Р№ РєСѓСЂСЃРѕСЂ СЃСЂР°Р·Сѓ, С‡С‚РѕР±С‹ РЅРµ РїРѕС‚РµСЂСЏС‚СЊ С‡РµРєРїРѕРёРЅС‚ РїСЂРё СЂРµР·РєРѕРј Р·Р°РєСЂС‹С‚РёРё.
        saveState();
      }
    }
    ensureSecretState();
  } catch(e) {}
}

function saveState() {
  try {
    // Р РѕР»СЊ РќРРљРћР“Р”Рђ РЅРµ СЃРѕС…СЂР°РЅСЏРµРј РІ localStorage вЂ” РѕРЅР° РІСЃРµРіРґР° РїСЂРёС…РѕРґРёС‚ РѕС‚ СЃРµСЂРІРµСЂР°.
    // Р­С‚Рѕ РїСЂРµРґРѕС‚РІСЂР°С‰Р°РµС‚ "Р·Р°СЃС‚СЂРµРІР°РЅРёРµ" adminMode РїСЂРё СЃРјРµРЅРµ СЂРѕР»Рё.
    const toSave = Object.assign({}, state, {
      _noptsMode:  false,
      adminMode:   false,   // РЅРµ РєСЌС€РёСЂСѓРµРј
      testerMode:  false,   // РЅРµ РєСЌС€РёСЂСѓРµРј
      gameRole:    'player' // РЅРµ РєСЌС€РёСЂСѓРµРј
    });
    localStorage.setItem(storageKey(), JSON.stringify(toSave));
  } catch(e) {}
  syncTopStatusBars();
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РџР•Р Р•РљР›Р®Р§РђРўР•Р›Р¬ РљР›РђР’РРђРўРЈР Р«
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
let _keyboardVisible = false;

function _normalizeForCompare(text) {
  if (!text) return '';
  let s = String(text).toUpperCase();
  s = s.replace(/РЃ/g, 'Р•');
  // Р›Р°С‚РёРЅСЃРєРёРµ "РїРѕС…РѕР¶РёРµ" СЃРёРјРІРѕР»С‹ -> РєРёСЂРёР»Р»РёС†Р°
  s = s.replace(/[ABCEHKMOPTXY]/g, ch => ({
    A:'Рђ', B:'Р’', C:'РЎ', E:'Р•', H:'Рќ', K:'Рљ', M:'Рњ',
    O:'Рћ', P:'Р ', T:'Рў', X:'РҐ', Y:'РЈ'
  }[ch] || ch));
  // РЈРЅРёС„РёС†РёСЂСѓРµРј СЂР°Р·РґРµР»РёС‚РµР»Рё Рё СѓР±РёСЂР°РµРј Р»РёС€РЅСЋСЋ РїСѓРЅРєС‚СѓР°С†РёСЋ
  s = s.replace(/[вЂђвЂ‘вЂ’вЂ“вЂ”в€’-]+/g, ' ');
  s = s.replace(/[\u00AB\u00BB"'`.,!?;:()[\]{}\/|_*+=~^@#$%&]/g, ' ');
  s = s.replace(/\s+/g, ' ').trim();
  return s;
}

function _setKeyboardVisibility(visible) {
  const inp = document.getElementById('cipher-input');
  const btn = document.getElementById('btn-keyboard');
  if (!inp) return;

  _keyboardVisible = !!visible;
  if (_keyboardVisible) {
    inp.removeAttribute('readonly');
    inp.setAttribute('inputmode', 'text');
    inp.focus();
    inp.click();
  } else {
    inp.setAttribute('readonly', true);
    inp.setAttribute('inputmode', 'none');
    inp.blur();
  }

  if (btn) {
    if (_keyboardVisible) {
      btn.style.background = 'rgba(255,224,51,.25)';
      btn.style.borderColor = 'rgba(255,224,51,.5)';
      btn.title = 'РЎРєСЂС‹С‚СЊ РєР»Р°РІРёР°С‚СѓСЂСѓ';
    } else {
      btn.style.background = 'rgba(255,224,51,.1)';
      btn.style.borderColor = 'rgba(255,224,51,.2)';
      btn.title = 'РџРѕРєР°Р·Р°С‚СЊ РєР»Р°РІРёР°С‚СѓСЂСѓ';
    }
  }
}

function toggleKeyboard() {
  const inp = document.getElementById('cipher-input');
  if (!inp) return;
  _setKeyboardVisibility(!_keyboardVisible);
}

function _canUseTextInputForCurrentCipher() {
  const chapter = CHAPTERS[state.chapter];
  if (!chapter || !Array.isArray(chapter.ciphers)) return false;
  const cipher = chapter.ciphers[state.cipherIdx];
  if (!cipher) return false;
  return cipher.type !== 'map' && cipher.type !== 'anagram' && cipher.type !== 'quiz';
}

function ensureCipherInputKeyboard() {
  if (!_canUseTextInputForCurrentCipher()) return;
  if (!_keyboardVisible) _setKeyboardVisibility(true);
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РќРђР’РР“РђР¦РРЇ
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
let currentChapter = 0;
function showScreen(id) {
  const TAB_SCREENS = ['s-leaderboard-tab','s-profile-tab','s-about-tab','s-achievements-tab','s-secret-tab','s-settings-tab'];
  if (TAB_SCREENS.includes(id)) return; // С‚РѕР»СЊРєРѕ С‡РµСЂРµР· switchTab

  if (id === 's-chapters') {
    switchTab('chapters');
    return;
  }

  hideBottomNav();
  document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
  const el = document.getElementById(id);
  if (el) el.classList.add('active');
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р Р•РќР”Р•Р  Р“Р›РђР’
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function renderChapters() {
  const list = document.getElementById('chapters-list');
  list.innerHTML = '';
  let completedCount = 0;

  // РРЅРґРµРєСЃ СЂР°СЃРїРёСЃР°РЅРёСЏ РіР»Р°РІ РїРѕ id РґР»СЏ Р±С‹СЃС‚СЂРѕРіРѕ РґРѕСЃС‚СѓРїР°
  const scheduleMap = {};
  for (const s of tgChapterSchedule) scheduleMap[s.id] = s;

  CHAPTERS.forEach((ch, i) => {
    const isDone  = !!state.completedChapters[ch.id];
    const inProgress = _getInProgressChapterState(i);
    if (isDone) completedCount++;

    // Р›РѕРіРёРєР° Р±Р»РѕРєРёСЂРѕРІРєРё РїРѕ СЂРѕР»Рё:
    // - admin: РІСЃС‘ РѕС‚РєСЂС‹С‚Рѕ, Р±РµСЃРєРѕРЅРµС‡РЅС‹Рµ Р¶РёР·РЅРё
    // - tester: РІСЃС‘ РѕС‚РєСЂС‹С‚Рѕ, 5 Р¶РёР·РЅРµР№
    // - player: С‚РѕР»СЊРєРѕ С‚Рѕ С‡С‚Рѕ РѕС‚РєСЂС‹С‚Рѕ С‡РµСЂРµР· tgOpenChapters + РїСЂРѕС€Р»Р°СЏ РіР»Р°РІР°
    let isLocked, visuallyLocked, schedInfo = null;
    if (state.adminMode || state.testerMode) {
      isLocked = false;
      visuallyLocked = false;
    } else {
      // Р”Р»СЏ РёРіСЂРѕРєР°: РіР»Р°РІР° РґРѕСЃС‚СѓРїРЅР° С‚РѕР»СЊРєРѕ РµСЃР»Рё СЏРІРЅРѕ СѓРєР°Р·Р°РЅР° РІ tgOpenChapters
      // Р•СЃР»Рё tgOpenChapters === null (РЅРµ РїРµСЂРµРґР°РЅ) вЂ” РІСЃРµ РіР»Р°РІС‹ Р·Р°РєСЂС‹С‚С‹ РґР»СЏ РёРіСЂРѕРєР°
      // Fallback: РµСЃР»Рё tgOpenChapters РЅРµ РїРѕР»СѓС‡РµРЅ РѕС‚ Р±РѕС‚Р°, РѕС‚РєСЂС‹РІР°РµРј РїРµСЂРІСѓСЋ РіР»Р°РІСѓ
      const serverAllows = tgOpenChapters !== null ? tgOpenChapters.has(ch.id) : (i === 0);
      const prevDone = i === 0 || !!state.completedChapters[CHAPTERS[i-1].id];
      const keepDoneOpen = isDone === true;
      isLocked = !((serverAllows && prevDone) || keepDoneOpen);
      visuallyLocked = isLocked;
      // РўР°Р№РјРµСЂ вЂ” РµСЃР»Рё РіР»Р°РІР° РЅРµ РѕС‚РєСЂС‹С‚Р° РЅРѕ РµСЃС‚СЊ СЂР°СЃРїРёСЃР°РЅРёРµ open_at
      if (isLocked && scheduleMap[ch.id] && scheduleMap[ch.id].open_at) {
        schedInfo = scheduleMap[ch.id];
      }
    }

    const card = document.createElement('div');
    card.className = 'chapter-card' + (visuallyLocked?' locked':'') + (isDone?' completed':'');

    // РРєРѕРЅРєРё С‚РёРїРѕРІ Р·Р°РґР°РЅРёР№
    const typeIcons = {'caesar':'рџ”ђ','morse':'рџ“Ў','atbash':'рџЄћ','num':'рџ”ў','anagram':'рџ”¤','math':'вћ—','photo':'рџ–ј','map':'рџ—є','quiz':'вќ“'};
    const uniqueTypes = [...new Set(ch.ciphers.map(c=>c.type))];
    const tags = uniqueTypes.map(t=>`<span class="ch-tag">${typeIcons[t]||'вќ“'} ${ch.ciphers.find(c=>c.type===t).typeLabel}</span>`).join('');

    // РЎС‚Р°С‚СѓСЃ
    let statusText, badgeIcon;
    if (isDone && state.chapterScores[ch.id] > 0) { statusText='вњ… Р—РђР’Р•Р РЁР•РќРћ'; badgeIcon='вњ…'; }
    else if (isDone) { statusText='рџ’” РџР РћР’РђР›Р•РќРћ'; badgeIcon='рџ’”'; }
    else if (inProgress) { statusText=`вЏЇ Р’ РџР РћР¦Р•РЎРЎР• ${inProgress.step}/${inProgress.total}`; badgeIcon='вЏЇ'; }
    else if (state.adminMode) { statusText='рџ‘‘ РћРўРљР Р«РўРћ'; badgeIcon='в–¶'; }
    else if (state.testerMode) { statusText='рџ§Є РћРўРљР Р«РўРћ'; badgeIcon='в–¶'; }
    else if (schedInfo) { statusText='вЏ° РЎРљРћР Рћ'; badgeIcon='вЏ°'; }
    else if (isLocked) { statusText='рџ”’ Р—РђРљР Р«РўРћ'; badgeIcon='рџ”’'; }
    // isLocked РѕСЃС‚Р°С‘С‚СЃСЏ вЂ” РїСЂРѕСЃС‚Рѕ СѓР»СѓС‡С€Р°РµРј РѕС‚РѕР±СЂР°Р¶РµРЅРёРµ
    else { statusText='в–¶ Р”РћРЎРўРЈРџРќРћ'; badgeIcon='в–¶'; }

    const scoreVal = isDone && state.chapterScores[ch.id]
      ? state.chapterScores[ch.id]
      : (inProgress && inProgress.score > 0 ? inProgress.score : '');

    // HTML РєР°СЂС‚РѕС‡РєРё вЂ” СЃ РѕРїС†РёРѕРЅР°Р»СЊРЅС‹Рј С‚Р°Р№РјРµСЂРѕРј РѕР±СЂР°С‚РЅРѕРіРѕ РѕС‚СЃС‡С‘С‚Р°
    const countdownId = `ch-countdown-${ch.id}`;
    const countdownHtml = schedInfo
      ? `<div id="${countdownId}" style="display:inline-flex;align-items:center;gap:6px;max-width:100%;margin-top:8px;padding:6px 10px;border-radius:8px;background:rgba(255,224,51,.16);border:1px solid rgba(255,224,51,.45);font-family:var(--head);font-size:13px;line-height:1.25;color:#fff7c2;letter-spacing:.08em;text-shadow:0 1px 4px rgba(0,0,0,.75);font-weight:700;box-shadow:0 0 10px rgba(255,224,51,.15)">вЏі РџРѕРґСЃС‡С‘С‚...</div>`
      : '';

    card.innerHTML = `
      <div class="ch-icon">${ch.stamp || 'рџ”ђ'}</div>
      <div class="ch-inner">
        <div class="ch-num">${ch.subtitle} &middot; ${statusText}</div>
        <div class="ch-title">${ch.title}</div>
        <div class="ch-place">${ch.place}</div>
        <div class="ch-tags">${tags}</div>
        ${countdownHtml}
      </div>
      <div class="ch-right">
        <div class="ch-badge">${badgeIcon}</div>
        ${scoreVal ? `<div class="ch-score">${scoreVal}</div>` : ''}
      </div>`;

    // Р—Р°РїСѓСЃРєР°РµРј С‚Р°Р№РјРµСЂ РѕР±СЂР°С‚РЅРѕРіРѕ РѕС‚СЃС‡С‘С‚Р° РµСЃР»Рё РµСЃС‚СЊ open_at
    if (schedInfo && schedInfo.open_at) {
      _startChapterCountdown(countdownId, schedInfo.open_at, ch.id);
    }

    // РљС‚Рѕ РјРѕР¶РµС‚ РёРіСЂР°С‚СЊ
    const canPlay = !!inProgress || state.adminMode || state.testerMode || !isLocked;
    if (canPlay) {
      card.onclick = () => { showBriefing(i); };
      card.style.cursor = 'pointer';
    } else {
      card.style.cursor = isDone ? 'default' : 'not-allowed';
    }
    list.appendChild(card);
  });

  // Р‘Р°РЅРЅРµСЂ СЂРµР¶РёРјР° вЂ” РІСЃС‚Р°РІР»СЏРµРј РїРµСЂРµРґ РїРµСЂРІРѕР№ РєР°СЂС‚РѕС‡РєРѕР№
  if (state.adminMode) {
    const banner = document.createElement('div');
    banner.style.cssText = 'margin:8px 12px;padding:10px 14px;background:rgba(255,215,0,.08);border:1px solid rgba(255,215,0,.25);border-radius:8px;font-size:var(--fs-xs);color:#ffd700;letter-spacing:.06em';
    banner.textContent = 'рџ‘‘ Р Р•Р–РРњ РђР”РњРРќРРЎРўР РђРўРћР Рђ В· Р’СЃРµ РіР»Р°РІС‹ РѕС‚РєСЂС‹С‚С‹ В· в€ћ Р¶РёР·РЅРµР№ В· Р РµР№С‚РёРЅРі РЅРµ СѓС‡РёС‚С‹РІР°РµС‚СЃСЏ';
    list.insertBefore(banner, list.firstChild);
  } else if (state.testerMode) {
    const banner = document.createElement('div');
    banner.style.cssText = 'margin:8px 12px;padding:10px 14px;background:rgba(100,200,255,.07);border:1px solid rgba(100,200,255,.25);border-radius:8px;font-size:var(--fs-xs);color:#64c8ff;letter-spacing:.06em';
    banner.textContent = 'рџ§Є Р Р•Р–РРњ РўР•РЎРўРР РћР’Р©РРљРђ В· Р’СЃРµ РіР»Р°РІС‹ РѕС‚РєСЂС‹С‚С‹ В· 5 Р¶РёР·РЅРµР№ В· Р РµР№С‚РёРЅРі РЅРµ СѓС‡РёС‚С‹РІР°РµС‚СЃСЏ';
    list.insertBefore(banner, list.firstChild);
  } else if (state._noptsMode) {
    const banner = document.createElement('div');
    banner.style.cssText = 'margin:8px 12px;padding:10px 14px;background:rgba(100,180,255,.06);border:1px solid rgba(100,180,255,.2);border-radius:8px;font-size:var(--fs-xs);color:#64b4ff;letter-spacing:.06em';
    banner.textContent = 'рџ‘Ѓ Р Р•Р–РРњ РџР РђРљРўРРљР В· РћС‡РєРё РЅРµ РЅР°С‡РёСЃР»СЏСЋС‚СЃСЏ';
    list.insertBefore(banner, list.firstChild);
  } else if (state.retryPenalty) {
    const banner = document.createElement('div');
    banner.style.cssText = 'margin:8px 12px;padding:10px 14px;background:rgba(255,100,50,.06);border:1px solid rgba(255,100,50,.2);border-radius:8px;font-size:var(--fs-xs);color:#ff6432;letter-spacing:.06em';
    banner.textContent = 'рџ”Ґ РџРћР’РўРћР РќРћР• РџР РћРҐРћР–Р”Р•РќРР• В· +10 СЃРµРє Рє РєР°Р¶РґРѕРјСѓ Р·Р°РґР°РЅРёСЋ';
    list.insertBefore(banner, list.firstChild);
  }

  // РћР±РЅРѕРІР»СЏРµРј С€Р°РїРєСѓ
  const total = CHAPTERS.length;
  const pct = Math.round((completedCount / total) * 100);
  const pbEl = document.getElementById('ch-progress-bar');
  if (pbEl) pbEl.style.width = pct + '%';
  const clEl = document.getElementById('stat-completed-label');
  if (clEl) clEl.textContent = completedCount + ' / ' + total + ' РіР»Р°РІ';
  const scEl = document.getElementById('stat-score');
  if (scEl) scEl.textContent = state.totalScore + ' РѕС‡';
  // РљРЅРѕРїРєР° СЃР±СЂРѕСЃР° вЂ” С‚РѕР»СЊРєРѕ РґР»СЏ admin (РЅРµ С‚РµСЃС‚РµСЂ!)
  const resetBtn = document.getElementById('btn-reset-header');
  if (resetBtn) resetBtn.style.display = (state.adminMode || window._adminMode) ? 'inline-block' : 'none';
  const plEl = document.getElementById('stat-players-label');
  const plCount = tgInitLB.length || state.leaderboard.length;
  if (plEl) plEl.textContent = 'рџ‘Ґ ' + (plCount || 'вЂ”') + ' РёРіСЂРѕРєРѕРІ';

  if (state.gameOver) {
    document.getElementById('btn-to-final').style.display = 'block';
  } else {
    document.getElementById('btn-to-final').style.display = 'none';
  }
  syncTopStatusBars();
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РўРђР™РњР•Р  РћР‘Р РђРўРќРћР“Рћ РћРўРЎР§РЃРўРђ Р”Рћ РћРўРљР Р«РўРРЇ Р“Р›РђР’Р«
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
const _countdownIntervals = {};

function _startChapterCountdown(elId, openAtIso, chapterId) {
  // РћСЃС‚Р°РЅР°РІР»РёРІР°РµРј СЃС‚Р°СЂС‹Р№ РµСЃР»Рё Р±С‹Р»
  if (_countdownIntervals[chapterId]) {
    clearInterval(_countdownIntervals[chapterId]);
    delete _countdownIntervals[chapterId];
  }
  const openAt = new Date(openAtIso).getTime();

  function _fmt(ms) {
    if (ms <= 0) return 'рџџў Р“Р›РђР’Рђ РћРўРљР Р«РўРђ';
    const totalSec = Math.floor(ms / 1000);
    const d = Math.floor(totalSec / 86400);
    const h = Math.floor((totalSec % 86400) / 3600);
    const m = Math.floor((totalSec % 3600) / 60);
    const s = totalSec % 60;
    const hh = String(h).padStart(2, '0');
    const mm = String(m).padStart(2, '0');
    const ss = String(s).padStart(2, '0');
    if (d > 0) return `вЏі РћРўРљР Р«РўРР• Р§Р•Р Р•Р— ${d}Рґ ${hh}:${mm}:${ss}`;
    if (h > 0) return `вЏі РћРўРљР Р«РўРР• Р§Р•Р Р•Р— ${hh}:${mm}:${ss}`;
    return `вЏі РћРўРљР Р«РўРР• Р§Р•Р Р•Р— ${mm}:${ss}`;
  }

  function _tick() {
    const el = document.getElementById(elId);
    if (!el) { clearInterval(_countdownIntervals[chapterId]); return; }
    const diff = openAt - Date.now();
    if (diff <= 0) {
      el.textContent = 'рџџў Р“Р›РђР’Рђ РћРўРљР Р«РўРђ В· РћР‘РќРћР’РРўР• Р­РљР РђРќ';
      el.style.background = 'rgba(85,221,85,.14)';
      el.style.borderColor = 'rgba(85,221,85,.55)';
      el.style.color = '#a8ffb2';
      clearInterval(_countdownIntervals[chapterId]);
      delete _countdownIntervals[chapterId];
      return;
    }
    el.textContent = _fmt(diff);
  }
  _tick();
  _countdownIntervals[chapterId] = setInterval(_tick, 1000);
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р‘Р РР¤РРќР“
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function showBriefing(idx) {
  if (idx < 0 || idx >= CHAPTERS.length) { showScreen('s-chapters'); renderChapters(); return; }
  currentChapter = idx;
  const ch = CHAPTERS[idx];
  const resume = _getInProgressChapterState(idx);
  const isDone = !!state.completedChapters[ch.id];

  document.getElementById('brief-num').textContent   = ch.subtitle + ' вЂ” ' + ch.place;
  document.getElementById('brief-title').textContent = ch.title;
  document.getElementById('brief-place').textContent = ch.place;
  document.getElementById('brief-stamp').textContent = ch.stamp;
  document.getElementById('brief-meta').innerHTML    = ch.meta.replace(/\n/g,'<br>');
  document.getElementById('brief-text').textContent  = ch.briefing;
  document.getElementById('brief-mission').textContent = ch.mission;

  const livesEl = document.getElementById('briefing-lives-display');
  if (livesEl) {
    const livesForView = resume ? resume.lives : (state.adminMode ? Infinity : 5);
    livesEl.textContent = _briefLivesLabel(livesForView);
  }
  const scoreEl = document.getElementById('briefing-score-display');
  if (scoreEl) {
    const doneScore = Number((state.chapterScores && state.chapterScores[ch.id]) || 0);
    scoreEl.textContent = String(resume ? resume.score : (isDone ? doneScore : 0));
  }

  const noteEl = document.getElementById('brief-progress-note');
  if (noteEl) {
    if (resume) {
      const penaltyPreview = _calcManualRestartPenaltyPoints(resume.score);
      noteEl.style.display = 'block';
      noteEl.textContent = `РЎРѕС…СЂР°РЅС‘РЅ РїСЂРѕРіСЂРµСЃСЃ: Р·Р°РґР°РЅРёРµ ${resume.step}/${resume.total} В· ${resume.score} РѕС‡ В· ${state.adminMode ? 'в€ћ' : resume.lives} Р¶РёР·РЅРµР№. РќР°С‡Р°С‚СЊ СЃРЅР°С‡Р°Р»Р°: С€С‚СЂР°С„ -10% (${penaltyPreview} РѕС‡).`;
    } else if (isDone) {
      const doneScore = Number((state.chapterScores && state.chapterScores[ch.id]) || 0);
      const penaltyPreview = _calcManualRestartPenaltyPoints(doneScore);
      noteEl.style.display = 'block';
      noteEl.textContent = `Р“Р»Р°РІР° СѓР¶Рµ РїСЂРѕР№РґРµРЅР°: ${doneScore} РѕС‡. РџСЂРё РїРѕРІС‚РѕСЂРµ РѕС‡РєРё СЌС‚РѕР№ РіР»Р°РІС‹ Р±СѓРґСѓС‚ СЃРЅСЏС‚С‹ СЃ РѕР±С‰РµРіРѕ СЂРµР№С‚РёРЅРіР°, РіР»Р°РІР° РѕР±РЅСѓР»РёС‚СЃСЏ Рё РЅР°С‡РЅС‘С‚СЃСЏ Р·Р°РЅРѕРІРѕ СЃРѕ С€С‚СЂР°С„РѕРј -10% (${penaltyPreview} РѕС‡).`;
    } else {
      noteEl.style.display = 'none';
      noteEl.textContent = '';
    }
  }

  const startBtn = document.getElementById('brief-start-btn');
  const resumeActions = document.getElementById('brief-resume-actions');
  const resumeBtn = document.getElementById('brief-resume-btn');
  const restartBtn = document.getElementById('brief-restart-btn');
  if (resume) {
    if (startBtn) startBtn.style.display = 'none';
    if (resumeActions) resumeActions.style.display = 'flex';
    if (resumeBtn) resumeBtn.textContent = `РџР РћР”РћР›Р–РРўР¬ РЎ Р—РђР”РђРќРРЇ ${resume.step}/${resume.total}`;
    if (restartBtn) restartBtn.textContent = 'РќРђР§РђРўР¬ РЎРќРђР§РђР›Рђ (-10%)';
  } else if (isDone) {
    if (startBtn) {
      startBtn.style.display = '';
      startBtn.textContent = 'в†є РџР•Р Р•РР“Р РђРўР¬ Р“Р›РђР’РЈ (-10%)';
    }
    if (resumeActions) resumeActions.style.display = 'none';
  } else {
    if (startBtn) {
      startBtn.style.display = '';
      startBtn.textContent = 'РџР РРЎРўРЈРџРРўР¬ Рљ Р’Р«РџРћР›РќР•РќРР®';
    }
    if (resumeActions) resumeActions.style.display = 'none';
  }
  showScreen('s-briefing');
}
function onBriefStartClicked() {
  const ch = CHAPTERS[currentChapter];
  const resume = _getInProgressChapterState(currentChapter);
  const isDone = !!(ch && state.completedChapters && state.completedChapters[ch.id]);
  if (!resume && isDone) {
    startCompletedChapterReplay(currentChapter);
    return;
  }
  startChapter(currentChapter);
}

function onBriefResumeClicked() {
  const resume = _getInProgressChapterState(currentChapter);
  if (!resume) {
    startChapter(currentChapter);
    return;
  }
  resumeChapter(currentChapter);
}

function onBriefRestartClicked() {
  const resume = _getInProgressChapterState(currentChapter);
  if (!resume) {
    const ch = CHAPTERS[currentChapter];
    const isDone = !!(ch && state.completedChapters && state.completedChapters[ch.id]);
    if (isDone) {
      startCompletedChapterReplay(currentChapter);
      return;
    }
    startChapter(currentChapter);
    return;
  }
  const scoreNow = Math.max(0, Number(resume.score || 0));
  const penaltyPoints = _calcManualRestartPenaltyPoints(scoreNow);
  openRestartCostModal({ scoreNow, penaltyPoints });
}

function startCompletedChapterReplay(idx) {
  const ch = CHAPTERS[idx];
  if (!ch) return;
  const chapterId = ch.id;
  const oldChapterScore = Math.max(0, Math.floor(Number((state.chapterScores && state.chapterScores[chapterId]) || 0)));
  const penaltyPoints = _calcManualRestartPenaltyPoints(oldChapterScore);

  if (state.completedChapters && state.completedChapters[chapterId]) {
    delete state.completedChapters[chapterId];
  }
  if (state.chapterScores && state.chapterScores[chapterId] !== undefined) {
    delete state.chapterScores[chapterId];
  }
  if (state.chapterStats && state.chapterStats[chapterId] !== undefined) {
    delete state.chapterStats[chapterId];
  }

  if (oldChapterScore > 0) {
    state.totalScore = Math.max(0, Number(state.totalScore || 0) - oldChapterScore);
  }
  state.gameOver = false;
  state._isRetryAttempt = true;

  saveState();
  updateLeaderboard();
  syncTopStatusBars();

  const completedAfterReplayStart = _stateCompletedCount();
  Promise.resolve(sendResultToBot({
    type: 'chapter_replay_start',
    chapter: chapterId,
    score: 0,
    total_score: Number(state.totalScore || 0),
    completed: completedAfterReplayStart,
    chapter_idx: idx,
    cipher_idx: 0,
    chapter_in_progress: true,
    restart_penalty_points: penaltyPoints,
    game_over: false
  })).catch(() => {});

  if (oldChapterScore > 0) {
    showPenaltyNotice(`Р“Р»Р°РІР° РѕР±РЅСѓР»РµРЅР°: -${oldChapterScore} РѕС‡ РёР· РѕР±С‰РµРіРѕ СЂРµР№С‚РёРЅРіР°. РџРѕРІС‚РѕСЂ СЃС‚Р°СЂС‚СѓРµС‚ СЃРѕ С€С‚СЂР°С„РѕРј -${penaltyPoints} РѕС‡ (10%).`, 7000);
  } else if (penaltyPoints > 0) {
    showPenaltyNotice(`РџРѕРІС‚РѕСЂ РіР»Р°РІС‹ Р·Р°РїСѓС‰РµРЅ. РЁС‚СЂР°С„ Рє РёС‚РѕРіСѓ РіР»Р°РІС‹: -${penaltyPoints} РѕС‡ (10%).`, 6200);
  } else {
    showToast('РџРѕРІС‚РѕСЂ РіР»Р°РІС‹ Р·Р°РїСѓС‰РµРЅ');
  }

  restartChapterFromStart(idx, { manualPenaltyPoints: penaltyPoints });
  autoSync(false, true);
}

let _restartCostPayload = null;
let _restartConfirmCooldownUntil = 0;
let _restartConfirmCooldownTimer = null;

function _setRestartConfirmButtonEnabled(enabled, label) {
  const btn = document.getElementById('restart-cost-confirm-btn');
  if (!btn) return;
  btn.disabled = !enabled;
  btn.style.opacity = enabled ? '' : '0.6';
  btn.textContent = label || 'в†є РќРђР§РђРўР¬ РЎРќРђР§РђР›Рђ (-10%)';
}

function _startRestartConfirmCooldown(ms = 2500) {
  _restartConfirmCooldownUntil = Date.now() + Math.max(0, Number(ms || 0));
  if (_restartConfirmCooldownTimer) {
    clearInterval(_restartConfirmCooldownTimer);
    _restartConfirmCooldownTimer = null;
  }
  const tick = () => {
    const leftMs = Math.max(0, _restartConfirmCooldownUntil - Date.now());
    if (leftMs <= 0) {
      if (_restartConfirmCooldownTimer) {
        clearInterval(_restartConfirmCooldownTimer);
        _restartConfirmCooldownTimer = null;
      }
      _setRestartConfirmButtonEnabled(true, 'в†є РќРђР§РђРўР¬ РЎРќРђР§РђР›Рђ (-10%)');
      return;
    }
    const leftSec = Math.ceil(leftMs / 1000);
    _setRestartConfirmButtonEnabled(false, `в†є РќРђР§РђРўР¬ РЎРќРђР§РђР›Рђ (-10%) В· ${leftSec}СЃ`);
  };
  tick();
  _restartConfirmCooldownTimer = setInterval(tick, 200);
}

function openRestartCostModal(payload) {
  const modal = document.getElementById('restart-cost-modal');
  const textEl = document.getElementById('restart-cost-modal-text');
  if (!modal || !textEl) {
    restartCostConfirmRestart();
    return;
  }
  const safeScore = Math.max(0, Number(payload && payload.scoreNow || 0));
  const safePenalty = Math.max(0, Number(payload && payload.penaltyPoints || 0));
  _restartCostPayload = { scoreNow: safeScore, penaltyPoints: safePenalty };
  textEl.textContent = `Р•СЃР»Рё РЅР°С‡Р°С‚СЊ РіР»Р°РІСѓ СЃРЅР°С‡Р°Р»Р°, Р±СѓРґРµС‚ РїСЂРёРјРµРЅС‘РЅ С€С‚СЂР°С„ -10% РѕС‚ РїРѕР»СѓС‡РµРЅРЅРѕРіРѕ СЂРµР·СѓР»СЊС‚Р°С‚Р° СЌС‚РѕР№ РіР»Р°РІС‹. РЎРµР№С‡Р°СЃ: ${safeScore} РѕС‡. РЁС‚СЂР°С„: -${safePenalty} РѕС‡. РџСЂРѕРґРѕР»Р¶РёС‚СЊ С‚РµРєСѓС‰СѓСЋ РіР»Р°РІСѓ РјРѕР¶РЅРѕ Р±РµСЃРїР»Р°С‚РЅРѕ.`;
  _startRestartConfirmCooldown(2500);
  modal.classList.add('show');
}

function closeRestartCostModal() {
  const modal = document.getElementById('restart-cost-modal');
  if (!modal) return;
  modal.classList.remove('show');
  _restartCostPayload = null;
  _restartConfirmCooldownUntil = 0;
  if (_restartConfirmCooldownTimer) {
    clearInterval(_restartConfirmCooldownTimer);
    _restartConfirmCooldownTimer = null;
  }
  _setRestartConfirmButtonEnabled(true, 'в†є РќРђР§РђРўР¬ РЎРќРђР§РђР›Рђ (-10%)');
}

function restartCostContinueFree() {
  closeRestartCostModal();
  onBriefResumeClicked();
}

function restartCostConfirmRestart() {
  if (Date.now() < _restartConfirmCooldownUntil) {
    showToast('вЏі РџРѕРґРѕР¶РґРёС‚Рµ СЃРµРєСѓРЅРґСѓ РїРµСЂРµРґ РїРµСЂРµР·Р°РїСѓСЃРєРѕРј');
    return;
  }
  const resume = _getInProgressChapterState(currentChapter);
  const scoreNow = resume ? Math.max(0, Number(resume.score || 0)) : 0;
  const payloadPenalty = _restartCostPayload ? Number(_restartCostPayload.penaltyPoints || 0) : 0;
  const penaltyPoints = Math.max(0, payloadPenalty || _calcManualRestartPenaltyPoints(scoreNow));
  const chapterId = _stateCurrentChapterId();
  if (chapterId > 0) {
    Promise.resolve(sendResultToBot({
      type: 'manual_restart',
      chapter: chapterId,
      score: scoreNow,
      restart_penalty_points: penaltyPoints,
      game_over: false
    })).catch(() => {});
  }
  closeRestartCostModal();
  restartChapterFromStart(currentChapter, { manualPenaltyPoints: penaltyPoints });
  autoSync(false, true);
  if (penaltyPoints > 0) {
    showPenaltyNotice(`РЁС‚СЂР°С„ РїРµСЂРµР·Р°РїСѓСЃРєР°: -${penaltyPoints} РѕС‡ (10%)`, 6200);
  } else {
    showToast('РџРµСЂРµР·Р°РїСѓСЃРє РіР»Р°РІС‹ Р±РµР· С€С‚СЂР°С„Р°');
  }
}

function resumeChapter(idx) {
  const resume = _getInProgressChapterState(idx);
  if (!resume) {
    startChapter(idx);
    return;
  }
  unlockAudioInteraction();
  state.chapter = idx;
  state.cipherIdx = resume.cipherIdx;
  state._chapterInProgress = true;
  if (state.adminMode) {
    state.lives = Infinity;
  } else {
    const safeLives = Math.max(1, Math.min(5, Number(state.lives || 5)));
    state.lives = Number.isFinite(safeLives) ? safeLives : 5;
  }
  _recoverPendingSuccessCheckpoint();
  const chapter = CHAPTERS[idx];
  if (
    chapter &&
    Array.isArray(chapter.ciphers) &&
    chapter.ciphers.length > 0 &&
    state._pendingChapterFinish === true &&
    Number(state.cipherIdx || 0) >= chapter.ciphers.length - 1
  ) {
    state._pendingChapterFinish = false;
    state._awaitingNextCipher = false;
    saveState();
    Promise.resolve(finishChapter()).catch((err) => {
      console.error('resumeChapter -> finishChapter failed:', err);
      showChapterEndFallback();
    });
    return;
  }

  _activeCipherKey = _makeCipherKey(idx, state.cipherIdx);
  _clearActiveCipherResolved();
  _nextCipherBusy = false;
  saveState();
  loadCipher();
  showScreen('s-cipher');
}

let _missionCinematicTimer = null;
function showMissionCinematic(chapter, onDone) {
  if (!chapter) { if (typeof onDone === 'function') onDone(); return; }
  const prev = document.getElementById('mission-cinematic-overlay');
  if (prev) prev.remove();
  if (_missionCinematicTimer) {
    clearTimeout(_missionCinematicTimer);
    _missionCinematicTimer = null;
  }

  const overlay = document.createElement('div');
  overlay.id = 'mission-cinematic-overlay';
  overlay.style.cssText = `
    position:fixed;inset:0;z-index:12000;background:radial-gradient(circle at 50% 30%, rgba(42,33,12,.95), rgba(8,8,8,.98));
    display:flex;align-items:center;justify-content:center;padding:18px;backdrop-filter:blur(2px)`;
  const briefing = String(chapter.briefing || chapter.mission || '').replace(/\s+/g, ' ').trim();
  const shortBriefing = briefing.length > 240 ? (briefing.slice(0, 237) + '...') : briefing;
  const missionLine = `${chapter.subtitle || 'Р“Р»Р°РІР°'} В· ${chapter.place || ''}`.trim();
  overlay.innerHTML = `
    <div style="width:min(560px,95vw);border:1px solid rgba(255,224,51,.45);border-radius:12px;background:linear-gradient(160deg,rgba(32,25,10,.95),rgba(10,10,8,.95));box-shadow:0 14px 36px rgba(0,0,0,.55);padding:18px 16px">
      <div style="display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:10px">
        <div style="font-family:var(--head);font-size:13px;letter-spacing:.12em;color:#ffe58f">РЎР•РљР Р•РўРќР«Р™ Р‘Р РР¤РРќР“</div>
        <button id="mission-cinematic-skip" class="btn btn-small" style="margin:0;padding:6px 10px;font-size:10px">РџР РћРџРЈРЎРўРРўР¬</button>
      </div>
      <div style="display:flex;align-items:center;gap:10px;margin-bottom:8px">
        <div style="font-size:26px;filter:drop-shadow(0 0 10px rgba(255,224,51,.4))">${chapter.stamp || 'рџЋ–пёЏ'}</div>
        <div style="font-family:var(--head);font-size:18px;line-height:1.2;color:var(--accent)">${chapter.title || 'РћРїРµСЂР°С†РёСЏ'}</div>
      </div>
      <div style="font-size:11px;color:#d7c99a;letter-spacing:.08em;margin-bottom:10px">${missionLine}</div>
      <div id="mission-cinematic-text" style="min-height:72px;border:1px dashed rgba(255,224,51,.35);border-radius:8px;padding:10px 12px;background:rgba(255,224,51,.05);font-size:13px;line-height:1.6;color:#f2e8c8"></div>
      <div style="margin-top:12px;display:flex;justify-content:flex-end">
        <div style="font-size:11px;color:#d7c99a;letter-spacing:.1em">Р РђР”РРћРљРђРќРђР› РћРўРљР Р«Рў</div>
      </div>
    </div>`;
  document.body.appendChild(overlay);

  const textEl = overlay.querySelector('#mission-cinematic-text');
  const fullText = `РћРїРµСЂР°С†РёСЏ РЅР°С‡Р°С‚Р°. ${shortBriefing}`;
  let idx = 0;
  const typeTimer = setInterval(() => {
    if (!textEl) return;
    idx += 2;
    textEl.textContent = fullText.slice(0, idx);
    if (idx >= fullText.length) clearInterval(typeTimer);
  }, 22);

  let done = false;
  const finish = () => {
    if (done) return;
    done = true;
    clearInterval(typeTimer);
    if (_missionCinematicTimer) {
      clearTimeout(_missionCinematicTimer);
      _missionCinematicTimer = null;
    }
    overlay.style.transition = 'opacity .18s ease';
    overlay.style.opacity = '0';
    setTimeout(() => {
      try { overlay.remove(); } catch (e) {}
      if (typeof onDone === 'function') onDone();
    }, 180);
  };

  const skipBtn = overlay.querySelector('#mission-cinematic-skip');
  if (skipBtn) skipBtn.onclick = finish;
  _missionCinematicTimer = setTimeout(finish, Math.max(3000, Math.min(6200, 1200 + fullText.length * 24)));
  try { playSound('hint'); } catch (e) {}
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РЎРўРђР Рў Р“Р›РђР’Р«
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function startChapter(idx) {
  unlockAudioInteraction();
  state.chapter           = idx;
  state.cipherIdx         = 0;
  state._chapterInProgress = true;
  state._awaitingNextCipher = false;
  state._pendingChapterFinish = false;
  state._nextCipherTargetIdx = null;
  _activeCipherKey        = _makeCipherKey(idx, 0);
  _clearActiveCipherResolved();
  _nextCipherBusy         = false;
  // Р–РёР·РЅРё: Р±РµСЃРєРѕРЅРµС‡РЅС‹Рµ С‚РѕР»СЊРєРѕ РґР»СЏ adminMode, РґР»СЏ testerMode Рё player вЂ” 5
  state.lives             = state.adminMode ? Infinity : 5;
  state.chapterScore      = 0;
  state._manualRestartPenaltyPoints = 0;
  state.streak            = 0;
  state._chapterStartTime = Date.now();
  state._chapterErrors    = 0;
  state._chapterHints     = 0;
  saveState();
  const chapterMeta = CHAPTERS[idx];
  showMissionCinematic(chapterMeta, () => {
    loadCipher();
    showScreen('s-cipher');
  });
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р—РђР“Р РЈР—РљРђ РЁРР¤Р Рђ
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function loadCipher() {
  if (_successAutoAdvanceTimer) {
    clearTimeout(_successAutoAdvanceTimer);
    _successAutoAdvanceTimer = null;
  }
  const ch = CHAPTERS[state.chapter];
  if (!ch) { showScreen('s-chapters'); renderChapters(); return; }
  const cipher = ch.ciphers[state.cipherIdx];
  if (!cipher) {
    Promise.resolve(finishChapter()).catch((err) => {
      console.error('loadCipher -> finishChapter failed:', err);
      showChapterEndFallback();
    });
    return;
  }
  _activeCipherKey = _makeCipherKey();
  _clearActiveCipherResolved();
  _nextCipherBusy = false;

  // РЎР±СЂР°СЃС‹РІР°РµРј quiz-container
  const qc = document.getElementById('quiz-container');
  if (qc) qc.style.display = 'none';

  // Р­Р»РµРјРµРЅС‚С‹ РІРІРѕРґР°
  const inp      = document.getElementById('cipher-input');
  const inpWrap  = document.getElementById('cipher-input-wrap');
  const checkBtn = document.getElementById('btn-check') || document.querySelector('.btn-check');
  const hintBtn  = document.getElementById('btn-hint');
  const boxEl    = document.getElementById('cipher-box');

  if (cipher.type !== 'quiz') {
    // РћР±С‹С‡РЅС‹Рµ Р·Р°РґР°РЅРёСЏ вЂ” РїРѕРєР°Р·С‹РІР°РµРј РІСЃС‘
    if (inp)      { inp.style.display = ''; inp.value = ''; inp.disabled = false; inp.className = 'cipher-input'; }
    if (inpWrap)  inpWrap.style.display  = '';
    if (checkBtn) { checkBtn.style.display = ''; checkBtn.disabled = false; }
    if (hintBtn)  { hintBtn.style.display = ''; hintBtn.disabled = false; hintBtn.textContent = 'рџ’Ў РџРћР”РЎРљРђР—РљРђ'; }
    if (boxEl)    boxEl.style.display    = '';
  } else {
    // Quiz вЂ” СЃРєСЂС‹РІР°РµРј input, РїРѕРєР°Р·С‹РІР°РµРј С‚РѕР»СЊРєРѕ quiz-container
    if (inp)      inp.style.display      = 'none';
    if (inpWrap)  inpWrap.style.display  = 'none';
    if (checkBtn) { checkBtn.style.display = 'none'; checkBtn.disabled = false; }
    // hint РѕСЃС‚Р°РІР»СЏРµРј РІРёРґРёРјС‹Рј РґР»СЏ quiz С‚РѕР¶Рµ
    if (boxEl)    boxEl.style.display    = 'none';
  }
  state.startTime = Date.now();

  // РџСЂРѕРіСЂРµСЃСЃ-С‚РѕС‡РєРё (5 С€С‚СѓРє)
  const prog = document.getElementById('cipher-progress');
  if (prog) {
    prog.innerHTML = ch.ciphers.map((c, i) =>
      `<div class="cipher-prog-dot ${i < state.cipherIdx ? 'done' : i === state.cipherIdx ? 'active' : ''}"></div>`
    ).join('');
  }

  const chapterLabelEl = document.getElementById('cipher-chapter-label');
  if (chapterLabelEl) chapterLabelEl.textContent = ch.subtitle + ' \u00B7 ' + ch.place;
  const scoreEl = document.getElementById('cipher-score-display');
  if (scoreEl) scoreEl.textContent = state.chapterScore + ' РѕС‡';
  const typeLabelEl = document.getElementById('cipher-type-label');
  if (typeLabelEl) typeLabelEl.textContent = cipher.typeLabel;
  const taskEl = document.getElementById('cipher-task');
  if (taskEl) taskEl.textContent = cipher.task;

  // РЁРёС„СЂРѕРІР°РЅРЅС‹Р№ С‚РµРєСЃС‚ / Р·Р°РґР°РЅРёРµ
  const box = document.getElementById('cipher-box');
  if (box) {
    if (cipher.type !== 'quiz') box.style.display = '';
    box.setAttribute('data-num', String(state.cipherIdx + 1).padStart(3,'0'));
    box.style.position = 'relative';

    if (cipher.type === 'morse') {
      box.innerHTML = '';
      animateMorse(box, cipher.encrypted);
    } else if (cipher.type === 'quiz') {
      box.style.display = '';
      box.innerHTML = '';
    } else if (cipher.type === 'anagram') {
      box.textContent = '';
      box.style.display = 'none';
    } else if (cipher.type === 'math') {
      box.style.display = '';
      box.innerHTML = `<div class="math-formula">${cipher.encrypted}</div>`;
    } else if (cipher.type === 'photo') {
      box.style.display = '';
      box.innerHTML = `<div class="photo-task-img">${cipher.image}</div>
        <div style="font-family:var(--mono);font-size:var(--fs-2xl);text-align:center;padding:8px 0">${cipher.encrypted}</div>`;
    } else if (cipher.type === 'map') {
      box.style.display = '';
      box.innerHTML = '';
      renderMap(cipher);
    } else {
      box.style.display = '';
      box.textContent = cipher.encrypted;
    }
  } else {
    console.warn('loadCipher: cipher-box element not found');
  }

  // РџРѕР»Рµ РІРІРѕРґР° вЂ” СЃРєСЂС‹РІР°РµРј РґР»СЏ РєР°СЂС‚С‹ Рё Р°РЅР°РіСЂР°РјРјС‹
  const inputWrap = document.querySelector('.cipher-input-wrap');
  const actions   = document.querySelector('.cipher-actions');
  const btnCheck  = document.querySelector('.btn-check');
  if (cipher.type === 'map') {
    if (inputWrap) inputWrap.style.display = 'none';
    if (btnCheck) { btnCheck.style.display = 'none'; btnCheck.disabled = false; }
  } else if (cipher.type === 'anagram') {
    if (inputWrap) inputWrap.style.display = 'none';
    if (btnCheck) { btnCheck.style.display = 'none'; btnCheck.disabled = false; }
  } else {
    if (inputWrap) inputWrap.style.display = '';
    if (btnCheck) { btnCheck.style.display = ''; btnCheck.disabled = false; }
  }

  // Р–РёР·РЅРё
  renderLives();
  _timerCancelled = false;
  startTimer();
  _syncTimerCancelButton();
  syncMusicButtons();

  // РЎР±СЂРѕСЃ РІРІРѕРґР° (inp СѓР¶Рµ РѕР±СЉСЏРІР»РµРЅ РІС‹С€Рµ)
  const inpReset = document.getElementById('cipher-input');
  if (inpReset) { inpReset.value = ''; inpReset.className = 'cipher-input'; inpReset.disabled = false; }
  // Р”Р»СЏ С‚РµРєСЃС‚РѕРІС‹С… Р·Р°РґР°РЅРёР№ СЃСЂР°Р·Сѓ РѕС‚РєСЂС‹РІР°РµРј РєР»Р°РІРёР°С‚СѓСЂСѓ Рё СЃС‚Р°РІРёРј РєСѓСЂСЃРѕСЂ.
  // Р”Р»СЏ map/anagram/quiz РєР»Р°РІРёР°С‚СѓСЂР° РЅРµ РЅСѓР¶РЅР°.
  const shouldShowKeyboard = cipher.type !== 'map' && cipher.type !== 'anagram' && cipher.type !== 'quiz';
  _setKeyboardVisibility(shouldShowKeyboard);
  if (shouldShowKeyboard && inpReset) {
    setTimeout(() => {
      try { inpReset.focus(); } catch (e) {}
    }, 0);
  }
  state.hintsUsed = false;
  const hb = document.getElementById('cipher-hint-box');
  const ht = document.getElementById('cipher-hint-text');
  const bh = document.getElementById('btn-hint');
  if (hb) hb.className = 'cipher-hint-box';
  if (ht) ht.textContent = '';
  if (bh) { bh.disabled = false; bh.textContent = 'рџ’Ў РџРћР”РЎРљРђР—РљРђ'; }

  // РЎРїСЂР°РІРѕС‡РЅРёРє
  renderRef(cipher.type, cipher.shift);
  // Р”Р»СЏ Р°РЅР°РіСЂР°РјРјС‹ Рё РєР°СЂС‚С‹ СЂРµРЅРґРµСЂРёРј РџРћРЎР›Р• renderRef (РёРЅР°С‡Рµ РїРµСЂРµР·Р°РїРёС€РµС‚)
  if (cipher.type === 'anagram') renderAnagram(cipher);
  if (cipher.type === 'map')     renderMap(cipher);
  if (cipher.type === 'quiz')    renderQuiz(cipher);
  if (shouldShowKeyboard && inp) inp.focus();
}

function renderLives() {
  const el = document.getElementById('cipher-attempts-label');
  if (!el) {
    syncTopStatusBars();
    return;
  }
  if (state.adminMode) {
    el.innerHTML = '<div style="line-height:1.2;text-align:center;color:gold">в™ѕпёЏ РђР”РњРРќ</div>';
    syncTopStatusBars();
    return;
  }
  // РўРµСЃС‚РёСЂРѕРІС‰РёРє Рё РёРіСЂРѕРє вЂ” РѕР±С‹С‡РЅС‹Рµ 5 Р¶РёР·РЅРµР№
  const n = Math.max(0, Math.min(5, state.lives));
  let row1 = '', row2 = '';
  for(let i=0;i<3;i++) row1 += i < n ? 'вќ¤пёЏ' : 'рџ–¤';
  for(let i=3;i<5;i++) row2 += i < n ? 'вќ¤пёЏ' : 'рџ–¤';
  const badge = state.testerMode
    ? '<div style="font-size:9px;color:rgba(100,200,255,.7);letter-spacing:.06em">рџ§Є РўР•РЎРў</div>'
    : '';
  el.innerHTML = '<div style="line-height:1.2;text-align:center"><div>' + row1 + '</div><div>' + row2 + '</div>' + badge + '</div>';
  syncTopStatusBars();
}

function animateLifeLoss() {
  const el = document.getElementById('cipher-attempts-label');
  if (!el) return;
  el.style.transition = 'transform .15s,filter .15s';
  el.style.transform = 'scale(1.3)';
  el.style.filter = 'drop-shadow(0 0 8px rgba(255,58,58,.9))';
  setTimeout(() => { el.style.transform = ''; el.style.filter = ''; renderLives(); }, 200);
}

let _timerInterval = null;
let _timerCancelled = false;
let _riskIntensity = 0;
let _timerStartMs = 0;

function _setRiskIntensity(v) {
  _riskIntensity = _clamp(Number(v) || 0, 0, 1);
}

function _resetCipherUrgencyUI() {
  _setRiskIntensity(0);
  const timerEl = document.getElementById('cipher-timer');
  if (timerEl) {
    timerEl.style.transform = '';
    timerEl.style.textShadow = '';
    timerEl.style.animation = '';
  }
  const box = document.getElementById('cipher-box');
  if (box) {
    box.style.boxShadow = '';
    box.style.borderColor = '';
  }
  const task = document.getElementById('cipher-task');
  if (task) task.style.textShadow = '';
}

function _applyCipherUrgency(elapsedSec) {
  const sec = Math.max(0, Number(elapsedSec || 0));
  let risk = 0;
  if (sec >= 25) risk = 0.25;
  if (sec >= 40) risk = 0.6;
  if (sec >= 55) risk = 1;
  _setRiskIntensity(risk);

  const timerEl = document.getElementById('cipher-timer');
  if (timerEl) {
    if (risk >= 1) {
      timerEl.style.animation = 'pulse 0.7s ease-in-out infinite';
      timerEl.style.transform = 'scale(1.08)';
      timerEl.style.textShadow = '0 0 18px rgba(255,58,58,.75)';
    } else if (risk >= 0.6) {
      timerEl.style.animation = 'pulse 1.2s ease-in-out infinite';
      timerEl.style.transform = 'scale(1.03)';
      timerEl.style.textShadow = '0 0 12px rgba(255,170,85,.6)';
    } else if (risk >= 0.25) {
      timerEl.style.animation = '';
      timerEl.style.transform = 'scale(1.01)';
      timerEl.style.textShadow = '0 0 8px rgba(255,224,51,.45)';
    } else {
      timerEl.style.animation = '';
      timerEl.style.transform = '';
      timerEl.style.textShadow = '';
    }
  }
  const box = document.getElementById('cipher-box');
  if (box) {
    if (risk >= 1) {
      box.style.borderColor = 'rgba(255,58,58,.65)';
      box.style.boxShadow = '0 0 18px rgba(255,58,58,.35)';
    } else if (risk >= 0.6) {
      box.style.borderColor = 'rgba(255,170,85,.55)';
      box.style.boxShadow = '0 0 14px rgba(255,170,85,.28)';
    } else if (risk >= 0.25) {
      box.style.borderColor = 'rgba(255,224,51,.45)';
      box.style.boxShadow = '0 0 10px rgba(255,224,51,.2)';
    } else {
      box.style.borderColor = '';
      box.style.boxShadow = '';
    }
  }
  const task = document.getElementById('cipher-task');
  if (task) task.style.textShadow = risk >= 0.6 ? '0 0 8px rgba(255,170,85,.35)' : '';
}

function _syncTimerCancelButton() {
  const btn = document.getElementById('btn-cancel-timer');
  if (!btn) return;
  if (_timerCancelled) {
    btn.disabled = true;
    btn.style.opacity = '0.55';
    btn.textContent = 'вЏ± РўРђР™РњР•Р  РћРўРљР›Р®Р§Р•Рќ';
  } else {
    btn.disabled = false;
    btn.style.opacity = '1';
    btn.textContent = 'вЏ№ РћРўРњР•РќРРўР¬ РўРђР™РњР•Р ';
  }
}

function startTimer() {
  if (_timerInterval) clearInterval(_timerInterval);
  const el = document.getElementById('cipher-timer');
  if (!el) return;
  _timerStartMs = Date.now();
  el.textContent = '0СЃ';
  el.style.color = 'var(--muted)';
  _applyCipherUrgency(0);
  _timerInterval = setInterval(() => {
    const sec = Math.round((Date.now() - _timerStartMs) / 1000);
    el.textContent = sec + 'СЃ';
    if (sec <= 10) el.style.color = 'var(--green)';
    else if (sec <= 30) el.style.color = 'var(--accent)';
    else if (sec <= 60) el.style.color = 'var(--accent2)';
    else el.style.color = 'var(--red)';
    _applyCipherUrgency(sec);
  }, 1000);
}

function stopTimer() {
  if (_timerInterval) { clearInterval(_timerInterval); _timerInterval = null; }
  const el = document.getElementById('cipher-timer');
  if (el) el.style.color = 'var(--muted)';
  _resetCipherUrgencyUI();
}

function cancelCipherTimer() {
  if (_timerCancelled) return;
  _timerCancelled = true;
  stopTimer();
  const el = document.getElementById('cipher-timer');
  if (el) {
    el.textContent = 'Р±РµР· С‚Р°Р№РјРµСЂР°';
    el.style.color = 'var(--green)';
  }
  _resetCipherUrgencyUI();
  _syncTimerCancelButton();
  showToast('вЏ± РўР°Р№РјРµСЂ РѕС‚РєР»СЋС‡С‘РЅ РґР»СЏ С‚РµРєСѓС‰РµРіРѕ Р·Р°РґР°РЅРёСЏ');
}

function getEffectiveElapsed(startTimeMs) {
  const elapsed = Math.round((Date.now() - startTimeMs) / 1000);
  return _timerCancelled ? 0 : elapsed;
}

function animateMorse(el, text) {
  el.innerHTML = '<span class="morse-animate">' +
    text.split('').map((ch, i) =>
      `<span style="animation-delay:${i*25}ms">${ch === ' ' ? '&nbsp;' : ch}</span>`
    ).join('') + '</span>';
}

function renderRef(type, shift) {
  const ref = document.getElementById('cipher-ref');
  // РђР»С„Р°РІРёС‚ С‚РѕР»СЊРєРѕ РґР»СЏ С€РёС„СЂРѕРІ: caesar, atbash, morse
  if (!['caesar', 'atbash', 'morse'].includes(type)) {
    if (ref) ref.innerHTML = '';
    return;
  }
  if (type === 'morse') {
    ref.innerHTML = `<div class="cipher-ref-title">// РўРђР‘Р›РР¦Рђ РњРћР Р—Р•</div>
    <div style="font-size:var(--fs-base);color:var(--accent);font-weight:700;margin-bottom:6px;letter-spacing:.04em;line-height:1.6">
      <span style="color:#ffe033">/ </span>вЂ” СЂР°Р·РґРµР»РёС‚РµР»СЊ Р±СѓРєРІ &nbsp;&nbsp;|&nbsp;&nbsp; <span style="color:#ffe033">// </span>вЂ” СЂР°Р·РґРµР»РёС‚РµР»СЊ СЃР»РѕРІ
    </div>
    <div style="font-size:var(--fs-sm);color:var(--muted);margin-bottom:8px;background:rgba(255,224,51,.05);border:1px solid rgba(255,224,51,.15);border-radius:4px;padding:7px 10px;line-height:1.8">
      РџСЂРёРјРµСЂ: <span style="color:#ffe033;font-family:var(--mono)">.-/.-././/--/.-.</span><br>
      Рђ<span style="color:#ffe033">/</span>Р›<span style="color:#ffe033">/</span>Р•<span style="color:#ffe033">/</span>Р•<span style="color:rgba(255,224,51,.4)">//</span>РњР  в†’ РђР›Р•Р• РњР 
    </div>
    <div style="font-size:var(--fs-lg);color:var(--accent2);font-weight:700;text-align:center;padding:8px 0;letter-spacing:.06em;border:1px solid rgba(255,90,90,.3);border-radius:4px;margin-bottom:8px">
      вљ пёЏ Р”РћР§РРўРђР™РўР• РўРђР‘Р›РР¦РЈ Р”Рћ РљРћРќР¦Рђ!
    </div>
    <div class="morse-ref">${Object.entries(MORSE_TABLE).map(([l,c])=>`<div class="morse-item">${l} ${c}</div>`).join('')}</div>`;
  } else if (type === 'atbash') {
    const a = RU_ALPHA;
    ref.innerHTML = `<div class="cipher-ref-title">// РўРђР‘Р›РР¦Рђ РђРўР‘РђРЁ (Р·РµСЂРєР°Р»СЊРЅС‹Р№ Р°Р»С„Р°РІРёС‚)</div>
    <div class="caesar-ref">${a.split('').map((l,i)=>`${a[a.length-1-i]}в†’${l}`).join('  ')}</div>`;
  } else if (type === 'num') {
    const a = RU_ALPHA;
    ref.innerHTML = `<div class="cipher-ref-title">// Р§РРЎР›РћР’РћР™ РљРћР” (Р±СѓРєРІР° = РЅРѕРјРµСЂ РІ Р°Р»С„Р°РІРёС‚Рµ)</div>
    <div class="caesar-ref">${a.split('').map((l,i)=>`${i+1}=${l}`).join('  ')}</div>`;
  } else if (type === 'anagram' || type === 'map') {
    ref.innerHTML = ''; // СЂРµРЅРґРµСЂРёС‚СЃСЏ РѕС‚РґРµР»СЊРЅРѕ
  } else if (type === 'math') {
    ref.innerHTML = `<div class="cipher-ref-title">// РџРћР”РЎРљРђР—РљРђ</div>
    <div style="font-size:var(--fs-sm);color:var(--muted);padding:8px 0;line-height:1.7">Р’РІРµРґРё РѕС‚РІРµС‚ С†РёС„СЂРѕР№ РІ РїРѕР»Рµ РЅРёР¶Рµ.</div>`;
  } else if (type === 'photo') {
    ref.innerHTML = `<div class="cipher-ref-title">// РџРћР”РЎРљРђР—РљРђ</div>
    <div style="font-size:var(--fs-sm);color:var(--muted);padding:8px 0;line-height:1.7">Р’РІРµРґРё РѕС‚РІРµС‚ СЃР»РѕРІР°РјРё РІ РїРѕР»Рµ РЅРёР¶Рµ.</div>`;
  } else {
    const alpha = RU_ALPHA;
    ref.innerHTML = `<div class="cipher-ref-title">// РђР›Р¤РђР’РРў Р”Р›РЇ Р РђРЎРЁРР¤Р РћР’РљР (СЃРґРІРёРі ${shift})</div>
    <div class="caesar-ref">${alpha.split('').map((l,i)=>{
      const fromIdx = (i + shift) % alpha.length;
      return `${alpha[fromIdx]}в†’${l}`;
    }).join('  ')}</div>`;
  }
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РЎРРЎРўР•РњРђ РћР§РљРћР’ Р—Рђ РЎРљРћР РћРЎРўР¬
//  РњР°РєСЃ = points С€РёС„СЂР°. Р—Р° РєР°Р¶РґС‹Рµ 15 СЃРµРє вЂ” -10% (РјРёРЅРёРјСѓРј 20%)
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function calcPoints(cipher, elapsed) {
  const max   = cipher.points;
  const secs  = Math.max(0, elapsed);
  const penalty = Math.floor(Math.max(0, secs - 10) / 10) * 0.08;
  let factor  = Math.max(0.20, 1 - penalty);
  // РЁС‚СЂР°С„ Р·Р° РїРѕРІС‚РѕСЂ: -5% Р·Р° РєР°Р¶РґС‹Р№ РїСЂРѕРІР°Р» РіР»Р°РІС‹ (5%, 10%, 15%...)
  if (state.retryPenalty) {
    const ch = CHAPTERS[state.chapter];
    const fails = ch && state.chapterFailCounts ? (state.chapterFailCounts[ch.id] || 1) : 1;
    const penaltyFactor = Math.max(0.50, 1 - fails * 0.05);
    factor = Math.max(0.10, factor * penaltyFactor);
  }
  const streakBonus = Math.min(0.30, (state.streak || 0) * 0.05);
  factor = Math.min(1.30, factor + streakBonus);
  return Math.round(max * factor);
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РџР РћР’Р•Р РљРђ РћРўР’Р•РўРђ
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
const _SHA256_K = [
  0x428a2f98, 0x71374491, 0xb5c0fbcf, 0xe9b5dba5, 0x3956c25b, 0x59f111f1, 0x923f82a4, 0xab1c5ed5,
  0xd807aa98, 0x12835b01, 0x243185be, 0x550c7dc3, 0x72be5d74, 0x80deb1fe, 0x9bdc06a7, 0xc19bf174,
  0xe49b69c1, 0xefbe4786, 0x0fc19dc6, 0x240ca1cc, 0x2de92c6f, 0x4a7484aa, 0x5cb0a9dc, 0x76f988da,
  0x983e5152, 0xa831c66d, 0xb00327c8, 0xbf597fc7, 0xc6e00bf3, 0xd5a79147, 0x06ca6351, 0x14292967,
  0x27b70a85, 0x2e1b2138, 0x4d2c6dfc, 0x53380d13, 0x650a7354, 0x766a0abb, 0x81c2c92e, 0x92722c85,
  0xa2bfe8a1, 0xa81a664b, 0xc24b8b70, 0xc76c51a3, 0xd192e819, 0xd6990624, 0xf40e3585, 0x106aa070,
  0x19a4c116, 0x1e376c08, 0x2748774c, 0x34b0bcb5, 0x391c0cb3, 0x4ed8aa4a, 0x5b9cca4f, 0x682e6ff3,
  0x748f82ee, 0x78a5636f, 0x84c87814, 0x8cc70208, 0x90befffa, 0xa4506ceb, 0xbef9a3f7, 0xc67178f2
];

function _rotr(x, n) {
  return (x >>> n) | (x << (32 - n));
}

function _utf8Bytes(str) {
  if (typeof TextEncoder !== 'undefined') {
    return Array.from(new TextEncoder().encode(str));
  }
  const out = [];
  const encoded = encodeURIComponent(str).replace(
    /%([0-9A-F]{2})/g,
    (_, p) => String.fromCharCode(parseInt(p, 16))
  );
  for (let i = 0; i < encoded.length; i++) out.push(encoded.charCodeAt(i));
  return out;
}

function _sha256Fallback(str) {
  const bytes = _utf8Bytes(String(str == null ? '' : str));
  const bitLen = bytes.length * 8;
  bytes.push(0x80);
  while ((bytes.length % 64) !== 56) bytes.push(0);

  const high = Math.floor(bitLen / 0x100000000) >>> 0;
  const low = bitLen >>> 0;
  bytes.push((high >>> 24) & 0xff, (high >>> 16) & 0xff, (high >>> 8) & 0xff, high & 0xff);
  bytes.push((low >>> 24) & 0xff, (low >>> 16) & 0xff, (low >>> 8) & 0xff, low & 0xff);

  let h0 = 0x6a09e667, h1 = 0xbb67ae85, h2 = 0x3c6ef372, h3 = 0xa54ff53a;
  let h4 = 0x510e527f, h5 = 0x9b05688c, h6 = 0x1f83d9ab, h7 = 0x5be0cd19;
  const w = new Array(64);

  for (let i = 0; i < bytes.length; i += 64) {
    for (let t = 0; t < 16; t++) {
      const j = i + t * 4;
      w[t] = ((bytes[j] << 24) | (bytes[j + 1] << 16) | (bytes[j + 2] << 8) | bytes[j + 3]) >>> 0;
    }
    for (let t = 16; t < 64; t++) {
      const s0 = _rotr(w[t - 15], 7) ^ _rotr(w[t - 15], 18) ^ (w[t - 15] >>> 3);
      const s1 = _rotr(w[t - 2], 17) ^ _rotr(w[t - 2], 19) ^ (w[t - 2] >>> 10);
      w[t] = (w[t - 16] + s0 + w[t - 7] + s1) >>> 0;
    }

    let a = h0, b = h1, c = h2, d = h3, e = h4, f = h5, g = h6, h = h7;
    for (let t = 0; t < 64; t++) {
      const S1 = _rotr(e, 6) ^ _rotr(e, 11) ^ _rotr(e, 25);
      const ch = (e & f) ^ (~e & g);
      const temp1 = (h + S1 + ch + _SHA256_K[t] + w[t]) >>> 0;
      const S0 = _rotr(a, 2) ^ _rotr(a, 13) ^ _rotr(a, 22);
      const maj = (a & b) ^ (a & c) ^ (b & c);
      const temp2 = (S0 + maj) >>> 0;
      h = g;
      g = f;
      f = e;
      e = (d + temp1) >>> 0;
      d = c;
      c = b;
      b = a;
      a = (temp1 + temp2) >>> 0;
    }

    h0 = (h0 + a) >>> 0;
    h1 = (h1 + b) >>> 0;
    h2 = (h2 + c) >>> 0;
    h3 = (h3 + d) >>> 0;
    h4 = (h4 + e) >>> 0;
    h5 = (h5 + f) >>> 0;
    h6 = (h6 + g) >>> 0;
    h7 = (h7 + h) >>> 0;
  }

  return [h0, h1, h2, h3, h4, h5, h6, h7]
    .map(v => v.toString(16).padStart(8, '0'))
    .join('');
}

async function sha256(str) {
  const text = String(str == null ? '' : str);
  try {
    if (window.crypto && window.crypto.subtle && typeof window.crypto.subtle.digest === 'function') {
      const buf = await window.crypto.subtle.digest('SHA-256', new TextEncoder().encode(text));
      return Array.from(new Uint8Array(buf)).map(b => b.toString(16).padStart(2, '0')).join('');
    }
  } catch (e) {
    console.warn('crypto.subtle unavailable, using fallback SHA-256:', e);
  }
  return _sha256Fallback(text);
}

async function checkAnswer() {
  if (_checkAnswerBusy) return;
  _checkAnswerBusy = true;
  try {
  if (_isActiveCipherResolved()) {
    // Already solved: if transition flags were dropped by sync, recover.
    _recoverSolvedCipherStall('checkAnswer');
    return;
  }
  const chapter = CHAPTERS[state.chapter];
  if (!chapter || !Array.isArray(chapter.ciphers)) {
    console.warn('checkAnswer: chapter is unavailable', state.chapter);
    _checkAnswerBusy = false;
    return;
  }
  const cipher = chapter.ciphers[state.cipherIdx];
  if (!cipher) {
    console.warn('checkAnswer: cipher is unavailable', state.cipherIdx);
    _checkAnswerBusy = false;
    return;
  }
  // Р•СЃР»Рё С‚РµРєСѓС‰РµРµ Р·Р°РґР°РЅРёРµ вЂ” quiz, РґРµР»РµРіРёСЂСѓРµРј РІ quizSubmit
  if (cipher.type === 'quiz') {
    if (typeof quizSubmit === 'function') quizSubmit();
    _checkAnswerBusy = false;
    return;
  }
  const inp    = document.getElementById('cipher-input');
  if (!inp) {
    console.warn('checkAnswer: cipher-input element not found');
    _checkAnswerBusy = false;
    return;
  }
  const rawVal = inp.value || '';
  const val = rawVal.trim().toUpperCase().replace(/\s+/g,' ');
  const correct = cipher.answer; // С…РµС€
  if (!correct || typeof correct !== 'string') {
    console.warn('checkAnswer: missing answer hash for cipher', cipher);
    _checkAnswerBusy = false;
    return;
  }

  if (!val) { inp.focus(); _checkAnswerBusy = false; return; }

  const elapsed = getEffectiveElapsed(state.startTime);

  const normVal = _normalizeForCompare(rawVal);

  // РќРµСЃРєРѕР»СЊРєРѕ СЌРєРІРёРІР°Р»РµРЅС‚РЅС‹С… РІР°СЂРёР°РЅС‚РѕРІ, С‡С‚РѕР±С‹ СѓР±СЂР°С‚СЊ Р»РѕР¶РЅС‹Рµ "РЅРµРІРµСЂРЅРѕ"
  const candidates = new Set([
    val,
    val.replace(/\s+/g, ''),
    normVal,
    normVal.replace(/\s+/g, ''),
    rawVal.trim(),
  ]);
  const digitsOnly = normVal.replace(/\D/g, '');
  if (digitsOnly) candidates.add(digitsOnly);

  const hashes = await Promise.all(Array.from(candidates).filter(Boolean).map(c => sha256(c)));
  let isCorrect = hashes.includes(correct);

  if (isCorrect) {
    // в”Ђв”Ђ РџР РђР’РР›Р¬РќРћ в”Ђв”Ђ
    inp.className  = 'cipher-input correct';
    inp.disabled   = true;
    _markActiveCipherResolved();
    const checkBtn = document.getElementById('btn-check') || document.querySelector('.btn-check');
    if (checkBtn) checkBtn.disabled = true;
    state.streak = (state.streak || 0) + 1;
    _registerSecretAnswerEvent({
      elapsed,
      noHint: Number(state._chapterHints || 0) <= 0,
      oneLife: !state.adminMode && Number(state.lives || 0) === 1,
      cipherType: cipher.type,
      streak: state.streak || 0,
    });
    playSound('correct');
    const pts      = calcPoints(cipher, elapsed);
    state.chapterScore += pts;
    saveState();
    autoSync(false, true);
    stopTimer();
    if (elapsed <= 5) state._fastAnswers = (state._fastAnswers || 0) + 1;
    registerSolvedCipherType(cipher.type);
    const isFirstTry = (state._chapterErrors || 0) === 0;
    const cipherTypeCtx = {
      cipherType:     cipher.type,
      quizCorrect:    cipher.type === 'quiz',
      mathCorrect:    cipher.type === 'math',
      anagramCorrect: cipher.type === 'anagram',
      mapCorrect:     cipher.type === 'map',
      morseFast:      cipher.type === 'morse' && elapsed <= 10,
    };
    try {
      checkAchievements({ elapsed, firstTry: isFirstTry, ...cipherTypeCtx });
    } catch (achErr) {
      console.warn('checkAnswer: achievements update skipped', achErr);
    }
    setTimeout(() => showSuccess(cipher, pts, elapsed), 400);

  } else {
    // в”Ђв”Ђ РќР•РџР РђР’РР›Р¬РќРћ в”Ђв”Ђ
    inp.className = 'cipher-input wrong';
    playSound('wrong');
    setTimeout(() => inp.className = 'cipher-input', 500);
    state.streak = 0;
    state._chapterErrors = (state._chapterErrors || 0) + 1;
    _registerSecretBreakEvent();
    if (!state.adminMode) {
      state.lives--;
      animateLifeLoss();
      screenPulseRed();
      playSound('life_lost');
    }
    saveState();
    autoSync(false, true);

    if (state.lives <= 0) {
      _handleOutOfLives();
    } else {
      renderLives();
    }
  }
  } finally {
    _checkAnswerBusy = false;
  }
}

function isCloseEnough(val, correct) {
  if (val === correct) return true;
  if (val.replace(/\s/g,'') === correct.replace(/\s/g,'')) return true;
  if (correct.length <= 8 && levenshtein(val, correct) === 1) return true;
  return false;
}

function levenshtein(a, b) {
  const m=a.length, n=b.length;
  const dp=Array.from({length:m+1},(_,i)=>Array.from({length:n+1},(_,j)=>i||j));
  for(let i=1;i<=m;i++) for(let j=1;j<=n;j++)
    dp[i][j]=a[i-1]===b[j-1]?dp[i-1][j-1]:1+Math.min(dp[i-1][j],dp[i][j-1],dp[i-1][j-1]);
  return dp[m][n];
}

function showHint() {
  const cipher = CHAPTERS[state.chapter].ciphers[state.cipherIdx];
  if (state.hintsUsed) return;
  state.hintsUsed = true;
  _registerSecretBreakEvent();

  // РџРѕРєР°Р·С‹РІР°РµРј РїРѕРґСЃРєР°Р·РєСѓ РІ hint-box
  playSound('hint');
  const hintBox  = document.getElementById('cipher-hint-box');
  const hintText = document.getElementById('cipher-hint-text');
  if (hintBox && hintText) {
    hintText.textContent = cipher.hint;
    hintBox.className = 'cipher-hint-box show';
  }
  const btn = document.getElementById('btn-hint');
  if (btn) { btn.disabled = true; btn.textContent = 'вњ“ РџРћР”РЎРљРђР—РљРђ'; }

  // Р”Р»СЏ РєР°СЂС‚С‹ вЂ” РґРѕРїРѕР»РЅРёС‚РµР»СЊРЅРѕ РїРѕРєР°Р·С‹РІР°РµРј hint РІ map-hint-text
  if (cipher.type === 'map') {
    const mht = document.getElementById('map-hint-text');
    if (mht) mht.textContent = 'рџ’Ў ' + cipher.hint;
  }

  // РџРѕРґСЃРєР°Р·РєР° СЃС‚РѕРёС‚ Р¶РёР·РЅСЊ (РЅРѕ РЅРµ РґР»СЏ Р°РґРјРёРЅР°)
  if (!state.adminMode && state.lives > 1) {
    state.lives--;
    state._chapterHints = (state._chapterHints || 0) + 1;
    renderLives();
  }
  saveState();
  autoSync(false, true);
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р­РљР РђРќ РЈРЎРџР•РҐРђ (РѕРґРёРЅ С€РёС„СЂ)
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function showSuccess(cipher, pts, elapsed) {
  if (_successAutoAdvanceTimer) {
    clearTimeout(_successAutoAdvanceTimer);
    _successAutoAdvanceTimer = null;
  }
  _markActiveCipherResolved();
  const ch = CHAPTERS[state.chapter];
  const chapterLen = (ch && Array.isArray(ch.ciphers)) ? ch.ciphers.length : 0;
  const currentIdx = Math.max(0, Number(state.cipherIdx || 0));
  const isLast = chapterLen > 0 ? currentIdx >= (chapterLen - 1) : true;
  state._chapterInProgress = true;
  state._awaitingNextCipher = !isLast;
  state._pendingChapterFinish = isLast;
  state._nextCipherTargetIdx = isLast ? null : (currentIdx + 1);
  saveState();
  autoSync(false, true);

  const quizOption = (cipher.options && cipher.options[cipher.correctIndex]) || 'Р’РµСЂРЅРѕ';
  const solvedLabel = cipher.type === 'quiz' ? quizOption : 'РћС‚РІРµС‚ РїСЂРёРЅСЏС‚';
  document.getElementById('succ-answer').textContent = solvedLabel;
  document.getElementById('succ-cipher-name').textContent = '// ' + cipher.typeLabel;
  const factEl = document.getElementById('succ-fact');
  if (factEl) {
    factEl.innerHTML = cipher.fact
      ? '<span style="color:var(--accent3);margin-right:4px">рџ“њ</span>' + cipher.fact
      : '';
  }
  document.getElementById('succ-pts').textContent = pts > 0 ? '+' + pts : '0';
  document.getElementById('succ-time').textContent = typeof elapsed === 'number' ? elapsed + 'СЃ' : elapsed;

  const speedEl = document.getElementById('succ-speed');
  const streak = state.streak || 0;
  let speedText = '';
  let speedColor = 'var(--muted)';
  if (elapsed <= 10) { speedText = 'вљЎ РњРѕР»РЅРёРµРЅРѕСЃРЅРѕ!'; speedColor = 'var(--green)'; }
  else if (elapsed <= 20) { speedText = 'рџ”Ґ Р‘С‹СЃС‚СЂРѕ!'; speedColor = 'var(--accent)'; }
  else if (elapsed > 60) { speedText = 'рџђў РњРµРґР»РµРЅРЅРѕ...'; }
  else { speedText = 'вЏ± ' + elapsed + 'СЃ'; }
  if (streak >= 2) {
    const bonus = Math.min(30, streak * 5);
    speedText += '  рџ”Ґ РЎС‚СЂРёРє x' + streak + ' (+' + bonus + '%)';
    speedColor = streak >= 4 ? 'var(--green)' : 'var(--accent)';
  }
  if (speedEl) {
    speedEl.textContent = speedText;
    speedEl.style.color = speedColor;
  }

  const btn = document.getElementById('succ-next-btn');
  if (btn) {
    btn.textContent = isLast ? 'Р—РђР’Р•Р РЁРРўР¬ Р“Р›РђР’РЈ в†’' : 'РЎР›Р•Р”РЈР®Р©РР™ РЁРР¤Р  в†’';
    btn.disabled = false;
    btn.onclick = () => nextCipher();
  }

  if (pts > 0) {
    launchConfetti();
    setTimeout(() => {
      const pointsEl = document.getElementById('succ-pts');
      if (pointsEl) flashScore(pointsEl);
    }, 300);
  }

  showScreen('s-success');
  _successAutoAdvanceTimer = setTimeout(() => {
    const successScreen = document.getElementById('s-success');
    if (!successScreen || !successScreen.classList.contains('active')) return;
    if (!state._awaitingNextCipher && !state._pendingChapterFinish) return;
    try {
      nextCipher();
    } catch (e) {
      console.error('showSuccess auto-advance failed:', e);
    }
  }, 12000);
}

async function nextCipher(expectedCipherKey = null) {
  if (_successAutoAdvanceTimer) {
    clearTimeout(_successAutoAdvanceTimer);
    _successAutoAdvanceTimer = null;
  }
  if (_nextCipherBusy) return;
  _nextCipherBusy = true;

  try {
    const ch = CHAPTERS[state.chapter];
    if (!ch || !Array.isArray(ch.ciphers)) {
      console.warn('nextCipher: chapter is unavailable', state.chapter);
      showScreen('s-chapters');
      renderChapters();
      return;
    }
    const activeKey = _activeCipherKey || _makeCipherKey();
    if (expectedCipherKey && expectedCipherKey !== activeKey) {
      console.warn('nextCipher: stale checkpoint ignored', expectedCipherKey, activeKey);
      return;
    }

    let wasAwaiting = state._awaitingNextCipher === true;
    let wasPendingFinish = state._pendingChapterFinish === true;
    if (!wasAwaiting && !wasPendingFinish) {
      const successScreen = document.getElementById('s-success');
      const successActive = !!(successScreen && successScreen.classList.contains('active'));
      const canRecover = successActive && _isActiveCipherResolved();
      if (!canRecover) {
        console.warn('nextCipher: transition ignored because no pending success checkpoint');
        return;
      }
      const currentIdx = Math.max(0, Math.min(ch.ciphers.length - 1, Number(state.cipherIdx || 0)));
      const isLast = currentIdx >= (ch.ciphers.length - 1);
      state._awaitingNextCipher = !isLast;
      state._pendingChapterFinish = isLast;
      state._nextCipherTargetIdx = isLast ? null : (currentIdx + 1);
      wasAwaiting = state._awaitingNextCipher === true;
      wasPendingFinish = state._pendingChapterFinish === true;
      console.warn('nextCipher: restored lost checkpoint flags from success screen');
    }

    if (wasPendingFinish) {
      state._awaitingNextCipher = false;
      state._pendingChapterFinish = false;
      state._nextCipherTargetIdx = null;
      saveState();
      try {
        await Promise.resolve(finishChapter());
      } catch (err) {
        console.error('finishChapter failed:', err);
        showChapterEndFallback();
      }
      return;
    }

    let targetIdx = _readNextCipherTargetIdx();
    if (targetIdx === null) {
      targetIdx = Number(state.cipherIdx || 0) + 1;
    }
    targetIdx = Math.max(0, Math.min(ch.ciphers.length, Math.floor(Number(targetIdx || 0))));

    state._awaitingNextCipher = false;
    state._pendingChapterFinish = false;
    state._nextCipherTargetIdx = null;

    if (targetIdx >= ch.ciphers.length) {
      saveState();
      try {
        await Promise.resolve(finishChapter());
      } catch (err) {
        console.error('finishChapter failed:', err);
        showChapterEndFallback();
      }
      return;
    }

    state.cipherIdx = targetIdx;
    saveState();
    _clearActiveCipherResolved();
    try {
      loadCipher();
      showScreen('s-cipher');
    } catch (err) {
      console.error('nextCipher: failed to open next cipher', err);
      showToast('Failed to open next task. Tap again.');
    }
  } finally {
    _nextCipherBusy = false;
  }
}

function showChapterEndFallback() {
  const ch = CHAPTERS[state.chapter];
  if (!ch) {
    showScreen('s-chapters');
    renderChapters();
    return;
  }
  const nameEl = document.getElementById('chend-chapter-name');
  const medalEl = document.getElementById('chend-medal');
  const scoreEl = document.getElementById('chend-score');
  const totalEl = document.getElementById('chend-total');
  const pctEl = document.getElementById('chend-pct');
  const penaltyEl = document.getElementById('chend-penalty');
  if (nameEl) nameEl.textContent = ch.title;
  if (medalEl) medalEl.textContent = 'рџҐ€';
  if (scoreEl) scoreEl.textContent = String(state.chapterScore || 0);
  if (totalEl) totalEl.textContent = String(state.totalScore || 0);
  if (pctEl) pctEl.textContent = '';
  if (penaltyEl) {
    penaltyEl.style.display = 'none';
    penaltyEl.textContent = '';
  }
  const nextBtn = document.getElementById('chend-next-btn');
  if (nextBtn) {
    nextBtn.textContent = 'в†ђ Рљ Р—РђР”РђРќРРЇРњ';
    nextBtn.onclick = () => { showScreen('s-chapters'); renderChapters(); };
  }
  showScreen('s-chapter-end');
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р—РђР’Р•Р РЁР•РќРР• Р“Р›РђР’Р«
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
async function finishChapter() {
  const ch = CHAPTERS[state.chapter];
  const alreadyDone = !!state.completedChapters[ch.id];
  const chTime = Math.round((Date.now() - (state._chapterStartTime || Date.now())) / 1000);
  const rawChapterScore = Math.max(0, Number(state.chapterScore || 0));
  const restartPenaltyPoints = Math.max(0, Math.floor(Number(state._manualRestartPenaltyPoints || 0)));
  const finalChapterScore = Math.max(0, rawChapterScore - restartPenaltyPoints);
  state.chapterScore = finalChapterScore;
  if (!state.chapterStats) state.chapterStats = {};
  state.chapterStats[ch.id] = {
    time: chTime, errors: state._chapterErrors || 0,
    hints: state._chapterHints || 0, score: finalChapterScore,
    max: ch.ciphers.reduce((s,c) => s+c.points, 0)
  };
  state.completedChapters[ch.id] = true;
  state._chapterInProgress = false;
  state._awaitingNextCipher = false;
  state._pendingChapterFinish = false;
  state._nextCipherTargetIdx = null;
  state._manualRestartPenaltyPoints = 0;
  if (!alreadyDone && !state._noptsMode) {
    state.chapterScores[ch.id] = finalChapterScore;
    state.totalScore          += finalChapterScore;
    if (state.retryPenalty) state.retryPenalty = false;
  } else if (state._noptsMode) {
    // Р‘РµР· РѕС‡РєРѕРІ вЂ” РїСЂРѕСЃС‚Рѕ РѕС‚РјРµС‡Р°РµРј РіР»Р°РІСѓ РїСЂРѕР№РґРµРЅРЅРѕР№
    state.chapterScores[ch.id] = 0;
  }

  // РњРµРґР°Р»Рё
  const maxPossible = ch.ciphers.reduce((s,c)=>s+c.points, 0);
  const pct = finalChapterScore / maxPossible;
  let medal = 'рџҐ‰';
  if (pct >= 0.85) medal = 'рџҐ‡';
  else if (pct >= 0.60) medal = 'рџҐ€';

  // РџСЂРѕРІРµСЂСЏРµРј РєРѕРЅРµС† РёРіСЂС‹
  const allDone = CHAPTERS.every(c => state.completedChapters[c.id]);
  if (allDone) state.gameOver = true;

  // РћР±РЅРѕРІР»СЏРµРј С‚Р°Р±Р»РёС†Сѓ Р»РёРґРµСЂРѕРІ Р»РѕРєР°Р»СЊРЅРѕ
  updateLeaderboard();
  saveState();

  // РћС‚РїСЂР°РІР»СЏРµРј РІ Р±РѕС‚
  sendResultToBot({
    type: 'chapter_complete',
    chapter: ch.id,
    chapter_title: ch.title,
    score: finalChapterScore,
    restart_penalty_points: restartPenaltyPoints,
    total_score: state.totalScore,
    user_id: getTgUserId(),
    game_over: allDone
  });

  document.getElementById('chend-chapter-name').textContent = ch.title;
  document.getElementById('chend-medal').textContent = medal;
  document.getElementById('chend-score').textContent = finalChapterScore;
  document.getElementById('chend-total').textContent = state.totalScore;
  document.getElementById('chend-pct').textContent = Math.round(pct*100) + '%';
  const penaltyEl = document.getElementById('chend-penalty');
  if (penaltyEl) {
    if (restartPenaltyPoints > 0) {
      penaltyEl.style.display = 'block';
      penaltyEl.textContent = `вљ– РЁС‚СЂР°С„ Р·Р° РїРµСЂРµР·Р°РїСѓСЃРє: -${restartPenaltyPoints} РѕС‡`;
    } else {
      penaltyEl.style.display = 'none';
      penaltyEl.textContent = '';
    }
  }

  const nextBtn = document.getElementById('chend-next-btn');
  if (allDone) {
    nextBtn.textContent = 'рџЏ† РЎРњРћРўР Р•РўР¬ РРўРћР“';
    nextBtn.onclick = () => { showFinal(); };
  } else {
    nextBtn.textContent = 'в†ђ Рљ Р—РђР”РђРќРРЇРњ';
    nextBtn.onclick = () => { showScreen('s-chapters'); renderChapters(); };
  }

  // Comeback вЂ” РїСЂРѕС€С‘Р» РіР»Р°РІСѓ РїРѕСЃР»Рµ РїРѕРІС‚РѕСЂРЅРѕР№ РїРѕРїС‹С‚РєРё
  const wasRetryWin = !!state._isRetryAttempt;
  if (wasRetryWin) {
    state._isRetryAttempt = false;
    checkAchievements({ retryWin: true, comeback: true });
  }
  // РџСЂРѕРІРµСЂСЏРµРј РґРѕСЃС‚РёР¶РµРЅРёСЏ РіР»Р°РІС‹
  const chNoErrors = (state._chapterErrors || 0) === 0;
  const chNoHints  = (state._chapterHints  || 0) === 0;
  if (chNoErrors) state._perfectChapters = (state._perfectChapters || 0) + 1;
  if (chNoHints)  state._noHintChapters  = (state._noHintChapters  || 0) + 1;
  const survived1Life = state.lives === 1;
  checkAchievements({
    chapterDone: true,
    chapterDoneNoErrors: chNoErrors,
    chapterDoneNoHints: chNoHints,
    survived1: survived1Life,
  });
  const artifactsGained = evaluateArtifacts({
    chapterDone: true,
    chapterId: ch.id,
    chapterDoneNoErrors: chNoErrors,
    chapterDoneNoHints: chNoHints,
    retryWin: wasRetryWin,
  });
  renderChapterEpicSummary(ch, state.chapterStats[ch.id] || {}, pct, medal, artifactsGained);
  playSound('chapter_win');
  launchConfetti(800, 20);
  showScreen('s-chapter-end');
}

function _chapterGradeInfo(pct) {
  const p = Number(pct || 0);
  if (p >= 0.9) return { grade: 'S', label: 'Р›Р•Р“Р•РќР”РђР РќРћ' };
  if (p >= 0.75) return { grade: 'A', label: 'РћРўР›РР§РќРћ' };
  if (p >= 0.6) return { grade: 'B', label: 'РҐРћР РћРЁРћ' };
  if (p >= 0.45) return { grade: 'C', label: 'РЎРўРђР‘РР›Р¬РќРћ' };
  return { grade: 'D', label: 'РўР Р•Р‘РЈР•РўРЎРЇ РџРћР’РўРћР ' };
}

function copyChapterReportCard() {
  const reportEl = document.getElementById('chend-epic-report');
  if (!reportEl) return;
  const payload = String(reportEl.dataset.report || '').trim();
  if (!payload) return;
  if (navigator.clipboard && navigator.clipboard.writeText) {
    navigator.clipboard.writeText(payload).then(
      () => showToast('рџ“‹ РћС‚С‡С‘С‚ РіР»Р°РІС‹ СЃРєРѕРїРёСЂРѕРІР°РЅ'),
      () => showToast('вљ  РќРµ СѓРґР°Р»РѕСЃСЊ СЃРєРѕРїРёСЂРѕРІР°С‚СЊ РѕС‚С‡С‘С‚')
    );
    return;
  }
  showToast('вљ  РљРѕРїРёСЂРѕРІР°РЅРёРµ РЅРµРґРѕСЃС‚СѓРїРЅРѕ РІ СЌС‚РѕРј СЂРµР¶РёРјРµ');
}

function renderChapterEpicSummary(chapter, stats, pct, medal, artifactsGained) {
  const screen = document.getElementById('s-chapter-end');
  if (!screen || !chapter) return;
  let report = document.getElementById('chend-epic-report');
  const nextBtn = document.getElementById('chend-next-btn');
  if (!report) {
    report = document.createElement('div');
    report.id = 'chend-epic-report';
    report.style.cssText = 'margin:0 0 14px 0';
    if (nextBtn && nextBtn.parentNode) nextBtn.parentNode.insertBefore(report, nextBtn);
    else screen.appendChild(report);
  }

  const totalTasks = Array.isArray(chapter.ciphers) ? chapter.ciphers.length : 0;
  const errors = Math.max(0, Number(stats.errors || 0));
  const hints = Math.max(0, Number(stats.hints || 0));
  const score = Math.max(0, Number(stats.score || 0));
  const timeSec = Math.max(0, Number(stats.time || 0));
  const accuracy = totalTasks ? Math.max(0, Math.round(((totalTasks - errors) / totalTasks) * 100)) : 100;
  const mm = String(Math.floor(timeSec / 60)).padStart(2, '0');
  const ss = String(timeSec % 60).padStart(2, '0');
  const gradeInfo = _chapterGradeInfo(pct);

  const gained = Array.isArray(artifactsGained) ? artifactsGained : [];
  const artifactsLine = gained.length
    ? gained.map(a => `${a.icon} ${a.name}`).join(' \u00B7 ')
    : 'РЅРµС‚ РЅРѕРІС‹С… Р°СЂС‚РµС„Р°РєС‚РѕРІ';

  report.dataset.report = [
    `РћС‚С‡С‘С‚ РїРѕ РіР»Р°РІРµ: ${chapter.title}`,
    `РњРµРґР°Р»СЊ: ${medal} В· Р Р°РЅРі: ${gradeInfo.grade} (${gradeInfo.label})`,
    `РћС‡РєРё РіР»Р°РІС‹: ${score}`,
    `РўРѕС‡РЅРѕСЃС‚СЊ: ${accuracy}%`,
    `РћС€РёР±РєРё: ${errors}, РїРѕРґСЃРєР°Р·РєРё: ${hints}`,
    `Р’СЂРµРјСЏ: ${mm}:${ss}`,
    `РђСЂС‚РµС„Р°РєС‚С‹: ${artifactsLine}`,
  ].join('\n');

  report.innerHTML = `
    <div style="background:linear-gradient(165deg,rgba(255,224,51,.16),rgba(18,16,10,.95));border:1px solid rgba(255,224,51,.42);border-radius:12px;padding:12px 12px 10px;box-shadow:0 10px 24px rgba(0,0,0,.45)">
      <div style="display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:10px">
        <div style="font-family:var(--head);font-size:12px;color:#ffe8a2;letter-spacing:.09em">Р‘РћР•Р’РћР™ РћРўР§РЃРў Р“Р›РђР’Р«</div>
        <div style="font-family:var(--head);font-size:12px;color:var(--accent)">Р РђРќР“ ${gradeInfo.grade}</div>
      </div>
      <div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-bottom:10px">
        <div style="background:rgba(0,0,0,.28);border:1px solid rgba(255,224,51,.2);border-radius:8px;padding:8px">
          <div style="font-size:10px;color:var(--muted)">РўРћР§РќРћРЎРўР¬</div>
          <div style="font-family:var(--head);font-size:18px;color:#fff2bf">${accuracy}%</div>
        </div>
        <div style="background:rgba(0,0,0,.28);border:1px solid rgba(255,224,51,.2);border-radius:8px;padding:8px">
          <div style="font-size:10px;color:var(--muted)">Р’Р Р•РњРЇ</div>
          <div style="font-family:var(--head);font-size:18px;color:#fff2bf">${mm}:${ss}</div>
        </div>
        <div style="background:rgba(0,0,0,.28);border:1px solid rgba(255,224,51,.2);border-radius:8px;padding:8px">
          <div style="font-size:10px;color:var(--muted)">РћРЁРР‘РљР</div>
          <div style="font-family:var(--head);font-size:18px;color:#ffd4a8">${errors}</div>
        </div>
        <div style="background:rgba(0,0,0,.28);border:1px solid rgba(255,224,51,.2);border-radius:8px;padding:8px">
          <div style="font-size:10px;color:var(--muted)">РџРћР”РЎРљРђР—РљР</div>
          <div style="font-family:var(--head);font-size:18px;color:#ffd4a8">${hints}</div>
        </div>
      </div>
      <div style="font-size:11px;color:#efe2ba;line-height:1.45;margin-bottom:10px">РђСЂС‚РµС„Р°РєС‚С‹: ${artifactsLine}</div>
      <button class="btn btn-small" style="width:100%" onclick="copyChapterReportCard()">рџ“‹ РЎРљРћРџРР РћР’РђРўР¬ РћРўР§РЃРў</button>
    </div>`;
}


function restartChapterFromStart(idx, options = {}) {
  if (_outOfLivesRestartTimer) {
    clearTimeout(_outOfLivesRestartTimer);
    _outOfLivesRestartTimer = null;
  }
  unlockAudioInteraction();
  const ch = CHAPTERS[idx];
  if (!ch) {
    showScreen('s-chapters');
    renderChapters();
    return;
  }
  _registerSecretBreakEvent();
  state.chapter = idx;
  state.cipherIdx = 0;
  state._chapterInProgress = true;
  state._awaitingNextCipher = false;
  state._pendingChapterFinish = false;
  state._nextCipherTargetIdx = null;
  _activeCipherKey = _makeCipherKey(idx, 0);
  _clearActiveCipherResolved();
  _nextCipherBusy = false;
  state.lives = state.adminMode ? Infinity : 5;
  state.chapterScore = 0;
  const manualPenaltyPoints = Math.max(0, Math.floor(Number(options && options.manualPenaltyPoints || 0)));
  state._manualRestartPenaltyPoints = manualPenaltyPoints;
  state.streak = 0;
  state.hintsUsed = false;
  state.startTime = Date.now();
  state._chapterStartTime = Date.now();
  state._chapterErrors = 0;
  state._chapterHints = 0;
  saveState();
  loadCipher();
  showScreen('s-cipher');
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РџР РћР’РђР› Р“Р›РђР’Р« (РєРѕРЅС‡РёР»РёСЃСЊ Р¶РёР·РЅРё)
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
async function failChapter() {
  const ch = CHAPTERS[state.chapter];
  const alreadyDone = !!state.completedChapters[ch.id];
  const chTime = Math.round((Date.now() - (state._chapterStartTime || Date.now())) / 1000);
  if (!state.chapterStats) state.chapterStats = {};
  state.chapterStats[ch.id] = {
    time: chTime, errors: state._chapterErrors || 0,
    hints: state._chapterHints || 0, score: state.chapterScore,
    max: ch.ciphers.reduce((s,c2) => s+c2.points, 0), failed: true
  };

  // РЎС‡РёС‚Р°РµРј СЃРєРѕР»СЊРєРѕ СЂР°Р· РїСЂРѕРІР°Р»РёР» СЌС‚Сѓ РіР»Р°РІСѓ
  if (!state.chapterFailCounts) state.chapterFailCounts = {};
  const failCount = (state.chapterFailCounts[ch.id] || 0) + 1;
  state.chapterFailCounts[ch.id] = failCount;

  // РЁС‚СЂР°С„ Р·Р° РїСЂРѕРІР°Р»: 5% * РєРѕР»РёС‡РµСЃС‚РІРѕ РїСЂРѕРІР°Р»РѕРІ (РјР°РєСЃ 50%)
  const penaltyPct = Math.min(0.50, failCount * 0.05);
  state._chapterInProgress = false;
  state._awaitingNextCipher = false;
  state._pendingChapterFinish = false;
  state._nextCipherTargetIdx = null;

  if (!alreadyDone && !state._noptsMode) {
    state.chapterScores[ch.id] = state.chapterScore;
    state.totalScore          += state.chapterScore;
  } else if (state._noptsMode) {
    state.chapterScores[ch.id] = 0;
  }

  const allDone = CHAPTERS.every(c => state.completedChapters[c.id]);
  if (allDone) state.gameOver = true;
  updateLeaderboard();
  saveState();

  sendResultToBot({
    type: 'chapter_failed',
    chapter: ch.id,
    chapter_title: ch.title,
    score: state.chapterScore,
    total_score: state.totalScore,
    user_id: getTgUserId()
  });

  document.getElementById('fail-chapter-name').textContent = ch.title;
  document.getElementById('fail-score-line').textContent =
    `РќР°Р±СЂР°РЅРѕ РѕС‡РєРѕРІ РІ СЌС‚РѕР№ РіР»Р°РІРµ: ${state.chapterScore}`;

  const nextIdx = state.chapter + 1;
  const nextBtn = document.getElementById('fail-next-btn');
  if (nextIdx < CHAPTERS.length && !allDone) {
    const nextCh = CHAPTERS[nextIdx];
    nextBtn.textContent = 'РЎР›Р•Р”РЈР®Р©РђРЇ Р“Р›РђР’Рђ: ' + nextCh.title + ' в†’';
    nextBtn.onclick = () => showBriefing(nextIdx);
    nextBtn.style.display = 'block';
  } else if (allDone) {
    nextBtn.textContent = 'рџЏ† РЎРњРћРўР Р•РўР¬ РРўРћР“';
    nextBtn.onclick = () => showFinal();
    nextBtn.style.display = 'block';
  } else {
    nextBtn.style.display = 'none';
  }
  // РџРѕРєР°Р·С‹РІР°РµРј РєРЅРѕРїРєСѓ РїРѕРІС‚РѕСЂРЅРѕР№ РїРѕРїС‹С‚РєРё СЃ РёРЅС„РѕСЂРјР°С†РёРµР№ Рѕ С€С‚СЂР°С„Рµ
  const retryBtn = document.getElementById('fail-retry-btn');
  const retryInfo = document.getElementById('fail-retry-info');
  const currentFailCount = state.chapterFailCounts[ch.id] || 1;
  const currentPenalty = Math.min(50, currentFailCount * 5);
  if (retryBtn) {
    retryBtn.style.display = 'block';
    retryBtn.textContent = 'рџ”„ РџРћРџР РћР‘РћР’РђРўР¬ РЎРќРћР’Рђ';
    retryBtn.onclick = () => {
      state._isRetryAttempt = true;
      retryChapterWithPenalty(state.chapter);
    };
  }
  if (retryInfo) {
    retryInfo.style.display = 'block';
    retryInfo.innerHTML = currentFailCount === 1
      ? `вљ пёЏ РџРѕРІС‚РѕСЂ: С€С‚СЂР°С„ <b>-${currentPenalty}%</b> РѕС‚ РЅР°Р±СЂР°РЅРЅС‹С… РѕС‡РєРѕРІ<br>Р–РёР·РЅРё РІРѕСЃСЃС‚Р°РЅРѕРІСЏС‚СЃСЏ`
      : `вљ пёЏ РџРѕРІС‚РѕСЂ #${currentFailCount}: С€С‚СЂР°С„ <b>-${currentPenalty}%</b> РѕС‚ РѕС‡РєРѕРІ<br>Р–РёР·РЅРё РІРѕСЃСЃС‚Р°РЅРѕРІСЏС‚СЃСЏ`;
  }
  playSound('game_lose');
  showScreen('s-chapter-fail');
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РџРћР’РўРћР РќРђРЇ РџРћРџР«РўРљРђ РЎ РЁРўР РђР¤РћРњ
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function retryChapterWithPenalty(idx) {
  const ch = CHAPTERS[idx];
  // РЎРєРѕР»СЊРєРѕ СЂР°Р· СѓР¶Рµ РїСЂРѕРІР°Р»РёРІР°Р» СЌС‚Сѓ РіР»Р°РІСѓ
  if (!state.chapterFailCounts) state.chapterFailCounts = {};
  const failCount = state.chapterFailCounts[ch.id] || 1;
  const penaltyPct = Math.min(0.50, failCount * 0.05); // 5%, 10%, ..., 50%

  // Р’С‹С‡РёС‚Р°РµРј С€С‚СЂР°С„ РёР· РћР‘Р©Р•Р“Рћ СЃС‡С‘С‚Р°
  const penalty = Math.round(state.totalScore * penaltyPct);
  state.totalScore = Math.max(0, state.totalScore - penalty);
  if (penalty > 0) {
    showPenaltyNotice(`РЎРЅРёР¶РµРЅРёРµ РѕР±С‰РµРіРѕ СЃС‡С‘С‚Р°: -${penalty} РѕС‡ (${Math.round(penaltyPct * 100)}%)`, 6200);
  } else {
    showToast('РџРѕРІС‚РѕСЂ РіР»Р°РІС‹ Р±РµР· СЃРїРёСЃР°РЅРёСЏ РѕС‡РєРѕРІ');
  }

  // РЈР±РёСЂР°РµРј РіР»Р°РІСѓ РёР· Р·Р°РІРµСЂС€С‘РЅРЅС‹С… С‡С‚РѕР±С‹ РјРѕР¶РЅРѕ Р±С‹Р»Рѕ РїСЂРѕР№С‚Рё СЃРЅРѕРІР°
  delete state.completedChapters[ch.id];
  delete state.chapterScores[ch.id];

  // РЎР±СЂР°СЃС‹РІР°РµРј Р¶РёР·РЅРё Рё СЃС‡С‘С‚С‡РёРєРё
  state.lives = 5;
  state.chapterScore = 0;
  state.streak = 0;
  state._chapterErrors = 0;
  state._chapterHints = 0;
  state._chapterInProgress = false;
  state._awaitingNextCipher = false;
  state._pendingChapterFinish = false;
  state._nextCipherTargetIdx = null;

  saveState();
  // РЎРёРЅС…СЂРѕРЅРёР·РёСЂСѓРµРј СЃ Р‘Р” СЃСЂР°Р·Сѓ вЂ” С„РёРєСЃРёСЂСѓРµРј С€С‚СЂР°С„
  autoSync(false);
  showBriefing(idx);
}

function retryChapter(idx) {
  // РЎС‚Р°СЂР°СЏ С„СѓРЅРєС†РёСЏ вЂ” РѕСЃС‚Р°РІР»СЏРµРј РґР»СЏ СЃРѕРІРјРµСЃС‚РёРјРѕСЃС‚Рё
  retryChapterWithPenalty(idx);
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р”Р•РўРђР›Р¬РќРђРЇ РЎРўРђРўРРЎРўРРљРђ + РџРћР”Р•Р›РРўР¬РЎРЇ
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function showMyStats() {
  const name  = tgUser ? (tgUser.first_name || 'РРіСЂРѕРє') : 'Р“РѕСЃС‚СЊ';
  const stats = state.chapterStats || {};
  const total = state.totalScore;
  const maxAll = CHAPTERS.reduce((s,ch) => s + ch.ciphers.reduce((a,c2) => a+c2.points, 0), 0);
  const pct   = Math.round(total / maxAll * 100);

  const rows = CHAPTERS.map(ch => {
    const s     = stats[ch.id];
    const score = state.chapterScores[ch.id] || 0;
    const maxCh = ch.ciphers.reduce((a,c2) => a+c2.points, 0);
    if (!state.completedChapters[ch.id]) {
      return '<div style="display:flex;align-items:center;gap:10px;padding:10px 0;border-bottom:1px solid rgba(255,255,255,.05);opacity:.35">'
        + '<div style="font-size:16px">рџ”’</div>'
        + '<div style="flex:1;font-size:var(--fs-sm);color:var(--muted)">' + ch.title + '</div>'
        + '<div style="font-size:11px;color:var(--muted)">РЅРµ РїСЂРѕР№РґРµРЅР°</div></div>';
    }
    const sp = Math.round(score / maxCh * 100);
    const medal = sp >= 85 ? 'рџҐ‡' : sp >= 60 ? 'рџҐ€' : 'рџҐ‰';
    const timeTxt = s ? (s.time >= 60 ? Math.floor(s.time/60) + 'Рј ' + (s.time%60) + 'СЃ' : s.time + 'СЃ') : 'вЂ”';
    const failTag = s && s.failed ? ' <span style="color:var(--accent2);font-size:9px">РџР РћР’РђР›</span>' : '';
    return '<div style="padding:10px 0;border-bottom:1px solid rgba(255,255,255,.05)">'
      + '<div style="display:flex;align-items:center;gap:8px;margin-bottom:4px">'
      + '<div style="font-size:16px">' + medal + '</div>'
      + '<div style="flex:1;font-family:var(--head);font-size:var(--fs-sm);color:#fdfaf0">' + ch.title + failTag + '</div>'
      + '<div style="font-family:var(--head);font-size:var(--fs-base);color:var(--accent)">' + score + '</div></div>'
      + '<div style="display:flex;gap:10px;font-size:10px;color:var(--muted);padding-left:24px">'
      + '<span>вЏ± ' + timeTxt + '</span>'
      + (s ? '<span>вќЊ ' + s.errors + ' РѕС€.</span>' : '')
      + (s ? '<span>рџ’Ў ' + s.hints + ' РїРѕРґСЃРє.</span>' : '')
      + '<span>' + sp + '%</span></div></div>';
  }).join('');

  let modal = document.getElementById('stats-modal');
  if (!modal) {
    modal = document.createElement('div');
    modal.id = 'stats-modal';
    modal.style.cssText = 'position:fixed;inset:0;z-index:500;background:rgba(0,0,0,.85);display:flex;align-items:flex-end;justify-content:center';
    modal.onclick = (e) => { if (e.target === modal) closeMyStats(); };
    document.body.appendChild(modal);
  }

  modal.innerHTML = '<div style="background:#161612;border-radius:16px 16px 0 0;width:100%;max-width:480px;max-height:85vh;overflow-y:auto;padding:20px 16px 32px">'
    + '<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:16px">'
    + '<div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent);letter-spacing:.06em">РњРћРЇ РЎРўРђРўРРЎРўРРљРђ</div>'
    + '<button onclick="closeMyStats()" style="background:rgba(255,255,255,.08);border:none;color:var(--muted);font-size:18px;width:32px;height:32px;border-radius:50%;cursor:pointer">вњ•</button></div>'
    + '<div style="background:#242418;border:1px solid rgba(255,224,51,.12);border-radius:8px;padding:14px;margin-bottom:14px;display:flex;justify-content:space-around;text-align:center">'
    + '<div><div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent)">' + total + '</div><div style="font-size:10px;color:var(--muted)">РћР§РљРћР’</div></div>'
    + '<div><div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent)">' + Object.keys(state.completedChapters).length + '/6</div><div style="font-size:10px;color:var(--muted)">Р“Р›РђР’</div></div>'
    + '<div><div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent)">' + pct + '%</div><div style="font-size:10px;color:var(--muted)">Р Р•Р—РЈР›Р¬РўРђРў</div></div></div>'
    + '<div style="font-family:var(--head);font-size:var(--fs-xs);color:var(--muted);letter-spacing:.1em;margin-bottom:8px">// РџРћ Р“Р›РђР’РђРњ</div>'
    + rows
    + '<button onclick="shareMyTotal()" style="width:100%;margin-top:16px;background:rgba(255,224,51,.1);border:1px solid rgba(255,224,51,.25);color:var(--accent);padding:12px;font-family:var(--head);font-size:var(--fs-sm);font-weight:600;border-radius:4px;cursor:pointer;letter-spacing:.08em">рџ“¤ РџРћР”Р•Р›РРўР¬РЎРЇ Р Р•Р—РЈР›Р¬РўРђРўРћРњ</button>'
    + '</div>';

  modal.style.display = 'flex';
}

function closeAchModal() {
  const m = document.getElementById('ach-modal');
  if (m) m.style.display = 'none';
}

function showMyAchievements() {
  // РЎРёРЅС…СЂРѕРЅРёР·РёСЂСѓРµРј СЃ СЃРµСЂРІРµСЂРѕРј РїРµСЂРµРґ РїРѕРєР°Р·РѕРј РґРѕСЃС‚РёР¶РµРЅРёР№
  fetchAndApplyState().then(() => _renderAchModal());
  function _renderAchModal() {
  let modal = document.getElementById('ach-modal');
  if (!modal) {
    modal = document.createElement('div');
    modal.id = 'ach-modal';
    modal.style.cssText = 'position:fixed;inset:0;z-index:500;background:rgba(0,0,0,.85);display:flex;align-items:flex-end;justify-content:center';
    modal.onclick = (e) => { if (e.target === modal) modal.style.display = 'none'; };
    document.body.appendChild(modal);
  }
  const inner = document.createElement('div');
  inner.style.cssText = 'background:#161612;border-radius:16px 16px 0 0;width:100%;max-width:480px;max-height:85vh;overflow-y:auto;padding:0 0 32px';
  inner.innerHTML = '<div style="display:flex;align-items:center;justify-content:space-between;padding:16px 16px 8px;position:sticky;top:0;background:#161612;z-index:1">'
    + '<div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent)">РњРћР Р”РћРЎРўРР–Р•РќРРЇ</div>'
    + '<button onclick="closeAchModal()" style="background:rgba(255,255,255,.08);border:none;color:var(--muted);font-size:18px;width:32px;height:32px;border-radius:50%;cursor:pointer">вњ•</button></div>';
  renderAchievementsTab(inner);
  modal.innerHTML = '';
  modal.appendChild(inner);
  modal.style.display = 'flex';
  } // end _renderAchModal
}

function closeMyStats() {
  const modal = document.getElementById('stats-modal');
  if (modal) modal.style.display = 'none';
}

function shareMyTotal() {
  const total  = state.totalScore;
  const done   = Object.keys(state.completedChapters).length;
  const maxAll = CHAPTERS.reduce((s,ch) => s + ch.ciphers.reduce((a,c2) => a+c2.points, 0), 0);
  const pct    = Math.round(total / maxAll * 100);
  const medal  = pct >= 90 ? 'рџЊџ' : pct >= 75 ? 'в­ђ' : pct >= 55 ? 'рџЋ–' : 'рџЏ…';
  const text   = medal + ' РЁРР¤Р РћР’РђР›Р¬Р©РРљ: ' + total + ' РѕС‡РєРѕРІ (' + pct + '%), ' + done + '/6 РіР»Р°РІ | РЎРЁ 3 Рі. РҐРѕР№РЅРёРєРё';
  if (tg && tg.HapticFeedback) tg.HapticFeedback.notificationOccurred('success');
  if (navigator.clipboard && navigator.clipboard.writeText) {
    navigator.clipboard.writeText(text).then(() => showToast('рџ“‹ РЎРєРѕРїРёСЂРѕРІР°РЅРѕ! Р’СЃС‚Р°РІСЊС‚Рµ РІ С‡Р°С‚'));
  } else {
    fallbackCopy(text);
  }
}

function fallbackCopy(text) {
  try {
    const ta = document.createElement('textarea');
    ta.value = text; ta.style.cssText = 'position:fixed;opacity:0';
    document.body.appendChild(ta); ta.select();
    document.execCommand('copy');
    document.body.removeChild(ta);
    showToast('рџ“‹ РЎРєРѕРїРёСЂРѕРІР°РЅРѕ! Р’СЃС‚Р°РІСЊС‚Рµ РІ С‡Р°С‚');
  } catch(e) { showToast('РќРµ СѓРґР°Р»РѕСЃСЊ СЃРєРѕРїРёСЂРѕРІР°С‚СЊ'); }
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р¤РРќРђР›
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
async function showFinal() {
  document.getElementById('final-score-val').textContent = state.totalScore;

  // ID РёРіСЂРѕРєР° РЅР° С„РёРЅР°Р»СЊРЅРѕРј СЌРєСЂР°РЅРµ
  const uid = getTgUserId();
  const finalUidEl = document.getElementById('final-uid');
  if (finalUidEl && uid) {
    finalUidEl.textContent = 'ID: ' + uid;
    finalUidEl.style.display = 'block';
  }

  const maxAll = CHAPTERS.reduce((s,ch)=>s+ch.ciphers.reduce((a,c)=>a+c.points,0),0);
  const pct    = Math.round(state.totalScore / maxAll * 100);
  let rank = 'РќРћР’РћР‘Р РђРќР•Р¦';
  if (pct >= 90) rank = 'РњРђР РЁРђР› РџРћР‘Р•Р”Р« рџЊџ';
  else if (pct >= 75) rank = 'РџРћР›РљРћР’РќРРљ Р РђР—Р’Р•Р”РљР в­ђ';
  else if (pct >= 55) rank = 'РљРђРџРРўРђРќ РџРћР”РџРћР›Р¬РЇ рџЋ–';
  else if (pct >= 35) rank = 'РЎР•Р Р–РђРќРў РЎР’РЇР—Р рџЏ…';

  document.getElementById('final-rank').textContent  = rank;
  document.getElementById('final-pct').textContent   = pct + '% РѕС‚ РјР°РєСЃРёРјСѓРјР°';

  // РС‚РѕРіРё РїРѕ РіР»Р°РІР°Рј
  let rows = '';
  CHAPTERS.forEach(ch => {
    const s = state.chapterScores[ch.id] || 0;
    const m = ch.ciphers.reduce((a,c)=>a+c.points,0);
    rows += `<div style="display:flex;justify-content:space-between;padding:8px 0;border-bottom:1px solid var(--border);font-size:17px">
      <span>${ch.subtitle}: ${ch.title}</span>
      <span style="color:var(--accent)">${s}/${m}</span>
    </div>`;
  });
  document.getElementById('final-chapter-scores').innerHTML = rows;

  setTimeout(() => document.getElementById('final-stars').classList.add('show'), 800);
  setTimeout(() => playSound('game_win'), 300);
  setTimeout(() => launchConfetti(1500, 40), 500);

  sendResultToBot({
    type: 'game_complete',
    total_score: state.totalScore,
    rank: rank,
    pct: pct,
    user_id: getTgUserId()
  });

  showScreen('s-final');
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РўРђР‘Р›РР¦Рђ Р›РР”Р•Р РћР’
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function updateLeaderboard() {
  if (state.adminMode || state.testerMode || state.gameRole === 'admin' || state.gameRole === 'tester') {
    return;
  }
  const name  = getTgUserName();
  const uid   = getTgUserId() || 'guest';
  const entry = { uid, name, score: state.totalScore, completed: Object.keys(state.completedChapters).length };
  const idx   = state.leaderboard.findIndex(e => e.uid === uid);
  if (idx >= 0) {
    state.leaderboard[idx] = entry;
  } else {
    state.leaderboard.push(entry);
  }
  state.leaderboard.sort((a,b) => b.score - a.score);
}

function renderLeaderboard() {
  const list = document.getElementById('lb-list-tab');
  // РџРѕРјРµС‡Р°РµРј С‚РµРєСѓС‰РµРіРѕ РёРіСЂРѕРєР°
  const myUid = getTgUserId() || 'guest';
  if (!state.leaderboard.length) {
    list.innerHTML = '<div class="lb-empty">Р—Р°РіСЂСѓР·РєР° СЂРµР№С‚РёРЅРіР°...<br><span style="font-size:var(--fs-base);color:var(--muted)">Р”Р°РЅРЅС‹Рµ РїСЂРёРґСѓС‚ РїСЂРё СЃР»РµРґСѓСЋС‰РµРј РѕС‚РєСЂС‹С‚РёРё С‡РµСЂРµР· Р±РѕС‚</span></div>';
    return;
  }
  list.innerHTML = state.leaderboard.slice(0,20).map((e,i) => {
    const rankClass = i===0?'gold':i===1?'silver':i===2?'bronze':'';
    const medals    = ['рџҐ‡','рџҐ€','рџҐ‰'];
    const isMe      = e.uid === myUid;
    const totalGlav = 6;
    const pct = Math.round((e.completed / totalGlav) * 100);
    const bar = 'в–€'.repeat(Math.round(pct/10)) + 'в–‘'.repeat(10 - Math.round(pct/10));
    const doneTag = e.completed >= totalGlav
      ? 'вњ… Р’СЃРµ РіР»Р°РІС‹ В· 100%'
      : `${e.completed}/${totalGlav} РіР»Р°РІ В· ${pct}%`;
    return `<div class="lb-row" style="${isMe?'background:rgba(255,224,51,.07);border-left:3px solid var(--accent)':''}">
      <div class="lb-rank ${rankClass}">${medals[i]||String(i+1)}</div>
      <div style="flex:1">
        <div class="lb-name">${e.name}${isMe?' рџ‘€':''}</div>
        <div class="lb-cls" style="font-size:var(--fs-xs);color:var(--muted)">${doneTag}</div>
      </div>
      <div class="lb-score">${e.score}</div>
    </div>`;
  }).join('');
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РЎР‘Р РћРЎ (С‚РѕР»СЊРєРѕ РґР»СЏ РіРѕСЃС‚РµР№ Р±РµР· tg-Р°РєРєР°СѓРЅС‚Р°)
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function confirmReset() {
  const isAdmin = state.adminMode || window._adminMode === true;
  if (!isAdmin) {
    if (getTgUserId()) {
      alert('РџСЂРѕРіСЂРµСЃСЃ СЃРѕС…СЂР°РЅС‘РЅ РІ СЃРёСЃС‚РµРјРµ Рё РЅРµ РјРѕР¶РµС‚ Р±С‹С‚СЊ СѓРґР°Р»С‘РЅ РёРіСЂРѕРєРѕРј.');
    }
    return;
  }

  if (!confirm('РЎР±СЂРѕСЃРёС‚СЊ С‚РµСЃС‚РѕРІС‹Р№ РїСЂРѕРіСЂРµСЃСЃ Рє РЅСѓР»СЋ?')) return;

  try { localStorage.removeItem(storageKey()); } catch(e) {}
  state = DEFAULT_STATE();
  state.adminMode = true;
  state.gameRole = 'admin';
  state.achievements = {};
  state.achievementPts = 0;
  state.referralSummary = normalizeReferralSummary(null);
  state.referralAgents = [];

  const syncUrl = window._syncUrl;
  const uid = getTgUserId();
  if (syncUrl && uid) {
    const dropReferrals = confirm('РЈРґР°Р»РёС‚СЊ С‚Р°РєР¶Рµ СЃРІСЏР·Рё Р°РіРµРЅС‚РѕРІ? РћРљ = СЃР±СЂРѕСЃ СЃ Р°РіРµРЅС‚Р°РјРё, РћС‚РјРµРЅР° = С‚РѕР»СЊРєРѕ РѕС‡РєРё/РіР»Р°РІС‹.');
    const resetUrl = syncUrl.replace('/game_sync', '/game_reset');
    const payload = { user_id: uid, drop_referrals: !!dropReferrals };
    const initDataRaw = getTgInitDataRaw();
    if (initDataRaw) payload.init_data = initDataRaw;
    fetch(resetUrl, {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify(payload)
    }).then(r => r.json())
      .then(d => console.log('Reset result:', d))
      .catch(e => console.warn('reset sync error:', e));
  }

  renderChapters();
  setTimeout(() => { try { fetchAndApplyState(); } catch(_) {} }, 120);
  showToast('рџ—‘ РџСЂРѕРіСЂРµСЃСЃ СЃР±СЂРѕС€РµРЅ');
}

function clearLocalGameCache() {
  try {
    localStorage.removeItem('pending_results');
    localStorage.removeItem(AUDIO_PREF_KEY);
    localStorage.removeItem(LEGACY_MUSIC_PREF_KEY);
  } catch (e) {
    console.warn('clearLocalGameCache error:', e);
  }
}

function confirmCacheReset() {
  if (!confirm('РћС‡РёСЃС‚РёС‚СЊ РєРµС€? РџСЂРѕРіСЂРµСЃСЃ Рё РіР»Р°РІС‹ РѕСЃС‚Р°РЅСѓС‚СЃСЏ.')) return;
  clearLocalGameCache();
  showToast('рџ§№ РљРµС€ РѕС‡РёС‰РµРЅ. РџРµСЂРµР·Р°РіСЂСѓР·РєР°...');
  setTimeout(() => window.location.reload(), 250);
}

function openBugReportModal() {
  const modal = document.getElementById('bug-report-modal');
  if (!modal) return;
  modal.classList.add('show');
}

function closeBugReportModal() {
  const modal = document.getElementById('bug-report-modal');
  if (!modal) return;
  modal.classList.remove('show');
}

function contactGameAdmin() {
  closeBugReportModal();
  const url = GAME_ADMIN_CHAT_URL;
  try {
    if (tg && typeof tg.openTelegramLink === 'function') {
      tg.openTelegramLink(url);
      return;
    }
    window.open(url, '_blank');
  } catch (e) {
    console.warn('contactGameAdmin error:', e);
    showToast('РќРµ СѓРґР°Р»РѕСЃСЊ РѕС‚РєСЂС‹С‚СЊ С‡Р°С‚ Р°РґРјРёРЅРёСЃС‚СЂР°С‚РѕСЂР°');
  }
}

document.addEventListener('click', (event) => {
  const bugModal = document.getElementById('bug-report-modal');
  if (bugModal && bugModal.classList.contains('show') && event.target === bugModal) {
    closeBugReportModal();
    return;
  }
  const restartModal = document.getElementById('restart-cost-modal');
  if (restartModal && restartModal.classList.contains('show') && event.target === restartModal) {
    closeRestartCostModal();
  }
});

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  WOW EFFECTS
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ

// в•ђв•ђ THREE.JS 3D INTRO в•ђв•ђ
function init3DIntro() {
  const canvas = document.getElementById('three-canvas');
  if (!canvas || typeof THREE === 'undefined') return;

  const W = window.innerWidth, H = window.innerHeight;
  const renderer = new THREE.WebGLRenderer({ canvas, alpha: true, antialias: true });
  renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
  renderer.setSize(W, H);

  const scene  = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(60, W / H, 0.1, 100);
  camera.position.set(0, 0, 5);

  // РљР°СЂРєР°СЃРЅС‹Р№ РєСѓР± вЂ” РѕСЃРЅРѕРІРЅРѕР№
  const cube = new THREE.Mesh(
    new THREE.BoxGeometry(1.6, 1.6, 1.6),
    new THREE.MeshBasicMaterial({ color: 0xffe033, wireframe: true, transparent: true, opacity: 0.28 })
  );
  scene.add(cube);

  // РљСѓР± РїРѕР±РѕР»СЊС€Рµ вЂ” РєСЂР°СЃРЅС‹Р№
  const cube2 = new THREE.Mesh(
    new THREE.BoxGeometry(2.8, 2.8, 2.8),
    new THREE.MeshBasicMaterial({ color: 0xff3a3a, wireframe: true, transparent: true, opacity: 0.09 })
  );
  scene.add(cube2);

  // РљРѕР»СЊС†Рѕ 1
  const ring1 = new THREE.Mesh(
    new THREE.TorusGeometry(2.2, 0.007, 2, 64),
    new THREE.MeshBasicMaterial({ color: 0xffe033, transparent: true, opacity: 0.18 })
  );
  ring1.rotation.x = Math.PI / 3;
  scene.add(ring1);

  // РљРѕР»СЊС†Рѕ 2
  const ring2 = new THREE.Mesh(
    new THREE.TorusGeometry(3.1, 0.004, 2, 64),
    new THREE.MeshBasicMaterial({ color: 0xff3a3a, transparent: true, opacity: 0.1 })
  );
  ring2.rotation.y = Math.PI / 4;
  scene.add(ring2);

  // РўРѕС‡РєРё-С‡Р°СЃС‚РёС†С‹
  const ptPos = new Float32Array(150 * 3);
  for (let i = 0; i < ptPos.length; i++) ptPos[i] = (Math.random() - 0.5) * 12;
  const ptGeo = new THREE.BufferGeometry();
  ptGeo.setAttribute('position', new THREE.BufferAttribute(ptPos, 3));
  const points = new THREE.Points(ptGeo,
    new THREE.PointsMaterial({ color: 0xffe033, size: 0.045, transparent: true, opacity: 0.55 })
  );
  scene.add(points);

  // Resize
  window.addEventListener('resize', () => {
    renderer.setSize(window.innerWidth, window.innerHeight);
    camera.aspect = window.innerWidth / window.innerHeight;
    camera.updateProjectionMatrix();
  });

  // РћСЃС‚Р°РЅР°РІР»РёРІР°РµРј РєРѕРіРґР° РёРЅС‚СЂРѕ РЅРµР°РєС‚РёРІРЅРѕ
  let active = true;
  const introEl = document.getElementById('s-intro');
  new MutationObserver(() => {
    active = introEl.classList.contains('active');
  }).observe(introEl, { attributes: true, attributeFilter: ['class'] });

  let f = 0;
  (function loop() {
    requestAnimationFrame(loop);
    if (!active) return;
    f++;
    const t = f * 0.008;
    cube.rotation.x  =  t * 0.7;
    cube.rotation.y  =  t;
    cube2.rotation.x = -t * 0.4;
    cube2.rotation.y =  t * 0.5;
    ring1.rotation.z =  t * 0.3;
    ring2.rotation.x =  t * 0.2;
    points.rotation.y = t * 0.1;
    camera.position.x = Math.sin(t * 0.3) * 0.3;
    camera.position.y = Math.cos(t * 0.2) * 0.15;
    renderer.render(scene, camera);
  })();
}


// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р”Р•РќР¬ РџРћР‘Р•Р”Р« вЂ” РРќРўР Рћ
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ

// в”Ђв”Ђ Р¤РµР№РµСЂРІРµСЂРє в”Ђв”Ђ
(function initFireworks(){
  const c = document.getElementById('fireworks-canvas');
  if(!c) return;
  const ctx = c.getContext('2d');
  function resize(){ c.width=window.innerWidth; c.height=window.innerHeight; }
  resize();
  window.addEventListener('resize', resize);

  const COLORS = [
    '#ff4422','#ff8800','#ffcc00','#ffe033',
    '#ffffff','#ff3388','#44aaff','#88ff44'
  ];
  let rockets = [], particles = [], frame = 0;
  let active = true;

  const intro = document.getElementById('s-intro');
  new MutationObserver(()=>{ active = intro.classList.contains('active'); })
    .observe(intro, {attributes:true, attributeFilter:['class']});

  function spawnRocket(){
    rockets.push({
      x: c.width * (.2 + Math.random() * .6),
      y: c.height,
      tx: c.width * (.1 + Math.random() * .8),
      ty: c.height * (.1 + Math.random() * .45),
      color: COLORS[Math.floor(Math.random()*COLORS.length)],
      trail: []
    });
  }

  function explode(r){
    const n = 60 + Math.floor(Math.random()*50);
    for(let i=0;i<n;i++){
      const angle = (Math.PI*2/n)*i + Math.random()*.3;
      const speed = 1.5 + Math.random()*2.5;
      particles.push({
        x:r.tx, y:r.ty,
        vx: Math.cos(angle)*speed,
        vy: Math.sin(angle)*speed,
        color: r.color,
        life: 1,
        decay: .012 + Math.random()*.018,
        size: 2 + Math.random()*2
      });
    }
    // РСЃРєСЂС‹ РІРЅРёР·
    for(let i=0;i<12;i++){
      particles.push({
        x:r.tx, y:r.ty,
        vx: (Math.random()-.5)*1.5,
        vy: Math.random()*2,
        color:'#ffeeaa',
        life:1, decay:.03, size:1.5
      });
    }
  }

  function draw(){
    requestAnimationFrame(draw);
    if(!active){ ctx.clearRect(0,0,c.width,c.height); return; }
    ctx.fillStyle='rgba(0,0,0,.18)';
    ctx.fillRect(0,0,c.width,c.height);
    frame++;

    // Р—Р°РїСѓСЃРєР°РµРј СЂР°РєРµС‚С‹
    if(frame > 60 && frame % 90 === 0) spawnRocket();
    if(frame > 120 && frame % 130 === 0) spawnRocket();

    // Р Р°РєРµС‚С‹
    rockets = rockets.filter(r=>{
      r.trail.push({x:r.x,y:r.y});
      if(r.trail.length>12) r.trail.shift();
      const dx=r.tx-r.x, dy=r.ty-r.y;
      const dist=Math.sqrt(dx*dx+dy*dy);
      if(dist < 5){ explode(r); return false; }
      const speed = Math.min(dist*.12, 14);
      r.x += dx/dist*speed; r.y += dy/dist*speed;
      // РҐРІРѕСЃС‚
      r.trail.forEach((pt,i)=>{
        const a = (i/r.trail.length)*.8;
        ctx.beginPath();
        ctx.arc(pt.x,pt.y,1.5,0,Math.PI*2);
        ctx.fillStyle = r.color;
        ctx.globalAlpha = a;
        ctx.fill();
      });
      ctx.globalAlpha=1;
      ctx.beginPath();
      ctx.arc(r.x,r.y,3,0,Math.PI*2);
      ctx.fillStyle='#fff';
      ctx.fill();
      return true;
    });

    // Р§Р°СЃС‚РёС†С‹ РІР·СЂС‹РІР°
    particles = particles.filter(p=>{
      p.x+=p.vx; p.y+=p.vy;
      p.vy+=.04; // РіСЂР°РІРёС‚Р°С†РёСЏ
      p.vx*=.97; p.vy*=.97;
      p.life-=p.decay;
      if(p.life<=0) return false;
      ctx.beginPath();
      ctx.arc(p.x,p.y,p.size*p.life,0,Math.PI*2);
      ctx.fillStyle=p.color;
      ctx.globalAlpha=p.life*.9;
      ctx.fill();
      ctx.globalAlpha=1;
      return true;
    });
  }
  draw();
})();

// в”Ђв”Ђ РџР°РґР°СЋС‰РёРµ С†РІРµС‚С‹ / Р»РµРїРµСЃС‚РєРё в”Ђв”Ђ
(function initPetals(){
  const c = document.getElementById('petals-canvas');
  if(!c) return;
  const ctx = c.getContext('2d');
  function resize(){ c.width=window.innerWidth; c.height=window.innerHeight; }
  resize();
  window.addEventListener('resize', resize);

  // Р¦РІРµС‚Р° Р»РµРїРµСЃС‚РєРѕРІ вЂ” РіРІРѕР·РґРёРєРё (РєСЂР°СЃРЅС‹Рµ Рё Р±РµР»С‹Рµ, РєР°Рє РІ Р‘РµР»Р°СЂСѓСЃРё РЅР° 9 РјР°СЏ)
  const PCOLORS = ['#cc2211','#dd3322','#ee4433','#ffffff','#ffeeee','#cc1133'];
  let petals=[], active=true;

  const intro = document.getElementById('s-intro');
  new MutationObserver(()=>{ active = intro.classList.contains('active'); })
    .observe(intro, {attributes:true, attributeFilter:['class']});

  for(let i=0;i<28;i++){
    petals.push({
      x: Math.random()*window.innerWidth,
      y: Math.random()*window.innerHeight - window.innerHeight,
      r: 3+Math.random()*5,
      vx: (Math.random()-.5)*.6,
      vy: .4+Math.random()*.7,
      rot: Math.random()*Math.PI*2,
      rotV: (Math.random()-.5)*.06,
      color: PCOLORS[Math.floor(Math.random()*PCOLORS.length)],
      wobble: Math.random()*Math.PI*2,
      wobbleS: .02+Math.random()*.03
    });
  }

  function drawPetal(p){
    ctx.save();
    ctx.translate(p.x, p.y);
    ctx.rotate(p.rot);
    ctx.beginPath();
    // Р¤РѕСЂРјР° Р»РµРїРµСЃС‚РєР° вЂ” СЌР»Р»РёРїСЃ СЃ Р·Р°РѕСЃС‚СЂРµРЅРёРµРј
    ctx.ellipse(0, 0, p.r, p.r*1.8, 0, 0, Math.PI*2);
    ctx.fillStyle = p.color;
    ctx.globalAlpha = .75;
    ctx.fill();
    // Р‘Р»РёРє
    ctx.beginPath();
    ctx.ellipse(-p.r*.2, -p.r*.3, p.r*.3, p.r*.5, -.5, 0, Math.PI*2);
    ctx.fillStyle = 'rgba(255,255,255,.3)';
    ctx.fill();
    ctx.restore();
    ctx.globalAlpha=1;
  }

  function loop(){
    requestAnimationFrame(loop);
    if(!active){ ctx.clearRect(0,0,c.width,c.height); return; }
    ctx.clearRect(0,0,c.width,c.height);
    petals.forEach(p=>{
      p.wobble += p.wobbleS;
      p.x += p.vx + Math.sin(p.wobble)*.5;
      p.y += p.vy;
      p.rot += p.rotV;
      if(p.y > c.height+20){
        p.y = -20; p.x = Math.random()*c.width;
        p.color = PCOLORS[Math.floor(Math.random()*PCOLORS.length)];
      }
      drawPetal(p);
    });
  }
  loop();
})();

// в”Ђв”Ђ РџРѕСЌС‚Р°РїРЅРѕРµ РїРѕСЏРІР»РµРЅРёРµ СЌР»РµРјРµРЅС‚РѕРІ РёРЅС‚СЂРѕ в”Ђв”Ђ
function introEntrance(){
  const seq = [
    ['intro-stamp',  300],
    ['intro-date',   700],
    ['intro-title',  1000],
    ['intro-ribbon', 1500],
    ['intro-desc',   1900],
    ['intro-quote',  2300],
    ['intro-btn',    2700],
    ['intro-how',    2950],
    ['intro-memory', 3200],
    ['skyline',      800],
  ];
  seq.forEach(([id, ms])=> setTimeout(()=>{
    const el = document.getElementById(id);
    if(el) el.classList.add('intro-show');
  }, ms));
}


// Р—Р°РїСѓСЃРєР°РµРј РїРѕСЃР»Рµ Р·Р°РіСЂСѓР·РєРё
if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', () => { init3DIntro(); introEntrance(); });
} else {
  init3DIntro();
  introEntrance();
}


// в”Ђв”Ђ Р§Р°СЃС‚РёС†С‹ РЅР° РёРЅС‚СЂРѕ в”Ђв”Ђ
(function initParticles(){
  const c = document.getElementById('particles-canvas');
  if(!c) return;
  const ctx = c.getContext('2d');
  let W, H, pts = [];

  function resize(){
    W = c.width  = window.innerWidth;
    H = c.height = window.innerHeight;
  }
  resize();
  window.addEventListener('resize', resize);

  const CHARS = '01-./В·вЂўвњ¦РђР‘Р’'.split('');
  for(let i=0;i<55;i++) pts.push({
    x: Math.random()*window.innerWidth,
    y: Math.random()*window.innerHeight,
    s: Math.random()*10+8,
    v: Math.random()*.4+.1,
    o: Math.random()*.5+.1,
    c: CHARS[Math.floor(Math.random()*CHARS.length)]
  });

  function draw(){
    ctx.clearRect(0,0,W,H);
    ctx.font = '12px monospace';
    pts.forEach(p=>{
      ctx.globalAlpha = p.o;
      ctx.fillStyle = '#ffe033';
      ctx.fillText(p.c, p.x, p.y);
      p.y += p.v;
      if(p.y > H+20){ p.y=-20; p.x=Math.random()*W; p.c=CHARS[Math.floor(Math.random()*CHARS.length)]; }
    });
    ctx.globalAlpha = 1;
    requestAnimationFrame(draw);
  }
  draw();
})();

// в”Ђв”Ђ Р”РµРєРѕСЂР°С‚РёРІРЅР°СЏ РєР°СЂС‚Р° (Р»РёРЅРёРё РєРѕРѕСЂРґРёРЅР°С‚) в”Ђв”Ђ
(function initMap(){
  const c = document.getElementById('map-canvas');
  if(!c) return;
  const ctx = c.getContext('2d');
  c.width  = window.innerWidth;
  c.height = window.innerHeight;
  ctx.strokeStyle = '#ffe033';
  ctx.lineWidth = .5;
  // РЎРµС‚РєР°
  for(let x=0;x<c.width;x+=40){
    ctx.globalAlpha = .3;
    ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,c.height); ctx.stroke();
  }
  for(let y=0;y<c.height;y+=40){
    ctx.globalAlpha = .3;
    ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(c.width,y); ctx.stroke();
  }
  // РљСЂРµСЃС‚РёРєРё
  ctx.globalAlpha = .8;
  [[.2,.3],[.7,.2],[.5,.7],[.8,.6],[.3,.8]].forEach(([rx,ry])=>{
    const x=rx*c.width, y=ry*c.height;
    ctx.beginPath();
    ctx.moveTo(x-8,y); ctx.lineTo(x+8,y);
    ctx.moveTo(x,y-8); ctx.lineTo(x,y+8);
    ctx.stroke();
    ctx.beginPath();
    ctx.arc(x,y,12,0,Math.PI*2);
    ctx.stroke();
  });
})();

// в”Ђв”Ђ Р Р°РґР°СЂ в”Ђв”Ђ
function startRadar(){
  const c = document.getElementById('radar-canvas');
  if(!c) return;
  const ctx = c.getContext('2d');
  let angle = 0;
  (function frame(){
    ctx.clearRect(0,0,70,70);
    const cx=35,cy=35,r=30;
    // РљСЂСѓРіРё
    ctx.strokeStyle='rgba(255,224,51,.5)'; ctx.lineWidth=.8;
    [10,20,30].forEach(rad=>{
      ctx.beginPath(); ctx.arc(cx,cy,rad,0,Math.PI*2); ctx.stroke();
    });
    // РљСЂРµСЃСЃ
    ctx.beginPath();
    ctx.moveTo(cx-r,cy); ctx.lineTo(cx+r,cy);
    ctx.moveTo(cx,cy-r); ctx.lineTo(cx,cy+r);
    ctx.stroke();
    // Р›СѓС‡
    const grad = ctx.createConicalGradient
      ? ctx.createConicalGradient(cx,cy,angle-Math.PI*.5)
      : null;
    ctx.save();
    ctx.translate(cx,cy);
    ctx.rotate(angle);
    const sweepGrad = ctx.createLinearGradient(0,0,r,0);
    sweepGrad.addColorStop(0,'rgba(255,224,51,.9)');
    sweepGrad.addColorStop(1,'rgba(255,224,51,0)');
    ctx.beginPath();
    ctx.moveTo(0,0);
    ctx.arc(0,0,r,-Math.PI*.3,0);
    ctx.closePath();
    ctx.fillStyle = sweepGrad;
    ctx.globalAlpha = .6;
    ctx.fill();
    ctx.restore();
    // РўРѕС‡РєРё
    ctx.fillStyle='#ff3a3a';
    ctx.globalAlpha=1;
    [[15,10],[22,18],[-10,20]].forEach(([dx,dy])=>{
      ctx.beginPath();
      ctx.arc(cx+dx,cy+dy,2,0,Math.PI*2);
      ctx.fill();
    });
    angle += .04;
    requestAnimationFrame(frame);
  })();
}

// в”Ђв”Ђ РљРѕРЅС„РµС‚С‚Рё РїСЂРё РїСЂР°РІРёР»СЊРЅРѕРј РѕС‚РІРµС‚Рµ в”Ђв”Ђ
function launchConfetti(){
  const colors=['#ffe033','#ff3a3a','#55dd55','#3af','#f3a','#fa3'];
  for(let i=0;i<50;i++){
    const el = document.createElement('div');
    el.className = 'confetti-piece';
    el.style.cssText = [
      `left:${Math.random()*100}vw`,
      `top:-10px`,
      `background:${colors[Math.floor(Math.random()*colors.length)]}`,
      `transform:rotate(${Math.random()*360}deg)`,
      `width:${6+Math.random()*6}px`,
      `height:${6+Math.random()*6}px`,
      `animation-duration:${.8+Math.random()*1.2}s`,
      `animation-delay:${Math.random()*.4}s`
    ].join(';');
    document.body.appendChild(el);
    setTimeout(()=>el.remove(), 2500);
  }
}

// в”Ђв”Ђ Р­С„С„РµРєС‚ РїРµС‡Р°С‚РЅРѕР№ РјР°С€РёРЅРєРё РґР»СЏ С€РёС„СЂР° в”Ђв”Ђ
function typewriterEffect(el, text, speed){
  el.textContent='';
  const cursor = document.createElement('span');
  cursor.className='cipher-cursor';
  el.appendChild(cursor);
  let i=0;
  const interval = setInterval(()=>{
    if(i < text.length){
      el.insertBefore(document.createTextNode(text[i]),cursor);
      i++;
    } else {
      clearInterval(interval);
      setTimeout(()=>cursor.remove(),1500);
    }
  }, speed||40);
}

// в”Ђв”Ђ Р“Р»РёС‚С‡ РїСЂРё РѕС€РёР±РєРµ в”Ђв”Ђ
function triggerGlitch(el){
  el.classList.add('glitch');
  el.setAttribute('data-enc', el.textContent);
  setTimeout(()=>el.classList.remove('glitch'), 450);
}

// в”Ђв”Ђ РџСѓР»СЊСЃ РєРЅРѕРїРєРё в”Ђв”Ђ
function pulseBtn(el){
  el.classList.add('pulse');
  setTimeout(()=>el.classList.remove('pulse'), 650);
}

// в”Ђв”Ђ Р’СЃРїС‹С€РєР° РѕС‡РєРѕРІ в”Ђв”Ђ
function flashScore(el){
  el.classList.add('score-flash');
  setTimeout(()=>el.classList.remove('score-flash'), 450);
}

// в”Ђв”Ђ РџРµСЂРµРѕРїСЂРµРґРµР»СЏРµРј loadCipher вЂ” РґРѕР±Р°РІР»СЏРµРј typewriter в”Ђв”Ђ
if (false) {
const _origLoadCipher = loadCipher;
loadCipher = function(){
  _origLoadCipher();
  const box = document.getElementById('cipher-box');
  const chapter = CHAPTERS[state.chapter];
  if (!box || !chapter || !Array.isArray(chapter.ciphers)) return;
  const cipher = chapter.ciphers[state.cipherIdx];
  if (!cipher) return;
  if(cipher.type !== 'morse' && cipher.type !== 'anagram' && cipher.type !== 'photo' && cipher.type !== 'map' && cipher.type !== 'math'){
    const originalText = box.textContent;
    if(originalText.trim()){
      box.textContent = '';
      typewriterEffect(box, originalText, 55);
    }
  }
};

// в”Ђв”Ђ РџРµСЂРµРѕРїСЂРµРґРµР»СЏРµРј checkAnswer вЂ” РґРѕР±Р°РІР»СЏРµРј СЌС„С„РµРєС‚С‹ в”Ђв”Ђ
const _origCheck = checkAnswer;
let _checkAnswerBusy = false;
checkAnswer = async function(){
  if (_checkAnswerBusy) return;
  if (_isActiveCipherResolved()) return;
  _checkAnswerBusy = true;
  const btn = document.querySelector('.btn-check');
  if (btn) {
    pulseBtn(btn);
    btn.disabled = true;
  }
  try {
    await _origCheck();
  } catch (e) {
    console.error('checkAnswer failed:', e);
    const chapter = CHAPTERS[state.chapter];
    const cipher = chapter && Array.isArray(chapter.ciphers) ? chapter.ciphers[state.cipherIdx] : null;

    if (cipher && cipher.type === 'quiz' && (!_quizState || !_quizState.cipher)) {
      console.warn('checkAnswer recover: empty quiz state, reloading cipher');
      try { loadCipher(); } catch (reloadErr) { console.error('quiz reload failed:', reloadErr); }
      showToast('вљ  Р—Р°РґР°РЅРёРµ РѕР±РЅРѕРІР»РµРЅРѕ. РќР°Р¶РјРёС‚Рµ В«РџСЂРѕРІРµСЂРёС‚СЊВ» СЃРЅРѕРІР°');
    } else {
      showToast('вљ  РћС€РёР±РєР° РїСЂРѕРІРµСЂРєРё. РџРѕРїСЂРѕР±СѓР№С‚Рµ РµС‰С‘ СЂР°Р·');
    }
  } finally {
    if (btn && !_isActiveCipherResolved()) btn.disabled = false;
    setTimeout(() => { _checkAnswerBusy = false; }, 120);
  }
};

// в”Ђв”Ђ РџРµСЂРµРѕРїСЂРµРґРµР»СЏРµРј showSuccess вЂ” РґРѕР±Р°РІР»СЏРµРј РєРѕРЅС„РµС‚С‚Рё в”Ђв”Ђ
const _origSuccess = showSuccess;
showSuccess = function(cipher, pts, elapsed){
  try {
    _origSuccess(cipher, pts, elapsed);
  } catch (e) {
    console.error('showSuccess failed:', e);
    showToast('вљ  РћС€РёР±РєР° СЌРєСЂР°РЅР° СЂРµР·СѓР»СЊС‚Р°С‚Р°. РџРѕРїСЂРѕР±СѓР№С‚Рµ РµС‰С‘ СЂР°Р·');
    return;
  }
  if(pts > 0) {
    launchConfetti();
    setTimeout(()=>{
      const el = document.getElementById('succ-pts');
      if(el) flashScore(el);
    }, 300);
  }
};

// в”Ђв”Ђ РџРµСЂРµРѕРїСЂРµРґРµР»СЏРµРј showBriefing вЂ” Р·Р°РїСѓСЃРєР°РµРј СЂР°РґР°СЂ в”Ђв”Ђ
}

const _origBriefing = showBriefing;
showBriefing = function(idx){
  _origBriefing(idx);
  setTimeout(startRadar, 100);
};

// в”Ђв”Ђ РђРЅРёРјРёСЂСѓРµРј РєР°СЂС‚РѕС‡РєРё СЃ Р·Р°РґРµСЂР¶РєРѕР№ в”Ђв”Ђ
const _origRenderChapters = renderChapters;
renderChapters = function(){
  _origRenderChapters();
  document.querySelectorAll('.chapter-card').forEach((card,i)=>{
    card.style.animationDelay = (i*80)+'ms';
  });
};




// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РќРР–РќР•Р• РњР•РќР® вЂ” РўРђР‘Р‘РђР 
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
let currentTab = 'chapters';

function showBottomNav() {
  const nav = document.getElementById('bottom-nav');
  if (nav) nav.style.display = 'flex';
}

function hideBottomNav() {
  const nav = document.getElementById('bottom-nav');
  if (nav) nav.style.display = 'none';
}

function switchTab(tab) {
  currentTab = tab;

  // Р”РµР°РєС‚РёРІРёСЂСѓРµРј РІСЃРµ СЌРєСЂР°РЅС‹
  document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
  // Р”РµР°РєС‚РёРІРёСЂСѓРµРј РІСЃРµ С‚Р°Р±С‹
  document.querySelectorAll('.bn-tab').forEach(b => b.classList.remove('active'));

  showBottomNav();

  const screenMap = {
    'chapters':     's-chapters',
    'leaderboard':  's-leaderboard-tab',
    'profile':      's-profile-tab',
    'about':        's-about-tab',
    'achievements': 's-achievements-tab',
    'secret':       's-secret-tab',
    'settings':     's-settings-tab',
  };
  const tabMap = {
    'chapters':     'bn-chapters',
    'leaderboard':  'bn-leaderboard',
    'profile':      'bn-profile',
    'about':        'bn-about',
    'achievements': 'bn-achievements',
    'secret':       'bn-secret',
    'settings':     'bn-settings',
  };

  const screenEl = document.getElementById(screenMap[tab]);
  const tabEl    = document.getElementById(tabMap[tab]);
  if (screenEl) screenEl.classList.add('active');
  if (tabEl)    tabEl.classList.add('active');

  if (tab === 'chapters')    { renderChapters(); fetchAndApplyState(); }
  if (tab === 'leaderboard') { renderLeaderboardTab(); fetchAndApplyState(); fetchAndApplyLeaderboard(); }
  if (tab === 'profile')     { renderProfileTab();     fetchAndApplyState(); }
  if (tab === 'about') {
    renderAboutTab();
    applyAboutBuildVersion();
  }
  if (tab === 'achievements') {
    const el = document.getElementById('achievements-tab-content');
    if (el) renderAchievementsTab(el);
  }
  if (tab === 'secret') {
    renderSecretMissionsTab();
    fetchAndApplyState();
  }
  if (tab === 'settings')     renderSettingsTab();
}

function renderAboutTab() {
  const el = document.getElementById('about-tab-content');
  if (!el) return;
  el.innerHTML = '<div style="background:linear-gradient(180deg,#1a1508 0%,#0d0b08 100%);padding:32px 20px 24px;text-align:center;border-bottom:1px solid rgba(255,224,51,.1)">'
    + '<div style="font-size:56px;margin-bottom:12px;filter:drop-shadow(0 0 24px rgba(255,224,51,.5))">рџ”ђ</div>'
    + '<div style="font-family:var(--head);font-size:var(--fs-2xl);color:var(--accent);letter-spacing:.08em;margin-bottom:4px">РЁРР¤Р РћР’РђР›Р¬Р©РРљ</div>'
    + '<div style="font-size:var(--fs-sm);color:var(--muted);letter-spacing:.06em">Р’Р•Р РЎРРЇ 1.0  В·  2025</div></div>'
    + '<div style="padding:20px 16px">'
    // Рћ РёРіСЂРµ
    + '<div style="background:rgba(255,224,51,.04);border:1px solid rgba(255,224,51,.1);border-radius:8px;padding:16px;margin-bottom:14px">'
    + '<div style="font-family:var(--head);font-size:var(--fs-base);color:var(--accent);letter-spacing:.06em;margin-bottom:8px">рџ“– Рћ РР“Р Р•</div>'
    + '<div style="font-size:var(--fs-sm);color:rgba(255,255,255,.8);line-height:1.7"><b style="color:#fdfaf0">РЁРёРІСЂРѕРІР°Р»СЊС‰РёРє</b> вЂ” РѕР±СЂР°Р·РѕРІР°С‚РµР»СЊРЅР°СЏ РёРіСЂР° Рѕ СЃРѕР±С‹С‚РёСЏС… Р’РµР»РёРєРѕР№ РћС‚РµС‡РµСЃС‚РІРµРЅРЅРѕР№ РІРѕР№РЅС‹ РЅР° С‚РµСЂСЂРёС‚РѕСЂРёРё Р‘РµР»Р°СЂСѓСЃРё. Р’С‹ вЂ” СЃРѕРІРµС‚СЃРєРёР№ СЂР°Р·РІРµРґС‡РёРє. Р Р°СЃС€РёС„СЂСѓР№С‚Рµ РґРѕРЅРµСЃРµРЅРёСЏ, РїСЂРѕР№РґРёС‚Рµ 6 РѕРїРµСЂР°С†РёР№ Рё РїСЂРёР±Р»РёР·СЊС‚Рµ Р”РµРЅСЊ РџРѕР±РµРґС‹.</div></div>'
    // РљР°Рє РёРіСЂР°С‚СЊ
    + '<div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:8px;padding:16px;margin-bottom:14px">'
    + '<div style="font-family:var(--head);font-size:var(--fs-base);color:var(--accent);letter-spacing:.06em;margin-bottom:10px">рџЋ® РљРђРљ РР“Р РђРўР¬</div>'
    + ['рџ—є Р’С‹Р±РµСЂРёС‚Рµ РѕРїРµСЂР°С†РёСЋ вЂ” РѕС‚РєСЂРѕР№С‚Рµ РіР»Р°РІСѓ Рё РїСЂРѕС‡РёС‚Р°Р№С‚Рµ Р±СЂРёС„РёРЅРі',
       'рџ”ђ Р Р°СЃС€РёС„СЂСѓР№С‚Рµ РїРѕСЃР»Р°РЅРёРµ вЂ” 6 С‚РёРїРѕРІ С€РёС„СЂРѕРІ',
       'вќ¤пёЏ 5 Р¶РёР·РЅРµР№ РЅР° РіР»Р°РІСѓ вЂ” РєР°Р¶РґР°СЏ РѕС€РёР±РєР° РјРёРЅСѓСЃ Р¶РёР·РЅСЊ',
       'вљЎ РЎРєРѕСЂРѕСЃС‚СЊ = РѕС‡РєРё вЂ” С‡РµРј Р±С‹СЃС‚СЂРµРµ, С‚РµРј Р±РѕР»СЊС€Рµ',
       'рџ”Ґ РЎС‚СЂРёРє вЂ” СЃРµСЂРёСЏ РїСЂР°РІРёР»СЊРЅС‹С… РѕС‚РІРµС‚РѕРІ РґР°С‘С‚ Р±РѕРЅСѓСЃ +5% Р·Р° РєР°Р¶РґС‹Р№',
       'рџЏ† РўР°Р±Р»РёС†Р° Р»РёРґРµСЂРѕРІ вЂ” СЃРѕСЂРµРІРЅСѓР№С‚РµСЃСЊ СЃ РґСЂСѓРіРёРјРё СѓС‡РµРЅРёРєР°РјРё'
      ].map(t => '<div style="font-size:var(--fs-sm);color:rgba(255,255,255,.75);padding:5px 0;border-bottom:1px solid rgba(255,255,255,.04)">' + t + '</div>').join('')
    + '</div>'
    // РћРїРµСЂР°С†РёРё
    + '<div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:8px;padding:16px;margin-bottom:14px">'
    + '<div style="font-family:var(--head);font-size:var(--fs-base);color:var(--accent);letter-spacing:.06em;margin-bottom:10px">рџ“љ РћРџР•Р РђР¦РР</div>'
    + [['I','рџЏ™','РџРѕРґРїРѕР»СЊРµ РњРёРЅСЃРєР°','РњРёРЅСЃРє В· РСЋР»СЊ 1942'],
       ['II','рџ’Ј','Р РµР»СЊСЃРѕРІР°СЏ РІРѕР№РЅР°','Р’РёС‚РµР±СЃРє В· РђРІРіСѓСЃС‚ 1943'],
       ['III','вљ”пёЏ','РћРїРµСЂР°С†РёСЏ Р‘Р°РіСЂР°С‚РёРѕРЅ','Р‘РµР»Р°СЂСѓСЃСЊ В· РСЋРЅСЊ 1944'],
       ['IV','рџљ©','РџРѕСЃР»РµРґРЅРёР№ С€РёС„СЂ','Р‘РµСЂР»РёРЅ В· РњР°Р№ 1945'],
       ['V','рџЊЌ','Р—РЅР°Р№ СЃРІРѕСЋ Р·РµРјР»СЋ','Р‘РµР»Р°СЂСѓСЃСЊ В· РСЃС‚РѕСЂРёСЏ'],
       ['VI','рџЋ–','Р”РѕСЂРѕРіР° Рє РџРѕР±РµРґРµ','Р‘РµР»Р°СЂСѓСЃСЊ В· 1941вЂ“1945']
      ].map(([n,ic,t,p]) => '<div style="display:flex;align-items:center;gap:10px;padding:7px 0;border-bottom:1px solid rgba(255,255,255,.04)">'
        + '<div style="font-size:18px">' + ic + '</div>'
        + '<div><div style="font-size:var(--fs-sm);color:#fdfaf0"><span style="color:var(--muted);font-size:10px">Р“Р›РђР’Рђ ' + n + ' В· </span>' + t + '</div>'
        + '<div style="font-size:10px;color:var(--muted)">' + p + '</div></div></div>'
      ).join('')
    + '</div>'
    // Р Р°Р·СЂР°Р±РѕС‚РєР°
    + '<div style="background:rgba(255,224,51,.04);border:1px solid rgba(255,224,51,.1);border-radius:8px;padding:16px;margin-bottom:14px">'
    + '<div style="font-family:var(--head);font-size:var(--fs-base);color:var(--accent);letter-spacing:.06em;margin-bottom:10px">рџ‘ЁвЂЌрџ’» Р РђР—Р РђР‘РћРўРљРђ</div>'
    + '<div style="display:flex;gap:12px;align-items:center;margin-bottom:10px"><div style="font-size:28px">рџЏ«</div><div><div style="font-size:var(--fs-sm);color:#fdfaf0;font-weight:600">РЎРЁ в„–3 Рі. РҐРѕР№РЅРёРєРё</div><div style="font-size:11px;color:var(--muted)">Р“РѕСЃСѓРґР°СЂСЃС‚РІРµРЅРЅРѕРµ СѓС‡СЂРµР¶РґРµРЅРёРµ РѕР±СЂР°Р·РѕРІР°РЅРёСЏ</div></div></div>'
    + '<div style="display:flex;gap:12px;align-items:center"><div style="font-size:28px">рџ‘ЁвЂЌрџЋ“</div><div><div style="font-size:var(--fs-sm);color:#fdfaf0;font-weight:600">Р“СѓРґ Р®СЂРёР№ РџРµС‚СЂРѕРІРёС‡</div><div style="font-size:11px;color:var(--muted)">Р Р°Р·СЂР°Р±РѕС‚С‡РёРє В· РЈС‡РёС‚РµР»СЊ РёРЅС„РѕСЂРјР°С‚РёРєРё</div></div></div></div>'
    // РџРѕРґРґРµСЂР¶РєР°
    + '<div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:8px;padding:16px;margin-bottom:14px">'
    + '<div style="font-family:var(--head);font-size:var(--fs-base);color:var(--accent);letter-spacing:.06em;margin-bottom:10px">рџ† РџРћР”Р”Р•Р Р–РљРђ</div>'
    + '<div style="display:flex;gap:12px;align-items:center;margin-bottom:8px"><div style="font-size:22px">рџ’¬</div><div><div style="font-size:var(--fs-sm);color:#fdfaf0">Telegram</div><div style="font-size:11px;color:var(--muted)">@Yury_hud</div></div></div>'
    + '<div style="display:flex;gap:12px;align-items:center;margin-bottom:8px"><div style="font-size:22px">рџ“§</div><div><div style="font-size:var(--fs-sm);color:#fdfaf0">Email</div><div style="font-size:11px;color:var(--muted)">uragud.2020@gmail.com</div></div></div>'
    + '<div style="display:flex;gap:12px;align-items:center"><div style="font-size:22px">рџ•ђ</div><div><div style="font-size:var(--fs-sm);color:#fdfaf0">Р’СЂРµРјСЏ РѕС‚РІРµС‚Р°</div><div style="font-size:11px;color:var(--muted)">РџРЅвЂ“РџС‚, 9:00вЂ“18:00</div></div></div></div>'
    + '<div style="text-align:center;padding:12px 0;color:var(--muted);font-size:10px;letter-spacing:.06em">РЁРР¤Р РћР’РђР›Р¬Р©РРљ v1.0 В· В© 2025 РЎРЁ в„–3 Рі. РҐРѕР№РЅРёРєРё</div>'
    + '</div>';
}

function applyAboutBuildVersion() {
  const el = document.getElementById('about-tab-content');
  if (!el) return;
  let buildVersion = 'vdev';
  try {
    const params = new URLSearchParams(window.location.search || '');
    const raw = (params.get('v') || window.__BOOT_VERSION || '').toString().trim();
    if (raw) buildVersion = 'v' + raw;
  } catch (e) {}
  const buildYear = String(new Date().getFullYear());
  let html = el.innerHTML || '';
  html = html.replace(/v1\.0/g, buildVersion);
  html = html.replace(/1\.0/g, buildVersion.toUpperCase());
  html = html.replace(/2025/g, buildYear);
  el.innerHTML = html;
}

function renderLeaderboardTab() {
  const list = document.getElementById('lb-list-tab');
  if (!list) return;
  syncTopStatusBars();
  // Р•СЃР»Рё leaderboard РїСѓСЃС‚РѕР№ РЅРѕ tgInitLB РµСЃС‚СЊ вЂ” РїРѕРґРіСЂСѓР¶Р°РµРј
  if (!state.leaderboard.length && tgInitLB.length) mergeBotLeaderboard();
  const lb    = state.leaderboard || [];
  const myUid = getTgUserId() || 'guest';
  const total = lb.length;

  // РџРµСЂРµРєР»СЋС‡Р°С‚РµР»СЊ РІРєР»Р°РґРѕРє вЂ” РѕР±СЉСЏРІР»СЏРµРј Р”Рћ header С‡С‚РѕР±С‹ РёСЃРїРѕР»СЊР·РѕРІР°С‚СЊ РІ С€Р°Р±Р»РѕРЅРµ
  const currentRatingTab = window._ratingTab || 'score';

  // РЁР°РїРєР°
  const header = `<div style="padding:12px 16px 8px">
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:10px">
      <div>
        <div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent)">Р Р•Р™РўРРќР“</div>
        <div style="font-size:10px;color:var(--muted);margin-top:2px">рџ‘Ґ ${total || 0} СѓС‡. В· <span style="color:rgba(255,224,51,.5)">рџ‘† РЅР°Р¶РјРё РЅР° СЃРµР±СЏ</span></div>
      </div>
      <button onclick="switchTab('leaderboard')"
        style="background:rgba(255,224,51,.1);border:1px solid rgba(255,224,51,.2);
        color:var(--accent);padding:6px 10px;border-radius:4px;font-family:var(--head);
        font-size:var(--fs-xs);cursor:pointer">рџ”„</button>
    </div>
    <div style="display:flex;gap:6px">
      <button onclick="window._ratingTab='score';renderLeaderboardTab()"
        style="flex:1;padding:7px;border-radius:6px;font-family:var(--head);font-size:10px;
        cursor:pointer;letter-spacing:.05em;border:1px solid rgba(255,224,51,.2);
        background:${currentRatingTab==='score'?'rgba(255,224,51,.15)':'transparent'};
        color:${currentRatingTab==='score'?'var(--accent)':'var(--muted)'}">в­ђ РћР§РљР</button>
      <button onclick="window._ratingTab='achievements';renderLeaderboardTab()"
        style="flex:1;padding:7px;border-radius:6px;font-family:var(--head);font-size:10px;
        cursor:pointer;letter-spacing:.05em;border:1px solid rgba(255,224,51,.2);
        background:${currentRatingTab==='achievements'?'rgba(255,224,51,.15)':'transparent'};
        color:${currentRatingTab==='achievements'?'var(--accent)':'var(--muted)'}">рџЏ… Р”РћРЎРўРР–Р•РќРРЇ</button>
    </div>
  </div>`;

  if (!lb.length) {
    list.innerHTML = header + `
    <div style="text-align:center;padding:50px 24px">
      <div style="font-size:56px;margin-bottom:16px;filter:drop-shadow(0 0 20px rgba(255,224,51,.3))">рџЏ†</div>
      <div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent);
        margin-bottom:8px;letter-spacing:.06em">РџРћРљРђ РџРЈРЎРўРћ</div>
      <div style="font-size:var(--fs-sm);color:var(--muted);line-height:1.6;margin-bottom:24px">
        РќРёРєС‚Рѕ РµС‰С‘ РЅРµ РїСЂРѕС€С‘Р» РЅРё РѕРґРЅРѕР№ РіР»Р°РІС‹.<br>
        <b style="color:#fdfaf0">РЎС‚Р°РЅСЊ РїРµСЂРІС‹Рј С€РёРІСЂРѕРІР°Р»СЊС‰РёРєРѕРј С€РєРѕР»С‹!</b>
      </div>
      <button onclick="showMyStats()" style="width:100%;margin-bottom:8px;background:rgba(255,224,51,.08);border:1px solid rgba(255,224,51,.2);color:var(--accent);padding:11px;font-family:var(--head);font-size:var(--fs-sm);font-weight:600;border-radius:4px;cursor:pointer;letter-spacing:.08em">рџ“Љ Р”Р•РўРђР›Р¬РќРђРЇ РЎРўРђРўРРЎРўРРљРђ</button>
    <button onclick="showMyAchievements()" style="width:100%;margin-bottom:10px;background:rgba(255,180,0,.06);border:1px solid rgba(255,180,0,.2);color:#ffb400;padding:11px;font-family:var(--head);font-size:var(--fs-sm);font-weight:600;border-radius:4px;cursor:pointer;letter-spacing:.08em">рџЏ… РњРћР Р”РћРЎРўРР–Р•РќРРЇ В· ${Object.keys(state.achievements||{}).length}/${ACHIEVEMENTS.length}</button>
    <button onclick="switchTab('chapters')"
        style="background:var(--accent);color:#0a0a08;border:none;padding:12px 24px;
        font-family:var(--head);font-size:var(--fs-base);font-weight:700;
        border-radius:4px;cursor:pointer;letter-spacing:.08em;width:100%">
        в–¶ РќРђР§РђРўР¬ РР“Р РЈ
      </button>
    </div>`;
    return;
  }

  const medals = ['рџҐ‡','рџҐ€','рџҐ‰'];
  const myRole2 = state.gameRole || ((tgInitMe && tgInitMe.role) || '');
  const isAdminUser = myRole2 === 'admin';
  // Р¤РёР»СЊС‚СЂСѓРµРј: РѕР±С‹С‡РЅС‹Рµ РёРіСЂРѕРєРё РЅРµ РІРёРґСЏС‚ Р°РґРјРёРЅРѕРІ/С‚РµСЃС‚РёСЂРѕРІС‰РёРєРѕРІ РІ СЂРµР№С‚РёРЅРіРµ
  const filteredLb = isAdminUser
    ? lb  // РђРґРјРёРЅ РІРёРґРёС‚ РІСЃРµС…
    : lb.filter(e => e.role !== 'admin' && e.role !== 'tester');

  const visibleLb = isAdminUser ? filteredLb : filteredLb.slice(0, 10);
  const rows = visibleLb.map((e,i) => {
    const isMe   = e.uid === myUid;
    const pct    = Math.round((e.completed / 6) * 100);
    const done   = e.completed >= 6 ? 'вњ… Р’СЃРµ РіР»Р°РІС‹' : `${e.completed}/6 РіР»Р°РІ В· ${pct}%`;
    const rankBg = i===0?'rgba(255,215,0,.06)':i===1?'rgba(192,192,192,.04)':i===2?'rgba(205,127,50,.04)':'';
    const roleIcon  = e.role === 'admin' ? ' рџ‘‘' : e.role === 'tester' ? ' рџ§Є' : '';
    const roleTag   = e.role === 'admin' ? ' <span style="font-size:9px;color:var(--accent);opacity:.7;letter-spacing:.04em">(РђР”РњРРќ)</span>'
                    : e.role === 'tester' ? ' <span style="font-size:9px;color:var(--muted);letter-spacing:.04em">(РўР•РЎРў)</span>'
                    : '';
    const roleLabel = e.role === 'admin' ? 'РђРґРјРёРЅРёСЃС‚СЂР°С‚РѕСЂ' : e.role === 'tester' ? 'РўРµСЃС‚РёСЂРѕРІС‰РёРє' : '';
    const clickAttr = isMe ? ' onclick="showMyStats()" ' : '';
    return `<div ${clickAttr}style="display:flex;align-items:center;gap:12px;padding:12px 16px;border-bottom:1px solid rgba(255,255,255,.04);background:${isMe?'rgba(255,224,51,.06)':rankBg};${isMe?'border-left:3px solid var(--accent);cursor:pointer':''}">
      <div style="font-size:22px;min-width:30px;text-align:center;line-height:1">
        ${medals[i] || '<span style="font-family:var(--head);font-size:var(--fs-sm);color:var(--muted)">'+(i+1)+'</span>'}
      </div>
      <div style="flex:1;min-width:0">
        <div style="font-family:var(--head);font-size:var(--fs-base);
          color:${isMe?'var(--accent)':'#fdfaf0'};
          white-space:nowrap;overflow:hidden;text-overflow:ellipsis">
          ${e.name}${roleIcon}${roleTag}${isMe?' рџ‘€':''}
        </div>
        <div style="font-size:10px;color:var(--muted);margin-top:2px;letter-spacing:.04em;display:flex;gap:6px">
          <span>${done}</span>
          ${roleLabel ? `<span style="color:rgba(255,224,51,.4)">${roleLabel}</span>` : ''}
        </div>
      </div>
      <div style="font-family:var(--head);font-size:var(--fs-xl);
        color:${isMe?'var(--accent)':'#c4b06a'};
        text-shadow:${isMe?'0 0 12px rgba(255,224,51,.5)':'none'}">
        ${e.score}
      </div>
    </div>`;
  }).join('');

  // РњРѕСЏ РїРѕР·РёС†РёСЏ РµСЃР»Рё РЅРµ РІ С‚РѕРї-20
  const myInTop = visibleLb.some(e => e.uid === myUid);
  const myPos   = myInTop ? '' : `
    <div style="margin:12px 16px;padding:12px;
      background:rgba(255,224,51,.05);border:1px solid rgba(255,224,51,.15);border-radius:6px;
      font-size:var(--fs-sm);color:var(--muted);text-align:center">
      рџ“Ќ Р’С‹ РЅРµ РІ С‚РѕРї-10. РќР°Р¶РјРёС‚Рµ РЅР° СЃРІРѕС‘ РёРјСЏ РІ СЂРµР№С‚РёРЅРіРµ РґР»СЏ СЃС‚Р°С‚РёСЃС‚РёРєРё.
    </div>`;

  if (currentRatingTab === 'achievements') {
    // Р РµР№С‚РёРЅРі РґРѕСЃС‚РёР¶РµРЅРёР№
    const achLb = visibleLb.map((e, i) => {
      const isMe = e.uid === myUid;
      const achCount = e.achievementCount || 0;
      const achPts   = e.achievementPts   || 0;
      const medal = i === 0 ? 'рџҐ‡' : i === 1 ? 'рџҐ€' : i === 2 ? 'рџҐ‰' : `<span style="font-family:var(--head);font-size:var(--fs-sm);color:var(--muted)">${i+1}</span>`;
      return `<div style="display:flex;align-items:center;gap:12px;padding:10px 16px;
        border-bottom:1px solid rgba(255,255,255,.04);
        background:${isMe?'rgba(255,224,51,.06)':''}">
        <div style="font-size:20px;min-width:28px;text-align:center">${medal}</div>
        <div style="flex:1">
          <div style="font-family:var(--head);font-size:var(--fs-sm);color:${isMe?'var(--accent)':'#fdfaf0'}">${e.name}${isMe?' рџ‘€':''}</div>
          <div style="font-size:10px;color:var(--muted)">${achCount} РґРѕСЃС‚РёР¶РµРЅРёР№</div>
        </div>
        <div style="font-family:var(--head);font-size:var(--fs-md);color:var(--accent)">+${achPts}в­ђ</div>
      </div>`;
    }).join('');
    list.innerHTML = header + (achLb || '<div style="padding:40px;text-align:center;color:var(--muted);font-style:italic">Р”Р°РЅРЅС‹Рµ Р·Р°РіСЂСѓР¶Р°СЋС‚СЃСЏ...</div>');
  } else {
    list.innerHTML = header + rows + myPos;
  }
}

function renderProfileTab() {
  const el = document.getElementById('profile-tab-content');
  if (!el) return;
  const uid = getTgUserId();
  const name = tgUser ? (tgUser.first_name || 'РРіСЂРѕРє') : 'Р“РѕСЃС‚СЊ';
  // Progress is rendered from current synchronized state
  const completed = _stateCompletedCount();
  const pct = Math.round((completed / CHAPTERS.length) * 100);

  // Role comes from synchronized game state
  const myRole = state.gameRole || (tgInitMe && tgInitMe.role) || 'player';
  const roleLabels = { admin: 'рџ‘‘ РђРґРјРёРЅРёСЃС‚СЂР°С‚РѕСЂ', tester: 'рџ§Є РўРµСЃС‚РёСЂРѕРІС‰РёРє', player: '' };

  // Р—РІР°РЅРёРµ РїРѕ РѕС‡РєР°Рј
  const titles = [
    [0,   'РќРѕРІРѕР±СЂР°РЅРµС†', 'рџЋ–'],
    [35,  'РЎРµСЂР¶Р°РЅС‚ СЃРІСЏР·Рё', 'рџЏ…'],
    [55,  'РљР°РїРёС‚Р°РЅ РїРѕРґРїРѕР»СЊСЏ', 'рџЋ–'],
    [75,  'РџРѕР»РєРѕРІРЅРёРє СЂР°Р·РІРµРґРєРё', 'в­ђ'],
    [90,  'РњР°СЂС€Р°Р» РџРѕР±РµРґС‹', 'рџЊџ'],
  ];
  const maxScore = CHAPTERS.reduce((s,ch) => s + ch.ciphers.reduce((a,c2) => a+c2.points, 0), 0);
  const scorePct = Math.round((state.totalScore / maxScore) * 100);
  const rank = titles.filter(t => scorePct >= t[0]).pop() || titles[0];

  const roleLabel2 = roleLabels[myRole] || '';
  const nextRankData = titles.find(([t]) => scorePct < t);
  const toNextRank = nextRankData ? Math.max(0, Math.ceil(nextRankData[0] / 100 * maxScore) - state.totalScore) : 0;
  const nextRankName2 = nextRankData ? nextRankData[1] : null;
  const refSummary = normalizeReferralSummary(state.referralSummary || (tgInitMe && tgInitMe.ref_summary));
  const refAgents = normalizeReferralAgents(state.referralAgents || (tgInitMe && tgInitMe.ref_agents));
  const inviterPct = Math.max(0, Math.floor(Number(refSummary.inviter_percent || 0)));
  const inviteePct = Math.max(1, Math.floor(Number(refSummary.invitee_percent || 1)));
  const inviteeBonusTotal = Math.max(0, Math.floor(Number(refSummary.invitee_bonus_points || 0)));
  const referrerId = Number(refSummary.referrer_id || 0);
  state.referralSummary = refSummary;
  state.referralAgents = refAgents;
  ensureSecretState();
  const secretMode = normalizeSecretMode(state.secretMode);
  const secretMeta = _secretGetModeMeta(secretMode);
  const secretSummary = normalizeSecretSummary(state.secretSummary);
  const secretMissions = normalizeSecretMissions(state.secretMissions);
  const secretRows = secretMissions.slice(0, 5).map((mission) => {
    const pctDone = mission.target > 0 ? Math.round((mission.progress / mission.target) * 100) : 0;
    const done = !!mission.completed;
    return `<div style="padding:7px 0;border-bottom:1px solid rgba(255,255,255,.05)">
      <div style="display:flex;justify-content:space-between;gap:8px;align-items:center">
        <div style="font-size:11px;color:${done ? '#bfffe7' : '#d5ecde'}">${mission.icon} ${escapeHtml(mission.name)}</div>
        <div style="font-family:var(--head);font-size:10px;color:${done ? '#8df5d4' : 'var(--muted)'}">${mission.progress}/${mission.target}</div>
      </div>
      <div style="margin-top:4px;height:5px;border-radius:999px;background:rgba(255,255,255,.08);overflow:hidden">
        <div style="height:100%;width:${pctDone}%;background:${done ? 'linear-gradient(90deg,#67ffd3,#c4ffee)' : 'linear-gradient(90deg,#7f9c8f,#c0d8cc)'}"></div>
      </div>
    </div>`;
  }).join('') || '<div style="font-size:var(--fs-sm);color:var(--muted);font-style:italic">РЎРїРёСЃРѕРє РјРёСЃСЃРёР№ Р·Р°РіСЂСѓР·РёС‚СЃСЏ РїРѕСЃР»Рµ РїРµСЂРІРѕР№ СЃРёРЅС…СЂРѕРЅРёР·Р°С†РёРё.</div>';
  const referralRows = refAgents.length
    ? refAgents.map((a, idx) => (
        `<div style="display:flex;align-items:center;gap:8px;padding:7px 0;border-bottom:1px solid rgba(255,255,255,.05)">
          <div style="font-size:11px;color:var(--muted);min-width:18px">${idx + 1}.</div>
          <div style="flex:1;min-width:0;font-size:var(--fs-sm);color:#fdfaf0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${escapeHtml(a.name)}</div>
          <div style="font-size:11px;color:var(--muted)">${a.completed}/6</div>
          <div style="font-family:var(--head);font-size:11px;color:var(--accent)">+${a.bonus_points} / +${a.invitee_bonus_points}</div>
        </div>`
      )).join('')
    : '<div style="font-size:var(--fs-sm);color:var(--muted);font-style:italic">РџРѕРєР° РЅРµС‚ Р·Р°РІРµСЂР±РѕРІР°РЅРЅС‹С… Р°РіРµРЅС‚РѕРІ.</div>';

  // РЎС‚СЂРѕРёРј РёСЃС‚РѕСЂРёСЋ РіР»Р°РІ
  const chapterHistory = CHAPTERS.map(ch => {
    const done  = !!state.completedChapters[ch.id];
    const score = state.chapterScores[ch.id] || 0;
    const icon  = done ? (score > 0 ? 'вњ…' : 'рџ’”') : 'рџ”’';
    const color = done ? (score > 0 ? 'var(--green)' : 'var(--accent2)') : 'rgba(255,255,255,.2)';
    return `<div style="display:flex;align-items:center;gap:10px;padding:8px 0;
        border-bottom:1px solid rgba(255,255,255,.04)">
      <div style="font-size:18px">${icon}</div>
      <div style="flex:1">
        <div style="font-size:var(--fs-sm);color:${done?'#fdfaf0':'rgba(255,255,255,.3)'}">${ch.title}</div>
        <div style="font-size:10px;color:var(--muted)">${ch.subtitle}</div>
      </div>
      <div style="font-family:var(--head);font-size:var(--fs-sm);color:${color}">
        ${score ? score + ' РѕС‡' : done ? 'вЂ”' : ''}
      </div>
    </div>`;
  }).join('');

  el.innerHTML = `
    <div style="text-align:center;margin-bottom:20px">
      <div style="font-size:52px;margin-bottom:10px;filter:drop-shadow(0 0 16px rgba(255,224,51,.4))">${rank[2]}</div>
      <div style="font-family:var(--head);font-size:var(--fs-2xl);color:var(--accent);letter-spacing:.04em">${name}</div>
      <div style="font-size:var(--fs-sm);color:var(--muted);margin-top:4px;letter-spacing:.06em">${rank[1].toUpperCase()}</div>
      ${roleLabel2 ? `<div style="margin-top:6px;font-family:var(--head);font-size:var(--fs-sm);color:var(--accent);letter-spacing:.06em;opacity:.8">${roleLabel2}</div>` : ''}
      <div style="margin-top:10px;display:inline-flex;align-items:center;gap:6px;
        background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.1);
        border-radius:20px;padding:5px 14px">
        <span style="font-size:10px;color:var(--muted);letter-spacing:.06em">TELEGRAM ID</span>
        <span style="font-family:var(--mono);font-size:var(--fs-sm);color:#fdfaf0">${uid || 'РіРѕСЃС‚СЊ'}</span>
      </div>
    </div>
    <div style="background:#141108;border:1px solid rgba(255,224,51,.12);border-radius:8px;padding:16px;margin-bottom:12px">
      <div style="display:flex;justify-content:space-around;text-align:center">
        <div>
          <div style="font-family:var(--head);font-size:var(--fs-2xl);color:var(--accent)">${state.totalScore}</div>
          <div style="font-size:10px;color:var(--muted);letter-spacing:.06em">РћР§РљРћР’</div>
        </div>
        <div>
          <div style="font-family:var(--head);font-size:var(--fs-2xl);color:var(--accent)">${completed}/${CHAPTERS.length}</div>
          <div style="font-size:10px;color:var(--muted);letter-spacing:.06em">Р“Р›РђР’</div>
        </div>
        <div>
          <div style="font-family:var(--head);font-size:var(--fs-2xl);color:var(--accent)">${pct}%</div>
          <div style="font-size:10px;color:var(--muted);letter-spacing:.06em">РџР РћР“Р Р•РЎРЎ</div>
        </div>
      </div>
      <!-- РџСЂРѕРіСЂРµСЃСЃ-Р±Р°СЂ -->
      <div style="margin-top:12px;background:rgba(255,255,255,.06);border-radius:4px;height:6px;overflow:hidden">
        <div style="height:100%;background:var(--accent);border-radius:4px;width:${pct}%;transition:width .5s"></div>
      </div>
    </div>
    <div style="background:#141108;border:1px solid rgba(120,215,255,.22);border-radius:8px;padding:14px;margin-bottom:12px">
      <div style="font-family:var(--head);font-size:var(--fs-xs);color:#9edfff;letter-spacing:.1em;margin-bottom:10px">рџ•µпёЏ РњРћР РђР“Р•РќРўР«</div>
      <div style="display:flex;justify-content:space-between;gap:10px;margin-bottom:10px">
        <div style="flex:1;background:rgba(120,215,255,.07);border:1px solid rgba(120,215,255,.16);border-radius:6px;padding:8px;text-align:center">
          <div style="font-family:var(--head);font-size:var(--fs-lg);color:#9edfff">${refSummary.invited_count}</div>
          <div style="font-size:10px;color:var(--muted);letter-spacing:.06em">РџР РР“Р›РђРЁР•РќРћ</div>
        </div>
        <div style="flex:1;background:rgba(120,215,255,.07);border:1px solid rgba(120,215,255,.16);border-radius:6px;padding:8px;text-align:center">
          <div style="font-family:var(--head);font-size:var(--fs-lg);color:#9edfff">${refSummary.active_count}</div>
          <div style="font-size:10px;color:var(--muted);letter-spacing:.06em">РђРљРўРР’РќР«РҐ</div>
        </div>
        <div style="flex:1;background:rgba(120,215,255,.07);border:1px solid rgba(120,215,255,.16);border-radius:6px;padding:8px;text-align:center">
          <div style="font-family:var(--head);font-size:var(--fs-lg);color:#9edfff">+${refSummary.bonus_points}</div>
          <div style="font-size:10px;color:var(--muted);letter-spacing:.06em">Р‘РћРќРЈРЎ</div>
        </div>
      </div>
      <div style="font-size:10px;color:#9edfff;line-height:1.45;margin-bottom:8px">
        Ваша ставка: <b>${inviterPct}%</b> (1 агент = 1%, 2 агента = 2%, ...)<br>
        Ваш бонус приглашённого: <b>${inviteePct}%</b> · уже получено <b>+${inviteeBonusTotal}</b>${referrerId > 0 ? ` · от ID ${referrerId}` : ''}
      </div>
      <div style="font-size:10px;color:var(--muted);margin-bottom:6px">Agent list (+you / +agent)</div>
      ${referralRows}
    </div>
    <div style="background:#121a17;border:1px solid rgba(103,255,211,.26);border-radius:8px;padding:14px;margin-bottom:12px">
      <div style="display:flex;justify-content:space-between;align-items:center;gap:8px;margin-bottom:9px">
        <div style="font-family:var(--head);font-size:var(--fs-xs);color:#8bf4d3;letter-spacing:.09em">рџ•¶ РўРђР™РќР«Р• РњРРЎРЎРР</div>
        <div style="font-size:10px;color:#9bd8c7">${secretSummary.completed}/${secretSummary.total}</div>
      </div>
      <div style="font-size:11px;color:#d9fff2;line-height:1.5;margin-bottom:8px">
        Р РµР¶РёРј: <b>${secretMeta.title}</b> В· Р±РѕРЅСѓСЃ СЂРµР¶РёРјР° <b>+${secretMeta.bonus_pct}%</b><br>
        РќР°РєРѕРїР»РµРЅРЅС‹Р№ Р±РѕРЅСѓСЃ РјРёСЃСЃРёР№: <b>+${secretSummary.bonus_points} РѕС‡</b>
      </div>
      ${secretRows}
      <button onclick="switchTab('secret')" style="width:100%;margin-top:10px;background:rgba(103,255,211,.1);border:1px solid rgba(103,255,211,.28);color:#8bf4d3;padding:9px;font-family:var(--head);font-size:11px;border-radius:6px;cursor:pointer;letter-spacing:.06em">РћРўРљР Р«РўР¬ Р¦Р•РќРўР  РњРРЎРЎРР™</button>
    </div>
    <div style="margin-top:16px">
      <div style="font-family:var(--head);font-size:var(--fs-xs);color:var(--muted);
        letter-spacing:.1em;margin-bottom:8px">// РњРћР Р“Р›РђР’Р«</div>
      ${chapterHistory}
    </div>
    <button onclick="switchTab('chapters')"
      style="width:100%;margin-top:16px;background:var(--accent);color:#0a0a08;
      border:none;padding:12px;font-family:var(--head);font-size:var(--fs-base);
      font-weight:700;border-radius:4px;cursor:pointer;letter-spacing:.08em">
      в–¶ РџР РћР”РћР›Р–РРўР¬ РР“Р РЈ
    </button>
    <div style="text-align:center;margin-top:12px;font-size:10px;color:var(--muted);letter-spacing:.04em">
      рџ’ѕ РџСЂРѕРіСЂРµСЃСЃ СЃРѕС…СЂР°РЅСЏРµС‚СЃСЏ Р°РІС‚РѕРјР°С‚РёС‡РµСЃРєРё
    </div>
    ${(state.adminMode || window._adminMode) ? '<button class="btn-back" onclick="confirmReset()" style="width:100%;margin-top:8px">рџ—‘ РЎР±СЂРѕСЃРёС‚СЊ С‚РµСЃС‚РѕРІС‹Р№ РїСЂРѕРіСЂРµСЃСЃ</button>' : ''}`;
}


// (РЎРёРЅС…СЂРѕРЅРёР·Р°С†РёСЏ РІС‹РїРѕР»РЅСЏРµС‚СЃСЏ Р°РІС‚РѕРјР°С‚РёС‡РµСЃРєРё С‡РµСЂРµР· autoSync)


// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РќРђР’РР“РђР¦РРЇ Р РџРђРЈР—Рђ
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
let pausedFromScreen = null;

function pauseGame() {
  _registerSecretBreakEvent();
  saveState();
  stopTimer();
  autoSync(false);
  pausedFromScreen = 's-cipher';
  hideBottomNav();
  const ch  = CHAPTERS[state.chapter];
  const el  = document.getElementById('pause-chapter-info');
  if (el && ch) {
    el.textContent = ch.subtitle + ' В· ' + ch.title + ' В· Р—Р°РґР°РЅРёРµ ' + (state.cipherIdx + 1) + '/' + ch.ciphers.length;
  }
  showScreen('s-pause');
}

function resumeGame() {
  const target = pausedFromScreen || 's-cipher';
  pausedFromScreen = null;
  hideBottomNav();
  showScreen(target);
  if (target === 's-cipher') startTimer();
}

function goToChapters() {
  unlockAudioInteraction();
  const cipherScreen = document.getElementById('s-cipher');
  if (cipherScreen && cipherScreen.classList.contains('active')) {
    state._chapterInProgress = true;
    _registerSecretBreakEvent();
  }
  stopTimer();
  saveState();
  showScreen('s-chapters');
  renderChapters();
  showBottomNav();
  // РЎРёРЅС…СЂРѕРЅРёР·Р°С†РёСЏ
  autoSync(false, true);
  _sendSyncBeaconIfPossible();
}

function exitToMain() {
  const cipherScreen = document.getElementById('s-cipher');
  if (cipherScreen && cipherScreen.classList.contains('active')) {
    state._chapterInProgress = true;
    _registerSecretBreakEvent();
  }
  saveState();
  autoSync(false, true); // СЃРѕС…СЂР°РЅСЏРµРј РїРµСЂРµРґ РІС‹С…РѕРґРѕРј
  _sendSyncBeaconIfPossible();
  if (tg) {
    setTimeout(() => tg.close(), 300); // РґР°С‘Рј РІСЂРµРјСЏ РЅР° РѕС‚РїСЂР°РІРєСѓ
  } else {
    goToChapters();
  }
}


// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р’РРљРўРћР РРќРђ (QUIZ)
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
let _quizState = { cipher: null, startTime: 0, selected: null, answered: false };

function renderQuiz(cipher) {
  _quizState = { cipher, startTime: Date.now(), selected: null, answered: false };

  // quiz-container вЂ” СѓР¶Рµ СЃРєСЂС‹С‚ РІ loadCipher, РїРѕРєР°Р·С‹РІР°РµРј
  const container = document.getElementById('quiz-container');
  if (!container) return;
  container.style.cssText = 'display:block;width:100%;padding:0 2px;margin-bottom:8px';

  const letters = ['Рђ', 'Р‘', 'Р’', 'Р“', 'Р”'];
  const optionsHtml = cipher.options.map((opt, i) => `
    <div id="quiz-opt-${i}" onclick="quizSelect(${i})"
      style="width:100%;padding:13px 16px;margin-bottom:8px;
      background:rgba(255,255,255,.04);border:1.5px solid rgba(255,255,255,.1);
      border-radius:10px;color:#fdfaf0;font-size:14px;cursor:pointer;
      display:flex;align-items:center;gap:12px;
      transition:all .15s ease;box-sizing:border-box;user-select:none">
      <div id="quiz-letter-${i}" style="flex-shrink:0;width:30px;height:30px;border-radius:50%;
        background:rgba(255,224,51,.08);border:1.5px solid rgba(255,224,51,.2);
        display:flex;align-items:center;justify-content:center;
        font-family:var(--head);font-size:13px;color:var(--accent);
        transition:all .15s">${letters[i]||String.fromCharCode(65+i)}</div>
      <span style="flex:1;line-height:1.5">${opt}</span>
    </div>`).join('');

  container.innerHTML = `
    <div id="quiz-options-list">${optionsHtml}</div>
    <div id="quiz-feedback" style="display:none;padding:10px 14px;border-radius:8px;
      margin-top:4px;font-size:13px;text-align:center;font-family:var(--head);
      letter-spacing:.04em"></div>`;

  // РћР±РЅРѕРІР»СЏРµРј РєРЅРѕРїРєСѓ "РџСЂРѕРІРµСЂРёС‚СЊ" вЂ” РѕРЅР° СЂР°Р±РѕС‚Р°РµС‚ РґР»СЏ quiz С‡РµСЂРµР· checkAnswer()
  // checkAnswer РѕРїСЂРµРґРµР»СЏРµС‚ С‡С‚Рѕ Р·Р°РґР°РЅРёРµ quiz Рё РІС‹Р·С‹РІР°РµС‚ quizSubmit
  const checkBtn = document.getElementById('btn-check') || document.querySelector('.btn-check');
  if (checkBtn) {
    checkBtn.style.display = '';
    checkBtn.textContent = 'вњ“ РџР РћР’Р•Р РРўР¬';
    checkBtn.disabled = false;
  }
}

function quizSelect(idx) {
  if (_quizState.answered) return;
  _quizState.selected = idx;

  // Р’РёР·СѓР°Р»СЊРЅС‹Р№ РІС‹Р±РѕСЂ
  _quizState.cipher.options.forEach((_, i) => {
    const opt = document.getElementById('quiz-opt-' + i);
    const let_ = document.getElementById('quiz-letter-' + i);
    if (!opt) return;
    if (i === idx) {
      opt.style.background    = 'rgba(255,224,51,.12)';
      opt.style.borderColor   = 'rgba(255,224,51,.45)';
      if (let_) { let_.style.background = 'rgba(255,224,51,.25)'; let_.style.borderColor = 'rgba(255,224,51,.6)'; let_.style.color = '#ffe033'; }
    } else {
      opt.style.background    = 'rgba(255,255,255,.04)';
      opt.style.borderColor   = 'rgba(255,255,255,.1)';
      if (let_) { let_.style.background = 'rgba(255,224,51,.08)'; let_.style.borderColor = 'rgba(255,224,51,.2)'; let_.style.color = 'var(--accent)'; }
    }
  });
}

function quizSubmit() {
  if (_quizState.answered) return;
  if (_isActiveCipherResolved()) {
    // Already solved: ignore repeated taps.
    return;
  }
  const chapter = CHAPTERS[state.chapter];
  const fallbackCipher = chapter && Array.isArray(chapter.ciphers) ? chapter.ciphers[state.cipherIdx] : null;
  if (!_quizState.cipher && fallbackCipher && fallbackCipher.type === 'quiz') {
    _quizState.cipher = fallbackCipher;
  }
  const cipher = _quizState.cipher;
  if (!cipher || !Array.isArray(cipher.options) || !Number.isInteger(cipher.correctIndex)) {
    console.warn('quizSubmit: invalid quiz state, forcing reload', _quizState);
    _quizState.answered = false;
    showToast('вљ  РћС€РёР±РєР° РІРёРєС‚РѕСЂРёРЅС‹. Р—Р°РґР°РЅРёРµ Р±СѓРґРµС‚ РїРµСЂРµР·Р°РіСЂСѓР¶РµРЅРѕ');
    setTimeout(() => {
      try { loadCipher(); } catch (e) { console.error('quizSubmit reload failed:', e); }
    }, 80);
    return;
  }
  if (_quizState.selected === null) {
    // РџРѕРґСЃРІРµС‡РёРІР°РµРј С‡С‚Рѕ РЅСѓР¶РЅРѕ РІС‹Р±СЂР°С‚СЊ
    const container = document.getElementById('quiz-container');
    if (container) {
      container.style.animation = 'none';
      container.style.border = '1.5px solid rgba(255,100,50,.4)';
      container.style.borderRadius = '10px';
      setTimeout(() => { container.style.border = ''; }, 800);
    }
    showToast('рџ‘† РЎРЅР°С‡Р°Р»Р° РІС‹Р±РµСЂРёС‚Рµ РІР°СЂРёР°РЅС‚ РѕС‚РІРµС‚Р°');
    return;
  }
  _quizState.answered = true;

  const correct = cipher.correctIndex;
  const sel     = _quizState.selected;
  const elapsed = getEffectiveElapsed(_quizState.startTime);
  const isOk    = sel === correct;

  // РџРѕРґСЃРІРµС‡РёРІР°РµРј СЂРµР·СѓР»СЊС‚Р°С‚
  cipher.options.forEach((_, i) => {
    const opt  = document.getElementById('quiz-opt-' + i);
    const let_ = document.getElementById('quiz-letter-' + i);
    if (!opt) return;
    opt.style.cursor = 'default';
    if (i === correct && isOk) {
      opt.style.background  = 'rgba(40,200,80,.15)';
      opt.style.borderColor = 'rgba(40,200,80,.5)';
      opt.style.color = '#50ee80';
      if (let_) { let_.style.background = 'rgba(40,200,80,.25)'; let_.style.borderColor = 'rgba(40,200,80,.6)'; let_.style.color = '#50ee80'; }
    } else if (i === sel && !isOk) {
      opt.style.background  = 'rgba(255,50,50,.12)';
      opt.style.borderColor = 'rgba(255,50,50,.45)';
      opt.style.color = '#ff7070';
      if (let_) { let_.style.background = 'rgba(255,50,50,.2)'; let_.style.borderColor = 'rgba(255,50,50,.5)'; let_.style.color = '#ff7070'; }
    } else {
      opt.style.opacity = '0.35';
    }
  });

  const feedback = document.getElementById('quiz-feedback');

  if (isOk) {
    _markActiveCipherResolved();
    const checkBtn = document.getElementById('btn-check') || document.querySelector('.btn-check');
    if (checkBtn) checkBtn.disabled = true;
    playSound('correct');
    if (feedback) {
      feedback.style.display = 'block';
      feedback.style.background = 'rgba(40,200,80,.1)';
      feedback.style.border = '1px solid rgba(40,200,80,.3)';
      feedback.style.color = '#50ee80';
      feedback.textContent = 'вњ… Р’РµСЂРЅРѕ!';
    }
    state.streak = (state.streak || 0) + 1;
    _registerSecretAnswerEvent({
      elapsed,
      noHint: Number(state._chapterHints || 0) <= 0,
      oneLife: !state.adminMode && Number(state.lives || 0) === 1,
      cipherType: 'quiz',
      streak: state.streak || 0,
    });
    const pts = calcPoints(cipher, elapsed);
    state.chapterScore += pts;
    saveState();
    autoSync(false, true);
    stopTimer();
    if (elapsed <= 5) state._fastAnswers = (state._fastAnswers || 0) + 1;
    registerSolvedCipherType('quiz');
    try {
      checkAchievements({ elapsed, firstTry: (state._chapterErrors||0)===0, cipherType: 'quiz', quizCorrect: true });
    } catch (achErr) {
      console.warn('quizSubmit: achievements update skipped', achErr);
    }
    setTimeout(() => {
      const qc = document.getElementById('quiz-container');
      if (qc) qc.style.display = 'none';
      const ciw = document.getElementById('cipher-input-wrap');
      if (ciw) ciw.style.display = '';
      showSuccess(cipher, pts, elapsed);
    }, 900);
  } else {
    playSound('wrong');
    screenPulseRed();
    if (feedback) {
      feedback.style.display = 'block';
      feedback.style.background = 'rgba(255,50,50,.1)';
      feedback.style.border = '1px solid rgba(255,50,50,.3)';
      feedback.style.color = '#ff8080';
      feedback.textContent = 'вќЊ РќРµРІРµСЂРЅРѕ вЂ” РІС‹Р±РµСЂРёС‚Рµ РґСЂСѓРіРѕР№ РІР°СЂРёР°РЅС‚';
    }
    state.streak = 0;
    state._chapterErrors = (state._chapterErrors||0) + 1;
    _registerSecretBreakEvent();
    if (!state.adminMode) {
      state.lives--;
      animateLifeLoss();
      screenPulseRed();
      playSound('life_lost');
    }
    saveState();
    autoSync(false, true);
    renderLives();

    if (state.lives <= 0) {
      _handleOutOfLives();
    } else {
      // РЎР±СЂР°СЃС‹РІР°РµРј С‡РµСЂРµР· 1.5СЃ вЂ” СѓР±РёСЂР°РµРј РїРѕРґСЃРІРµС‚РєСѓ, РґР°С‘Рј РІС‹Р±СЂР°С‚СЊ СЃРЅРѕРІР°
      setTimeout(() => {
        _quizState.answered = false;
        _quizState.selected = null;
        cipher.options.forEach((_, i) => {
          const opt2  = document.getElementById('quiz-opt-' + i);
          const let2  = document.getElementById('quiz-letter-' + i);
          if (!opt2) return;
          opt2.style.background  = 'rgba(255,255,255,.04)';
          opt2.style.borderColor = 'rgba(255,255,255,.1)';
          opt2.style.color = '#fdfaf0';
          opt2.style.opacity = '1';
          opt2.style.cursor = 'pointer';
          if (let2) { let2.style.background = 'rgba(255,224,51,.08)'; let2.style.borderColor = 'rgba(255,224,51,.2)'; let2.style.color = 'var(--accent)'; }
        });
        if (feedback) feedback.style.display = 'none';
      }, 1500);
    }
  }
}

function checkQuizAnswer(idx) { quizSelect(idx); }


// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ

// РЈРґР°Р»С‘РЅ РґСѓР±Р»РёСЂСѓСЋС‰РёР№ Р±Р»РѕРє quiz (renderQuiz/quizSubmit), РѕСЃС‚Р°РІР»РµРЅР° РµРґРёРЅР°СЏ РІРµСЂСЃРёСЏ РІС‹С€Рµ.
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РђРќРђР“Р РђРњРњРђ
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
let anagramState = { letters: [], placed: [], indices: [] };

function renderAnagram(cipher) {
  const box = document.getElementById('cipher-box');
  box.style.display = 'none';

  // РџРµСЂРµРјРµС€Р°РЅРЅС‹Рµ Р±СѓРєРІС‹
  const letters = cipher.encrypted.split('');
  anagramState = { letters: [...letters], placed: Array(letters.length).fill(null), usedIdx: Array(letters.length).fill(false) };

  const ref = document.getElementById('cipher-ref');
  ref.innerHTML = `
    <div class="cipher-ref-title">// РЎРћРЎРўРђР’Р¬ РЎР›РћР’Рћ</div>
    <div style="font-size:var(--fs-sm);color:var(--accent);font-weight:700;margin-bottom:10px;text-align:center;letter-spacing:.04em">
      рџ‘† РўР°РїР°Р№ РЅР° Р±СѓРєРІС‹ РЅРёР¶Рµ вЂ” СЃРѕСЃС‚Р°РІР»СЏР№ СЃР»РѕРІРѕ<br>
      <span style="color:var(--muted);font-size:var(--fs-xs);font-weight:400">(РІРІРѕРґ РІСЂСѓС‡РЅСѓСЋ РЅРµ СЂРµРєРѕРјРµРЅРґСѓРµС‚СЃСЏ)</span>
    </div>
    <div class="anagram-answer" id="ag-answer">${
      letters.map((_,i) => `<div class="anagram-slot" id="ag-slot-${i}" onclick="removeFromSlot(${i})"></div>`).join('')
    }</div>
    <div class="anagram-pool" id="ag-pool">${
      letters.map((l,i) => `<div class="anagram-letter" id="ag-letter-${i}" onclick="addToSlot(${i})">${l}</div>`).join('')
    }</div>
    <div style="text-align:center">
      <button class="anagram-reset" onclick="resetAnagram()">в†є РЎР±СЂРѕСЃРёС‚СЊ</button>
    </div>`;
}

function addToSlot(letterIdx) {
  if (anagramState.usedIdx[letterIdx]) return;
  const slotIdx = anagramState.placed.indexOf(null);
  if (slotIdx === -1) return;
  anagramState.placed[slotIdx] = letterIdx;
  anagramState.usedIdx[letterIdx] = true;
  // РћР±РЅРѕРІР»СЏРµРј UI
  document.getElementById(`ag-letter-${letterIdx}`).classList.add('used');
  const slot = document.getElementById(`ag-slot-${slotIdx}`);
  slot.textContent = anagramState.letters[letterIdx];
  slot.classList.add('filled');
  // Р•СЃР»Рё РІСЃРµ СЃР»РѕС‚С‹ Р·Р°РїРѕР»РЅРµРЅС‹ вЂ” Р°РІС‚РѕРїСЂРѕРІРµСЂРєР°
  if (!anagramState.placed.includes(null)) {
    setTimeout(checkAnagram, 300);
  }
}

function removeFromSlot(slotIdx) {
  const letterIdx = anagramState.placed[slotIdx];
  if (letterIdx === null) return;
  anagramState.placed[slotIdx] = null;
  anagramState.usedIdx[letterIdx] = false;
  document.getElementById(`ag-letter-${letterIdx}`).classList.remove('used');
  const slot = document.getElementById(`ag-slot-${slotIdx}`);
  slot.textContent = '';
  slot.classList.remove('filled');
}

function resetAnagram() {
  const cipher = CHAPTERS[state.chapter].ciphers[state.cipherIdx];
  renderAnagram(cipher);
}

async function checkAnagram() {
  if (_isActiveCipherResolved()) {
    // Already solved: ignore repeated taps.
    return;
  }
  const cipher  = CHAPTERS[state.chapter].ciphers[state.cipherIdx];
  const answer  = anagramState.placed.map(i => i !== null ? anagramState.letters[i] : '').join('');
  const elapsed = getEffectiveElapsed(state.startTime);

  // РџРѕРґСЃРІРµС‚РёС‚СЊ СЃР»РѕС‚С‹
  const slots = document.querySelectorAll('.anagram-slot');
  const ansHash = await sha256(answer.toUpperCase());
  if (ansHash === cipher.answer) {
    _markActiveCipherResolved();
    state.streak = (state.streak || 0) + 1;
    _registerSecretAnswerEvent({
      elapsed,
      noHint: Number(state._chapterHints || 0) <= 0,
      oneLife: !state.adminMode && Number(state.lives || 0) === 1,
      cipherType: 'anagram',
      streak: state.streak || 0,
    });
    playSound('correct');
    stopTimer();
    slots.forEach(s => { s.style.borderColor = '#55dd55'; s.style.background = 'rgba(85,221,85,.15)'; });
    const pts = calcPoints(cipher, elapsed);
    state.chapterScore += pts;
    if (elapsed <= 5) state._fastAnswers = (state._fastAnswers || 0) + 1;
    registerSolvedCipherType('anagram');
    try {
      checkAchievements({
        elapsed,
        firstTry: (state._chapterErrors || 0) === 0,
        cipherType: 'anagram',
        anagramCorrect: true
      });
    } catch (achErr) {
      console.warn('checkAnagram: achievements update skipped', achErr);
    }
    saveState();
    autoSync(false, true);
    setTimeout(() => showSuccess(cipher, pts, elapsed), 600);
  } else {
    state.streak = 0;
    state._chapterErrors = (state._chapterErrors || 0) + 1;
    _registerSecretBreakEvent();
    slots.forEach(s => { s.style.borderColor = '#ff3a3a'; s.style.background = 'rgba(255,58,58,.1)'; });
    setTimeout(() => slots.forEach(s => { s.style.borderColor = ''; s.style.background = ''; }), 800);
    screenPulseRed();
    if (!state.adminMode) {
      state.lives--;
      animateLifeLoss();
      playSound('life_lost');
    } else {
      playSound('wrong');
    }
    saveState();
    autoSync(false, true);
    if (state.lives <= 0) {
      _handleOutOfLives();
    } else { renderLives(); setTimeout(resetAnagram, 900); }
  }
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РљРђР РўРђ Р‘Р•Р›РђР РЈРЎР
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
const MAP_CITIES = [
  { name:'РњРРќРЎРљ',    x:188, y:175, hint:'РЎС‚РѕР»РёС†Р°' },
  { name:'Р‘Р Р•РЎРў',    x:62,  y:268, hint:'Р®РіРѕ-Р·Р°РїР°Рґ' },
  { name:'Р“Р РћР”РќРћ',   x:75,  y:148, hint:'РЎРµРІРµСЂРѕ-Р·Р°РїР°Рґ' },
  { name:'Р’РРўР•Р‘РЎРљ',  x:255, y:82,  hint:'РЎРµРІРµСЂ' },
  { name:'РњРћР“РР›РЃР’',  x:268, y:198, hint:'Р’РѕСЃС‚РѕРє' },
  { name:'Р“РћРњР•Р›Р¬',   x:278, y:298, hint:'Р®РіРѕ-РІРѕСЃС‚РѕРє' },
  { name:'РќРђР РћР’Р›РЇ',  x:245, y:300, hint:'Р®РіРѕ-РІРѕСЃС‚РѕРє, РџСЂРёРїСЏС‚СЊ' },
  { name:'РџРћР›РћР¦Рљ',   x:215, y:72,  hint:'РЎРµРІРµСЂ' },
  { name:'Р‘РђР РђРќРћР’РР§Р',x:130, y:212, hint:'Р¦РµРЅС‚СЂ-Р·Р°РїР°Рґ' },
  { name:'РџРРќРЎРљ',    x:110, y:285, hint:'Р®Рі' },
  { name:'Р‘РћР‘Р РЈР™РЎРљ', x:228, y:240, hint:'Р¦РµРЅС‚СЂ' },
];

let mapAnswered = false;

function renderMap(cipher) {
  mapAnswered = false;
  const target = cipher.mapCity;
  const ref = document.getElementById('cipher-ref');

  const dots = MAP_CITIES.map(c => {
    const isTarget = false; // РЅРµ РїРѕРєР°Р·С‹РІР°РµРј РїСЂР°РІРёР»СЊРЅС‹Р№ Р·Р°СЂР°РЅРµРµ
    return `<g class="map-city-dot" id="mcdot-${c.name}" onclick="checkMapAnswer('${c.name}')">
      <circle cx="${c.x}" cy="${c.y}" r="6" fill="#ffe033" opacity=".7" stroke="#0a0a08" stroke-width="1"/>
      <text class="map-city-label" x="${c.x + 9}" y="${c.y + 4}">${c.name}</text>
    </g>`;
  }).join('');

  ref.innerHTML = `
    <div class="cipher-ref-title">// РљРђР РўРђ Р‘Р•Р›РђР РЈРЎР вЂ” С‚Р°РїРЅРё РїРѕ РЅСѓР¶РЅРѕРјСѓ РіРѕСЂРѕРґСѓ</div>
    <div class="map-container">
      <svg viewBox="0 0 360 370" xmlns="http://www.w3.org/2000/svg">
        
        <path d="M80,20 L100,15 L130,18 L160,12 L195,18 L230,14 L265,22 L290,35 L310,55 L318,80
                 L322,105 L315,130 L320,155 L315,175 L322,200 L318,225 L308,248 L295,268
                 L275,285 L255,298 L230,308 L205,318 L175,322 L148,318 L120,310 L95,298
                 L72,282 L55,262 L42,238 L35,212 L30,185 L28,158 L32,132 L28,105
                 L35,80 L48,58 L65,40 Z"
              fill="#1a1a0c" stroke="#3a3a20" stroke-width="1.5"/>
        
        <path d="M188,175 L200,140 L215,100 L225,70" stroke="#1a3a5a" stroke-width="2" fill="none" opacity=".5"/>
        <path d="M188,175 L170,200 L145,230 L120,258 L95,275" stroke="#1a3a5a" stroke-width="1.5" fill="none" opacity=".4"/>
        
        ${dots}
      </svg>
    </div>
    <div class="map-hint-text" id="map-hint-text">РўР°РїРЅРё РїРѕ РіРѕСЂРѕРґСѓ РЅР° РєР°СЂС‚Рµ</div>`;
}

async function checkMapAnswer(clicked, target) {
  if (mapAnswered) return;
  if (_isActiveCipherResolved()) {
    // Already solved: ignore repeated taps.
    return;
  }
  const elapsed = getEffectiveElapsed(state.startTime);
  const cipher  = CHAPTERS[state.chapter].ciphers[state.cipherIdx];

  const dotEl = document.getElementById(`mcdot-${clicked}`);

  const clickedHash = await sha256(clicked);
  if (clickedHash === cipher.answer) {
    mapAnswered = true;
    _markActiveCipherResolved();
    state.streak = (state.streak || 0) + 1;
    _registerSecretAnswerEvent({
      elapsed,
      noHint: Number(state._chapterHints || 0) <= 0,
      oneLife: !state.adminMode && Number(state.lives || 0) === 1,
      cipherType: 'map',
      streak: state.streak || 0,
    });
    playSound('correct');
    stopTimer();
    if (dotEl) dotEl.querySelector('circle').setAttribute('fill','#55dd55');
    document.getElementById('map-hint-text').textContent = 'вњ… Р’РµСЂРЅРѕ! ' + (cipher.mapCity || clicked);
    const pts = calcPoints(cipher, elapsed);
    state.chapterScore += pts;
    if (elapsed <= 5) state._fastAnswers = (state._fastAnswers || 0) + 1;
    registerSolvedCipherType('map');
    try {
      checkAchievements({
        elapsed,
        firstTry: (state._chapterErrors || 0) === 0,
        cipherType: 'map',
        mapCorrect: true
      });
    } catch (achErr) {
      console.warn('checkMapAnswer: achievements update skipped', achErr);
    }
    saveState();
    autoSync(false, true);
    setTimeout(() => showSuccess(cipher, pts, elapsed), 800);
  } else {
    state.streak = 0;
    state._chapterErrors = (state._chapterErrors || 0) + 1;
    _registerSecretBreakEvent();
    if (dotEl) {
      const c = dotEl.querySelector('circle');
      c.setAttribute('fill','#ff3a3a');
      setTimeout(() => c.setAttribute('fill','#ffe033'), 700);
    }
    document.getElementById('map-hint-text').textContent = 'вќЊ РќРµ Р·РґРµСЃСЊ. РџРѕРїСЂРѕР±СѓР№ РµС‰С‘ СЂР°Р·.';
    screenPulseRed();
    if (!state.adminMode) {
      state.lives--;
      animateLifeLoss();
      playSound('life_lost');
    } else {
      playSound('wrong');
    }
    saveState();
    autoSync(false, true);
    if (state.lives <= 0) {
      _handleOutOfLives();
    } else { renderLives(); }
  }
}


// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р—Р’РЈРљРћР’Р«Р• Р­Р¤Р¤Р•РљРўР« (Web Audio API вЂ” Р±РµР· С„Р°Р№Р»РѕРІ)
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
let _audioCtx = null;
let _bgMusic = null;
let _audioUnlocked = false;
const AUDIO_PREF_KEY = 'cipher_audio_prefs_v2';
const LEGACY_MUSIC_PREF_KEY = 'cipher_music_enabled_v1';
let musicEnabled = true;
let sfxEnabled = true;
let musicVolume = 0.45;
let sfxVolume = 0.8;
let musicTrackIndex = 0;
const _brokenMusicTrackIds = new Set();
let _musicFailoverInProgress = false;
let _musicValidationStarted = false;

const GAME_MUSIC_TRACKS = [
  {
    id: 'epic_boss_battle',
    title: 'Epic Boss Battle (CC0)',
    url: 'https://opengameart.org/sites/default/files/Juhani%20Junkala%20-%20Epic%20Boss%20Battle%20%5BSeamlessly%20Looping%5D.wav'
  },
  {
    id: 'determination',
    title: 'Determination (CC0)',
    url: 'https://opengameart.org/sites/default/files/determination.mp3'
  },
  {
    id: 'prepare_your_swords',
    title: 'Prepare Your Swords (CC0)',
    url: 'https://opengameart.org/sites/default/files/prepare_your_swords.mp3'
  },
  {
    id: 'adventuring_song',
    title: 'Adventuring Song (CC0)',
    url: 'https://opengameart.org/sites/default/files/adventuring_song.mp3'
  },
  {
    id: 'town_theme',
    title: 'Town Theme (CC0)',
    url: 'https://opengameart.org/sites/default/files/TownTheme.mp3'
  }
];

function _clamp(v, min, max) {
  return Math.max(min, Math.min(max, v));
}

function _normVolume(raw, fallback) {
  const n = Number(raw);
  if (!Number.isFinite(n)) return fallback;
  const normalized = n > 1 ? (n / 100) : n;
  return _clamp(normalized, 0, 1);
}

function getCurrentMusicTrack() {
  return GAME_MUSIC_TRACKS[musicTrackIndex] || GAME_MUSIC_TRACKS[0];
}

function getNextWorkingTrackIndex(startIdx = 0) {
  const total = GAME_MUSIC_TRACKS.length;
  if (!total) return -1;
  for (let step = 1; step <= total; step++) {
    const idx = (startIdx + step) % total;
    const track = GAME_MUSIC_TRACKS[idx];
    if (track && !_brokenMusicTrackIds.has(track.id)) return idx;
  }
  return -1;
}

function onMusicTrackFailed(trackId) {
  if (_musicFailoverInProgress) return;
  _musicFailoverInProgress = true;
  try {
    if (trackId) _brokenMusicTrackIds.add(trackId);
    const nextIdx = getNextWorkingTrackIndex(musicTrackIndex);
    if (nextIdx === -1) {
      musicEnabled = false;
      saveAudioPreferences();
      syncMusicButtons();
      const audio = ensureBackgroundMusic();
      if (audio) audio.pause();
      showToast('рџ”‡ РњСѓР·С‹РєР° РЅРµРґРѕСЃС‚СѓРїРЅР°: С‚СЂРµРєРё РЅРµ Р·Р°РіСЂСѓР·РёР»РёСЃСЊ');
      return;
    }
    musicTrackIndex = nextIdx;
    saveAudioPreferences();
    syncMusicButtons();
    const track = getCurrentMusicTrack();
    if (track) showToast('рџЋј РџРµСЂРµРєР»СЋС‡РµРЅРѕ: ' + track.title);
    applyBackgroundMusic(true);
  } finally {
    _musicFailoverInProgress = false;
  }
}

function probeMusicTrack(url, timeoutMs = 7000) {
  return new Promise(resolve => {
    let done = false;
    const audio = new Audio();
    const cleanup = () => {
      audio.oncanplaythrough = null;
      audio.onloadedmetadata = null;
      audio.onerror = null;
      try {
        audio.pause();
        audio.removeAttribute('src');
        audio.load();
      } catch (e) {}
    };
    const finish = (ok) => {
      if (done) return;
      done = true;
      clearTimeout(timer);
      cleanup();
      resolve(ok);
    };
    const timer = setTimeout(() => finish(false), timeoutMs);
    audio.preload = 'auto';
    audio.oncanplaythrough = () => finish(true);
    audio.onloadedmetadata = () => finish(true);
    audio.onerror = () => finish(false);
    try {
      audio.src = url;
      audio.load();
    } catch (e) {
      finish(false);
    }
  });
}

async function validateMusicTracksInBackground() {
  if (_musicValidationStarted) return;
  _musicValidationStarted = true;
  for (const track of GAME_MUSIC_TRACKS) {
    const ok = await probeMusicTrack(track.url, 7000);
    if (!ok) _brokenMusicTrackIds.add(track.id);
  }
  const current = getCurrentMusicTrack();
  if (current && _brokenMusicTrackIds.has(current.id)) {
    onMusicTrackFailed(current.id);
  }
}

function loadAudioPreferences() {
  try {
    const raw = localStorage.getItem(AUDIO_PREF_KEY);
    if (raw) {
      const parsed = JSON.parse(raw);
      musicEnabled = parsed.musicEnabled !== false;
      sfxEnabled = parsed.sfxEnabled !== false;
      musicVolume = _normVolume(parsed.musicVolume, 0.45);
      sfxVolume = _normVolume(parsed.sfxVolume, 0.8);
      const idx = Number(parsed.musicTrackIndex);
      musicTrackIndex = Number.isInteger(idx) ? _clamp(idx, 0, GAME_MUSIC_TRACKS.length - 1) : 0;
      return;
    }
    const old = localStorage.getItem(LEGACY_MUSIC_PREF_KEY);
    if (old !== null) musicEnabled = old === '1';
  } catch (e) {}
}

function saveAudioPreferences() {
  try {
    localStorage.setItem(AUDIO_PREF_KEY, JSON.stringify({
      musicEnabled,
      sfxEnabled,
      musicVolume,
      sfxVolume,
      musicTrackIndex
    }));
    localStorage.setItem(LEGACY_MUSIC_PREF_KEY, musicEnabled ? '1' : '0');
  } catch (e) {}
}

function ensureBackgroundMusic() {
  if (_bgMusic) return _bgMusic;
  try {
    _bgMusic = new Audio();
    _bgMusic.loop = true;
    _bgMusic.preload = 'auto';
    _bgMusic.addEventListener('error', () => {
      const failedId = _bgMusic && _bgMusic.dataset ? _bgMusic.dataset.trackId : '';
      console.warn('background music load error', failedId, _bgMusic && _bgMusic.error);
      onMusicTrackFailed(failedId);
    });
  } catch (e) {
    _bgMusic = null;
  }
  return _bgMusic;
}

function applyBackgroundMusic(forceTrackReload = false) {
  const audio = ensureBackgroundMusic();
  if (!audio) return;
  let track = getCurrentMusicTrack();
  if (!track) return;
  if (_brokenMusicTrackIds.has(track.id)) {
    const nextIdx = getNextWorkingTrackIndex(musicTrackIndex);
    if (nextIdx !== -1 && nextIdx !== musicTrackIndex) {
      musicTrackIndex = nextIdx;
      saveAudioPreferences();
      track = getCurrentMusicTrack();
    }
  }
  if (!track || _brokenMusicTrackIds.has(track.id)) {
    audio.pause();
    return;
  }

  if (forceTrackReload || audio.dataset.trackId !== track.id) {
    audio.src = track.url;
    audio.dataset.trackId = track.id;
  }

  const riskBoost = 1 + (_riskIntensity * 0.18);
  audio.volume = _clamp(musicVolume * riskBoost, 0, 1);
  if (!musicEnabled || !_audioUnlocked || document.hidden) {
    audio.pause();
    return;
  }
  const p = audio.play();
  if (p && typeof p.catch === 'function') p.catch(() => {});
}

function unlockAudioInteraction() {
  _audioUnlocked = true;
  const ctx = getAudio();
  if (ctx && ctx.state === 'suspended') {
    try { ctx.resume(); } catch (e) {}
  }
  void validateMusicTracksInBackground();
  applyBackgroundMusic(false);
}

function syncMusicButtons() {
  const btn = document.getElementById('btn-toggle-music');
  if (!btn) return;
  const track = getCurrentMusicTrack();
  btn.textContent = musicEnabled ? 'рџЋµ РњРЈР—Р«РљРђ: ON' : 'рџ”‡ РњРЈР—Р«РљРђ: OFF';
  btn.style.opacity = musicEnabled ? '1' : '0.7';
  if (track) btn.title = 'РўСЂРµРє: ' + track.title;
}

function toggleMusicEnabled() {
  musicEnabled = !musicEnabled;
  if (musicEnabled) _audioUnlocked = true;
  saveAudioPreferences();
  syncMusicButtons();
  applyBackgroundMusic(false);
  showToast(musicEnabled ? 'рџЋµ РњСѓР·С‹РєР° РІРєР»СЋС‡РµРЅР°' : 'рџ”‡ РњСѓР·С‹РєР° РІС‹РєР»СЋС‡РµРЅР°');
}

function toggleSfxEnabled() {
  sfxEnabled = !sfxEnabled;
  saveAudioPreferences();
  showToast(sfxEnabled ? 'рџ”Љ Р­С„С„РµРєС‚С‹ РІРєР»СЋС‡РµРЅС‹' : 'рџ”‡ Р­С„С„РµРєС‚С‹ РІС‹РєР»СЋС‡РµРЅС‹');
}

function setMusicTrack(indexLike) {
  const idx = Number(indexLike);
  if (!Number.isInteger(idx) || idx < 0 || idx >= GAME_MUSIC_TRACKS.length) return;
  musicTrackIndex = idx;
  // Р’С‹Р±РѕСЂ С‚СЂРµРєР° РІ РЅР°СЃС‚СЂРѕР№РєР°С… вЂ” СЏРІРЅРѕРµ РґРµР№СЃС‚РІРёРµ РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ.
  // РЎСЂР°Р·Сѓ СЂР°Р·СЂРµС€Р°РµРј Рё Р·Р°РїСѓСЃРєР°РµРј РјСѓР·С‹РєСѓ, РЅРµ РґРѕР¶РёРґР°СЏСЃСЊ СЃС‚Р°СЂС‚Р° РіР»Р°РІС‹.
  _audioUnlocked = true;
  if (!musicEnabled) musicEnabled = true;
  const ctx = getAudio();
  if (ctx && ctx.state === 'suspended') {
    try { ctx.resume(); } catch (e) {}
  }
  const selectedTrack = GAME_MUSIC_TRACKS[idx];
  if (selectedTrack) _brokenMusicTrackIds.delete(selectedTrack.id);
  saveAudioPreferences();
  applyBackgroundMusic(true);
  syncMusicButtons();
  const track = getCurrentMusicTrack();
  if (track) showToast('рџЋј РўСЂРµРє: ' + track.title);
}

function setMusicVolume(valueLike) {
  const n = Number(valueLike);
  if (!Number.isFinite(n)) return;
  const normalized = n > 1 ? (n / 100) : n;
  musicVolume = _clamp(normalized, 0, 1);
  if (musicVolume > 0 && !musicEnabled) musicEnabled = true;
  unlockAudioInteraction();
  saveAudioPreferences();
  syncMusicButtons();
  applyBackgroundMusic(false);
  const slider = document.getElementById('music-volume-range');
  if (slider) slider.value = String(Math.round(musicVolume * 100));
  const v = document.getElementById('music-vol-value');
  if (v) v.textContent = Math.round(musicVolume * 100) + '%';
}

function setSfxVolume(valueLike) {
  const n = Number(valueLike);
  if (!Number.isFinite(n)) return;
  const normalized = n > 1 ? (n / 100) : n;
  sfxVolume = _clamp(normalized, 0, 1);
  if (sfxVolume > 0 && !sfxEnabled) sfxEnabled = true;
  unlockAudioInteraction();
  saveAudioPreferences();
  const slider = document.getElementById('sfx-volume-range');
  if (slider) slider.value = String(Math.round(sfxVolume * 100));
  try { playSound('hint'); } catch (e) {}
  const v = document.getElementById('sfx-vol-value');
  if (v) v.textContent = Math.round(sfxVolume * 100) + '%';
}

function setSecretMode(modeLike) {
  ensureSecretState();
  const nextMode = normalizeSecretMode(modeLike);
  const prevMode = normalizeSecretMode(state.secretMode);
  if (prevMode === nextMode) return;

  state.secretMode = nextMode;
  _registerSecretBreakEvent();
  saveState();

  const meta = _secretGetModeMeta(nextMode);
  if (nextMode === 'none') {
    showPenaltyNotice('РўРёС…РёР№ СЂРµР¶РёРј РІС‹РєР»СЋС‡РµРЅ. Р‘РѕРЅСѓСЃС‹ С‚Р°Р№РЅС‹С… РјРёСЃСЃРёР№ РЅРµ РЅР°С‡РёСЃР»СЏСЋС‚СЃСЏ.', 5200);
  } else {
    showPenaltyNotice(
      `РђРєС‚РёРІРёСЂРѕРІР°РЅ СЂРµР¶РёРј В«${meta.title}В». Р’С‹РїРѕР»РЅРё СѓСЃР»РѕРІРёРµ Рё РїРѕР»СѓС‡Рё Р±РѕРЅСѓСЃ +${meta.bonus_pct}% Рє РѕС‡РєР°Рј РјРёСЃСЃРёРё.`,
      6200
    );
  }

  autoSync(false, true);
  if (currentTab === 'settings') renderSettingsTab();
  if (currentTab === 'secret') renderSecretMissionsTab();
}

loadAudioPreferences();
function getAudio() {
  if (!_audioCtx) {
    try { _audioCtx = new (window.AudioContext || window.webkitAudioContext)(); } catch(e) {}
  }
  return _audioCtx;
}

function playSound(type) {
  if (!sfxEnabled || sfxVolume <= 0) return;
  const ctx = getAudio();
  if (!ctx) return;
  if (ctx.state === 'suspended') {
    try { ctx.resume(); } catch (e) {}
  }
  const level = (base) => Math.max(0.0001, base * sfxVolume * (1 + _riskIntensity * 0.35));
  try {
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();
    osc.connect(gain);
    gain.connect(ctx.destination);
    const now = ctx.currentTime;

    if (type === 'correct') {
      osc.type = 'sine';
      osc.frequency.setValueAtTime(523, now);
      osc.frequency.setValueAtTime(784, now + 0.1);
      gain.gain.setValueAtTime(level(0.3), now);
      gain.gain.exponentialRampToValueAtTime(0.001, now + 0.4);
      osc.start(now); osc.stop(now + 0.4);
    } else if (type === 'wrong') {
      osc.type = 'sawtooth';
      osc.frequency.setValueAtTime(150, now);
      osc.frequency.setValueAtTime(100, now + 0.15);
      gain.gain.setValueAtTime(level(0.2), now);
      gain.gain.exponentialRampToValueAtTime(0.001, now + 0.3);
      osc.start(now); osc.stop(now + 0.3);
    } else if (type === 'chapter_win') {
      const freqs = [523, 659, 784, 1047];
      freqs.forEach((f, i) => {
        const o = ctx.createOscillator();
        const g = ctx.createGain();
        o.connect(g); g.connect(ctx.destination);
        o.type = 'sine';
        o.frequency.setValueAtTime(f, now + i * 0.1);
        g.gain.setValueAtTime(level(0.25), now + i * 0.1);
        g.gain.exponentialRampToValueAtTime(0.001, now + i * 0.1 + 0.3);
        o.start(now + i * 0.1); o.stop(now + i * 0.1 + 0.3);
      });
    } else if (type === 'life_lost') {
      osc.type = 'square';
      osc.frequency.setValueAtTime(200, now);
      osc.frequency.exponentialRampToValueAtTime(80, now + 0.3);
      gain.gain.setValueAtTime(level(0.2), now);
      gain.gain.exponentialRampToValueAtTime(0.001, now + 0.3);
      osc.start(now); osc.stop(now + 0.3);
    } else if (type === 'hint') {
      osc.type = 'sine';
      osc.frequency.setValueAtTime(440, now);
      gain.gain.setValueAtTime(level(0.15), now);
      gain.gain.exponentialRampToValueAtTime(0.001, now + 0.2);
      osc.start(now); osc.stop(now + 0.2);
    } else if (type === 'game_win') {
      [523,659,784,880,1047].forEach((f, i) => {
        const o = ctx.createOscillator();
        const g = ctx.createGain();
        o.connect(g); g.connect(ctx.destination);
        o.type = i % 2 === 0 ? 'sine' : 'triangle';
        o.frequency.setValueAtTime(f, now + i * 0.08);
        g.gain.setValueAtTime(level(0.2), now + i * 0.08);
        g.gain.exponentialRampToValueAtTime(0.001, now + 1.5);
        o.start(now + i * 0.08); o.stop(now + 1.5);
      });
    } else if (type === 'game_lose') {
      osc.type = 'triangle';
      osc.frequency.setValueAtTime(294, now);
      osc.frequency.linearRampToValueAtTime(196, now + 0.2);
      osc.frequency.linearRampToValueAtTime(131, now + 0.45);
      gain.gain.setValueAtTime(level(0.25), now);
      gain.gain.exponentialRampToValueAtTime(0.001, now + 0.55);
      osc.start(now); osc.stop(now + 0.55);
    }
  } catch(e) {}
}


// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РљРћРќР¤Р•РўРўР (СѓР»СѓС‡С€РµРЅРЅРѕРµ)
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
document.addEventListener('pointerdown', unlockAudioInteraction, { once: true, passive: true });
document.addEventListener('keydown', unlockAudioInteraction, { once: true });
document.addEventListener('visibilitychange', () => applyBackgroundMusic(false));
applyBackgroundMusic(true);

function launchConfetti(duration = 800, intensity = 20) {
  // Р’СЃРїС‹С€РєРё РїРѕ РєСЂР°СЏРј СЌРєСЂР°РЅР° РІРјРµСЃС‚Рѕ РєРѕРЅС„РµС‚С‚Рё СЃРІРµСЂС…Сѓ
  const colors = ['#ffe033','#ff4455','#55ee66','#4488ff','#ff88ff'];
  const positions = [
    { left: '0', top: '20%' },
    { left: '0', top: '50%' },
    { right: '0', top: '20%' },
    { right: '0', top: '50%' },
  ];
  positions.forEach((pos, idx) => {
    setTimeout(() => {
      const flash = document.createElement('div');
      const color = colors[Math.floor(Math.random() * colors.length)];
      flash.style.cssText = `position:fixed;${Object.entries(pos).map(([k,v])=>k+':'+v).join(';')};
        width:60px;height:60px;border-radius:50%;pointer-events:none;z-index:9995;
        background:radial-gradient(circle,${color}cc 0%,${color}00 70%);
        animation:flashPulse .6s ease-out forwards`;
      document.body.appendChild(flash);
      setTimeout(() => flash.remove(), 700);
    }, idx * 80);
  });
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  Р­РљР РђРќ Р—РђР“Р РЈР—РљР
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function showSplash() {
  const splash = document.createElement('div');
  splash.id = 'splash-screen';
  splash.style.cssText = `position:fixed;inset:0;z-index:9000;background:#0a0a08;
    display:flex;flex-direction:column;align-items:center;justify-content:center;
    transition:opacity .5s ease`;

  splash.innerHTML = `
    <div style="font-size:72px;margin-bottom:16px;animation:popIn .6s ease both">рџ”ђ</div>
    <div style="font-family:var(--head);font-size:var(--fs-hero);color:var(--accent);
      letter-spacing:.06em;text-align:center;line-height:.9;
      text-shadow:0 0 40px rgba(255,224,51,.6)">РЁРР¤Р Рћ<br>Р’РђР›Р¬Р©РРљ</div>
    <div style="margin-top:24px;width:200px;height:3px;background:rgba(255,255,255,.1);border-radius:2px;overflow:hidden">
      <div id="splash-bar" style="height:100%;width:0%;background:var(--accent);border-radius:2px;
        transition:width .05s linear;box-shadow:0 0 8px var(--accent)"></div>
    </div>
    <div style="margin-top:10px;font-size:var(--fs-xs);color:var(--muted);letter-spacing:.1em">Р—РђР“Р РЈР—РљРђ...</div>
  `;
  document.body.appendChild(splash);

  // РђРЅРёРјРёСЂСѓРµРј РїСЂРѕРіСЂРµСЃСЃ-Р±Р°СЂ
  let pct = 0;
  const bar = splash.querySelector('#splash-bar');
  const interval = setInterval(() => {
    pct += Math.random() * 15;
    if (pct >= 100) { pct = 100; clearInterval(interval); }
    bar.style.width = pct + '%';
    if (pct >= 100) {
      setTimeout(() => {
        splash.style.opacity = '0';
        splash.style.pointerEvents = 'none'; // РЅРµРјРµРґР»РµРЅРЅРѕ СЂР°Р·СЂРµС€Р°РµРј РєР»РёРєРё РїРѕРґ СЃРїР»СЌС€РµРј
        setTimeout(() => { try { splash.remove(); } catch(_){} }, 500);
      }, 200);
    }
  }, 80);
  // Р“Р°СЂР°РЅС‚РёСЂРѕРІР°РЅРЅРѕРµ СѓРґР°Р»РµРЅРёРµ СЃРїР»СЌС€Р° С‡РµСЂРµР· 3 СЃРµРєСѓРЅРґС‹ (Р·Р°С‰РёС‚Р° РѕС‚ Р·Р°РІРёСЃР°РЅРёСЏ)
  setTimeout(() => {
    clearInterval(interval);
    try {
      splash.style.opacity = '0';
      splash.style.pointerEvents = 'none';
      setTimeout(() => { try { splash.remove(); } catch(_){} }, 500);
    } catch(_){}
  }, 3000);
}

// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РџРЈР›Р¬РЎРђР¦РРЇ Р­РљР РђРќРђ РџР Р РџРћРўР•Р Р• Р–РР—РќР
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
function screenPulseRed() {
  const overlay = document.createElement('div');
  overlay.style.cssText = `position:fixed;inset:0;z-index:9998;pointer-events:none;
    background:rgba(255,50,50,0);transition:background .1s ease`;
  document.body.appendChild(overlay);
  requestAnimationFrame(() => {
    overlay.style.background = 'rgba(255,50,50,0.18)';
    setTimeout(() => {
      overlay.style.background = 'rgba(255,50,50,0)';
      setTimeout(() => overlay.remove(), 200);
    }, 150);
  });
}


// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  РЎРРЎРўР•РњРђ Р”РћРЎРўРР–Р•РќРР™ (52 С€С‚СѓРєРё)
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
const ACHIEVEMENTS = [
  { id:'fast_5', icon:'вљЎ', name:'РњРѕР»РЅРёСЏ', desc:'Р”Р°Р№ РїСЂР°РІРёР»СЊРЅС‹Р№ РѕС‚РІРµС‚ Р·Р° 5 СЃРµРєСѓРЅРґ', pts:10 },
  { id:'fast_10', icon:'рџљЂ', name:'РЈСЃРєРѕСЂРµРЅРёРµ', desc:'5 Р±С‹СЃС‚СЂС‹С… РѕС‚РІРµС‚РѕРІ (<=10 СЃРµРє)', pts:25 },
  { id:'fast_25', icon:'рџЊЄпёЏ', name:'РЁС‚СѓСЂРј', desc:'25 Р±С‹СЃС‚СЂС‹С… РѕС‚РІРµС‚РѕРІ (<=10 СЃРµРє)', pts:60 },
  { id:'streak_3', icon:'рџ”Ґ', name:'РќР° СЃРµСЂРёРё', desc:'РЎРµСЂРёСЏ РёР· 3 РїСЂР°РІРёР»СЊРЅС‹С… РѕС‚РІРµС‚РѕРІ', pts:15 },
  { id:'streak_5', icon:'рџ’Ґ', name:'Р‘РµР· РїСЂРѕРјР°С…Р°', desc:'РЎРµСЂРёСЏ РёР· 5 РїСЂР°РІРёР»СЊРЅС‹С… РѕС‚РІРµС‚РѕРІ', pts:30 },
  { id:'streak_8', icon:'в„пёЏ', name:'РћРіРЅРµРЅРЅС‹Р№ С‚РµРјРї', desc:'РЎРµСЂРёСЏ РёР· 8 РїСЂР°РІРёР»СЊРЅС‹С… РѕС‚РІРµС‚РѕРІ', pts:55 },
  { id:'chapter_1', icon:'рџЏ…', name:'РџРµСЂРІС‹Р№ СЂСѓР±РµР¶', desc:'Р—Р°РІРµСЂС€Рё 1 РіР»Р°РІСѓ', pts:20 },
  { id:'chapter_3', icon:'рџҐ€', name:'РџРѕР»РїСѓС‚Рё', desc:'Р—Р°РІРµСЂС€Рё 3 РіР»Р°РІС‹', pts:45 },
  { id:'chapter_6', icon:'рџҐ‡', name:'РћРїРµСЂР°С†РёСЏ Р·Р°РІРµСЂС€РµРЅР°', desc:'Р—Р°РІРµСЂС€Рё РІСЃРµ 6 РіР»Р°РІ', pts:120 },
  { id:'flawless_1', icon:'рџЋЇ', name:'РЎРЅР°Р№РїРµСЂ', desc:'Р—Р°РІРµСЂС€Рё РіР»Р°РІСѓ Р±РµР· РѕС€РёР±РѕРє', pts:40 },
  { id:'flawless_3', icon:'рџ’Ћ', name:'РҐРѕР»РѕРґРЅС‹Р№ СЂР°СЃС‡С‘С‚', desc:'3 РіР»Р°РІС‹ Р±РµР· РѕС€РёР±РѕРє', pts:95 },
  { id:'no_hints_1', icon:'рџ§ ', name:'РђРЅР°Р»РёС‚РёРє', desc:'Р—Р°РІРµСЂС€Рё РіР»Р°РІСѓ Р±РµР· РїРѕРґСЃРєР°Р·РѕРє', pts:30 },
  { id:'no_hints_3', icon:'рџЋ“', name:'РњР°СЃС‚РµСЂ С€РёС„СЂР°', desc:'3 РіР»Р°РІС‹ Р±РµР· РїРѕРґСЃРєР°Р·РѕРє', pts:70 },
  { id:'score_500', icon:'в­ђ', name:'РџРµСЂРІС‹Рµ РѕС‡РєРё', desc:'РќР°Р±РµСЂРё 500 РѕС‡РєРѕРІ', pts:10 },
  { id:'score_1200', icon:'рџЊџ', name:'РљСЂРµРїРєРёР№ СЂРµР·СѓР»СЊС‚Р°С‚', desc:'РќР°Р±РµСЂРё 1200 РѕС‡РєРѕРІ', pts:25 },
  { id:'score_2500', icon:'рџЏ†', name:'Р’ СЌР»РёС‚Рµ', desc:'РќР°Р±РµСЂРё 2500 РѕС‡РєРѕРІ', pts:55 },
  { id:'score_4000', icon:'рџ‘‘', name:'Р›РµРіРµРЅРґР° С€С‚Р°Р±Р°', desc:'РќР°Р±РµСЂРё 4000 РѕС‡РєРѕРІ', pts:90 },
  { id:'type_morse', icon:'рџ“Ў', name:'Р Р°РґРёСЃС‚', desc:'Р РµС€Рё 3 Р·Р°РґР°РЅРёСЏ РњРѕСЂР·Рµ', pts:20 },
  { id:'type_math', icon:'вћ—', name:'РЁС‚Р°Р±РЅРѕР№ РјР°С‚РµРјР°С‚РёРє', desc:'Р РµС€Рё 3 РјР°С‚РµРјР°С‚РёС‡РµСЃРєРёС… Р·Р°РґР°РЅРёСЏ', pts:20 },
  { id:'type_map', icon:'рџ—єпёЏ', name:'РќР°РІРёРіР°С‚РѕСЂ', desc:'Р РµС€Рё 2 Р·Р°РґР°РЅРёСЏ РїРѕ РєР°СЂС‚Рµ', pts:20 },
  { id:'all_types', icon:'рџЋ­', name:'РЈРЅРёРІРµСЂСЃР°Р»', desc:'Р РµС€Рё РІСЃРµ 6 С‚РёРїРѕРІ Р·Р°РґР°РЅРёР№', pts:60 },
  { id:'comeback', icon:'рџ’Є', name:'Р’РѕР·РІСЂР°С‰РµРЅРёРµ', desc:'РџСЂРѕР№РґРё РіР»Р°РІСѓ РїРѕСЃР»Рµ РїСЂРѕРІР°Р»Р°', pts:35 },
  { id:'survivor', icon:'рџ›ЎпёЏ', name:'РќР° РІРѕР»РѕСЃРєРµ', desc:'Р—Р°РІРµСЂС€Рё РіР»Р°РІСѓ СЃ 1 Р¶РёР·РЅСЊСЋ', pts:35 },
  { id:'school_time', icon:'рџЏ«', name:'РЁРєРѕР»СЊРЅС‹Р№ С‡Р°СЃ', desc:'РРіСЂР°Р№ СЃ 8:00 РґРѕ 15:00', pts:10 },
  { id:'weekend', icon:'рџ“†', name:'Р’С‹С…РѕРґРЅРѕР№ СЂР°Р·РІРµРґС‡РёРє', desc:'РРіСЂР°Р№ РІ СЃСѓР±Р±РѕС‚Сѓ РёР»Рё РІРѕСЃРєСЂРµСЃРµРЅСЊРµ', pts:10 },
  { id:'agent_1', icon:'рџ•µпёЏ', name:'Р’РµСЂР±РѕРІС‰РёРє', desc:'РџСЂРёРіР»Р°СЃРё РїРµСЂРІРѕРіРѕ Р°РіРµРЅС‚Р°', pts:20 },
  { id:'agent_3', icon:'рџ§©', name:'РЎРµС‚СЊ РёРЅС„РѕСЂРјР°С‚РѕСЂРѕРІ', desc:'РџСЂРёРіР»Р°СЃРё 3 Р°РіРµРЅС‚РѕРІ', pts:45 },
  { id:'agent_bonus_150', icon:'рџЋЃ', name:'РџСЂРµРјРёСЏ С€С‚Р°Р±Р°', desc:'РџРѕР»СѓС‡Рё +150 СЂРµС„РµСЂР°Р»СЊРЅС‹С… РѕС‡РєРѕРІ', pts:60 },
  { id:'secret_first', icon:'рџ•¶пёЏ', name:'РўРµРЅСЊ РІ СЌС„РёСЂРµ', desc:'Р’С‹РїРѕР»РЅРё 1 С‚Р°Р№РЅСѓСЋ РјРёСЃСЃРёСЋ.', pts:25 },
  { id:'secret_starter_3', icon:'рџ§­', name:'РўРёС…РёР№ РєРѕРЅС‚СѓСЂ', desc:'Р’С‹РїРѕР»РЅРё 3 СЃС‚Р°СЂС‚РѕРІС‹Рµ С‚Р°Р№РЅС‹Рµ РјРёСЃСЃРёРё.', pts:45 },
  { id:'secret_starter_5', icon:'рџ›°пёЏ', name:'Р РµР¶РёРј РїСЂРёР·СЂР°РєР°', desc:'Р’С‹РїРѕР»РЅРё РІСЃРµ 5 СЃС‚Р°СЂС‚РѕРІС‹С… С‚Р°Р№РЅС‹С… РјРёСЃСЃРёР№.', pts:75 },
  { id:'secret_advanced_3', icon:'рџЋ–пёЏ', name:'РЁС‚Р°Р±РЅР°СЏ РіР»СѓР±РёРЅР°', desc:'Р’С‹РїРѕР»РЅРё 3 РїСЂРѕРґРІРёРЅСѓС‚С‹Рµ С‚Р°Р№РЅС‹Рµ РјРёСЃСЃРёРё.', pts:85 },
  { id:'secret_hard_combo', icon:'вљ™пёЏ', name:'РўСЂРѕР№РЅРѕР№ РїСЂРѕС‚РѕРєРѕР»', desc:'Р’С‹РїРѕР»РЅРё В«РћРїРµСЂР°С†РёСЏ Р±РµР· СЃР»РµРґРѕРІВ», В«Р Р°РґРёРѕСЌС„РёСЂВ» Рё В«РџРѕСЃР»РµРґРЅРёР№ СЂСѓР±РµР¶В».', pts:130 },
  { id:'secret_bonus_200', icon:'рџ’ј', name:'Р—Р°РєСЂС‹С‚С‹Р№ С„РѕРЅРґ', desc:'РќР°РєРѕРїРё +200 РѕС‡РєРѕРІ Р±РѕРЅСѓСЃР° Р·Р° С‚Р°Р№РЅС‹Рµ РјРёСЃСЃРёРё.', pts:60 },
  { id:'secret_bonus_450', icon:'рџ‘‘', name:'РЎРµРєСЂРµС‚РЅС‹Р№ СЂРµР·РµСЂРІ', desc:'РќР°РєРѕРїРё +450 РѕС‡РєРѕРІ Р±РѕРЅСѓСЃР° Р·Р° С‚Р°Р№РЅС‹Рµ РјРёСЃСЃРёРё.', pts:110 },
  { id:'secret_all_10', icon:'рџ›ЎпёЏ', name:'РќРµРІРёРґРёРјС‹Р№ С„СЂРѕРЅС‚', desc:'Р’С‹РїРѕР»РЅРё РІСЃРµ 10 С‚Р°Р№РЅС‹С… РјРёСЃСЃРёР№.', pts:160 },
  { id:'secret_all_15', icon:'рџ—ќпёЏ', name:'РљР»СЋС‡ С€С‚Р°Р±Р°', desc:'Р’С‹РїРѕР»РЅРё РІСЃРµ 15 С‚Р°Р№РЅС‹С… РјРёСЃСЃРёР№.', pts:200, reward:150 },
  { id:'secret_map_runner', icon:'рџ§­', name:'РњР°СЂС€СЂСѓС‚ РїСЂРѕРІРѕРґРЅРёРєР°', desc:'Р’С‹РїРѕР»РЅРё РјРёСЃСЃРёСЋ В«РњР°СЂС€СЂСѓС‚ РїСЂРѕРІРѕРґРЅРёРєР°В».', pts:75, reward:55 },
  { id:'secret_type_mix', icon:'рџ§Є', name:'РљРѕРјР±РёРЅРёСЂРѕРІР°РЅРЅС‹Р№ РєР°РЅР°Р»', desc:'Р’С‹РїРѕР»РЅРё РјРёСЃСЃРёСЋ В«РљРѕРјР±РёРЅРёСЂРѕРІР°РЅРЅС‹Р№ РєР°РЅР°Р»В».', pts:80, reward:60 },
  { id:'secret_clean_two', icon:'рџ“‹', name:'РђРєРєСѓСЂР°С‚РЅС‹Р№ С€С‚Р°Р±', desc:'Р’С‹РїРѕР»РЅРё РјРёСЃСЃРёСЋ В«РђРєРєСѓСЂР°С‚РЅС‹Р№ С€С‚Р°Р±В».', pts:90, reward:70 },
  { id:'secret_evening_watch', icon:'рџЊ†', name:'Р’РµС‡РµСЂРЅРёР№ РґРѕР·РѕСЂ', desc:'Р’С‹РїРѕР»РЅРё РјРёСЃСЃРёСЋ В«Р’РµС‡РµСЂРЅРёР№ РґРѕР·РѕСЂВ».', pts:70, reward:50 },
  { id:'secret_shock_score', icon:'вљЎ', name:'РЁС‚СѓСЂРјРѕРІРѕР№ С‚РµРјРї', desc:'Р’С‹РїРѕР»РЅРё РјРёСЃСЃРёСЋ В«РЁС‚СѓСЂРјРѕРІРѕР№ С‚РµРјРїВ».', pts:95, reward:75 },
  { id:'secret_advanced_6', icon:'рџ›°пёЏ', name:'Р“Р»СѓР±РѕРєР°СЏ СЂР°Р·РІРµРґРєР°', desc:'Р’С‹РїРѕР»РЅРё 6 РїСЂРѕРґРІРёРЅСѓС‚С‹С… С‚Р°Р№РЅС‹С… РјРёСЃСЃРёР№.', pts:130, reward:90 },
  { id:'secret_bonus_700', icon:'рџ’°', name:'Р РµР·РµСЂРІ РѕРїРµСЂР°С†РёРё', desc:'РќР°РєРѕРїРё +700 РѕС‡РєРѕРІ Р±РѕРЅСѓСЃР° Р·Р° С‚Р°Р№РЅС‹Рµ РјРёСЃСЃРёРё.', pts:120, reward:90 },
  { id:'secret_bonus_1000', icon:'рџЏ¦', name:'Р¤РѕРЅРґ С„СЂРѕРЅС‚Р°', desc:'РќР°РєРѕРїРё +1000 РѕС‡РєРѕРІ Р±РѕРЅСѓСЃР° Р·Р° С‚Р°Р№РЅС‹Рµ РјРёСЃСЃРёРё.', pts:160, reward:120 },
  { id:'streak_12', icon:'рџЊ‹', name:'РќРµСЃРѕРєСЂСѓС€РёРјР°СЏ СЃРµСЂРёСЏ', desc:'РЎРµСЂРёСЏ РёР· 12 РїСЂР°РІРёР»СЊРЅС‹С… РѕС‚РІРµС‚РѕРІ.', pts:95, reward:70 },
  { id:'fast_60', icon:'рџ›©пёЏ', name:'РЎРєРѕСЂРѕСЃС‚РЅР°СЏ СЌСЃРєР°РґСЂРёР»СЊСЏ', desc:'60 Р±С‹СЃС‚СЂС‹С… РѕС‚РІРµС‚РѕРІ (<=10 СЃРµРє).', pts:110, reward:85 },
  { id:'agent_5', icon:'рџ•ёпёЏ', name:'РљСѓСЂР°С‚РѕСЂ СЃРµС‚Рё', desc:'РџСЂРёРіР»Р°СЃРё 5 Р°РіРµРЅС‚РѕРІ.', pts:90, reward:70 },
  { id:'collector_8', icon:'рџ—ѓпёЏ', name:'РљРѕР»Р»РµРєС†РёРѕРЅРµСЂ', desc:'РџРѕР»СѓС‡Рё 8 РґРѕСЃС‚РёР¶РµРЅРёР№', pts:30 },
  { id:'collector_16', icon:'рџ§©', name:'РђСЂС…РёРІР°СЂРёСѓСЃ', desc:'РџРѕР»СѓС‡Рё 16 РґРѕСЃС‚РёР¶РµРЅРёР№', pts:65 },
  { id:'collector_24', icon:'рџ§°', name:'РҐСЂР°РЅРёС‚РµР»СЊ РґРѕСЃСЊРµ', desc:'РџРѕР»СѓС‡Рё 24 РґРѕСЃС‚РёР¶РµРЅРёСЏ', pts:95, reward:75 },
  { id:'collector_32', icon:'рџЏ›пёЏ', name:'РњР°СЃС‚РµСЂ Р°СЂС…РёРІР°', desc:'РџРѕР»СѓС‡Рё 32 РґРѕСЃС‚РёР¶РµРЅРёСЏ', pts:140, reward:110 },
];

const ARTIFACTS = [
  { id:'artifact_ch1', icon:'рџ“њ', rarity:'common', name:'Р”РѕРЅРµСЃРµРЅРёРµ РџРѕР»РµСЃСЊСЏ', desc:'РћС‚РєСЂС‹С‚Рѕ Р·Р° Р·Р°РІРµСЂС€РµРЅРёРµ РіР»Р°РІС‹ I' },
  { id:'artifact_ch2', icon:'рџ§ѕ', rarity:'common', name:'РџРѕРґРїРѕР»СЊРЅС‹Р№ РїСЂРѕРїСѓСЃРє', desc:'РћС‚РєСЂС‹С‚Рѕ Р·Р° Р·Р°РІРµСЂС€РµРЅРёРµ РіР»Р°РІС‹ II' },
  { id:'artifact_ch3', icon:'рџ“»', rarity:'rare', name:'Р Р°РґРёРѕРіСЂР°РјРјР° С€С‚Р°Р±Р°', desc:'РћС‚РєСЂС‹С‚Рѕ Р·Р° Р·Р°РІРµСЂС€РµРЅРёРµ РіР»Р°РІС‹ III' },
  { id:'artifact_ch4', icon:'рџ—‚пёЏ', rarity:'rare', name:'РћРїРµСЂР°С‚РёРІРЅР°СЏ СЃРІРѕРґРєР°', desc:'РћС‚РєСЂС‹С‚Рѕ Р·Р° Р·Р°РІРµСЂС€РµРЅРёРµ РіР»Р°РІС‹ IV' },
  { id:'artifact_ch5', icon:'рџ§­', rarity:'epic', name:'РљРѕРјРїР°СЃ РґРёРІРµСЂСЃР°РЅС‚Р°', desc:'РћС‚РєСЂС‹С‚Рѕ Р·Р° Р·Р°РІРµСЂС€РµРЅРёРµ РіР»Р°РІС‹ V' },
  { id:'artifact_ch6', icon:'рџ•ЉпёЏ', rarity:'epic', name:'РЁРёС„СЂ РџРѕР±РµРґС‹', desc:'РћС‚РєСЂС‹С‚Рѕ Р·Р° Р·Р°РІРµСЂС€РµРЅРёРµ РіР»Р°РІС‹ VI' },
  { id:'artifact_flawless', icon:'рџЋ–пёЏ', rarity:'rare', name:'Р—РЅР°Рє С‚РѕС‡РЅРѕСЃС‚Рё', desc:'Р“Р»Р°РІР° Р±РµР· РѕС€РёР±РѕРє' },
  { id:'artifact_silent', icon:'рџ•¶пёЏ', rarity:'rare', name:'РўРёС…РёР№ РєР°РЅР°Р»', desc:'Р“Р»Р°РІР° Р±РµР· РїРѕРґСЃРєР°Р·РѕРє' },
  { id:'artifact_fast', icon:'вЏ±пёЏ', rarity:'rare', name:'РҐСЂРѕРЅРѕРјРµС‚СЂ С€С‚Р°Р±Р°', desc:'10 Р±С‹СЃС‚СЂС‹С… РѕС‚РІРµС‚РѕРІ' },
  { id:'artifact_retry', icon:'рџ”Ѓ', rarity:'epic', name:'РќР°С€РёРІРєР° С„РµРЅРёРєСЃР°', desc:'РџРѕР±РµРґР° РїРѕСЃР»Рµ РїСЂРѕРІР°Р»Р°' },
  { id:'artifact_polyglot', icon:'рџ§ ', rarity:'epic', name:'РЁРёС„СЂРѕРІР°Р»СЊРЅС‹Р№ РґРёСЃРє', desc:'РћСЃРІРѕРµРЅС‹ РІСЃРµ С‚РёРїС‹ С€РёС„СЂРѕРІ' },
  { id:'artifact_secret_shadow', icon:'рџ•¶пёЏ', rarity:'rare', name:'РџРµС‡Р°С‚СЊ С‚РµРЅРё', desc:'РџРѕР»СѓС‡РµРЅРѕ Р·Р° РїРµСЂРІСѓСЋ С‚Р°Р№РЅСѓСЋ РјРёСЃСЃРёСЋ.' },
  { id:'artifact_secret_modes', icon:'рџ›°пёЏ', rarity:'epic', name:'РџСѓР»СЊС‚ СЂРµР¶РёРјРѕРІ', desc:'Р’С‹РїРѕР»РЅРµРЅС‹ РІСЃРµ 5 СЃС‚Р°СЂС‚РѕРІС‹С… С‚Р°Р№РЅС‹С… РјРёСЃСЃРёР№.' },
  { id:'artifact_secret_night', icon:'рџЊ™', rarity:'rare', name:'РќРѕС‡РЅРѕР№ РїСЂРѕРїСѓСЃРє', desc:'Р’С‹РїРѕР»РЅРµРЅР° РјРёСЃСЃРёСЏ В«РќРѕС‡РЅРѕР№ РґРѕР·РѕСЂВ».' },
  { id:'artifact_secret_radio', icon:'рџ“»', rarity:'epic', name:'РљРѕРґРѕРІС‹Р№ СЌС„РёСЂ', desc:'Р’С‹РїРѕР»РЅРµРЅР° РјРёСЃСЃРёСЏ В«Р Р°РґРёРѕСЌС„РёСЂВ».' },
  { id:'artifact_secret_network', icon:'рџ§©', rarity:'epic', name:'РЎРµС‚РєР° Р°РіРµРЅС‚РѕРІ', desc:'Р’С‹РїРѕР»РЅРµРЅР° РјРёСЃСЃРёСЏ В«РЎРµС‚СЊ РёРЅС„РѕСЂРјР°С‚РѕСЂРѕРІВ».' },
  { id:'artifact_secret_legend', icon:'рџ‘‘', rarity:'legendary', name:'РђСЂС…РёРІ РЅРµРІРёРґРёРјРѕРіРѕ С„СЂРѕРЅС‚Р°', desc:'Р’С‹РїРѕР»РЅРµРЅС‹ РІСЃРµ 10 С‚Р°Р№РЅС‹С… РјРёСЃСЃРёР№.' },
  { id:'artifact_secret_map', icon:'рџ§­', rarity:'rare', name:'РњР°СЂС€СЂСѓС‚РЅР°СЏ Р»РёРЅРµР№РєР°', desc:'Р’С‹РїРѕР»РЅРµРЅР° РјРёСЃСЃРёСЏ В«РњР°СЂС€СЂСѓС‚ РїСЂРѕРІРѕРґРЅРёРєР°В».', reward:45 },
  { id:'artifact_secret_mix', icon:'рџ§Є', rarity:'epic', name:'РџРѕР»РµРІРѕР№ РґРµС€РёС„СЂР°С‚РѕСЂ', desc:'Р’С‹РїРѕР»РЅРµРЅР° РјРёСЃСЃРёСЏ В«РљРѕРјР±РёРЅРёСЂРѕРІР°РЅРЅС‹Р№ РєР°РЅР°Р»В».', reward:50 },
  { id:'artifact_secret_clean', icon:'рџ“‹', rarity:'epic', name:'РџСЂРѕС‚РѕРєРѕР» С‚РѕС‡РЅРѕСЃС‚Рё', desc:'Р’С‹РїРѕР»РЅРµРЅР° РјРёСЃСЃРёСЏ В«РђРєРєСѓСЂР°С‚РЅС‹Р№ С€С‚Р°Р±В».', reward:60 },
  { id:'artifact_secret_evening', icon:'рџЊ†', rarity:'rare', name:'Р’РµС‡РµСЂРЅСЏСЏ СЃРІРѕРґРєР°', desc:'Р’С‹РїРѕР»РЅРµРЅР° РјРёСЃСЃРёСЏ В«Р’РµС‡РµСЂРЅРёР№ РґРѕР·РѕСЂВ».', reward:40 },
  { id:'artifact_secret_strike', icon:'вљЎ', rarity:'epic', name:'РЁС‚СѓСЂРјРѕРІРѕР№ РѕСЂРґРµСЂ', desc:'Р’С‹РїРѕР»РЅРµРЅР° РјРёСЃСЃРёСЏ В«РЁС‚СѓСЂРјРѕРІРѕР№ С‚РµРјРїВ».', reward:70 },
  { id:'artifact_secret_elite', icon:'рџ—ќпёЏ', rarity:'legendary', name:'РљР»СЋС‡ С€С‚Р°Р±Р°', desc:'Р’С‹РїРѕР»РЅРµРЅС‹ РІСЃРµ 15 С‚Р°Р№РЅС‹С… РјРёСЃСЃРёР№.', reward:120 },
  { id:'artifact_secret_bonus_1000', icon:'рџЏ¦', rarity:'legendary', name:'Р—РѕР»РѕС‚РѕР№ СЂРµР·РµСЂРІ', desc:'РќР°РєРѕРїР»РµРЅРѕ +1000 РѕС‡РєРѕРІ Р±РѕРЅСѓСЃР° С‚Р°Р№РЅС‹С… РјРёСЃСЃРёР№.', reward:95 },
  { id:'artifact_agents_hq', icon:'рџ•ёпёЏ', rarity:'epic', name:'РЁС‚Р°Р± Р°РіРµРЅС‚СѓСЂС‹', desc:'Р’ СЃРµС‚Рё 5 Р°РєС‚РёРІРЅС‹С… Р°РіРµРЅС‚РѕРІ.', reward:70 },
  { id:'artifact_speed_60', icon:'рџ›©пёЏ', rarity:'epic', name:'РљСЂС‹Р»Рѕ РїРµСЂРµС…РІР°С‚Р°', desc:'РЎРґРµР»Р°РЅРѕ 60 Р±С‹СЃС‚СЂС‹С… РѕС‚РІРµС‚РѕРІ.', reward:75 },
  { id:'artifact_collector_24', icon:'рџ§°', rarity:'epic', name:'РЎСѓРЅРґСѓРє Р°СЂС…РёРІР°СЂРёСѓСЃР°', desc:'РџРѕР»СѓС‡РµРЅРѕ 24 РґРѕСЃС‚РёР¶РµРЅРёСЏ.', reward:80 },
  { id:'artifact_legend', icon:'рџЏ›пёЏ', rarity:'legendary', name:'РђСЂС…РёРІ РџРѕР±РµРґС‹', desc:'Р’СЃРµ РіР»Р°РІС‹ Р·Р°РІРµСЂС€РµРЅС‹ СЃ РІС‹СЃРѕРєРёРј СЂРµР·СѓР»СЊС‚Р°С‚РѕРј' },
];

if (!state.achievements) state.achievements = {};
if (!state.achievementPts) state.achievementPts = 0;
if (!state.artifacts) state.artifacts = {};
if (!state.solvedTypes) state.solvedTypes = {};
if (!state.solvedTypeCounts) state.solvedTypeCounts = {};

function registerSolvedCipherType(type) {
  const t = String(type || '').trim().toLowerCase();
  if (!t) return;
  if (!state.solvedTypes || typeof state.solvedTypes !== 'object') state.solvedTypes = {};
  if (!state.solvedTypeCounts || typeof state.solvedTypeCounts !== 'object') state.solvedTypeCounts = {};
  state.solvedTypes[t] = true;
  state.solvedTypeCounts[t] = Math.max(0, Number(state.solvedTypeCounts[t] || 0)) + 1;
}

function unlockAchievement(id) {
  if (!state.achievements) state.achievements = {};
  if (state.achievements[id]) return;
  const ach = ACHIEVEMENTS.find(a => a.id === id);
  if (!ach) return;
  state.achievements[id] = Date.now();
  state.achievementPts = (state.achievementPts || 0) + ach.pts;
  const reward = Math.max(0, Math.floor(Number(ach.reward || 0)));
  if (reward > 0) {
    state.totalScore = Math.max(0, Math.floor(Number(state.totalScore || 0)) + reward);
  }
  saveState();
  showAchievementToast(ach, reward);
  setTimeout(() => autoSync(false), 350);
}

function showAchievementToast(ach, rewardPoints = 0) {
  const old = document.getElementById('ach-toast');
  if (old) old.remove();
  const el = document.createElement('div');
  el.id = 'ach-toast';
  el.style.cssText = `position:fixed;top:80px;left:50%;transform:translateX(-50%);
    z-index:9997;background:linear-gradient(135deg,#1a1508,#2a2010);
    border:1px solid rgba(255,224,51,.45);border-radius:12px;
    padding:12px 18px;display:flex;align-items:center;gap:12px;
    box-shadow:0 8px 32px rgba(0,0,0,.6),0 0 20px rgba(255,224,51,.15);
    max-width:300px`;
  el.innerHTML = `
    <div style="font-size:28px;flex-shrink:0">${ach.icon}</div>
    <div>
      <div style="font-size:9px;color:rgba(255,224,51,.68);letter-spacing:.1em;margin-bottom:2px">Р”РћРЎРўРР–Р•РќРР•</div>
      <div style="font-family:var(--head);font-size:var(--fs-sm);color:var(--accent)">${ach.name}</div>
      <div style="font-size:10px;color:var(--muted);margin-top:2px">${ach.desc} В· +${ach.pts} РѕС‡</div>
      ${rewardPoints > 0 ? `<div style="font-size:10px;color:#9dffb7;margin-top:2px">РќР°РіСЂР°РґР°: +${rewardPoints} Рє РѕР±С‰РµРјСѓ СЃС‡С‘С‚Сѓ</div>` : ''}
    </div>`;
  document.body.appendChild(el);
  setTimeout(() => {
    el.style.transition = 'opacity .3s,transform .3s';
    el.style.opacity = '0';
    el.style.transform = 'translateX(-50%) translateY(-10px)';
    setTimeout(() => el.remove(), 300);
  }, 2700);
}

function _artifactRarityStyle(rarity) {
  if (rarity === 'legendary') return { border:'rgba(255,186,61,.65)', glow:'0 0 18px rgba(255,186,61,.3)', text:'#ffdca2' };
  if (rarity === 'epic') return { border:'rgba(255,115,115,.55)', glow:'0 0 16px rgba(255,115,115,.2)', text:'#ffcfbf' };
  if (rarity === 'rare') return { border:'rgba(120,198,255,.55)', glow:'0 0 14px rgba(120,198,255,.2)', text:'#cce9ff' };
  return { border:'rgba(160,255,184,.45)', glow:'0 0 10px rgba(160,255,184,.15)', text:'#d8ffe2' };
}

function unlockArtifact(id) {
  if (!state.artifacts) state.artifacts = {};
  if (state.artifacts[id]) return null;
  const art = ARTIFACTS.find(a => a.id === id);
  if (!art) return null;
  state.artifacts[id] = Date.now();
  const reward = Math.max(0, Math.floor(Number(art.reward || 0)));
  if (reward > 0) {
    state.totalScore = Math.max(0, Math.floor(Number(state.totalScore || 0)) + reward);
  }
  saveState();
  showArtifactToast(art, reward);
  setTimeout(() => autoSync(false), 420);
  return art;
}

function showArtifactToast(artifact, rewardPoints = 0) {
  const old = document.getElementById('artifact-toast');
  if (old) old.remove();
  const style = _artifactRarityStyle(artifact.rarity);
  const el = document.createElement('div');
  el.id = 'artifact-toast';
  el.style.cssText = `position:fixed;top:140px;left:50%;transform:translateX(-50%);z-index:9997;
    background:linear-gradient(140deg,#11120d,#1d1a12);border:1px solid ${style.border};border-radius:12px;
    padding:11px 14px;display:flex;align-items:center;gap:10px;box-shadow:${style.glow};max-width:320px`;
  el.innerHTML = `
    <div style="font-size:25px">${artifact.icon}</div>
    <div>
      <div style="font-size:9px;letter-spacing:.11em;color:${style.text};opacity:.9">РђР РўР•Р¤РђРљРў РќРђР™Р”Р•Рќ</div>
      <div style="font-family:var(--head);font-size:12px;color:${style.text};margin-top:2px">${artifact.name}</div>
      <div style="font-size:10px;color:var(--muted);margin-top:2px">${artifact.desc}</div>
      ${rewardPoints > 0 ? `<div style="font-size:10px;color:#9dffb7;margin-top:2px">РќР°РіСЂР°РґР°: +${rewardPoints} Рє РѕР±С‰РµРјСѓ СЃС‡С‘С‚Сѓ</div>` : ''}
    </div>`;
  document.body.appendChild(el);
  setTimeout(() => {
    el.style.transition = 'opacity .25s,transform .25s';
    el.style.opacity = '0';
    el.style.transform = 'translateX(-50%) translateY(-8px)';
    setTimeout(() => el.remove(), 260);
  }, 2600);
}

function evaluateArtifacts(context = {}) {
  if (!state.artifacts) state.artifacts = {};
  const gained = [];
  const pushIf = (id, cond) => {
    if (!cond) return;
    const unlocked = unlockArtifact(id);
    if (unlocked) gained.push(unlocked);
  };

  pushIf('artifact_ch1', !!state.completedChapters[1]);
  pushIf('artifact_ch2', !!state.completedChapters[2]);
  pushIf('artifact_ch3', !!state.completedChapters[3]);
  pushIf('artifact_ch4', !!state.completedChapters[4]);
  pushIf('artifact_ch5', !!state.completedChapters[5]);
  pushIf('artifact_ch6', !!state.completedChapters[6]);

  pushIf('artifact_flawless', !!context.chapterDoneNoErrors);
  pushIf('artifact_silent', !!context.chapterDoneNoHints);
  pushIf('artifact_fast', (state._fastAnswers || 0) >= 10);
  pushIf('artifact_retry', !!context.retryWin);

  const solvedTypeCount = Object.keys(state.solvedTypes || {}).length;
  pushIf('artifact_polyglot', solvedTypeCount >= 6);

  const secretStats = _secretMissionStats();
  const ref = normalizeReferralSummary(state.referralSummary);
  const achievementCount = Object.keys(state.achievements || {}).length;
  pushIf('artifact_secret_shadow', secretStats.completedTotal >= 1);
  pushIf('artifact_secret_modes', secretStats.completedStarter >= 5);
  pushIf('artifact_secret_night', _secretMissionCompleted('sm_night_watch'));
  pushIf('artifact_secret_radio', _secretMissionCompleted('sm_morse_five_fast'));
  pushIf('artifact_secret_network', _secretMissionCompleted('sm_network_three'));
  pushIf('artifact_secret_legend', secretStats.completedTotal >= 10);
  pushIf('artifact_secret_map', _secretMissionCompleted('sm_map_runner'));
  pushIf('artifact_secret_mix', _secretMissionCompleted('sm_type_mix'));
  pushIf('artifact_secret_clean', _secretMissionCompleted('sm_clean_two_chapters'));
  pushIf('artifact_secret_evening', _secretMissionCompleted('sm_evening_watch'));
  pushIf('artifact_secret_strike', _secretMissionCompleted('sm_chapter_score_700'));
  pushIf('artifact_secret_bonus_1000', secretStats.bonusPoints >= 1000);
  pushIf('artifact_secret_elite', secretStats.completedTotal >= 15);
  pushIf('artifact_agents_hq', ref.active_count >= 5);
  pushIf('artifact_speed_60', (state._fastAnswers || 0) >= 60);
  pushIf('artifact_collector_24', achievementCount >= 24);

  const done = _stateCompletedCount();
  pushIf('artifact_legend', done >= 6 && Number(state.totalScore || 0) >= 2500);
  return gained;
}

function checkAchievements(context = {}) {
  const done = _stateCompletedCount();
  const score = Number(state.totalScore || 0);

  if (context.cipherType) {
    registerSolvedCipherType(context.cipherType);
  }

  if (context.elapsed <= 5) unlockAchievement('fast_5');
  if ((state._fastAnswers || 0) >= 5) unlockAchievement('fast_10');
  if ((state._fastAnswers || 0) >= 25) unlockAchievement('fast_25');
  if ((state._fastAnswers || 0) >= 60) unlockAchievement('fast_60');

  if ((state.streak || 0) >= 3) unlockAchievement('streak_3');
  if ((state.streak || 0) >= 5) unlockAchievement('streak_5');
  if ((state.streak || 0) >= 8) unlockAchievement('streak_8');
  if ((state.streak || 0) >= 12) unlockAchievement('streak_12');

  if (done >= 1) unlockAchievement('chapter_1');
  if (done >= 3) unlockAchievement('chapter_3');
  if (done >= 6) unlockAchievement('chapter_6');

  if (context.chapterDoneNoErrors) unlockAchievement('flawless_1');
  if ((state._perfectChapters || 0) >= 3) unlockAchievement('flawless_3');

  if (context.chapterDoneNoHints) unlockAchievement('no_hints_1');
  if ((state._noHintChapters || 0) >= 3) unlockAchievement('no_hints_3');

  if (score >= 500) unlockAchievement('score_500');
  if (score >= 1200) unlockAchievement('score_1200');
  if (score >= 2500) unlockAchievement('score_2500');
  if (score >= 4000) unlockAchievement('score_4000');

  const solvedTypes = state.solvedTypes || {};
  const solvedTypeCounts = state.solvedTypeCounts || {};
  const solvedTypeCount = Object.keys(solvedTypes).length;
  if ((solvedTypeCounts.morse || 0) >= 3) unlockAchievement('type_morse');
  if ((solvedTypeCounts.math || 0) >= 3) unlockAchievement('type_math');
  if ((solvedTypeCounts.map || 0) >= 2) unlockAchievement('type_map');
  if (solvedTypeCount >= 6) unlockAchievement('all_types');

  if (context.retryWin || context.comeback) unlockAchievement('comeback');
  if (context.survived1) unlockAchievement('survivor');

  const hour = new Date().getHours();
  const day = new Date().getDay();
  if (hour >= 8 && hour < 15) unlockAchievement('school_time');
  if (day === 0 || day === 6) unlockAchievement('weekend');
  const ref = normalizeReferralSummary(state.referralSummary);
  if (ref.invited_count >= 1) unlockAchievement('agent_1');
  if (ref.invited_count >= 3) unlockAchievement('agent_3');
  if (ref.invited_count >= 5) unlockAchievement('agent_5');
  if (ref.bonus_points >= 150) unlockAchievement('agent_bonus_150');

  const secretStats = _secretMissionStats();
  if (secretStats.completedTotal >= 1) unlockAchievement('secret_first');
  if (secretStats.completedStarter >= 3) unlockAchievement('secret_starter_3');
  if (secretStats.completedStarter >= 5) unlockAchievement('secret_starter_5');
  if (secretStats.completedAdvanced >= 3) unlockAchievement('secret_advanced_3');
  if (secretStats.completedAdvanced >= 6) unlockAchievement('secret_advanced_6');
  if (_secretMissionCompleted('sm_map_runner')) unlockAchievement('secret_map_runner');
  if (_secretMissionCompleted('sm_type_mix')) unlockAchievement('secret_type_mix');
  if (_secretMissionCompleted('sm_clean_two_chapters')) unlockAchievement('secret_clean_two');
  if (_secretMissionCompleted('sm_evening_watch')) unlockAchievement('secret_evening_watch');
  if (_secretMissionCompleted('sm_chapter_score_700')) unlockAchievement('secret_shock_score');
  if (secretStats.bonusPoints >= 200) unlockAchievement('secret_bonus_200');
  if (secretStats.bonusPoints >= 450) unlockAchievement('secret_bonus_450');
  if (secretStats.bonusPoints >= 700) unlockAchievement('secret_bonus_700');
  if (secretStats.bonusPoints >= 1000) unlockAchievement('secret_bonus_1000');
  if (
    _secretMissionCompleted('sm_flawless_chapter') &&
    _secretMissionCompleted('sm_morse_five_fast') &&
    _secretMissionCompleted('sm_last_life_chapter')
  ) {
    unlockAchievement('secret_hard_combo');
  }
  if (secretStats.completedTotal >= 10) unlockAchievement('secret_all_10');
  if (secretStats.completedTotal >= 15) unlockAchievement('secret_all_15');

  const count = Object.keys(state.achievements || {}).length;
  if (count >= 8) unlockAchievement('collector_8');
  if (count >= 16) unlockAchievement('collector_16');
  if (count >= 24) unlockAchievement('collector_24');
  if (count >= 32) unlockAchievement('collector_32');

  evaluateArtifacts(context);
}

function renderAchievementsTab(container) {
  const myAchs = state.achievements || {};
  const total = ACHIEVEMENTS.length;
  const earned = Object.keys(myAchs).length;
  const earnedPts = state.achievementPts || 0;
  const progressPct = total ? Math.round((earned / total) * 100) : 0;
  const myArtifacts = state.artifacts || {};
  const artifactTotal = ARTIFACTS.length;
  const artifactEarned = Object.keys(myArtifacts).length;
  const artifactPct = artifactTotal ? Math.round((artifactEarned / artifactTotal) * 100) : 0;
  ensureSecretState();
  const secretStats = _secretMissionStats();
  const secretMode = normalizeSecretMode(state.secretMode);
  const secretMeta = _secretGetModeMeta(secretMode);
  const secretMissions = secretStats.missions;
  const starterRows = secretMissions.filter((m) => m.tier === 'starter');
  const advancedRows = secretMissions.filter((m) => m.tier !== 'starter');
  const starterTotal = starterRows.length;
  const advancedTotal = advancedRows.length;
  const secretProgressPct = secretStats.total > 0
    ? Math.round((secretStats.completed / secretStats.total) * 100)
    : 0;
  const missionRow = (mission) => {
    const done = !!mission.completed;
    const pct = mission.target > 0 ? Math.round((mission.progress / mission.target) * 100) : 0;
    return `<div style="padding:7px 0;border-bottom:1px solid rgba(255,255,255,.05)">
      <div style="display:flex;align-items:center;justify-content:space-between;gap:10px">
        <div style="font-size:11px;color:${done ? '#cffff0' : '#d3f2e5'};line-height:1.35">${mission.icon} ${escapeHtml(mission.name)}</div>
        <div style="font-family:var(--head);font-size:10px;color:${done ? '#8df5d4' : '#9ad3c0'}">${mission.progress}/${mission.target}</div>
      </div>
      <div style="font-size:10px;color:#9cc7ba;line-height:1.35;margin-top:3px">${escapeHtml(mission.desc)}</div>
      <div style="margin-top:4px;height:5px;border-radius:999px;background:rgba(255,255,255,.08);overflow:hidden">
        <div style="height:100%;width:${pct}%;background:${done ? 'linear-gradient(90deg,#67ffd3,#d0fff2)' : 'linear-gradient(90deg,#6f8f82,#b4d0c2)'}"></div>
      </div>
    </div>`;
  };
  const starterHtml = starterRows.length
    ? starterRows.map(missionRow).join('')
    : '<div style="font-size:10px;color:#9cc7ba;font-style:italic">РЎС‚Р°СЂС‚РѕРІС‹Рµ РјРёСЃСЃРёРё Р·Р°РіСЂСѓР·СЏС‚СЃСЏ РїРѕСЃР»Рµ СЃРёРЅС…СЂРѕРЅРёР·Р°С†РёРё.</div>';
  const advancedHtml = advancedRows.length
    ? advancedRows.map(missionRow).join('')
    : '<div style="font-size:10px;color:#9cc7ba;font-style:italic">РџСЂРѕРґРІРёРЅСѓС‚С‹Рµ РјРёСЃСЃРёРё Р·Р°РіСЂСѓР·СЏС‚СЃСЏ РїРѕСЃР»Рµ СЃРёРЅС…СЂРѕРЅРёР·Р°С†РёРё.</div>';

  let html = `<div style="padding:16px">
    <div style="background:linear-gradient(160deg,rgba(255,224,51,.16),rgba(20,17,8,.95));
      border:1px solid rgba(255,224,51,.45);border-radius:12px;padding:14px;margin-bottom:14px;
      box-shadow:0 8px 24px rgba(0,0,0,.45)">
      <div style="display:flex;justify-content:space-around;text-align:center">
        <div>
          <div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent)">${earned}</div>
          <div style="font-size:10px;color:#f2e5b0;letter-spacing:.05em">РџРћР›РЈР§Р•РќРћ</div>
        </div>
        <div>
          <div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent)">${total}</div>
          <div style="font-size:10px;color:#f2e5b0;letter-spacing:.05em">Р’РЎР•Р“Рћ</div>
        </div>
        <div>
          <div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent)">${earnedPts}</div>
          <div style="font-size:10px;color:#f2e5b0;letter-spacing:.05em">РћР§РљРћР’</div>
        </div>
      </div>
      <div style="margin-top:10px;background:rgba(0,0,0,.35);border-radius:999px;height:8px;overflow:hidden;">
        <div style="height:100%;width:${progressPct}%;background:linear-gradient(90deg,#ffe033,#fff1a8);box-shadow:0 0 8px rgba(255,224,51,.8)"></div>
      </div>
      <div style="margin-top:6px;font-size:10px;color:#f0df9d;text-align:right;">${progressPct}% РєРѕР»Р»РµРєС†РёРё РґРѕСЃС‚РёР¶РµРЅРёР№</div>
    </div>
    <div style="background:linear-gradient(160deg,rgba(85,255,210,.12),rgba(12,20,17,.96));
      border:1px solid rgba(103,255,211,.4);border-radius:12px;padding:14px;margin-bottom:14px;
      box-shadow:0 8px 24px rgba(0,0,0,.42)">
      <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:10px">
        <div>
          <div style="font-family:var(--head);font-size:12px;color:#9df7da;letter-spacing:.08em">рџ•¶ РўРђР™РќР«Р• РњРРЎРЎРР</div>
          <div style="font-size:10px;color:#c9f7ea;margin-top:3px">РђРєС‚РёРІРЅС‹Р№ СЂРµР¶РёРј: <b>${secretMeta.title}</b> (+${secretMeta.bonus_pct}%)</div>
        </div>
        <div style="font-family:var(--head);font-size:13px;color:#8ff2d2">${secretStats.completed}/${secretStats.total}</div>
      </div>
      <div style="margin-top:9px;background:rgba(0,0,0,.35);border-radius:999px;height:8px;overflow:hidden;">
        <div style="height:100%;width:${secretProgressPct}%;background:linear-gradient(90deg,#67ffd3,#d0fff2);box-shadow:0 0 10px rgba(103,255,211,.8)"></div>
      </div>
      <div style="margin-top:6px;font-size:10px;color:#c7efe3;text-align:right;">Р‘РѕРЅСѓСЃ РјРёСЃСЃРёР№: +${secretStats.bonusPoints} РѕС‡</div>
      <div style="display:flex;gap:8px;margin-top:9px">
        <div style="flex:1;background:rgba(103,255,211,.08);border:1px solid rgba(103,255,211,.18);border-radius:7px;padding:7px;text-align:center">
          <div style="font-family:var(--head);font-size:12px;color:#a9ffe5">${secretStats.completedStarter}/${starterTotal || 0}</div>
          <div style="font-size:9px;color:#9ed8c8">РЎРўРђР РўРћР’Р«Р•</div>
        </div>
        <div style="flex:1;background:rgba(103,255,211,.08);border:1px solid rgba(103,255,211,.18);border-radius:7px;padding:7px;text-align:center">
          <div style="font-family:var(--head);font-size:12px;color:#a9ffe5">${secretStats.completedAdvanced}/${advancedTotal || 0}</div>
          <div style="font-size:9px;color:#9ed8c8">РџР РћР”Р’РРќРЈРўР«Р•</div>
        </div>
      </div>
      <div style="margin-top:10px;padding:8px;border-radius:8px;background:rgba(103,255,211,.05);border:1px dashed rgba(103,255,211,.2)">
        <div style="font-family:var(--head);font-size:10px;color:#8ff2d2;letter-spacing:.07em">Р‘Р«РЎРўР Р«Р™ РЎРўРђР Рў</div>
        ${starterHtml}
      </div>
      <div style="margin-top:10px;padding:8px;border-radius:8px;background:rgba(255,255,255,.03);border:1px dashed rgba(255,255,255,.16)">
        <div style="font-family:var(--head);font-size:10px;color:#b8e0d2;letter-spacing:.07em">РќРћР’Р«Р™ Р Р•Р–РРњ В· РћРџР•Р РђР¦РР</div>
        ${advancedHtml}
      </div>
      <div style="margin-top:8px;font-size:10px;color:#9cc7ba;line-height:1.45">
        РђРЅС‚РёР°Р±СѓР·: СЃРµСЂРёСЏ В«РЎРєРѕСЂРѕСЃС‚РЅРѕРіРѕ СЂР°РґРёСЃС‚Р°В» СЃР±СЂР°СЃС‹РІР°РµС‚СЃСЏ РїСЂРё РѕС€РёР±РєРµ, РїРѕРґСЃРєР°Р·РєРµ, РїР°СѓР·Рµ РёР»Рё РІС‹С…РѕРґРµ РёР· РіР»Р°РІС‹.
      </div>
      <button onclick="switchTab('secret')" style="width:100%;margin-top:10px;background:rgba(103,255,211,.1);border:1px solid rgba(103,255,211,.28);color:#8bf4d3;padding:9px;font-family:var(--head);font-size:11px;border-radius:6px;cursor:pointer;letter-spacing:.06em">РћРўРљР Р«РўР¬ Р­РљР РђРќ РўРђР™РќР«РҐ РњРРЎРЎРР™</button>
    </div>
    <div style="display:grid;grid-template-columns:repeat(2,1fr);gap:10px">`;

  ACHIEVEMENTS.forEach(ach => {
    const has = !!myAchs[ach.id];
    html += `<div style="background:${has ? 'linear-gradient(170deg,#2b240c,#171208)' : 'linear-gradient(170deg,#1a1711,#100e0a)'};
      border:1px solid ${has ? 'rgba(255,224,51,.55)' : 'rgba(255,255,255,.14)'};
      box-shadow:${has ? '0 0 18px rgba(255,224,51,.22)' : '0 0 0 rgba(0,0,0,0)'};
      border-radius:10px;padding:11px;opacity:${has ? '1' : '0.84'}">
      <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:8px;">
        <div style="font-size:24px;filter:${has ? 'drop-shadow(0 0 10px rgba(255,224,51,.45))' : 'none'}">${ach.icon}</div>
        <div style="font-size:9px;color:${has ? '#fff0b0' : '#c8c1a6'};letter-spacing:.05em">${has ? 'вњ… РџРћР›РЈР§Р•РќРћ' : 'рџ”’ РќР• РџРћР›РЈР§Р•РќРћ'}</div>
      </div>
      <div style="font-family:var(--head);font-size:11px;color:${has ? '#ffe680' : '#f1e7c7'};
        letter-spacing:.04em;margin-top:6px">${ach.name}</div>
      <div style="font-size:10px;color:${has ? '#d8cfb0' : '#b6ad90'};margin-top:4px;line-height:1.45">${ach.desc}</div>
      <div style="font-size:10px;color:${has ? 'var(--accent)' : '#d5c99c'};margin-top:7px;font-family:var(--head)">+${ach.pts} РѕС‡РєРѕРІ</div>
      ${Number(ach.reward || 0) > 0 ? `<div style="font-size:9px;color:${has ? '#9dffb7' : '#8eaa8f'};margin-top:4px">РќР°РіСЂР°РґР°: +${Number(ach.reward || 0)} Рє СЃС‡С‘С‚Сѓ</div>` : ''}
    </div>`;
  });

  html += `</div>
    <div style="margin-top:14px;background:linear-gradient(160deg,rgba(130,220,255,.12),rgba(18,16,10,.94));
      border:1px solid rgba(130,220,255,.35);border-radius:12px;padding:14px;margin-bottom:14px;
      box-shadow:0 8px 22px rgba(0,0,0,.42)">
      <div style="display:flex;justify-content:space-between;align-items:center;gap:10px">
        <div style="font-family:var(--head);font-size:12px;color:#d7f0ff;letter-spacing:.08em">рџ—ѓпёЏ РђР РўР•Р¤РђРљРўР«</div>
        <div style="font-family:var(--head);font-size:12px;color:#bfe8ff">${artifactEarned}/${artifactTotal}</div>
      </div>
      <div style="margin-top:10px;background:rgba(0,0,0,.35);border-radius:999px;height:8px;overflow:hidden;">
        <div style="height:100%;width:${artifactPct}%;background:linear-gradient(90deg,#7fc5ff,#d7f0ff);box-shadow:0 0 10px rgba(127,197,255,.8)"></div>
      </div>
      <div style="margin-top:6px;font-size:10px;color:#cfeaff;text-align:right;">${artifactPct}% РєРѕР»Р»РµРєС†РёРё Р°СЂС‚РµС„Р°РєС‚РѕРІ</div>
    </div>
    <div style="display:grid;grid-template-columns:repeat(2,1fr);gap:10px">`;

  ARTIFACTS.forEach(artifact => {
    const has = !!myArtifacts[artifact.id];
    const rarityStyle = _artifactRarityStyle(artifact.rarity);
    const rarityLabel =
      artifact.rarity === 'legendary' ? 'Р›Р•Р“Р•РќР”РђР РќР«Р™' :
      artifact.rarity === 'epic' ? 'Р­РџРР§Р•РЎРљРР™' :
      artifact.rarity === 'rare' ? 'Р Р•Р”РљРР™' : 'РћР‘Р«Р§РќР«Р™';
    html += `<div style="background:${has ? 'linear-gradient(168deg,#1f1a12,#13100b)' : 'linear-gradient(168deg,#171510,#0f0d09)'};
      border:1px solid ${has ? rarityStyle.border : 'rgba(255,255,255,.13)'};
      box-shadow:${has ? rarityStyle.glow : 'none'};
      border-radius:10px;padding:11px;opacity:${has ? '1' : '0.82'}">
      <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:8px">
        <div style="font-size:24px;filter:${has ? 'drop-shadow(0 0 8px rgba(255,255,255,.25))' : 'none'}">${artifact.icon}</div>
        <div style="font-size:9px;color:${has ? rarityStyle.text : '#b8b1a0'};letter-spacing:.05em">${has ? 'вњ… РќРђР™Р”Р•Рќ' : 'рџ”’ РЎРљР Р«Рў'}</div>
      </div>
      <div style="font-family:var(--head);font-size:11px;color:${has ? rarityStyle.text : '#e2dac0'};letter-spacing:.04em;margin-top:6px">${artifact.name}</div>
      <div style="font-size:9px;color:${has ? rarityStyle.text : '#b8b09a'};opacity:.92;margin-top:4px;letter-spacing:.06em">${rarityLabel}</div>
      <div style="font-size:10px;color:${has ? '#d9d0b6' : '#aea58c'};margin-top:5px;line-height:1.45">${artifact.desc}</div>
      ${Number(artifact.reward || 0) > 0 ? `<div style="font-size:9px;color:${has ? '#9dffb7' : '#8eaa8f'};margin-top:4px">РќР°РіСЂР°РґР°: +${Number(artifact.reward || 0)} Рє СЃС‡С‘С‚Сѓ</div>` : ''}
    </div>`;
  });

  html += '</div></div>';
  container.innerHTML = html;
}

function renderSecretMissionsTab() {
  const el = document.getElementById('secret-tab-content');
  if (!el) return;
  ensureSecretState();

  const secretMode = normalizeSecretMode(state.secretMode);
  const secretMeta = _secretGetModeMeta(secretMode);
  const secretStats = _secretMissionStats();
  const missions = secretStats.missions || [];
  const starterRows = missions.filter((m) => m.tier === 'starter');
  const advancedRows = missions.filter((m) => m.tier !== 'starter');
  const starterTotal = starterRows.length;
  const advancedTotal = advancedRows.length;
  const progressPct = secretStats.total > 0
    ? Math.round((secretStats.completed / secretStats.total) * 100)
    : 0;

  const modeCards = SECRET_MODE_ORDER.map((mode) => {
    const meta = _secretGetModeMeta(mode);
    const active = mode === secretMode;
    const isNone = mode === 'none';
    const label = isNone ? 'РћР±С‹С‡РЅС‹Р№' : 'РђРєС‚РёРІРёСЂРѕРІР°С‚СЊ';
    return `<div style="background:${active ? 'rgba(103,255,211,.14)' : 'rgba(255,255,255,.03)'};
      border:1px solid ${active ? 'rgba(103,255,211,.45)' : 'rgba(255,255,255,.12)'};
      border-radius:9px;padding:10px">
      <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:8px">
        <div style="font-family:var(--head);font-size:11px;color:${active ? '#9df7da' : '#e9e0c2'};letter-spacing:.05em">${escapeHtml(meta.title)}</div>
        <div style="font-size:9px;color:${active ? '#9df7da' : '#b5ab8f'}">${meta.bonus_pct > 0 ? '+' + meta.bonus_pct + '%' : '0%'}</div>
      </div>
      <div style="font-size:10px;color:#b7dacd;line-height:1.4;margin-top:4px">${escapeHtml(meta.short || 'Р±РµР· Р±РѕРЅСѓСЃР°')}</div>
      <button onclick="setSecretMode('${mode}')" style="width:100%;margin-top:8px;background:${active ? 'rgba(103,255,211,.22)' : 'rgba(255,224,51,.1)'};border:1px solid ${active ? 'rgba(103,255,211,.45)' : 'rgba(255,224,51,.25)'};color:${active ? '#8bf4d3' : '#ffe8a8'};padding:8px;border-radius:6px;font-family:var(--head);font-size:10px;letter-spacing:.06em;cursor:pointer">${active ? 'РђРљРўРР’Р•Рќ' : label}</button>
    </div>`;
  }).join('');

  const missionCard = (mission) => {
    const done = !!mission.completed;
    const pct = mission.target > 0 ? Math.round((mission.progress / mission.target) * 100) : 0;
    const completedAt = mission.completed_at ? new Date(mission.completed_at) : null;
    const completedAtText = completedAt && Number.isFinite(completedAt.getTime())
      ? completedAt.toLocaleDateString('ru-RU')
      : '';
    const rewardLine = done
      ? `РќР°РіСЂР°РґР° РїРѕР»СѓС‡РµРЅР°: +${Math.max(0, Number(mission.reward_points || 0))} РѕС‡`
      : `РќР°РіСЂР°РґР°: Р±РѕРЅСѓСЃ +${Math.max(0, Number(mission.bonus_pct || 0))}%`;
    return `<div style="background:${done ? 'linear-gradient(165deg,rgba(28,55,46,.95),rgba(12,24,20,.96))' : 'linear-gradient(165deg,rgba(25,22,16,.95),rgba(14,12,9,.95))'};
      border:1px solid ${done ? 'rgba(103,255,211,.45)' : 'rgba(255,255,255,.14)'};
      border-radius:10px;padding:11px">
      <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:10px">
        <div style="font-size:20px;line-height:1">${escapeHtml(mission.icon || 'рџ•¶пёЏ')}</div>
        <div style="flex:1;min-width:0">
          <div style="font-family:var(--head);font-size:11px;color:${done ? '#cffff0' : '#f2e6c3'};letter-spacing:.04em">${escapeHtml(mission.name || 'РўР°Р№РЅР°СЏ РјРёСЃСЃРёСЏ')}</div>
          <div style="font-size:10px;color:${done ? '#bff3df' : '#bcb39a'};line-height:1.45;margin-top:4px">${escapeHtml(mission.desc || '')}</div>
        </div>
        <div style="text-align:right">
          <div style="font-family:var(--head);font-size:11px;color:${done ? '#8ff2d2' : '#ddd2b1'}">${mission.progress}/${mission.target}</div>
          <div style="font-size:9px;color:${done ? '#8ff2d2' : '#9b9278'}">${done ? 'Р“РћРўРћР’Рћ' : 'Р’ РџР РћР¦Р•РЎРЎР•'}</div>
        </div>
      </div>
      <div style="margin-top:8px;height:6px;border-radius:999px;background:rgba(0,0,0,.35);overflow:hidden">
        <div style="height:100%;width:${pct}%;background:${done ? 'linear-gradient(90deg,#67ffd3,#d0fff2)' : 'linear-gradient(90deg,#c9b364,#f4dfa2)'}"></div>
      </div>
      <div style="display:flex;justify-content:space-between;gap:8px;margin-top:6px">
        <div style="font-size:10px;color:${done ? '#9fe8cf' : '#d8cba8'}">${rewardLine}</div>
        <div style="font-size:9px;color:#9cc7ba">${completedAtText}</div>
      </div>
    </div>`;
  };

  el.innerHTML = `
    <div style="padding:18px 14px 22px">
      <div style="background:linear-gradient(160deg,rgba(85,255,210,.16),rgba(11,19,16,.96));
        border:1px solid rgba(103,255,211,.4);border-radius:12px;padding:14px;margin-bottom:12px;
        box-shadow:0 8px 24px rgba(0,0,0,.42)">
        <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:10px">
          <div>
            <div style="font-family:var(--head);font-size:15px;color:#9df7da;letter-spacing:.08em">рџ•¶ Р¦Р•РќРўР  РўРђР™РќР«РҐ РњРРЎРЎРР™</div>
            <div style="font-size:11px;color:#c9f7ea;margin-top:4px">РђРєС‚РёРІРЅС‹Р№ СЂРµР¶РёРј: <b>${escapeHtml(secretMeta.title)}</b> (+${secretMeta.bonus_pct}%)</div>
          </div>
          <div style="font-family:var(--head);font-size:14px;color:#8ff2d2">${secretStats.completed}/${secretStats.total}</div>
        </div>
        <div style="margin-top:10px;background:rgba(0,0,0,.35);border-radius:999px;height:8px;overflow:hidden">
          <div style="height:100%;width:${progressPct}%;background:linear-gradient(90deg,#67ffd3,#d0fff2);box-shadow:0 0 10px rgba(103,255,211,.8)"></div>
        </div>
        <div style="display:flex;justify-content:space-between;gap:8px;margin-top:8px;font-size:10px;color:#bfeee0">
          <span>Р’С‹РїРѕР»РЅРµРЅРѕ: <b>${secretStats.completed}</b></span>
          <span>Р‘РѕРЅСѓСЃ РјРёСЃСЃРёР№: <b>+${secretStats.bonusPoints} РѕС‡</b></span>
        </div>
      </div>

      <div style="background:rgba(255,224,51,.05);border:1px solid rgba(255,224,51,.2);border-radius:10px;padding:12px;margin-bottom:12px">
        <div style="font-family:var(--head);font-size:11px;color:#ffe8a8;letter-spacing:.07em;margin-bottom:8px">РќРћР’Р«Р™ Р Р•Р–РРњ РћРџР•Р РђР¦РР™</div>
        <div style="font-size:10px;color:#d5ccad;line-height:1.45;margin-bottom:10px">
          РўР°Р№РЅС‹Рµ РјРёСЃСЃРёРё РѕС‚Р»РёС‡Р°СЋС‚СЃСЏ РѕС‚ РѕР±С‹С‡РЅС‹С… РіР»Р°РІ: Р·РґРµСЃСЊ РІР°Р¶РЅС‹ СЃС‚РёР»СЊ РїСЂРѕС…РѕР¶РґРµРЅРёСЏ, РґРёСЃС†РёРїР»РёРЅР°, СЃРєРѕСЂРѕСЃС‚СЊ Рё С‚Р°РєС‚РёРєР°.
          Р РµР¶РёРјС‹ РїСЂРёРјРµРЅСЏСЋС‚СЃСЏ РІ Р·Р°РґР°РЅРёСЏС… РіР»Р°РІ Рё РґР°СЋС‚ РѕС‚РґРµР»СЊРЅС‹Рµ Р±РѕРЅСѓСЃС‹.
        </div>
        <div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px">
          ${modeCards}
        </div>
      </div>

      <div style="display:flex;gap:8px;margin-bottom:8px">
        <div style="flex:1;background:rgba(103,255,211,.08);border:1px solid rgba(103,255,211,.18);border-radius:8px;padding:8px;text-align:center">
          <div style="font-family:var(--head);font-size:12px;color:#a9ffe5">${secretStats.completedStarter}/${starterTotal || 0}</div>
          <div style="font-size:9px;color:#9ed8c8">РЎРўРђР РўРћР’Р«Р•</div>
        </div>
        <div style="flex:1;background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.18);border-radius:8px;padding:8px;text-align:center">
          <div style="font-family:var(--head);font-size:12px;color:#f2e5c0">${secretStats.completedAdvanced}/${advancedTotal || 0}</div>
          <div style="font-size:9px;color:#b6ac90">РџР РћР”Р’РРќРЈРўР«Р•</div>
        </div>
      </div>

      <div style="font-family:var(--head);font-size:10px;color:#8ff2d2;letter-spacing:.07em;margin:10px 0 7px">РЎРўРђР РўРћР’Р«Р• РњРРЎРЎРР</div>
      <div style="display:grid;gap:8px">
        ${starterRows.length ? starterRows.map(missionCard).join('') : '<div style="font-size:10px;color:#9cc7ba;font-style:italic">РњРёСЃСЃРёРё РїРѕСЏРІСЏС‚СЃСЏ РїРѕСЃР»Рµ СЃРёРЅС…СЂРѕРЅРёР·Р°С†РёРё.</div>'}
      </div>

      <div style="font-family:var(--head);font-size:10px;color:#b8e0d2;letter-spacing:.07em;margin:12px 0 7px">РџР РћР”Р’РРќРЈРўР«Р• РњРРЎРЎРР</div>
      <div style="display:grid;gap:8px">
        ${advancedRows.length ? advancedRows.map(missionCard).join('') : '<div style="font-size:10px;color:#9cc7ba;font-style:italic">РњРёСЃСЃРёРё РїРѕСЏРІСЏС‚СЃСЏ РїРѕСЃР»Рµ СЃРёРЅС…СЂРѕРЅРёР·Р°С†РёРё.</div>'}
      </div>

      <div style="margin-top:10px;font-size:10px;color:#9cc7ba;line-height:1.45">
        РђРЅС‚РёР°Р±СѓР·: РїСЂРѕРіСЂРµСЃСЃ СЃРєРѕСЂРѕСЃС‚РЅС‹С… СЃРµСЂРёР№ РѕР±РЅСѓР»СЏРµС‚СЃСЏ РїСЂРё РѕС€РёР±РєРµ, РїРѕРґСЃРєР°Р·РєРµ, РїР°СѓР·Рµ РёР»Рё РІС‹С…РѕРґРµ РёР· РіР»Р°РІС‹.
      </div>
      <button onclick="switchTab('chapters')" style="width:100%;margin-top:10px;background:rgba(255,224,51,.15);border:1px solid rgba(255,224,51,.35);color:#ffe8a8;padding:10px;border-radius:7px;font-family:var(--head);font-size:11px;letter-spacing:.07em;cursor:pointer">РџР•Р Р•Р™РўР Рљ Р“Р›РђР’РђРњ</button>
    </div>
  `;
}

function renderSettingsTab() {
  const el = document.getElementById('settings-tab-content');
  if (!el) return;
  ensureSecretState();
  const musicOn = musicEnabled;
  const sfxOn = sfxEnabled;
  const musicPct = Math.round(musicVolume * 100);
  const sfxPct = Math.round(sfxVolume * 100);
  const currentTrack = getCurrentMusicTrack();
  const trackOptions = GAME_MUSIC_TRACKS.map((track, idx) =>
    `<option value="${idx}" ${idx === musicTrackIndex ? 'selected' : ''}>${track.title}</option>`
  ).join('');
  const secretMode = normalizeSecretMode(state.secretMode);
  const secretMeta = _secretGetModeMeta(secretMode);
  const secretSummary = normalizeSecretSummary(state.secretSummary);
  const secretOptions = SECRET_MODE_ORDER.map((mode) => {
    const meta = _secretGetModeMeta(mode);
    const bonus = meta.bonus_pct > 0 ? ` +${meta.bonus_pct}%` : '';
    return `<option value="${mode}" ${mode === secretMode ? 'selected' : ''}>${meta.title}${bonus}</option>`;
  }).join('');

  el.innerHTML = `
    <div style="padding:20px 16px">
      <div style="font-family:var(--head);font-size:var(--fs-xl);color:var(--accent);letter-spacing:.06em;margin-bottom:16px;">вљ™пёЏ РќРђРЎРўР РћР™РљР</div>

      <div class="settings-card">
        <div class="settings-card-title">рџЋµ Р¤РћРќРћР’РђРЇ РњРЈР—Р«РљРђ</div>
        <div class="settings-inline">
          <button onclick="toggleMusicEnabled();renderSettingsTab();" class="btn btn-small" style="width:auto;padding:8px 18px;margin:0;">
            ${musicOn ? 'рџ”Љ Р’РљР›' : 'рџ”‡ Р’Р«РљР›'}
          </button>
          <div style="font-size:11px;color:#dfd5b3;line-height:1.4">
            РўРµРєСѓС‰РёР№ С‚СЂРµРє:<br><span style="color:#fff0bf">${currentTrack ? currentTrack.title : '-'}</span>
          </div>
        </div>
        <div class="settings-caption">Р’СЃС‚СЂРѕРµРЅРѕ 5 Р±РµСЃРїР»Р°С‚РЅС‹С… С‚СЂРµРєРѕРІ (royalty-free).</div>
        <select class="settings-select" onchange="setMusicTrack(this.value);renderSettingsTab();">
          ${trackOptions}
        </select>
        <div class="settings-inline" style="margin-top:10px">
          <div style="font-size:12px;color:#f5ebc7">Р“СЂРѕРјРєРѕСЃС‚СЊ РјСѓР·С‹РєРё</div>
          <div id="music-vol-value" style="font-family:var(--head);font-size:12px;color:var(--accent)">${musicPct}%</div>
        </div>
        <input id="music-volume-range" class="settings-range" type="range" min="0" max="100" step="1" value="${musicPct}" oninput="setMusicVolume(this.value)" onchange="setMusicVolume(this.value)">
      </div>

      <div class="settings-card">
        <div class="settings-card-title">рџ”‰ Р—Р’РЈРљРћР’Р«Р• Р­Р¤Р¤Р•РљРўР«</div>
        <div class="settings-inline">
          <button onclick="toggleSfxEnabled();renderSettingsTab();" class="btn btn-small" style="width:auto;padding:8px 18px;margin:0;">
            ${sfxOn ? 'рџ”Љ Р’РљР›' : 'рџ”‡ Р’Р«РљР›'}
          </button>
          <div style="font-size:11px;color:#dfd5b3;line-height:1.4">
            РЎРѕР±С‹С‚РёСЏ:<br><span style="color:#fff0bf">РїРѕР±РµРґР° / РїРѕСЂР°Р¶РµРЅРёРµ / РїРѕС‚РµСЂСЏ Р¶РёР·РЅРё</span>
          </div>
        </div>
        <div class="settings-inline" style="margin-top:10px">
          <div style="font-size:12px;color:#f5ebc7">Р“СЂРѕРјРєРѕСЃС‚СЊ СЌС„С„РµРєС‚РѕРІ</div>
          <div id="sfx-vol-value" style="font-family:var(--head);font-size:12px;color:var(--accent)">${sfxPct}%</div>
        </div>
        <input id="sfx-volume-range" class="settings-range" type="range" min="0" max="100" step="1" value="${sfxPct}" oninput="setSfxVolume(this.value)" onchange="setSfxVolume(this.value)">
        <div class="settings-caption">Р­С„С„РµРєС‚С‹ СЂРµРіСѓР»РёСЂСѓСЋС‚СЃСЏ РѕС‚РґРµР»СЊРЅРѕ РѕС‚ С„РѕРЅРѕРІРѕР№ РјСѓР·С‹РєРё.</div>
      </div>

      <div class="settings-card">
        <div class="settings-card-title">рџ•¶ РўРРҐРР™ Р Р•Р–РРњ В· РўРђР™РќР«Р• РњРРЎРЎРР</div>
        <div class="settings-caption" style="margin-top:0">
          Р’С‹Р±РµСЂРё СЃРїРµС†РёР°Р»СЊРЅС‹Р№ СЂРµР¶РёРј. РџСЂРё РІС‹РїРѕР»РЅРµРЅРёРё СѓСЃР»РѕРІРёР№ РјРёСЃСЃРёРё РЅР°С‡РёСЃР»СЏРµС‚СЃСЏ Р±РѕРЅСѓСЃ Рє РѕС‡РєР°Рј.
        </div>
        <select class="settings-select" onchange="setSecretMode(this.value);">
          ${secretOptions}
        </select>
        <div style="margin-top:8px;padding:9px 10px;border-radius:8px;background:rgba(103,255,211,.08);border:1px solid rgba(103,255,211,.24)">
          <div style="font-family:var(--head);font-size:11px;color:#8bf4d3;letter-spacing:.07em">РђРљРўРР’РќР«Р™ Р Р•Р–РРњ</div>
          <div style="font-size:13px;color:#d5fff3;margin-top:4px">${secretMeta.title} В· Р±РѕРЅСѓСЃ +${secretMeta.bonus_pct}%</div>
          <div style="font-size:11px;color:#9bd8c7;margin-top:3px">${secretMeta.short}</div>
        </div>
        <div class="settings-caption" style="margin-top:10px">
          РџСЂРѕРіСЂРµСЃСЃ С‚Р°Р№РЅС‹С… РјРёСЃСЃРёР№: <b>${secretSummary.completed}/${secretSummary.total}</b> В· Р±РѕРЅСѓСЃ РїРѕР»СѓС‡РµРЅ: <b>+${secretSummary.bonus_points} РѕС‡</b>.
        </div>
        <div class="settings-caption" style="margin-top:6px">
          РђРЅС‚РёР°Р±СѓР·: СЃРµСЂРёСЏ СЂРµР¶РёРјР° В«РЎРєРѕСЂРѕСЃС‚РЅРѕР№ СЂР°РґРёСЃС‚В» СЃР±СЂР°СЃС‹РІР°РµС‚СЃСЏ РїСЂРё РѕС€РёР±РєРµ, РїРѕРґСЃРєР°Р·РєРµ, РїР°СѓР·Рµ РёР»Рё РІС‹С…РѕРґРµ РёР· РіР»Р°РІС‹.
        </div>
      </div>

      <div class="settings-card">
        <div class="settings-card-title">рџ§№ РћР§РРЎРўРљРђ РљР•РЁРђ</div>
        <div class="settings-caption" style="margin-top:0;margin-bottom:10px;">РћС‡РёС‰Р°РµС‚ Р»РѕРєР°Р»СЊРЅС‹Р№ РєРµС€ РёРіСЂС‹ РґР»СЏ СЌС‚РѕРіРѕ СѓСЃС‚СЂРѕР№СЃС‚РІР° РІРѕ РІСЃРµС… СЂРµР¶РёРјР°С… (player/tester/admin).</div>
        <button onclick="confirmCacheReset();" class="btn" style="width:auto;padding:8px 16px;">рџ§№ РћР§РРЎРўРљРђ РљР•РЁРђ</button>
      </div>
      ${(state.adminMode || window._adminMode) ? `
      <div class="settings-card">
        <div class="settings-card-title">рџ—‘ РЎР‘Р РћРЎ РўР•РЎРўРћР’РћР“Рћ РџР РћР“Р Р•РЎРЎРђ</div>
        <div class="settings-caption" style="margin-top:0;margin-bottom:10px;">РџРѕР»РЅС‹Р№ СЃР±СЂРѕСЃ РёРіСЂРѕРІРѕРіРѕ РїСЂРѕРіСЂРµСЃСЃР° РІ Р‘Р” РґРѕСЃС‚СѓРїРµРЅ С‚РѕР»СЊРєРѕ Р°РґРјРёРЅРёСЃС‚СЂР°С‚РѕСЂСѓ.</div>
        <button onclick="confirmReset();" class="btn btn-danger" style="width:auto;padding:8px 20px;">рџ—‘ РЎР‘Р РћРЎРРўР¬ РџР РћР“Р Р•РЎРЎ</button>
      </div>` : ''}
    </div>`;
  syncMusicButtons();
}
window.showScreen = showScreen;
window.switchTab = switchTab;
window.renderChapters = renderChapters;
window.renderSecretMissionsTab = renderSecretMissionsTab;
window.onBriefStartClicked = onBriefStartClicked;
window.onBriefResumeClicked = onBriefResumeClicked;
window.onBriefRestartClicked = onBriefRestartClicked;
window.resumeChapter = resumeChapter;
window.toggleMusicEnabled = toggleMusicEnabled;
window.toggleSfxEnabled = toggleSfxEnabled;
window.setMusicTrack = setMusicTrack;
window.setMusicVolume = setMusicVolume;
window.setSfxVolume = setSfxVolume;
window.setSecretMode = setSecretMode;
window.cancelCipherTimer = cancelCipherTimer;
window.confirmCacheReset = confirmCacheReset;
window.openBugReportModal = openBugReportModal;
window.closeBugReportModal = closeBugReportModal;
window.contactGameAdmin = contactGameAdmin;
window.openRestartCostModal = openRestartCostModal;
window.closeRestartCostModal = closeRestartCostModal;
window.restartCostContinueFree = restartCostContinueFree;
window.restartCostConfirmRestart = restartCostConfirmRestart;
window.checkAnswer = checkAnswer;
window.nextCipher = nextCipher;
window.showHint = showHint;
window.copyChapterReportCard = copyChapterReportCard;
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
//  INIT
// в•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђв•ђ
if (document.readyState === "loading") { showSplash(); } else { showSplash(); }
loadState();
if (tg && typeof tg.onEvent === 'function') {
  try {
    tg.onEvent('deactivated', () => {
      saveState();
      autoSync(false, true);
      _sendSyncBeaconIfPossible();
    });
    tg.onEvent('activated', () => {
      fetchAndApplyState();
    });
  } catch (e) {}
}
syncMusicButtons();
applyBackgroundMusic(false);
const _cipherInput = document.getElementById('cipher-input');
if (_cipherInput) {
  _cipherInput.addEventListener('focus', ensureCipherInputKeyboard);
  _cipherInput.addEventListener('click', ensureCipherInputKeyboard);
  _cipherInput.addEventListener('touchstart', ensureCipherInputKeyboard, { passive: true });
}

// РџСЂРёРјРµРЅСЏРµРј СЂРѕР»СЊ РёР· window-С„Р»Р°РіРѕРІ Р”Рћ mergeBotLeaderboard Рё renderChapters.
// Р­С‚Рѕ РіР°СЂР°РЅС‚РёСЂСѓРµС‚ С‡С‚Рѕ adminMode/testerMode РїСЂР°РІРёР»СЊРЅС‹ РґР°Р¶Рµ РµСЃР»Рё tgInitMe = null.
// window._adminMode / _testerMode / _gameRole СѓСЃС‚Р°РЅРѕРІР»РµРЅС‹ РїСЂРё РїР°СЂСЃРёРЅРіРµ startParam.
(function _applyRoleFromWindow() {
  if (window._adminMode === true) {
    state.adminMode  = true;
    state.testerMode = false;
    state.gameRole   = 'admin';
  } else if (window._testerMode === true) {
    state.adminMode  = false;
    state.testerMode = true;
    state.gameRole   = 'tester';
  } else {
    // Р РѕР»СЊ player РёР»Рё РЅРµ РѕРїСЂРµРґРµР»РµРЅР° вЂ” СЃР±СЂР°СЃС‹РІР°РµРј РІСЃС‘
    state.adminMode  = false;
    state.testerMode = false;
    state.gameRole   = window._gameRole || 'player';
  }
  // РћР±РЅРѕРІР»СЏРµРј tgOpenChapters РёР· startParam 'open'
  // (tgOpenChapters СѓР¶Рµ РјРѕРі Р±С‹С‚СЊ СѓСЃС‚Р°РЅРѕРІР»РµРЅ РїСЂРё РїР°СЂСЃРёРЅРіРµ РІС‹С€Рµ)
})();

mergeBotLeaderboard();

async function fetchAndApplyState() {
  const uid = getTgUserId();
  const syncUrl = window._syncUrl;
  if (!uid || !syncUrl) return;
  try {
    const base = syncUrl.replace('/game_sync', '');
    const initDataRaw = getTgInitDataRaw();
    let stateUrl = base + '/game_state?user_id=' + encodeURIComponent(uid);
    if (initDataRaw) stateUrl += '&init_data=' + encodeURIComponent(initDataRaw);

    const resp = await fetch(stateUrl);
    const data = await resp.json().catch(() => null);
    if (!data || typeof data !== 'object') return;

    if (data.allowed === false) {
      if (data.access_reason === 'maintenance') {
        document.body.innerHTML = '<div style="display:flex;flex-direction:column;align-items:center;justify-content:center;height:100vh;background:#0d0b08;color:#fff;text-align:center;padding:32px;font-family:sans-serif"><div style="font-size:64px;margin-bottom:16px">рџ› пёЏ</div><div style="font-size:22px;font-weight:700;color:#ffe033;margin-bottom:12px">Game is under maintenance</div><div style="font-size:15px;color:rgba(255,255,255,.7);max-width:360px;line-height:1.5">Access is temporarily limited.</div>' + (data.maintenance_until ? '<div style="font-size:13px;color:rgba(255,255,255,.55);margin-top:14px">Until: ' + String(data.maintenance_until) + '</div>' : '') + '</div>';
      } else {
        document.body.innerHTML = '<div style="display:flex;flex-direction:column;align-items:center;justify-content:center;height:100vh;background:#0d0b08;color:#fff;text-align:center;padding:32px;font-family:sans-serif"><div style="font-size:64px;margin-bottom:16px">рџљ«</div><div style="font-size:22px;font-weight:700;color:#ffe033;margin-bottom:12px">Access denied</div><div style="font-size:15px;color:rgba(255,255,255,.7);max-width:320px">Open the game from Telegram button after access is granted.</div></div>';
      }
      return;
    }

    if (!resp.ok || !data.ok) return;
    if (data.banned) {
      document.body.innerHTML = '<div style="display:flex;flex-direction:column;align-items:center;justify-content:center;height:100vh;background:#0d0b08;color:#fff;text-align:center;padding:32px;font-family:sans-serif"><div style="font-size:64px;margin-bottom:16px">рџљ«</div><div style="font-size:22px;font-weight:700;color:#ffe033;margin-bottom:12px">Access blocked</div><div style="font-size:15px;color:rgba(255,255,255,.6);max-width:280px">Your account was blocked by administrator.</div></div>';
      try { localStorage.removeItem(storageKey()); } catch(e2) {}
      return;
    }

    if (data.admin_mode === true) {
      state.adminMode = true;
      state.testerMode = false;
    } else if (data.tester_mode === true) {
      state.adminMode = false;
      state.testerMode = true;
    } else {
      state.adminMode = false;
      state.testerMode = false;
    }
    if (data.role) state.gameRole = data.role;
    const refSummary = normalizeReferralSummary(data.ref_summary || (tgInitMe && tgInitMe.ref_summary) || state.referralSummary);
    const refAgents = normalizeReferralAgents(data.ref_agents || (tgInitMe && tgInitMe.ref_agents) || state.referralAgents);
    state.referralSummary = refSummary;
    state.referralAgents = refAgents;
    const secretChanged = _applySecretStateFromServer(
      data.secret_mode,
      data.secret_summary,
      data.secret_missions
    );

    if (Array.isArray(data.open_chapters)) {
      let chaptersToOpen = data.open_chapters;
      if (chaptersToOpen.length === 0 && !data.admin_mode && !data.tester_mode) {
        chaptersToOpen = [1];
      }
      tgOpenChapters = new Set(chaptersToOpen);
    }
    if (Array.isArray(data.chapter_schedule)) {
      tgChapterSchedule = data.chapter_schedule;
    }

    const prevResetToken = _getStoredResetToken();
    if (typeof data.reset_token === 'number' && data.reset_token > 0) {
      _serverResetToken = data.reset_token;
      _storeResetToken(_serverResetToken);
    }

    tgInitMe = Object.assign({}, (tgInitMe || {}), {
      uid: String(uid),
      score: Number(data.score || 0),
      completed: Number(data.completed || 0),
      game_over: !!data.game_over,
      role: data.role || state.gameRole || 'player',
      admin_mode: !!data.admin_mode,
      tester_mode: !!data.tester_mode,
      restart_mode: data.restart_mode || null,
      reset_token: _serverResetToken || 0,
      banned: !!data.banned,
      ref_summary: refSummary,
      ref_agents: refAgents,
      secret_mode: state.secretMode,
      secret_summary: state.secretSummary,
      secret_missions: state.secretMissions,
    });

    const srvScore = typeof data.score === 'number' ? data.score : state.totalScore;
    const srvCompleted = typeof data.completed === 'number' ? data.completed : _stateCompletedCount();
    const tokenChanged = (_serverResetToken > 0) && (
      prevResetToken <= 0 ? _hasLocalProgressData() : (_serverResetToken !== prevResetToken)
    );
    const forcedByTokenReset = (
      tokenChanged &&
      Number(srvScore || 0) === 0 &&
      Number(srvCompleted || 0) === 0 &&
      _hasLocalProgressData()
    );

    const restartMode = (data.restart_mode === 'penalty' || data.restart_mode === 'nopts')
      ? data.restart_mode
      : null;
    const canApplyRestartMode = !!restartMode && _canApplyServerResetNow();

    if (canApplyRestartMode) {
      state.totalScore = 0;
      state.completedChapters = {};
      state.chapterScores = {};
      state.chapterStats = {};
      state.chapterFailCounts = {};
      state.chapter = 0;
      state.cipherIdx = 0;
      state.chapterScore = 0;
      state.lives = state.adminMode ? Infinity : 5;
      state._chapterInProgress = false;
      state._awaitingNextCipher = false;
      state._pendingChapterFinish = false;
      state._nextCipherTargetIdx = null;
      state._manualRestartPenaltyPoints = 0;
      state.gameOver = false;
      state.retryPenalty = restartMode === 'penalty';
      state._noptsMode = restartMode === 'nopts';
      state.achievements = {};
      state.achievementPts = 0;
      saveState();
    } else if (forcedByTokenReset) {
      _wipeLocalProgressForServerReset();
      saveState();
    } else {
      const mergeResult = _applyServerProgress(srvScore, srvCompleted, !!data.game_over, forcedByTokenReset);
      if (!mergeResult.applied) {
        // Server is behind local cache; push local snapshot to backend.
        autoSync(false);
      } else {
        saveState();
      }
    }
    try {
      checkAchievements({});
    } catch (achErr) {
      console.warn('fetchAndApplyState achievements update skipped', achErr);
    }
    if (secretChanged) {
      _refreshCurrentTabAfterSync();
    }

    _lastSyncedScore = Number(state.totalScore || 0);
    _lastSyncedCompleted = _stateCompletedCount();
    _lastSyncedSignature = [
      Number(state.totalScore || 0),
      _lastSyncedCompleted,
      _stateCurrentChapterId(),
      Number(state.chapterScore || 0),
      Number(state.cipherIdx || 0),
      Number(state.lives || 0),
      state.gameOver ? 1 : 0,
      Object.keys(state.achievements || {}).length,
      Number(state.achievementPts || 0),
      state.secretMode,
      normalizeSecretSummary(state.secretSummary).completed,
      normalizeSecretSummary(state.secretSummary).bonus_points
    ].join('|');
    _refreshCurrentTabAfterSync();
  } catch (e) {
    console.warn('fetchAndApplyState:', e);
  }
}

let _lbSyncInFlight = false;
async function fetchAndApplyLeaderboard() {
  const uid = getTgUserId();
  const syncUrl = window._syncUrl;
  if (!uid || !syncUrl || _lbSyncInFlight) return;
  _lbSyncInFlight = true;
  try {
    const base = syncUrl.replace('/game_sync', '');
    const initDataRaw = getTgInitDataRaw();
    let lbUrl = base + '/game_leaderboard?user_id=' + encodeURIComponent(uid);
    if (initDataRaw) lbUrl += '&init_data=' + encodeURIComponent(initDataRaw);
    const resp = await fetch(lbUrl);
    const data = await resp.json().catch(() => null);
    if (!resp.ok || !data || !data.ok || !Array.isArray(data.leaderboard)) return;

    state.leaderboard = data.leaderboard.map(r => ({
      uid: String(r.uid),
      name: r.name || 'РРіСЂРѕРє',
      score: Number(r.score || 0),
      completed: Number(r.completed || 0),
      role: r.role || 'player',
      achievementCount: Number(r.achievementCount || 0),
      achievementPts: Number(r.achievementPts || 0),
    }));
    tgInitLB = state.leaderboard.slice();
    if (currentTab === 'leaderboard') renderLeaderboardTab();
  } catch (e) {
    console.warn('fetchAndApplyLeaderboard:', e);
  } finally {
    _lbSyncInFlight = false;
  }
}

renderChapters();
fetchAndApplyState();
fetchAndApplyLeaderboard();
flushPendingResults();
try {
} catch(e) {}

