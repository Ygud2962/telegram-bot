import re
import logging
import asyncio
from datetime import datetime, timedelta
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import Application, CommandHandler, CallbackQueryHandler, MessageHandler, filters, CallbackContext
from telegram.error import TimedOut, NetworkError, BadRequest, Forbidden
import database as db
import os
import pytz  # pip install pytz

TOKEN = os.environ.get('BOT_TOKEN')
if not TOKEN:
    print("ОШИБКА: Токен не найден! Установите переменную окружения BOT_TOKEN")
    exit(1)

print("Бот запускается с токеном из переменных окружения")

logging.basicConfig(format='%(asctime)s - %(name)s - %(levelname)s - %(message)s', level=logging.INFO)
logger = logging.getLogger(__name__)

#================== НАСТРОЙКИ ==================
ADMIN_IDS = [516406248]
REQUEST_TIMEOUT = 30.0
TEACHER_IDS = {
    'Коротчикова Л.В.': 1665763898,
    'Вергейчик В.Л.': 1970780275,
    'Нечаева Е.Я.': 0,
    'Лебедевская М.П.': 0,
    'Сивый А.В.': 0,
    'Пинчук А.В.': 0,
    'Юнах Т.В.': 0,
    'Бельский С.В.': 0,
    'Назаренко Т.К.': 0,
    'Брель Ю.В.': 0,
    'Шатило Н.И.': 0,
    'Ковальчук К.Р.': 0,
    'Тихоненко О.А.': 0,
    'Мележ А.Г.': 0,
    'Михасёв В.А.': 0,
    'Прихач Е.В.': 967925839,
    'Михалко Е.П.': 0,
    'Явош С.В.': 0,
    'Гапоненко Ю.В.': 0,
    'Приходько И.Н.': 0,
    'Гуд Ю.П.': 516406248,
    'Смольский С.М.': 0,
    'Стишенок Н.И.': 0,
    'Анисковец Н.В.': 0,
    'Анисенко С.В.': 0,
    'Королёва Ж.В.': 0,
    'Грицкевич Л.В.': 0,
    'Даниленко А.А.': 0,
    'Зубок С.Н.': 0,
    'Канунова В.Г.': 0,
    'Корольчук О.Г.': 0,
    'Леонова Д.А.': 0,
    'Лукьяненко Л.И.': 0,
    'Штыхнова Л.Г.': 0,
    'Артюх М.П.': 0,
    'Хорошко Т.А.': 0
}
ALL_CLASSES = ['5а', '5б', '5в', '6а', '6б', '6в', '7а', '7б', '7в', '8а', '8б', '9а', '9б', '10а', '10б', '11']
SUBJECTS = [
    "Математика", "Математика(проф.)", "Русский язык", "Русский язык(проф.)",
    "Белорусский язык", "Белорусский язык(проф.)", "Русская литература",
    "Белорусская литература", "Физика", "Информатика",
    "Английский язык", "Английский язык(проф.)", "Химия", "Химия(проф.)",
    "Биология", "Биология(проф.)", "Обществоведение", "Обществоведение(проф.)",
    "История", "География", "Физкультура", "ОБЖ", "Трудовое обучение",
    "Искусство", "Астрономия", "ЧЗС", "Черчение", "ДП", "МП", "Человек и мир"
]

# 🔑 КЛЮЧЕВОЕ ИЗМЕНЕНИЕ: УБРАНЫ СУББОТА И ВОСКРЕСЕНЬЕ
DAYS_OF_WEEK = ["Понедельник", "Вторник", "Среда", "Четверг", "Пятница"]  # Только будние дни!

BELLS_SCHEDULE_HTML = """
🕐 РАСПИСАНИЕ ЗВОНКОВ И ПИТАНИЯ
─────────────────────────────────
🔔 1 урок:    08:00 – 08:45
🔔 2 урок:    09:00 – 09:45
🔔 3 урок:    10:00 – 10:45
🍽️ Обед для 5-7 классов
─────────────────────────────────
🔔 4 урок:    11:00 – 11:45
🍽️ Обед для 8-11 классов
─────────────────────────────────
🔔 5 урок:    12:00 – 12:45
🔔 6 урок:    12:55 – 13:40
🍎 Полдник для всех
─────────────────────────────────
🔔 7 урок:    14:00 – 14:45
─────────────────────────────────
"""

#================== СТРУКТУРИРОВАННОЕ РАСПИСАНИЕ ==================
# ⚠️ ВАЖНО: В реальном проекте здесь должен быть ПОЛНЫЙ словарь расписания для всех классов БЕЗ СУББОТЫ!
SCHEDULE_STRUCTURED = {
    '5а': {
        'Понедельник': [
            (1, 'Математика', 'Коротчикова Л.В.'),
            (2, 'Физкультура', 'Вергейчик В.Л.'),
            (3, 'Английский язык', 'Нечаева Е.Я./Приходько И.Н.'),
            (4, 'Белорусский язык', 'Лебедевская М.П.'),
            (5, 'История', 'Сивый А.В.'),
        ],
        'Вторник': [
            (1, 'Рус. литература', 'Пинчук А.В.'),
            (2, 'Математика', 'Коротчикова Л.В.'),
            (3, 'Русский язык', 'Пинчук А.В.'),
            (4, 'Физкультура', 'Вергейчик В.Л.'),
            (5, 'Английский язык', 'Нечаева Е.Я./Приходько И.Н.')
        ],
        'Среда': [
            (1, 'ОБЖ', 'Юнах Т.В.'),
            (2, 'Русский язык', 'Пинчук А.В.'),
            (3, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (4, 'Математика', 'Коротчикова Л.В.'),
            (5, 'Английский язык', 'Нечаева Е.Я./Приходько И.Н.'),
            (6, 'Английский язык', 'Приходько И.Н.')
        ],
        'Четверг': [
            (1, 'Бел. литература', 'Лебедевская М.П.'),
            (2, 'Русский язык', 'Пинчук А.В.'),
            (3, 'Белорусский язык', 'Лебедевская М.П.'),
            (4, 'Математика', 'Коротчикова Л.В.'),
            (5, 'История', 'Сивый А.В.')
        ],
        'Пятница': [
            (1, 'Рус. литература', 'Пинчук А.В.'),
            (2, 'Белорусский язык', 'Лебедевская М.П.'),
            (3, 'Искусство', 'Бельский С.В.'),
            (4, 'Математика', 'Коротчикова Л.В.'),
            (5, 'Человек и мир', 'Бельский С.В.'),
            (6, 'Физкультура', 'Вергейчик В.Л.')
        ]
    },
    '5б': {
        'Понедельник': [
            (1, 'Математика', 'Назаренко Т.К.'),
            (2, 'Русский язык', 'Шатило Н.И.'),
            (3, 'Английский язык', 'Ковальчук К.Р./Тихоненко О.А.'),
            (4, 'Физкультура', 'Вергейчик В.Л.'),
            (5, 'ОБЖ', 'Юнах Т.В.')
        ],
        'Вторник': [
            (1, 'Бел. литература', 'Брель Ю.В.'),
            (2, 'Белорусский язык', 'Брель Ю.В.'),
            (3, 'Математика', 'Назаренко Т.К.'),
            (4, 'Английский язык', 'Ковальчук К.Р./Тихоненко О.А.'),
            (5, 'Рус. литература', 'Шатило Н.И.'),
            (6, 'Человек и мир', 'Бельский С.В.')
        ],
        'Среда': [
            (1, 'Математика', 'Назаренко Т.К.'),
            (2, 'Физкультура', 'Вергейчик В.Л.'),
            (3, 'Русский язык', 'Шатило Н.И.'),
            (4, 'Искусство', 'Бельский С.В.'),
            (5, 'История', 'Сивый А.В.')
        ],
        'Четверг': [
            (1, 'Математика', 'Назаренко Т.К.'),
            (2, 'Английский язык', 'Ковальчук К.Р./Тихоненко О.А.'),
            (3, 'Русский язык', 'Шатило Н.И.'),
            (4, 'Рус. литература', 'Шатило Н.И.'),
            (5, 'Белорусский язык', 'Брель Ю.В.'),
            (6, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.')
        ],
        'Пятница': [
            (1, 'Физкультура', 'Вергейчик В.Л.'),
            (2, 'Математика', 'Назаренко Т.К.'),
            (3, 'История', 'Сивый А.В.'),
            (4, 'Белорусский язык', 'Брель Ю.В.'),
            (5, 'Бел. литература', 'Брель Ю.В.')
        ]
    },
    '5в': {
        'Понедельник': [
            (1, 'Русский язык', 'Михалко Е.П.'),
            (2, 'Математика', 'Прихач Е.В.'),
            (3, 'История', 'Сивый А.В.'),
            (4, 'Английский язык', 'Ковальчук К.Р./Тихоненко О.А.'),
            (5, 'Физкультура', 'Явош С.В.'),
            (6, 'Рус. литература', 'Михалко Е.П.')
        ],
        'Вторник': [
            (1, 'Физкультура', 'Явош С.В.'),
            (2, 'Русский язык', 'Михалко Е.П.'),
            (3, 'Математика', 'Прихач Е.В.'),
            (4, 'Человек и мир', 'Бельский С.В.'),
            (5, 'Рус. литература', 'Михалко Е.П.')
        ],
        'Среда': [
            (1, 'Бел. литература', 'Гапоненко Ю.В.'),
            (2, 'Английский язык', 'Ковальчук К.Р./Тихоненко О.А.'),
            (3, 'Математика', 'Прихач Е.В.'),
            (4, 'Белорусский язык', 'Гапоненко Ю.В.'),
            (5, 'Искусство', 'Бельский С.В.')
        ],
        'Четверг': [
            (1, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (2, 'Математика', 'Прихач Е.В.'),
            (3, 'Русский язык', 'Михалко Е.П.'),
            (4, 'Английский язык', 'Ковальчук К.Р./Тихоненко О.А.'),
            (5, 'Белорусский язык', 'Гапоненко Ю.В.'),
            (6, 'Бел. литература', 'Гапоненко Ю.В.')
        ],
        'Пятница': [
            (1, 'ОБЖ', 'Юнах Т.В.'),
            (2, 'Математика', 'Прихач Е.В.'),
            (3, 'Физкультура', 'Явош С.В.'),
            (4, 'История', 'Сивый А.В.'),
            (5, 'Белорусский язык', 'Гапоненко Ю.В.')
        ]
    },
    '6а': {
        'Понедельник': [
            (1, 'Математика', 'Прихач Е.В.'),
            (2, 'Физкультура', 'Явош С.В.'),
            (3, 'Белорусский язык', 'Гапоненко Ю.В.'),
            (4, 'История', 'Сивый А.В.'),
            (5, 'Английский язык', 'Приходько И.Н./Тихоненко О.А.'),
            (6, 'Трудовое обучение', 'Стишенок Н.И./Михасёв В.А.')
        ],
        'Вторник': [
            (1, 'Английский язык', 'Приходько И.Н./Тихоненко О.А.'),
            (2, 'Математика', 'Прихач Е.В.'),
            (3, 'Бел. литература', 'Гапоненко Ю.В.'),
            (4, 'Русский язык', 'Михалко Е.П.'),
            (5, 'Белорусский язык', 'Гапоненко Ю.В.'),
            (6, 'Рус. литература', 'Михалко Е.П.')
        ],
        'Среда': [
            (1, 'Физкультура', 'Явош С.В.'),
            (2, 'Русский язык', 'Михалко Е.П.'),
            (3, 'География', 'Юнах Т.В.'),
            (4, 'Английский язык', 'Приходько И.Н./Тихоненко О.А.'),
            (5, 'Математика', 'Прихач Е.В.'),
            (6, 'Русская литература', 'Михалко Е.П.')
        ],
        'Четверг': [
            (1, 'Математика', 'Прихач Е.В.'),
            (2, 'Искусство', 'Мележ А.Г.'),
            (3, 'Информатика', 'Гуд Ю.П./Прихач Е.В.'),
            (4, 'Бел. литература', 'Гапоненко Ю.В.'),
            (5, 'Биология', 'Даниленко А.А.')
        ],
        'Пятница': [
            (1, 'Русский язык', 'Михалко Е.П.'),
            (2, 'История', 'Сивый А.В.'),
            (3, 'Трудовое обучение', 'Стишенок Н.И./Михасёв В.А.'),
            (4, 'Физкультура', 'Явош С.В.'),
            (5, 'Математика', 'Прихач Е.В.'),
            (6, 'Белорусский язык', 'Гапоненко Ю.В.')
        ]
    },
    '6б': {
        'Понедельник': [
            (1, 'Английский язык', 'Приходько И.Н./Ковальчук К.Р.'),
            (2, 'Математика', 'Анисковец Н.В.'),
            (3, 'Трудовое обучение', 'Стишенок Н.И./Михасёв В.А.'),
            (4, 'Физкультура', 'Явош С.В.'),
            (5, 'Русский язык', 'Михалко Е.П.')
        ],
        'Вторник': [
            (1, 'Рус. литература', 'Михалко Е.П.'),
            (2, 'Искусство', 'Мележ А.Г.'),
            (3, 'Английский язык', 'Приходько И.Н./Ковальчук К.Р.'),
            (4, 'Белорусский язык', 'Анисенко С.В.'),
            (5, 'Бел. литература', 'Анисенко С.В.'),
            (6, 'Математика', 'Анисковец Н.В.')
        ],
        'Среда': [
            (1, 'Рус. литература', 'Михалко Е.П.'),
            (2, 'Белорусский язык', 'Анисенко С.В.'),
            (3, 'Математика', 'Анисковец Н.В.'),
            (4, 'Физкультура', 'Явош С.В.'),
            (5, 'Русский язык', 'Михалко Е.П.'),
            (6, 'История', 'Королёва Ж.В.')
        ],
        'Четверг': [
            (1, 'Математика', 'Анисковец Н.В.'),
            (2, 'Бел. литература', 'Анисенко С.В.'),
            (3, 'Белорусский язык', 'Анисенко С.В.'),
            (4, 'Биология', 'Даниленко А.А.'),
            (5, 'История', 'Королёва Ж.В.'),
            (6, 'География', 'Юнах Т.В.')
        ],
        'Пятница': [
            (1, 'Математика', 'Анисковец Н.В.'),
            (2, 'Английский язык', 'Приходько И.Н./Ковальчук К.Р.'),
            (3, 'Русский язык', 'Михалко Е.П.'),
            (4, 'Информатика', 'Гуд Ю.П./Прихач Е.В.'),
            (5, 'Физкультура', 'Явош С.В.'),
            (6, 'Трудовое обучение', 'Стишенок Н.И./Михасёв В.А.')
        ]
    },
    '6в': {
        'Понедельник': [
            (1, 'Русский язык', 'Шатило Н.И.'),
            (2, 'Физкультура', 'Смольский С.М.'),
            (3, 'Информатика', 'Гуд Ю.П./Прихач Е.В.'),
            (4, 'Рус. литература', 'Шатило Н.И.'),
            (5, 'Математика', 'Назаренко Т.К.'),
            (6, 'Английский язык', 'Нечаева Е.Я./Штыхнова Л.Г.')
        ],
        'Вторник': [
            (1, 'История', 'Королёва Ж.В.'),
            (2, 'Математика', 'Назаренко Т.К.'),
            (3, 'Русский язык', 'Шатило Н.И.'),
            (4, 'Бел. литература', 'Грицкевич Л.В.'),
            (5, 'Белорусский язык', 'Грицкевич Л.В.'),
            (6, 'Искусство', 'Мележ А.Г.')
        ],
        'Среда': [
            (1, 'Английский язык', 'Нечаева Е.Я./Штыхнова Л.Г.'),
            (2, 'Бел. литература', 'Грицкевич Л.В.'),
            (3, 'Белорусский язык', 'Грицкевич Л.В.'),
            (4, 'Математика', 'Назаренко Т.К.'),
            (5, 'Биология', 'Даниленко А.А.')
        ],
        'Четверг': [
            (1, 'Рус. литература', 'Шатило Н.И.'),
            (2, 'Русский язык', 'Шатило Н.И.'),
            (3, 'Математика', 'Назаренко Т.К.'),
            (4, 'История', 'Королёва Ж.В.'),
            (5, 'Физкультура', 'Смольский С.М.'),
            (6, 'Белорусский язык', 'Грицкевич Л.В.')
        ],
        'Пятница': [
            (1, 'Физкультура', 'Смольский С.М.'),
            (2, 'Трудовое обучение', 'Михасёв В.А./Стишенок Н.И.'),
            (3, 'Математика', 'Назаренко Т.К.'),
            (4, 'Английский язык', 'Нечаева Е.Я./Штыхнова Л.Г.'),
            (5, 'Трудовое обучение', 'Михасёв В.А./Стишенок Н.И.'),
            (6, 'География', 'Юнах Т.В.')
        ]
    },
    '7а': {
        'Понедельник': [
            (1, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (2, 'Русский язык', 'Леонова Д.А.'),
            (3, 'Биология', 'Даниленко А.А.'),
            (4, 'География', 'Юнах Т.В.'),
            (5, 'Математика', 'Анисковец Н.В.'),
            (6, 'Физкультура', 'Смольский С.М.'),
            (7, 'История', 'Королёва Ж.В.')
        ],
        'Вторник': [
            (1, 'Физкультура', 'Смольский С.М.'),
            (2, 'Рус. литература', 'Леонова Д.А.'),
            (3, 'Физика', 'Зубок С.Н.'),
            (4, 'Информатика', 'Гуд Ю.П./Назаренко Т.К.'),
            (5, 'Математика', 'Анисковец Н.В.'),
            (6, 'Английский язык', 'Нечаева Е.Я./Ковальчук К.Р.')
        ],
        'Среда': [
            (1, 'Белорусский язык', 'Анисенко С.В.'),
            (2, 'Физика', 'Зубок С.Н.'),
            (3, 'Физкультура', 'Смольский С.М.'),
            (4, 'Математика', 'Анисковец Н.В.'),
            (5, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (6, 'Химия', 'Корольчук О.Г.'),
            (7, 'Английский язык', 'Ковальчук К.Р.')
        ],
        'Четверг': [
            (1, 'Бел. литература', 'Анисенко С.В.'),
            (2, 'История', 'Королёва Ж.В.'),
            (3, 'Биология', 'Даниленко А.А.'),
            (4, 'Русский язык', 'Леонова Д.А.'),
            (5, 'Математика', 'Анисковец Н.В.'),
            (6, 'Английский язык', 'Нечаева Е.Я./Ковальчук К.Р.')
        ],
        'Пятница': [
            (1, 'Бел. литература', 'Анисенко С.В.'),
            (2, 'Белорусский язык', 'Анисенко С.В.'),
            (3, 'Искусство', 'Мележ А.Г.'),
            (4, 'Математика', 'Анисковец Н.В.'),
            (5, 'Английский язык', 'Нечаева Е.Я./Ковальчук К.Р.'),
            (6, 'История', 'Королёва Ж.В.')
        ]
    },
    '7б': {
        'Понедельник': [
            (1, 'Бел. литература', 'Гапоненко Ю.В.'),
            (2, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (3, 'География', 'Юнах Т.В.'),
            (4, 'Белорусский язык', 'Гапоненко Ю.В.'),
            (5, 'Биология', 'Канунова В.Г.'),
            (6, 'Математика', 'Прихач Е.В.')
        ],
        'Вторник': [
            (1, 'Математика', 'Прихач Е.В.'),
            (2, 'Физика', 'Зубок С.Н.'),
            (3, 'Русский язык', 'Михалко Е.П.'),
            (4, 'Искусство', 'Мележ А.Г.'),
            (5, 'Биология', 'Канунова В.Г.'),
            (6, 'Бел. литература', 'Гапоненко Ю.В.')
        ],
        'Среда': [
            (1, 'Английский язык', 'Приходько И.Н./Ковальчук К.Р.'),
            (2, 'Белорусский язык', 'Гапоненко Ю.В.'),
            (3, 'Физика', 'Зубок С.Н.'),
            (4, 'Математика', 'Прихач Е.В.'),
            (5, 'Физкультура', 'Вергейчик В.Л.'),
            (6, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.')
        ],
        'Четверг': [
            (1, 'Английский язык', 'Приходько И.Н./Ковальчук К.Р.'),
            (2, 'Русский язык', 'Михалко Е.П.'),
            (3, 'Химия', 'Канунова В.Г.'),
            (4, 'Информатика', 'Гуд Ю.П./Назаренко Т.К.'),
            (5, 'Математика', 'Прихач Е.В.'),
            (6, 'История', 'Королёва Ж.В.'),
            (7, 'Физкультура', 'Вергейчик В.Л.')
        ],
        'Пятница': [
            (1, 'Английский язык', 'Приходько И.Н./Ковальчук К.Р.'),
            (2, 'Рус. литература', 'Михалко Е.П.'),
            (3, 'Математика', 'Прихач Е.В.'),
            (4, 'История', 'Королёва Ж.В.'),
            (5, 'Физкультура', 'Вергейчик В.Л.')
        ]
    },
    '7в': {
        'Понедельник': [
            (1, 'Математика', 'Гуд Ю.П.'),
            (2, 'Информатика', 'Гуд Ю.П./Назаренко Т.К.'),
            (3, 'Физкультура', 'Смольский С.М.'),
            (4, 'Химия', 'Канунова В.Г.'),
            (5, 'Английский язык', 'Ковальчук К.Р./Штыхнова Л.Г.'),
            (6, 'Биология', 'Даниленко А.А.')
        ],
        'Вторник': [
            (1, 'Русский язык', 'Леонова Д.А.'),
            (2, 'Математика', 'Гуд Ю.П.'),
            (3, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (4, 'Физика', 'Зубок С.Н.'),
            (5, 'Искусство', 'Мележ А.Г.'),
            (6, 'Физкультура', 'Смольский С.М.')
        ],
        'Среда': [
            (1, 'Физика', 'Зубок С.Н.'),
            (2, 'Бел. литература', 'Лебедевская М.П.'),
            (3, 'Биология', 'Даниленко А.А.'),
            (4, 'Английский язык', 'Ковальчук К.Р./Штыхнова Л.Г.'),
            (5, 'Математика', 'Гуд Ю.П.'),
            (6, 'История', 'Сивый А.В.')
        ],
        'Четверг': [
            (1, 'История', 'Сивый А.В.'),
            (2, 'Математика', 'Гуд Ю.П.'),
            (3, 'География', 'Юнах Т.В.'),
            (4, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (5, 'Белорусский язык', 'Лебедевская М.П.'),
            (6, 'Бел. литература', 'Лебедевская М.П.')
        ],
        'Пятница': [
            (1, 'Математика', 'Гуд Ю.П.'),
            (2, 'Физкультура', 'Смольский С.М.'),
            (3, 'Английский язык', 'Ковальчук К.Р./Штыхнова Л.Г.'),
            (4, 'Русский язык', 'Леонова Д.А.'),
            (5, 'Белорусский язык', 'Лебедевская М.П.'),
            (6, 'Рус. литература', 'Леонова Д.А.')
        ]
    },
    '8а': {
        'Понедельник': [
            (1, 'Физкультура', 'Явош С.В.'),
            (2, 'География', 'Юнах Т.В.'),
            (3, 'Математика', 'Коротчикова Л.В.'),
            (4, 'Английский язык', 'Нечаева Е.Я./Штыхнова Л.Г.'),
            (5, 'Русский язык', 'Шатило Н.И.'),
            (6, 'Рус. литература', 'Шатило Н.И.')
        ],
        'Вторник': [
            (1, 'Бел. литература', 'Грицкевич Л.В.'),
            (2, 'Физкультура', 'Явош С.В.'),
            (3, 'Биология', 'Канунова В.Г.'),
            (4, 'Математика', 'Коротчикова Л.В.'),
            (5, 'Химия', 'Корольчук О.Г.'),
            (6, 'Белорусский язык', 'Грицкевич Л.В.'),
            (7, 'Английский язык', 'Нечаева Е.Я.')
        ],
        'Среда': [
            (1, 'Математика', 'Коротчикова Л.В.'),
            (2, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (3, 'Английский язык', 'Нечаева Е.Я./Штыхнова Л.Г.'),
            (4, 'Русский язык', 'Шатило Н.И.'),
            (5, 'Физика', 'Стишенок Н.И.'),
            (6, 'Искусство', 'Бельский С.В.'),
            (7, 'История', 'Сивый А.В.')
        ],
        'Четверг': [
            (1, 'Биология', 'Канунова В.Г.'),
            (2, 'История', 'Сивый А.В.'),
            (3, 'Математика', 'Коротчикова Л.В.'),
            (4, 'Химия', 'Корольчук О.Г.'),
            (5, 'Белорусский язык', 'Грицкевич Л.В.'),
            (6, 'Рус. литература', 'Шатило Н.И.')
        ],
        'Пятница': [
            (1, 'Английский язык', 'Нечаева Е.Я./Штыхнова Л.Г.'),
            (2, 'Математика', 'Коротчикова Л.В.'),
            (3, 'География', 'Юнах Т.В.'),
            (4, 'Физика', 'Стишенок Н.И.'),
            (5, 'Информатика', 'Назаренко Т.К./Гуд Ю.П.'),
            (6, 'Физкультура', 'Явош С.В.')
        ]
    },
    '8б': {
        'Понедельник': [
            (1, 'География', 'Юнах Т.В.'),
            (2, 'Русский язык', 'Михалко Е.П.'),
            (3, 'Математика', 'Анисковец Н.В.'),
            (4, 'История', 'Бельский С.В.'),
            (5, 'Бел. литература', 'Брель Ю.В.'),
            (6, 'Английский язык', 'Приходько И.Н./Тихоненко О.А.')
        ],
        'Вторник': [
            (1, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (2, 'Физкультура', 'Вергейчик В.Л.'),
            (3, 'Белорусский язык', 'Брель Ю.В.'),
            (4, 'Математика', 'Анисковец Н.В.'),
            (5, 'Информатика', 'Гуд Ю.П./Назаренко Т.К.'),
            (6, 'Английский язык', 'Приходько И.Н./Тихоненко О.А.')
        ],
        'Среда': [
            (1, 'Физкультура', 'Вергейчик В.Л.'),
            (2, 'Математика', 'Анисковец Н.В.'),
            (3, 'История', 'Бельский С.В.'),
            (4, 'Физика', 'Стишенок Н.И.'),
            (5, 'Химия', 'Корольчук О.Г.'),
            (6, 'Биология', 'Канунова В.Г.'),
            (7, 'Искусство', 'Бельский С.В.')
        ],
        'Четверг': [
            (1, 'Русский язык', 'Михалко Е.П.'),
            (2, 'Математика', 'Анисковец Н.В.'),
            (3, 'Физика', 'Стишенок Н.И.'),
            (4, 'Рус. литература', 'Михалко Е.П.'),
            (5, 'Английский язык', 'Приходько И.Н./Тихоненко О.А.'),
            (6, 'Белорусский язык', 'Брель Ю.В.')
        ],
        'Пятница': [
            (1, 'Биология', 'Канунова В.Г.'),
            (2, 'Математика', 'Анисковец Н.В.'),
            (3, 'Физкультура', 'Вергейчик В.Л.'),
            (4, 'Рус. литература', 'Михалко Е.П.'),
            (5, 'География', 'Юнах Т.В.'),
            (6, 'Химия', 'Корольчук О.Г.')
        ]
    },
    '9а': {
        'Понедельник': [
            (1, 'Математика', 'Анисковец Н.В.'),
            (2, 'Английский язык', 'Нечаева Е.Я./Приходько И.Н.'),
            (3, 'Химия', 'Канунова В.Г.'),
            (4, 'Русский язык', 'Пинчук А.В.'),
            (5, 'Информатика', 'Гуд Ю.П./Прихач Е.В.'),
            (6, 'Рус. литература', 'Пинчук А.В.'),
            (7, 'Физкультура', 'Вергейчик В.Л.')
        ],
        'Вторник': [
            (1, 'Физика', 'Зубок С.Н.'),
            (2, 'Математика', 'Анисковец Н.В.'),
            (3, 'История', 'Королёва Ж.В.'),
            (4, 'Бел. литература', 'Лукьяненко Л.И.'),
            (5, 'Белорусский язык', 'Лукьяненко Л.И.'),
            (6, 'Обществоведение', 'Сивый А.В.')
        ],
        'Среда': [
            (1, 'Биология', 'Даниленко А.А.'),
            (2, 'Английский язык', 'Нечаева Е.Я./Приходько И.Н.'),
            (3, 'Химия', 'Канунова В.Г.'),
            (4, 'Математика', 'Анисковец Н.В.'),
            (5, 'Бел. литература', 'Лукьяненко Л.И.'),
            (6, 'География', 'Юнах Т.В.'),
            (7, 'Английский язык', 'Нечаева Е.Я.')
        ],
        'Четверг': [
            (1, 'Белорусский язык', 'Лукьяненко Л.И.'),
            (2, 'География', 'Юнах Т.В.'),
            (3, 'Математика', 'Анисковец Н.В.'),
            (4, 'Английский язык', 'Нечаева Е.Я./Приходько И.Н.'),
            (5, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (6, 'Физкультура', 'Вергейчик В.Л.'),
            (7, 'Биология', 'Даниленко А.А.')
        ],
        'Пятница': [
            (1, 'Физика', 'Зубок С.Н.'),
            (2, 'Физкультура', 'Вергейчик В.Л.'),
            (3, 'Математика', 'Анисковец Н.В.'),
            (4, 'Русский язык', 'Пинчук А.В.'),
            (5, 'История', 'Королёва Ж.В.'),
            (6, 'Английский язык', 'Нечаева Е.Я.')
        ]
    },
    '9б': {
        'Понедельник': [
            (1, 'Химия', 'Канунова В.Г.'),
            (2, 'Бел. литература', 'Брель Ю.В.'),
            (3, 'Белорусский язык', 'Брель Ю.В.'),
            (4, 'Физкультура', 'Смольский С.М.'),
            (5, 'Биология', 'Даниленко А.А.'),
            (6, 'Математика', 'Гуд Ю.П.')
        ],
        'Вторник': [
            (1, 'Химия', 'Канунова В.Г.'),
            (2, 'Английский язык', 'Ковальчук К.Р./Тихоненко О.А.'),
            (3, 'Русский язык', 'Леонова Д.А.'),
            (4, 'Бел. литература', 'Брель Ю.В.'),
            (5, 'История', 'Бельский С.В.'),
            (6, 'Математика', 'Гуд Ю.П.'),
            (7, 'Обществоведение', 'Сивый А.В.')
        ],
        'Среда': [
            (1, 'Белорусский язык', 'Брель Ю.В.'),
            (2, 'Информатика', 'Гуд Ю.П./Прихач Е.В.'),
            (3, 'Физика', 'Стишенок Н.И.'),
            (4, 'Математика', 'Гуд Ю.П.'),
            (5, 'Физкультура', 'Смольский С.М.'),
            (6, 'Английский язык', 'Ковальчук К.Р./Тихоненко О.А.')
        ],
        'Четверг': [
            (1, 'Физкультура', 'Смольский С.М.'),
            (2, 'Биология', 'Даниленко А.А.'),
            (3, 'Трудовое обучение', 'Мележ А.Г./Михасёв В.А.'),
            (4, 'География', 'Юнах Т.В.'),
            (5, 'Математика', 'Гуд Ю.П.'),
            (6, 'История', 'Бельский С.В.')
        ],
        'Пятница': [
            (1, 'Физика', 'Стишенок Н.И.'),
            (2, 'Математика', 'Гуд Ю.П.'),
            (3, 'Рус. литература', 'Леонова Д.А.'),
            (4, 'Английский язык', 'Ковальчук К.Р./Тихоненко О.А.'),
            (5, 'Русский язык', 'Леонова Д.А.'),
            (6, 'История', 'Бельский С.В.')
        ]
    },
    '10а': {
        'Понедельник': [
            (1, 'Физкультура', 'Вергейчик В.Л./Смольский С.М.'),
            (2, 'Английский язык', 'Артюх М.П./Тихоненко О.А.'),
            (3, 'Химия', 'Корольчук О.Г.'),
            (4, 'Информатика', 'Гуд Ю.П./Назаренко Т.К.'),
            (5, 'Физика', 'Стишенок Н.И.'),
            (6, 'Бел. язык', 'Брель Ю.В./Анисенко С.В.')
        ],
        'Вторник': [
            (1, 'Математика', 'Коротчикова Л.В./Назаренко Т.К.'),
            (2, 'Физкультура', 'Смольский С.М.'),
            (3, 'Химия', 'Корольчук О.Г.'),
            (4, 'Биология', 'Канунова В.Г.'),
            (5, 'История', 'Сивый А.В.'),
            (6, 'Бел. язык(проф)', 'Анисенко С.В.')
        ],
        'Среда': [
            (1, 'Русский язык', 'Пинчук А.В.'),
            (2, 'ДП/МП', 'Даниленко А.А./Смольский С.М.'),
            (3, 'Бел. литература', 'Анисенко С.В.'),
            (4, 'Физкультура', 'Вергейчик В.Л./Смольский С.М.'),
            (5, 'Математика', 'Коротчикова Л.В./Назаренко Т.К.'),
            (6, 'Физика', 'Стишенок Н.И.'),
            (7, 'Математика(проф)', 'Коротчикова Л.В.')
        ],
        'Четверг': [
            (1, 'Рус. литература', 'Пинчук А.В.'),
            (2, 'Математика', 'Коротчикова Л.В./Назаренко Т.К.'),
            (3, 'Русский язык', 'Пинчук А.В.'),
            (4, 'История', 'Сивый А.В.'),
            (5, 'География', 'Юнах Т.В.'),
            (6, 'Обществоведение', 'Сивый А.В.')
        ],
        'Пятница': [
            (1, 'Математика', 'Коротчикова Л.В./Назаренко Т.К.'),
            (2, 'Биология', 'Канунова В.Г.'),
            (3, 'Бел. язык/Матем(проф)', 'Брель Ю.В./Коротчикова Л.В.'),
            (4, 'Черчение', 'Михасёв В.А.'),
            (5, 'Английский язык', 'Артюх М.П./Тихоненко О.А.'),
            (6, 'Бел. язык(проф)', 'Анисенко С.В.')
        ]
    },
    '10б': {
        'Понедельник': [
            (1, 'Рус. литература', 'Леонова Д.А.'),
            (2, 'Биология/Химия', 'Даниленко А.А./Корольчук О.Г.'),
            (3, 'Математика', 'Назаренко Т.К.'),
            (4, 'Англ. язык/Химия', 'Приходько И.Н./Корольчук О.Г.'),
            (5, 'Физкультура', 'Смольский С.М./Вергейчик В.Л.'),
            (6, 'История', 'Королёва Ж.В./Сивый А.В.'),
            (7, 'биология/английский язык', 'Даниленко А.А./Приходько И.Н.')
        ],
        'Вторник': [
            (1, 'Бел. литература', 'Лебедевская М.П.'),
            (2, 'Биология/Химия', 'Канунова В.Г./Корольчук О.Г.'),
            (3, 'Физика', 'Коротчикова Л.В.'),
            (4, 'Английский язык/Химия', 'Приходько И.Н./Корольчук О.Г.'),
            (5, 'Физкультура', 'Смольский С.М./Вергейчик В.Л.'),
            (6, 'Биология(проф)', 'Даниленко А.А.')
        ],
        'Среда': [
            (1, 'Белорусский язык', 'Лебедевская М.П.'),
            (2, 'Математика', 'Назаренко Т.К.'),
            (3, 'Информатика', 'Гуд Ю.П./Назаренко Т.К.'),
            (4, 'История', 'Королёва Ж.В./Сивый А.В.'),
            (5, 'Англ. язык/История', 'Тихоненко О.А./Королёва Ж.В.'),
            (6, 'Русский язык', 'Леонова Д.А.'),
            (7, 'Биология/химия', 'Даниленко А.А./Корольчук О.Г.')
        ],
        'Четверг': [
            (1, 'География', 'Юнах Т.В.'),
            (2, 'Белорусский язык', 'Лебедевская М.П.'),
            (3, 'Рус. литература', 'Леонова Д.А.'),
            (4, 'Физкультура', 'Смольский С.М.'),
            (5, 'Математика', 'Назаренко Т.К.'),
            (6, 'ДП/МП', 'Даниленко А.А./Смольский С.М.'),
            (7, 'Английский язык', 'Приходько И.Н.')
        ],
        'Пятница': [
            (1, 'Черчение', 'Михасёв В.А.'),
            (2, 'Англ. язык/История', 'Тихоненко О.А./Королёва Ж.В.'),
            (3, 'Физика', 'Коротчикова Л.В.'),
            (4, 'Математика', 'Назаренко Т.К.'),
            (5, 'Биология/Химия', 'Канунова В.Г./Корольчук О.Г.'),
            (6, 'Обществоведение', 'Сивый А.В.')
        ]
    },
    '11': {
        'Понедельник': [
            (1, 'Биология/Химия', 'Даниленко А.А./Корольчук О.Г.'),
            (2, 'Математика', 'Коротчикова Л.В.'),
            (3, 'Обществоведение', 'Королёва Ж.В.'),
            (4, 'Физика', 'Стишенок Н.И.'),
            (5, 'История', 'Королёва Ж.В.'),
            (6, 'Физкультура', 'Вергейчик В.Л./Явош С.В.'),
            (7, 'Англ. яз/Рус. яз', 'Тихоненко О.А./Пинчук А.В.')
        ],
        'Вторник': [
            (1, 'Бел. литература', 'Лукьяненко Л.И.'),
            (2, 'История', 'Королёва Ж.В.'),
            (3, 'Физкультура', 'Вергейчик В.Л./Явош С.В.'),
            (4, 'Англ. яз/Рус. яз', 'Хорошко Т.А./Пинчук А.В.'),
            (5, 'Англ. яз/Рус. яз', 'Тихоненко О.А./Пинчук А.В.'),
            (6, 'Рус. яз/Химия', 'Пинчук А.В./Корольчук О.Г.')
        ],
        'Среда': [
            (1, 'Информатика', 'Гуд Ю.П./Прихач Е.В.'),
            (2, 'География', 'Юнах Т.В.'),
            (3, 'Математика', 'Коротчикова Л.В.'),
            (4, 'Биология/Химия', 'Даниленко А.А./Корольчук О.Г.'),
            (5, 'Белорусский язык', 'Лукьяненко Л.И.'),
            (6, 'ДП/МП', 'Даниленко А.А./Явош С.В.')
        ],
        'Четверг': [
            (1, 'Математика', 'Коротчикова Л.В.'),
            (2, 'Астрономия', 'Стишенок Н.И.'),
            (3, 'Бел. литература', 'Лукьяненко Л.И.'),
            (4, 'Физика', 'Стишенок Н.И.'),
            (5, 'Биология/Химия', 'Канунова В.Г./Корольчук О.Г.'),
            (6, 'Англ. яз/Биология', 'Тихоненко О.А./Канунова В.Г.')
        ],
        'Пятница': [
            (1, 'Физкультура', 'Явош С.В.'),
            (2, 'Рус. литература', 'Пинчук А.В.'),
            (3, 'Англ. яз/Рус. яз', 'Хорошко Т.А./Пинчук А.В.'),
            (4, 'Биология/Химия', 'Канунова В.Г./Корольчук О.Г.'),
            (5, 'Математика', 'Коротчикова Л.В.'),
            (6, 'Биология/Англ. яз', 'Канунова В.Г./Тихоненко О.А.'),
            (7, 'Химия(проф)', 'Корольчук О.Г.')
        ]
    }
}

#================== ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ ==================
def get_lesson_time(lesson_number):
    """Возвращает время урока по его номеру."""
    lesson_times = {
        1: "08:00-08:45",
        2: "09:00-09:45",
        3: "10:00-10:45",
        4: "11:00-11:45",
        5: "12:00-12:45",
        6: "12:55-13:40",
        7: "14:00-14:45",
        8: "15:00-15:45",
        9: "16:00-16:45",
        10: "17:00-17:45"
    }
    return lesson_times.get(lesson_number, "??:??-??:??")

def get_current_lesson_info():
    """Определяет текущий/следующий урок по времени в часовом поясе Минска (UTC+3)."""
    tz_minsk = pytz.timezone('Europe/Minsk')
    now = datetime.now(tz_minsk)
    current_hour = now.hour
    current_minute = now.minute
    current_minutes = current_hour * 60 + current_minute
    
    # Интервалы уроков с учетом точного времени начала 6-го урока (12:55)
    lesson_intervals = [
        (8, 0, 8, 45, 1),    # 1 урок
        (9, 0, 9, 45, 2),    # 2 урок
        (10, 0, 10, 45, 3),  # 3 урок
        (11, 0, 11, 45, 4),  # 4 урок
        (12, 0, 12, 45, 5),  # 5 урок
        (12, 55, 13, 40, 6), # 6 урок НАЧИНАЕТСЯ В 12:55
        (14, 0, 14, 45, 7)   # 7 урок
    ]
    
    # Проверяем, идёт ли сейчас урок
    for start_h, start_m, end_h, end_m, num in lesson_intervals:
        start_total = start_h * 60 + start_m
        end_total = end_h * 60 + end_m
        if start_total <= current_minutes <= end_total:
            time_left = end_total - current_minutes
            return {
                'status': 'lesson',
                'number': num,
                'time_left': time_left,
                'start_time': f"{start_h:02d}:{start_m:02d}",
                'end_time': f"{end_h:02d}:{end_m:02d}"
            }
    
    # Проверяем следующий урок
    for start_h, start_m, end_h, end_m, num in lesson_intervals:
        start_total = start_h * 60 + start_m
        if current_minutes < start_total:
            minutes_until = start_total - current_minutes
            return {
                'status': 'break',
                'next_number': num,
                'minutes_until': minutes_until,
                'start_time': f"{start_h:02d}:{start_m:02d}",
                'end_time': f"{end_h:02d}:{end_m:02d}"
            }
    
    # После всех уроков или до начала первого урока
    return {'status': 'finished'}

def format_schedule_day(class_name, day, structured_lessons, target_date=None):
    """Форматирует расписание на день в новом формате с заменами."""
    if not structured_lessons:
        return "На этот день расписания нет."
    
    structured_lessons.sort(key=lambda x: x[0])
    substitutions = []
    
    # Корректная проверка даты для получения замен
    if target_date and target_date not in ('None', 'null', None):
        substitutions = db.get_substitutions_for_class_date(class_name, target_date)
    
    sub_dict = {}
    for sub in substitutions:
        lesson_num = sub[3]
        sub_dict[lesson_num] = {
            'old_subject': sub[4],
            'new_subject': sub[5],
            'old_teacher': sub[6],
            'new_teacher': sub[7]
        }
    
    result_lines = []
    header = f"📅 <b>{day.upper()} - {class_name.upper()}</b>"
    result_lines.append(header)
    result_lines.append("─" * 18)
    
    for lesson_num, subject, teacher in structured_lessons:
        lesson_time = get_lesson_time(lesson_num)
        if 1 <= lesson_num <= 7:
            emoji = ["1️⃣", "2️⃣", "3️⃣", "4️⃣", "5️⃣", "6️⃣", "7️⃣"][lesson_num - 1]
            lesson_str = f"{emoji}"
        else:
            lesson_str = f"{lesson_num}."
        
        main_line = f"{lesson_str} <b>{lesson_time}</b> ➡️ {subject} ✅ {teacher}"
        result_lines.append(main_line)
        
        if lesson_num in sub_dict:
            sub = sub_dict[lesson_num]
            result_lines.append(f"   └─ 🔄 <b>ЗАМЕНА:</b> {sub['new_subject']} ✅ {sub['new_teacher']}")
    
    return "\n".join(result_lines)

def format_weekly_schedule(class_name):
    """Форматирует расписание на всю неделю в новом формате."""
    if class_name not in SCHEDULE_STRUCTURED:
        return f"Расписание для класса {class_name} не найдено."
    
    result_lines = []
    result_lines.append(f"📅 <b>РАСПИСАНИЕ НА НЕДЕЛЮ - {class_name.upper()}</b>")
    result_lines.append("=" * 30)
    
    days_order = ["Понедельник", "Вторник", "Среда", "Четверг", "Пятница"]
    for day in days_order:
        if day in SCHEDULE_STRUCTURED[class_name]:
            lessons = SCHEDULE_STRUCTURED[class_name][day]
            if lessons:
                result_lines.append(f"\n<b>📌 {day.upper()}</b>")
                result_lines.append("─" * 18)
                for lesson_num, subject, teacher in lessons:
                    lesson_time = get_lesson_time(lesson_num)
                    if 1 <= lesson_num <= 7:
                        emoji = ["1️⃣", "2️⃣", "3️⃣", "4️⃣", "5️⃣", "6️⃣", "7️⃣"][lesson_num - 1]
                        lesson_str = f"{emoji}"
                    else:
                        lesson_str = f"{lesson_num}."
                    line = f"{lesson_str} <b>{lesson_time}</b> ➡️ {subject} ✅ {teacher}"
                    result_lines.append(line)
    
    return "\n".join(result_lines)

def format_substitution(sub):
    """Форматирует замену в красивом виде."""
    if len(sub) >= 9:
        return (f"↪️ {sub[8]} класс, {sub[3]} урок:\n"
                f"    `{sub[4]}` ({sub[6]})\n"
                f"   → `{sub[5]}` ({sub[7]})")
    return str(sub)

def get_all_teachers():
    """Извлекает всех уникальных учителей из расписания."""
    teachers = set()
    for class_name, days in SCHEDULE_STRUCTURED.items():
        for day, lessons in days.items():
            for lesson in lessons:
                if len(lesson) >= 3:
                    teacher_name = lesson[2]
                    if teacher_name:
                        teacher_list = [t.strip() for t in teacher_name.split('/')]
                        for teacher in teacher_list:
                            # Удаляем скобки и содержимое в скобках
                            teacher_clean = re.sub(r'\s*\(.*?\)', '', teacher).strip()
                            if teacher_clean and teacher_clean not in ['', ' ']:
                                teachers.add(teacher_clean)
    return sorted(list(teachers))

def get_teacher_schedule(teacher_name):
    """Получает расписание учителя из общей структуры."""
    schedule = {}
    for class_name, days in SCHEDULE_STRUCTURED.items():
        for day, lessons in days.items():
            for lesson in lessons:
                if len(lesson) >= 3:
                    current_teacher = lesson[2]
                    teacher_list = [t.strip() for t in current_teacher.split('/')]
                    teacher_list_clean = [re.sub(r'\s*\(.*?\)', '', t).strip() for t in teacher_list]
                    if teacher_name in teacher_list_clean:
                        if day not in schedule:
                            schedule[day] = []
                        lesson_info = {
                            'class': class_name,
                            'number': lesson[0],
                            'subject': lesson[1],
                            'time': get_lesson_time(lesson[0]),
                            'full_teacher': current_teacher
                        }
                        schedule[day].append(lesson_info)
    
    for day in schedule:
        schedule[day].sort(key=lambda x: x['number'])
    
    return schedule

def format_teacher_schedule(teacher_name, schedule):
    """Форматирует расписание учителя с отметкой текущего урока (🟢)."""
    today = datetime.now().date()
    
    # === ОПРЕДЕЛЕНИЕ ТЕКУЩЕГО УРОКА ДЛЯ ОТМЕТКИ ===
    tz_minsk = pytz.timezone('Europe/Minsk')
    now = datetime.now(tz_minsk)
    current_weekday = now.weekday()
    current_day_name = DAYS_OF_WEEK[current_weekday] if current_weekday < 5 else "Пятница"
    
    # Получаем информацию о текущем уроке (только если он идёт прямо сейчас)
    current_lesson_info = get_current_lesson_info()
    current_lesson_number = current_lesson_info['number'] if current_lesson_info['status'] == 'lesson' else None
    
    teacher_substitutions = {}
    for i in range(30):
        target_date = today + timedelta(days=i)
        date_str = target_date.strftime('%Y-%m-%d')
        subs = db.get_substitutions_by_teacher_and_date(teacher_name, date_str)
        if subs:
            teacher_substitutions[date_str] = subs
    
    text = f"<b>👨‍🏫 {teacher_name}</b>\n"
    text += "=" * 30 + "\n"
    
    if schedule:
        total_lessons = sum(len(lessons) for lessons in schedule.values())
        classes = set()
        subjects = set()
        for day_lessons in schedule.values():
            for lesson in day_lessons:
                classes.add(lesson['class'])
                subjects.add(lesson['subject'])
        
        text += f"<b>📊 Статистика:</b>\n"
        text += f"• Уроков: <b>{total_lessons}</b>\n"
        text += f"• Классы: <b>{', '.join(sorted(classes))}</b>\n"
        text += f"• Предметы: <b>{', '.join(sorted(subjects))}</b>\n"
    else:
        text += "<i>❌ Нет уроков в расписании</i>\n"
    
    total_subs = sum(len(subs) for subs in teacher_substitutions.values())
    if total_subs > 0:
        text += f"• <b>⚠️ Замен: {total_subs}</b>\n"
    
    text += "\n" + "=" * 30 + "\n"
    text += "<b>📅 ОСНОВНОЕ РАСПИСАНИЕ:</b>\n"
    
    days_order = ["Понедельник", "Вторник", "Среда", "Четверг", "Пятница"]
    has_main_schedule = False
    for day in days_order:
        if day in schedule and schedule[day]:
            has_main_schedule = True
            text += f"<b>{day.upper()}</b>\n"
            text += "─" * 18 + "\n"
            sorted_lessons = sorted(schedule[day], key=lambda x: x['number'])
            for lesson in sorted_lessons:
                # Формируем базовые элементы урока
                if 1 <= lesson['number'] <= 7:
                    emoji = ["1️⃣", "2️⃣", "3️⃣", "4️⃣", "5️⃣", "6️⃣", "7️⃣"][lesson['number'] - 1]
                    lesson_marker = emoji
                else:
                    lesson_marker = f"{lesson['number']}."
                
                col1 = f"{lesson_marker} <b>{lesson['time']}</b>"
                col2 = f"<code>{lesson['class'].upper()}</code> ➡️ {lesson['subject']}"
                teachers = lesson['full_teacher'].split('/')
                if len(teachers) > 1:
                    col2 += " <i>(с совм.)</i>"
                
                # 🔑 КЛЮЧЕВОЕ ИЗМЕНЕНИЕ: Добавляем 🟢 если это текущий урок сегодня
                if day == current_day_name and lesson['number'] == current_lesson_number:
                    text += f"🟢 {col1}   {col2}\n"
                else:
                    text += f"{col1}   {col2}\n"
            text += "\n"
    
    if not has_main_schedule:
        text += "<i>Нет уроков на неделю</i>\n"
    
    text += "=" * 30 + "\n"
    
    # Раздел замен остаётся без изменений (без 🟢, так как замены на будущее)
    if teacher_substitutions:
        text += "<b>🔄 ЗАМЕНЫ (30 дней):</b>\n"
        current_date = today
        shown_dates = 0
        for i in range(30):
            date_obj = today + timedelta(days=i)
            date_str = date_obj.strftime('%Y-%m-%d')
            if date_str in teacher_substitutions:
                weekday = date_obj.weekday()
                if weekday < 5:
                    day_name = DAYS_OF_WEEK[weekday]
                else:
                    continue  # Пропускаем выходные
                
                text += f"<b>{day_name}</b> <i>({date_obj.strftime('%d.%m')})</i>\n"
                text += "─" * 18 + "\n"
                for sub in teacher_substitutions[date_str]:
                    lesson_num = sub[3]
                    lesson_time = get_lesson_time(lesson_num)
                    if 1 <= lesson_num <= 7:
                        emoji = ["1️⃣", "2️⃣", "3️⃣", "4️⃣", "5️⃣", "6️⃣", "7️⃣"][lesson_num - 1]
                        lesson_marker = emoji
                    else:
                        lesson_marker = f"{lesson_num}."
                    
                    # Проверяем, является ли учитель новым учителем в замене
                    if sub[7] == teacher_name:
                        text += f"{lesson_marker} <b>{lesson_time}</b> <code>{sub[8]}</code> ➡️ {sub[5]}\n"
                        text += f"   🔄 вместо {sub[6]} ({sub[4]})\n"
                    # Или старым учителем, которого заменили
                    elif sub[6] == teacher_name:
                        text += f"{lesson_marker} <b>{lesson_time}</b> <code>{sub[8]}</code> ➡️ {sub[4]}\n"
                        text += f"   🔄 заменён на {sub[7]} ({sub[5]})\n"
                shown_dates += 1
                if shown_dates >= 7:
                    break
        
        if shown_dates == 0:
            text += "<i>На ближайшие 30 дней замен нет</i>\n"
    else:
        text += "<b>🔄 ЗАМЕНЫ:</b>\n"
        text += "<i>На ближайшие 30 дней замен нет</i>\n"
    
    text += "\n" + "=" * 30 + "\n"
    text += f"<i>ℹ️ 🟢 — текущий урок | Расписание и замены на 30 дней</i>"
    return text

def parse_substitution_input(text):
    """Парсит ввод замены в формате 'Предмет → Предмет / Учитель → Учитель'."""
    new_subject = None
    new_teacher = None
    
    # Разделяем на предмет и учителя
    parts = text.split('/')
    if len(parts) >= 1:
        subject_part = parts[0].strip()
        if '→' in subject_part:
            subject_parts = subject_part.split('→')
            if len(subject_parts) == 2:
                new_subject = subject_parts[1].strip()
        elif subject_part:
            # Если нет стрелки, но есть текст - это новый предмет
            new_subject = subject_part
    
    if len(parts) >= 2:
        teacher_part = parts[1].strip()
        if '→' in teacher_part:
            teacher_parts = teacher_part.split('→')
            if len(teacher_parts) == 2:
                new_teacher = teacher_parts[1].strip()
        elif teacher_part:
            # Если нет стрелки, но есть текст - это новый учитель
            new_teacher = teacher_part
    
    return new_subject, new_teacher

async def send_substitution_notification(context, teacher_name, substitution_data):
    """Отправляет уведомление учителю о новой замене с логированием и защитой от ошибок."""
    teacher_name_clean = teacher_name.replace('_', ' ').strip()
    teacher_id = TEACHER_IDS.get(teacher_name_clean)
    logger.info(f"📨 Попытка отправить уведомление учителю: '{teacher_name_clean}' (ID в словаре: {teacher_id})")
    
    if not teacher_id or teacher_id == 0:
        logger.warning(f"❌ Учитель '{teacher_name_clean}' не найден в TEACHER_IDS или имеет некорректный ID=0")
        for admin_id in ADMIN_IDS:
            try:
                await context.bot.send_message(
                    chat_id=admin_id,
                    text=f"⚠️ Замена добавлена для '{teacher_name_clean}', но уведомление НЕ отправлено (нет валидного Telegram ID)",
                    parse_mode='HTML'
                )
            except Exception as e:
                logger.error(f"Ошибка отправки уведомления админу: {e}")
        return
    
    try:
        lesson_time = get_lesson_time(substitution_data['lesson'])
        notification_message = (
            f"<b>🔔 ВАМ НАЗНАЧЕНА ЗАМЕНА!</b>\n"
            f"<b>📅 Дата:</b> {substitution_data['date']}\n"
            f"<b>📅 День недели:</b> {substitution_data['day']}\n"
            f"<b>🕐 Время урока:</b> {lesson_time}\n"
            f"<b>🔢 Номер урока:</b> {substitution_data['lesson']}\n"
            f"<b>🏫 Класс:</b> {substitution_data['class_name']}\n"
            f"<b>📚 Замена:</b> {substitution_data['old_subject']} ({substitution_data['old_teacher']}) → "
            f"{substitution_data['new_subject']} (Вы)\n"
            f"<i>Замена добавлена администратором.</i>"
        )
        await context.bot.send_message(
            chat_id=teacher_id,
            text=notification_message,
            parse_mode='HTML'
        )
        logger.info(f"✅ Уведомление о замене отправлено учителю {teacher_name_clean} (ID: {teacher_id})")
    except Exception as e:
        error_msg = str(e)
        logger.error(f"❌ Ошибка отправки уведомления учителю {teacher_name_clean} (ID: {teacher_id}): {error_msg}")
        for admin_id in ADMIN_IDS:
            try:
                await context.bot.send_message(
                    chat_id=admin_id,
                    text=f"❌ Не удалось отправить уведомление учителю '{teacher_name_clean}' (ID: {teacher_id}):\n{error_msg[:100]}",
                    parse_mode='HTML'
                )
            except Exception as e2:
                logger.error(f"Ошибка отправки уведомления админу об ошибке: {e2}")

#================== ФУНКЦИЯ ПРОВЕРКИ ТЕХРЕЖИМА ==================
async def check_maintenance_mode(update: Update, context: CallbackContext) -> bool:
    """
    Проверяет техрежим. Возвращает True если нужно прервать обработку.
    """
    maintenance = db.get_maintenance_status()
    user_id = update.effective_user.id
    
    # Админы всегда проходят проверку
    if user_id in ADMIN_IDS:
        return False
    
    # Если техрежим выключен — пропускаем
    if not maintenance['enabled']:
        return False
    
    # Формируем сообщение для пользователя
    msg = "⚠️ <b>ТЕХНИЧЕСКИЕ РАБОТЫ</b>\n"
    msg += "Бот временно недоступен для пользователей.\n"
    if maintenance['until']:
        msg += f"🕗 Восстановление ориентировочно: <b>{maintenance['until']}</b>\n"
    if maintenance['message']:
        msg += f"\n💬 <i>{maintenance['message']}</i>\n"
    msg += "\nПриносим извинения за неудобства."
    
    # Отправляем уведомление в зависимости от типа обновления
    try:
        if update.callback_query:
            # Для кнопок — показываем алерт + редактируем сообщение
            await update.callback_query.answer("⚠️ Технические работы", show_alert=True)
            await update.callback_query.edit_message_text(
                msg,
                parse_mode='HTML',
                reply_markup=InlineKeyboardMarkup([[
                    InlineKeyboardButton("🔄 Обновить статус", callback_data='check_maintenance_status')
                ]])
            )
        else:
            # Для текстовых сообщений
            await update.message.reply_text(
                msg,
                parse_mode='HTML',
                reply_markup=InlineKeyboardMarkup([[
                    InlineKeyboardButton("🔄 Проверить статус", callback_data='check_maintenance_status')
                ]])
            )
    except Exception as e:
        logger.warning(f"Ошибка отправки уведомления о техрежиме: {e}")
        # Если не удалось отредактировать — просто отправляем новое сообщение
        if update.callback_query:
            await update.callback_query.message.reply_text(msg, parse_mode='HTML')
        else:
            await update.message.reply_text(msg, parse_mode='HTML')
    
    return True  # Прерываем дальнейшую обработку

async def check_maintenance_status(query, context):
    """Проверяет текущий статус техрежима (для кнопки обновления)."""
    maintenance = db.get_maintenance_status()
    if not maintenance['enabled']:
        msg = "🟢 <b>Бот работает штатно</b>\nВсе функции доступны."
        keyboard = [[InlineKeyboardButton("🏠 Главное меню", callback_data='back_to_main')]]
    else:
        msg = "⚠️ <b>ТЕХНИЧЕСКИЕ РАБОТЫ</b>\nБот временно недоступен."
        if maintenance['until']:
            msg += f"\n🕗 Восстановление: <b>{maintenance['until']}</b>"
        keyboard = [[InlineKeyboardButton("🔄 Обновить", callback_data='check_maintenance_status')]]
    
    reply_markup = InlineKeyboardMarkup(keyboard)
    try:
        await query.edit_message_text(msg, reply_markup=reply_markup, parse_mode='HTML')
    except:
        await query.answer("Статус не изменился", show_alert=False)

#================== ФУНКЦИИ РАССЫЛКИ ТЕХНИЧЕСКИХ УВЕДОМЛЕНИЙ ==================
async def show_users_stats(query, context):
    """Показывает статистику пользователей."""
    if query.from_user.id not in ADMIN_IDS:
        return
    
    user_count = db.get_user_count()
    users = db.get_all_users()
    
    text = f"<b>👥 СТАТИСТИКА ПОЛЬЗОВАТЕЛЕЙ</b>\n"
    text += f"• Всего пользователей: <b>{user_count}</b>\n"
    
    if users:
        text += "<b>Последние 10 пользователей:</b>\n"
        for user in users[-10:]:
            user_id, username, first_name, last_name = user
            name = f"{first_name or ''} {last_name or ''}".strip() or "Без имени"
            if username:
                name += f" (@{username})"
            text += f"• ID: <code>{user_id}</code> — {name}\n"
    else:
        text += "<i>Нет зарегистрированных пользователей</i>"
    
    keyboard = [
        [InlineKeyboardButton("↩️ В админ-панель", callback_data='admin_panel')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    await query.edit_message_text(text, reply_markup=reply_markup, parse_mode='HTML')

async def start_technical_broadcast(query, context):
    """Начинает процесс рассылки технического уведомления."""
    if query.from_user.id not in ADMIN_IDS:
        return
    
    context.user_data['broadcasting'] = True
    context.user_data['broadcast_step'] = 'time'
    
    keyboard = [
        [InlineKeyboardButton("❌ Отмена", callback_data='cancel_broadcast')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "<b>📢 РАССЫЛКА ТЕХНИЧЕСКОГО УВЕДОМЛЕНИЯ</b>\n"
        "Введите <b>время окончания технических работ</b> в формате:\n"
        "<code>ДД.ММ ЧЧ:ММ</code>\n"
        "Пример: <code>05.02 18:30</code>\n"
        "<i>Это время будет указано в уведомлении всем пользователям.</i>",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def handle_broadcast_time(update: Update, context: CallbackContext):
    """Обрабатывает ввод времени технических работ."""
    if not context.user_data.get('broadcasting') or context.user_data.get('broadcast_step') != 'time':
        return
    
    time_input = update.message.text.strip()
    
    # Проверяем формат времени
    if not re.match(r'^\d{2}\.\d{2} \d{2}:\d{2}$', time_input):
        await update.message.reply_text(
            "❌ Неверный формат времени!\n"
            "Введите в формате: <code>ДД.ММ ЧЧ:ММ</code>\n"
            "Пример: <code>05.02 18:30</code>",
            parse_mode='HTML'
        )
        return
    
    context.user_data['broadcast_time'] = time_input
    context.user_data['broadcast_step'] = 'confirm'
    
    # Формируем шаблонное сообщение
    broadcast_message = (
        f"⚠️ <b>ВНИМАНИЕ! ТЕХНИЧЕСКИЕ РАБОТЫ</b> ⚠️\n"
        f"Сервис временно недоступен в связи с техническими работами.\n"
        f"🕗 <b>Работы завершатся:</b> {time_input}\n"
        f"Приносим извинения за временные неудобства.\n"
        f"Бот возобновит работу сразу после окончания работ."
    )
    
    # Предпросмотр сообщения
    keyboard = [
        [InlineKeyboardButton("✅ Отправить всем", callback_data='confirm_broadcast')],
        [InlineKeyboardButton("✏️ Изменить время", callback_data='edit_broadcast_time')],
        [InlineKeyboardButton("❌ Отмена", callback_data='cancel_broadcast')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await update.message.reply_text(
        "<b>🔍 ПРЕДПРОСМОТР УВЕДОМЛЕНИЯ:</b>\n" + broadcast_message,
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def confirm_broadcast(query, context):
    """Подтверждает и отправляет рассылку ВСЕМ пользователям из базы данных."""
    if query.from_user.id not in ADMIN_IDS:
        return
    
    broadcast_time = context.user_data.get('broadcast_time')
    if not broadcast_time:
        await query.edit_message_text("❌ Ошибка: время не указано.")
        context.user_data.clear()
        return
    
    # Формируем финальное сообщение
    broadcast_message = (
        f"⚠️ <b>ВНИМАНИЕ! ТЕХНИЧЕСКИЕ РАБОТЫ</b> ⚠️\n"
        f"Сервис временно недоступен в связи с техническими работами.\n"
        f"🕗 <b>Работы завершатся:</b> {broadcast_time}\n"
        f"Приносим извинения за временные неудобства.\n"
        f"Бот возобновит работу сразу после окончания работ."
    )
    
    users = db.get_all_users()
    total = len(users)
    sent = 0
    failed = 0
    
    # Отправляем уведомление админу о начале рассылки
    status_message = await query.edit_message_text(
        f"<b>📤 НАЧАТА РАССЫЛКА</b>\n"
        f"Всего пользователей: <b>{total}</b>\n"
        f"Отправлено: <b>{sent}</b>\n"
        f"Ошибок: <b>{failed}</b>\n"
        f"Статус: ⏳ В процессе...",
        parse_mode='HTML'
    )
    
    # Отправляем сообщение КАЖДОМУ пользователю из базы данных
    for i, (user_id, username, first_name, last_name) in enumerate(users):
        try:
            await context.bot.send_message(
                chat_id=user_id,
                text=broadcast_message,
                parse_mode='HTML'
            )
            sent += 1
        except Forbidden as e:
            # Пользователь заблокировал бота — пропускаем без ошибки
            failed += 1
            logger.warning(f"Пользователь {user_id} заблокировал бота: {e}")
        except Exception as e:
            failed += 1
            logger.warning(f"Не удалось отправить сообщение пользователю {user_id}: {e}")
        
        # Обновляем статус каждые 5 пользователей
        if i % 5 == 0 or i == total - 1:
            try:
                await status_message.edit_text(
                    f"<b>📤 РАССЫЛКА В ПРОЦЕССЕ</b>\n"
                    f"Всего пользователей: <b>{total}</b>\n"
                    f"Успешно отправлено: <b>{sent}</b>\n"
                    f"Ошибок: <b>{failed}</b>\n"
                    f"Статус: {'✅ Завершено' if i == total - 1 else '⏳ В процессе...'}",
                    parse_mode='HTML'
                )
            except Exception as e:
                logger.warning(f"Не удалось обновить статус рассылки: {e}")
    
    # Финальное сообщение
    keyboard = [
        [InlineKeyboardButton("↩️ В админ-панель", callback_data='admin_panel')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await status_message.edit_text(
        f"<b>✅ РАССЫЛКА ЗАВЕРШЕНА</b>\n"
        f"Всего пользователей: <b>{total}</b>\n"
        f"Успешно отправлено: <b>{sent}</b>\n"
        f"Ошибок: <b>{failed}</b>\n"
        f"<b>Текст уведомления:</b>\n{broadcast_message}",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )
    
    context.user_data.clear()
    logger.info(f"Рассылка завершена: отправлено {sent}/{total}, ошибок {failed}")

async def edit_broadcast_time(query, context):
    """Возвращает к редактированию времени."""
    context.user_data['broadcast_step'] = 'time'
    
    keyboard = [
        [InlineKeyboardButton("❌ Отмена", callback_data='cancel_broadcast')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "<b>✏️ ИЗМЕНЕНИЕ ВРЕМЕНИ</b>\n"
        "Введите новое <b>время окончания технических работ</b> в формате:\n"
        "<code>ДД.ММ ЧЧ:ММ</code>\n"
        "Пример: <code>05.02 18:30</code>",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def cancel_broadcast(query, context):
    """Отменяет рассылку."""
    context.user_data.clear()
    
    keyboard = [
        [InlineKeyboardButton("↩️ В админ-панель", callback_data='admin_panel')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "<b>❌ РАССЫЛКА ОТМЕНЕНА</b>\n"
        "Уведомление не было отправлено никому.",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

#================== ФУНКЦИИ УПРАВЛЕНИЯ ТЕХРЕЖИМОМ ==================
async def enable_maintenance_mode(query, context):
    """Начинает процесс включения техрежима с выбором времени."""
    if query.from_user.id not in ADMIN_IDS:
        return
    
    context.user_data['setting_maintenance'] = True
    context.user_data['maintenance_step'] = 'duration'
    
    keyboard = [
        [
            InlineKeyboardButton("1 час", callback_data='maintenance_1_hour'),
            InlineKeyboardButton("3 часа", callback_data='maintenance_3_hours')
        ],
        [
            InlineKeyboardButton("5 часов", callback_data='maintenance_5_hours'),
            InlineKeyboardButton("До завтра", callback_data='maintenance_until_tomorrow')
        ],
        [
            InlineKeyboardButton("Бессрочно", callback_data='maintenance_indefinite'),
            InlineKeyboardButton("Настроить вручную", callback_data='maintenance_custom')
        ],
        [InlineKeyboardButton("❌ Отмена", callback_data='admin_panel')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "<b>🔧 ВКЛЮЧЕНИЕ ТЕХРЕЖИМА</b>\n"
        "Бот станет доступен ТОЛЬКО администраторам.\n"
        "Обычные пользователи увидят уведомление о технических работах.\n"
        "<b>Выберите длительность техрежима:</b>",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def handle_maintenance_duration(query, context):
    """Обрабатывает выбор длительности техрежима."""
    if query.from_user.id not in ADMIN_IDS:
        return
    
    tz_minsk = pytz.timezone('Europe/Minsk')
    now = datetime.now(tz_minsk)
    maintenance_until = None
    
    if query.data == 'maintenance_1_hour':
        maintenance_until = (now + timedelta(hours=1)).strftime('%d.%m %H:%M')
    elif query.data == 'maintenance_3_hours':
        maintenance_until = (now + timedelta(hours=3)).strftime('%d.%m %H:%M')
    elif query.data == 'maintenance_5_hours':
        maintenance_until = (now + timedelta(hours=5)).strftime('%d.%m %H:%M')
    elif query.data == 'maintenance_until_tomorrow':
        tomorrow = now + timedelta(days=1)
        maintenance_until = tomorrow.replace(hour=8, minute=0, second=0).strftime('%d.%m %H:%M')
    elif query.data == 'maintenance_indefinite':
        maintenance_until = None
    elif query.data == 'maintenance_custom':
        context.user_data['maintenance_step'] = 'input_until'
        
        keyboard = [
            [InlineKeyboardButton("❌ Отмена", callback_data='admin_panel')],
            [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
        ]
        reply_markup = InlineKeyboardMarkup(keyboard)
        
        await query.edit_message_text(
            "<b>🕗 УКАЖИТЕ ВРЕМЯ ОКОНЧАНИЯ</b>\n"
            "Форматы:\n"
            "• <code>ЧЧ:ММ</code> — сегодня в это время\n"
            "• <code>ДД.ММ ЧЧ:ММ</code> — конкретная дата\n"
            "Примеры:\n"
            "<code>18:30</code> или <code>05.02 18:30</code>",
            reply_markup=reply_markup,
            parse_mode='HTML'
        )
        return
    
    context.user_data['maintenance_until'] = maintenance_until
    context.user_data['maintenance_step'] = 'message'
    
    keyboard = [
        [InlineKeyboardButton("⏭️ Пропустить сообщение", callback_data='skip_maintenance_message')],
        [InlineKeyboardButton("❌ Отмена", callback_data='admin_panel')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    msg = "<b>💬 ДОПОЛНИТЕЛЬНОЕ СООБЩЕНИЕ</b>\n"
    msg += "Введите причину или детали работ (необязательно):\n"
    msg += "<i>Пример: 'Обновление базы данных расписания'</i>"
    
    if maintenance_until:
        msg += f"\n<b>Техрежим будет действовать до:</b> {maintenance_until}"
    else:
        msg += f"\n<b>Техрежим будет бессрочным.</b>"
    
    await query.edit_message_text(
        msg,
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def handle_maintenance_input(update: Update, context: CallbackContext):
    """Обрабатывает ввод времени окончания техрежима вручную."""
    if not context.user_data.get('setting_maintenance') or context.user_data.get('maintenance_step') != 'input_until':
        return
    
    time_input = update.message.text.strip()
    tz_minsk = pytz.timezone('Europe/Minsk')
    now = datetime.now(tz_minsk)
    
    # Пытаемся распарсить ввод
    try:
        # Проверяем формат "ЧЧ:ММ" (сегодня)
        if re.match(r'^\d{2}:\d{2}$', time_input):
            hour, minute = map(int, time_input.split(':'))
            target_time = now.replace(hour=hour, minute=minute, second=0, microsecond=0)
            # Если время уже прошло сегодня, ставим на завтра
            if target_time < now:
                target_time += timedelta(days=1)
            maintenance_until = target_time.strftime('%d.%m %H:%M')
        # Проверяем формат "ДД.ММ ЧЧ:ММ"
        elif re.match(r'^\d{2}\.\d{2} \d{2}:\d{2}$', time_input):
            day_month, time_part = time_input.split(' ')
            day, month = map(int, day_month.split('.'))
            hour, minute = map(int, time_part.split(':'))
            year = now.year
            # Если дата уже прошла в этом году, ставим следующий год
            target_time = datetime(year, month, day, hour, minute, tzinfo=tz_minsk)
            if target_time < now:
                target_time = datetime(year + 1, month, day, hour, minute, tzinfo=tz_minsk)
            maintenance_until = target_time.strftime('%d.%m %H:%M')
        else:
            await update.message.reply_text(
                "❌ Неверный формат времени!\n"
                "Используйте:\n"
                "• <code>ЧЧ:ММ</code> — сегодня\n"
                "• <code>ДД.ММ ЧЧ:ММ</code> — конкретная дата",
                parse_mode='HTML'
            )
            return
    except Exception as e:
        logger.error(f"Ошибка парсинга времени: {e}")
        await update.message.reply_text(
            "❌ Ошибка при обработке времени. Проверьте формат.",
            parse_mode='HTML'
        )
        return
    
    context.user_data['maintenance_until'] = maintenance_until
    context.user_data['maintenance_step'] = 'message'
    
    keyboard = [
        [InlineKeyboardButton("⏭️ Пропустить сообщение", callback_data='skip_maintenance_message')],
        [InlineKeyboardButton("❌ Отмена", callback_data='admin_panel')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await update.message.reply_text(
        f"<b>🕗 Время установлено:</b> {maintenance_until}\n"
        "<b>💬 Введите дополнительное сообщение (причину работ):</b>\n"
        "<i>Пример: 'Обновление расписания'</i>\n"
        "Или нажмите кнопку 'Пропустить', чтобы продолжить без сообщения.",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def skip_maintenance_message(query, context):
    """Пропускает сообщение и сразу включает техрежим."""
    db.set_maintenance_mode(
        True,
        context.user_data.get('maintenance_until'),
        None
    )
    context.user_data.clear()
    await confirm_maintenance_activated(query)

async def confirm_maintenance_activated(query):
    """Подтверждение включения техрежима."""
    maintenance = db.get_maintenance_status()
    msg = "✅ <b>ТЕХРЕЖИМ ВКЛЮЧЕН</b>\n"
    msg += "⚠️ Бот теперь доступен ТОЛЬКО администраторам.\n"
    
    if maintenance['until']:
        msg += f"🕗 Восстановление: <b>{maintenance['until']}</b>\n"
    if maintenance['message']:
        msg += f"💬 Причина: <i>{maintenance['message']}</i>\n"
    
    keyboard = [
        [InlineKeyboardButton("↩️ В админ-панель", callback_data='admin_panel')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        msg,
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def disable_maintenance_mode(query, context):
    """Выключает техрежим."""
    if query.from_user.id not in ADMIN_IDS:
        return
    
    db.set_maintenance_mode(False)
    
    keyboard = [
        [InlineKeyboardButton("↩️ В админ-панель", callback_data='admin_panel')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "✅ <b>ТЕХРЕЖИМ ОТКЛЮЧЕН</b>\n"
        "Бот снова доступен всем пользователям.",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

#================== ДОБАВЛЕНА ФУНКЦИЯ ПОКАЗА ДАТЫ ВЫБОРА (ТОЛЬКО БУДНИЕ ДНИ) ==================
async def show_date_selection(query, context):
    """Показывает выбор даты для добавления замены (ТОЛЬКО будние дни: Пн-Пт)."""
    tz_minsk = pytz.timezone('Europe/Minsk')
    today = datetime.now(tz_minsk).date()
    
    keyboard = []
    days_shown = 0
    max_days = 10  # Показываем максимум 10 будних дней вперёд
    
    # Показываем даты ТОЛЬКО с понедельника по пятницу
    current_date = today
    while days_shown < max_days:
        weekday = current_date.weekday()
        # Пропускаем субботу (5) и воскресенье (6)
        if weekday < 5:
            day_name = DAYS_OF_WEEK[weekday]
            date_str = current_date.strftime('%Y-%m-%d')
            button_text = f"{day_name} ({current_date.strftime('%d.%m')})"
            keyboard.append([InlineKeyboardButton(button_text, callback_data=f'date_{date_str}')])
            days_shown += 1
        current_date += timedelta(days=1)
    
    keyboard.append([
        InlineKeyboardButton("❌ Отмена", callback_data='cancel_adding')
    ])
    keyboard.append([InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')])
    
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "<b>➕ ДОБАВЛЕНИЕ ЗАМЕНЫ</b>\n"
        "<b>📅 Выберите дату замены:</b>\n"
        "<i>Показаны только будние дни (Пн-Пт)</i>",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

#================== КОМАНДА /start С СОХРАНЕНИЕМ ПОЛЬЗОВАТЕЛЯ ДО ПРОВЕРКИ ТЕХРЕЖИМА ==================
async def start(update: Update, context: CallbackContext):
    """Главное меню бота с сохранением пользователя в БД ДО проверки техрежима."""
    # 🔑 КЛЮЧЕВОЕ ИСПРАВЛЕНИЕ: Сначала сохраняем пользователя, ПОТОМ проверяем техрежим
    user = update.effective_user
    db.add_user(
        user_id=user.id,
        username=user.username,
        first_name=user.first_name,
        last_name=user.last_name,
        language_code=user.language_code
    )
    
    # Проверяем техрежим ПОСЛЕ сохранения пользователя
    if await check_maintenance_mode(update, context):
        return
    
    keyboard = [
        [InlineKeyboardButton("⏰ Сейчас", callback_data='menu_now')],
        [InlineKeyboardButton("👥 Мой класс", callback_data='menu_my_class')],
        [InlineKeyboardButton("👨‍🏫 Расписание учителей", callback_data='menu_teacher')],
        [InlineKeyboardButton("🕐 Расписание звонков", callback_data='menu_bells')],
        [InlineKeyboardButton("📚 Расписание уроков", callback_data='menu_schedule')],
        [InlineKeyboardButton("🔄 Замены", callback_data='menu_substitutions')],
        [InlineKeyboardButton("🔍 Поиск учителя", callback_data='menu_search_teacher')],
        [InlineKeyboardButton("🆘 Помощь", callback_data='menu_help')],
        [InlineKeyboardButton("👑 Админ-панель", callback_data='admin_panel')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    try:
        await update.message.reply_text(
            '🏫 *Школьный бот*\nВыберите раздел:',
            reply_markup=reply_markup,
            parse_mode='Markdown'
        )
    except Exception as e:
        logger.error(f"Ошибка в команде start: {e}")
        await update.message.reply_text("Произошла ошибка. Попробуйте позже.")

#================== ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ ДЛЯ КНОПКИ СТАРТ ==================
def add_start_button(keyboard=None):
    """Добавляет кнопку 'Старт' в клавиатуру."""
    if keyboard is None:
        keyboard = []
    keyboard.append([InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')])
    return keyboard

#================== ФУНКЦИИ ДЛЯ "МОЙ КЛАСС" ==================
async def show_my_class_menu(query, context):
    """Показывает меню 'Мой класс'."""
    user_id = query.from_user.id
    
    # Проверяем, является ли пользователь учителем
    teacher_name = None
    for name, tid in TEACHER_IDS.items():
        if tid == user_id:
            teacher_name = name
            break
    
    if teacher_name:
        # Если пользователь учитель - показываем его расписание
        teacher_schedule = get_teacher_schedule(teacher_name)
        schedule_text = format_teacher_schedule(teacher_name, teacher_schedule)
        
        keyboard = [
            [InlineKeyboardButton("↩️ В главное меню", callback_data='back_to_main')],
            [InlineKeyboardButton("🔄 Обновить", callback_data='my_class_teacher')]
        ]
        reply_markup = InlineKeyboardMarkup(keyboard)
        
        # Обрезаем если сообщение слишком длинное
        if len(schedule_text) > 4096:
            schedule_text = schedule_text[:4090] + "\n...(обрезано)"
        
        await query.edit_message_text(
            schedule_text,
            reply_markup=reply_markup,
            parse_mode='HTML'
        )
        return
    
    # Если не учитель - проверяем сохраненный класс
    user_class = db.get_user_class(user_id)
    if user_class:
        # Показываем меню для выбранного класса
        keyboard = [
            [InlineKeyboardButton("📅 Понедельник", callback_data=f'myclass_schedule_понедельник')],
            [InlineKeyboardButton("📅 Вторник", callback_data=f'myclass_schedule_вторник')],
            [InlineKeyboardButton("📅 Среда", callback_data=f'myclass_schedule_среда')],
            [InlineKeyboardButton("📅 Четверг", callback_data=f'myclass_schedule_четверг')],
            [InlineKeyboardButton("📅 Пятница", callback_data=f'myclass_schedule_пятница')],
            [InlineKeyboardButton("📋 Вся неделя", callback_data=f'myclass_weekly_{user_class}')],
            [InlineKeyboardButton("✏️ Изменить класс", callback_data='myclass_change')],
            [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
        ]
        reply_markup = InlineKeyboardMarkup(keyboard)
        
        await query.edit_message_text(
            f"<b>👥 МОЙ КЛАСС: {user_class.upper()}</b>\n"
            "Выберите день недели для просмотра расписания:",
            reply_markup=reply_markup,
            parse_mode='HTML'
        )
    else:
        # Предлагаем выбрать класс
        await show_class_selection_for_my_class(query, context)

async def show_class_selection_for_my_class(query, context):
    """Показывает выбор класса для функции 'Мой класс'."""
    keyboard = [
        [InlineKeyboardButton("5А", callback_data='myclass_set_5а'), InlineKeyboardButton("5Б", callback_data='myclass_set_5б'), InlineKeyboardButton("5В", callback_data='myclass_set_5в')],
        [InlineKeyboardButton("6А", callback_data='myclass_set_6а'), InlineKeyboardButton("6Б", callback_data='myclass_set_6б'), InlineKeyboardButton("6В", callback_data='myclass_set_6в')],
        [InlineKeyboardButton("7А", callback_data='myclass_set_7а'), InlineKeyboardButton("7Б", callback_data='myclass_set_7б'), InlineKeyboardButton("7В", callback_data='myclass_set_7в')],
        [InlineKeyboardButton("8А", callback_data='myclass_set_8а'), InlineKeyboardButton("8Б", callback_data='myclass_set_8б')],
        [InlineKeyboardButton("9А", callback_data='myclass_set_9а'), InlineKeyboardButton("9Б", callback_data='myclass_set_9б')],
        [InlineKeyboardButton("10А", callback_data='myclass_set_10а'), InlineKeyboardButton("10Б", callback_data='myclass_set_10б')],
        [InlineKeyboardButton("11", callback_data='myclass_set_11')],
        [InlineKeyboardButton("↩️ Назад", callback_data='menu_my_class')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "<b>👥 МОЙ КЛАСС</b>\n"
        "Выберите ваш класс для быстрого доступа к расписанию:",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def set_my_class(query, context):
    """Устанавливает класс для пользователя."""
    user_id = query.from_user.id
    class_name = query.data.replace('myclass_set_', '')
    db.set_user_class(user_id, class_name)
    
    keyboard = [
        [InlineKeyboardButton("📅 Понедельник", callback_data=f'myclass_schedule_понедельник')],
        [InlineKeyboardButton("📅 Вторник", callback_data=f'myclass_schedule_вторник')],
        [InlineKeyboardButton("📅 Среда", callback_data=f'myclass_schedule_среда')],
        [InlineKeyboardButton("📅 Четверг", callback_data=f'myclass_schedule_четверг')],
        [InlineKeyboardButton("📅 Пятница", callback_data=f'myclass_schedule_пятница')],
        [InlineKeyboardButton("📋 Вся неделя", callback_data=f'myclass_weekly_{class_name}')],
        [InlineKeyboardButton("✏️ Изменить класс", callback_data='myclass_change')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        f"✅ <b>Класс {class_name.upper()} сохранен как 'Мой класс'</b>\n"
        "Теперь вы можете быстро просматривать расписание вашего класса.\n"
        "Выберите день недели для просмотра расписания:",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def show_my_class_schedule(query, context):
    """Показывает расписание для сохраненного класса."""
    user_id = query.from_user.id
    user_class = db.get_user_class(user_id)
    
    if not user_class:
        await query.edit_message_text(
            "<b>❌ Класс не выбран</b>\n"
            "Пожалуйста, выберите класс для использования функции 'Мой класс'.",
            parse_mode='HTML'
        )
        return
    
    day = query.data.replace('myclass_schedule_', '').capitalize()
    today = datetime.now().date()
    
    day_mapping = {
        'Понедельник': 0,
        'Вторник': 1,
        'Среда': 2,
        'Четверг': 3,
        'Пятница': 4
    }
    
    target_date_str = None
    if day in day_mapping:
        # Находим ближайшую дату с таким днем недели
        current_weekday = today.weekday()
        target_weekday = day_mapping[day]
        days_ahead = target_weekday - current_weekday
        if days_ahead < 0:
            days_ahead += 7
        target_date = today + timedelta(days=days_ahead)
        target_date_str = target_date.strftime('%Y-%m-%d')
    
    if user_class in SCHEDULE_STRUCTURED and day in SCHEDULE_STRUCTURED[user_class]:
        structured_lessons = SCHEDULE_STRUCTURED[user_class][day]
        schedule_text = format_schedule_day(user_class, day, structured_lessons, target_date_str)
    else:
        schedule_text = f"Расписание для класса {user_class} на {day} не найдено."
    
    keyboard = [
        [InlineKeyboardButton("📋 Вся неделя", callback_data=f'myclass_weekly_{user_class}')],
        [InlineKeyboardButton("↩️ Назад к меню", callback_data='menu_my_class')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        schedule_text,
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def show_my_class_weekly(query, context):
    """Показывает расписание на всю неделю для сохраненного класса."""
    user_id = query.from_user.id
    user_class = db.get_user_class(user_id)
    
    if not user_class:
        await query.edit_message_text(
            "<b>❌ Класс не выбран</b>\n"
            "Пожалуйста, выберите класс для использования функции 'Мой класс'.",
            parse_mode='HTML'
        )
        return
    
    class_name = query.data.replace('myclass_weekly_', '')
    schedule_text = format_weekly_schedule(class_name)
    
    keyboard = [
        [InlineKeyboardButton("↩️ Назад к меню", callback_data='menu_my_class')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    # Обрезаем сообщение если оно слишком длинное
    if len(schedule_text) > 4096:
        schedule_text = schedule_text[:4090] + "\n...(обрезано)"
    
    await query.edit_message_text(
        schedule_text,
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

#================== ФУНКЦИИ ДЛЯ "СЕЙЧАС" ==================
async def show_now_class_selection(query, context):
    """Показывает выбор класса для функции 'Сейчас'."""
    keyboard = [
        [InlineKeyboardButton("5А", callback_data='now_class_5а'), InlineKeyboardButton("5Б", callback_data='now_class_5б'), InlineKeyboardButton("5В", callback_data='now_class_5в')],
        [InlineKeyboardButton("6А", callback_data='now_class_6а'), InlineKeyboardButton("6Б", callback_data='now_class_6б'), InlineKeyboardButton("6В", callback_data='now_class_6в')],
        [InlineKeyboardButton("7А", callback_data='now_class_7а'), InlineKeyboardButton("7Б", callback_data='now_class_7б'), InlineKeyboardButton("7В", callback_data='now_class_7в')],
        [InlineKeyboardButton("8А", callback_data='now_class_8а'), InlineKeyboardButton("8Б", callback_data='now_class_8б')],
        [InlineKeyboardButton("9А", callback_data='now_class_9а'), InlineKeyboardButton("9Б", callback_data='now_class_9б')],
        [InlineKeyboardButton("10А", callback_data='now_class_10а'), InlineKeyboardButton("10Б", callback_data='now_class_10б')],
        [InlineKeyboardButton("11", callback_data='now_class_11')],
        [InlineKeyboardButton("↩️ Назад", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "<b>⏰ ТЕКУЩИЙ УРОК</b>\nВыберите ваш класс:",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def show_current_lesson(query, context):
    """Показывает текущий/следующий урок для выбранного класса с защитой от ошибки 'message not modified'."""
    # Извлекаем имя класса из коллбэка
    if query.data.startswith('now_class_'):
        class_name = query.data.replace('now_class_', '')
        context.user_data['current_class'] = class_name
    else:
        # Для кнопки "Обновить" используем сохранённый класс
        class_name = context.user_data.get('current_class', '8а')
    
    tz_minsk = pytz.timezone('Europe/Minsk')
    now = datetime.now(tz_minsk)
    weekday = now.weekday()
    
    # Проверка выходного дня (суббота = 5, воскресенье = 6)
    if weekday >= 5:  # Суббота или воскресенье
        day_name = "Суббота" if weekday == 5 else "Воскресенье"
        keyboard = [
            [InlineKeyboardButton("🔄 Обновить", callback_data=f'now_class_{class_name}')],
            [InlineKeyboardButton("↩️ Назад к выбору класса", callback_data='menu_now')],
            [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
        ]
        reply_markup = InlineKeyboardMarkup(keyboard)
        
        message_text = (
            f"📅 <b>{day_name}</b>\n"
            f"🕗 Сегодня нет уроков.\n"
            f"Следующие уроки будут в понедельник.\n"
            f"<i>Обновлено: {now.strftime('%H:%M:%S')}</i>"
        )
        
        try:
            await query.edit_message_text(
                message_text,
                reply_markup=reply_markup,
                parse_mode='HTML'
            )
        except BadRequest as e:
            if "message is not modified" in str(e).lower():
                await query.answer("ℹ️ Информация уже актуальна", show_alert=False)
            else:
                raise
        return
    
    day_name = DAYS_OF_WEEK[weekday]
    current_info = get_current_lesson_info()
    
    # Формируем сообщение с временной меткой обновления
    if current_info['status'] == 'finished':
        text = f"✅ <b>Уроки закончились!</b>\n"
        text += f"📅 Сегодня: <b>{day_name}</b>\n"
        text += f"🕗 Все уроки завершены на сегодня.\n"
        text += f"<i>Обновлено: {now.strftime('%H:%M:%S')}</i>"
    elif current_info['status'] == 'break':
        minutes = current_info['minutes_until']
        next_num = current_info['next_number']
        text = f"⏸️ <b>СЕЙЧАС ПЕРЕМЕНА</b>\n"
        text += f"📅 Сегодня: <b>{day_name}</b>\n"
        text += f"⏰ Следующий урок (<b>{next_num}</b>) начнётся через <b>{minutes} мин</b>\n"
        text += f"🕐 Время урока: {current_info['start_time']}–{current_info['end_time']}\n"
        
        # Показываем расписание следующего урока
        if class_name in SCHEDULE_STRUCTURED and day_name in SCHEDULE_STRUCTURED[class_name]:
            lessons = SCHEDULE_STRUCTURED[class_name][day_name]
            next_lesson = next((l for l in lessons if l[0] == next_num), None)
            if next_lesson:
                subject, teacher = next_lesson[1], next_lesson[2]
                text += f"📚 <b>Предмет:</b> {subject}\n"
                text += f"👨‍🏫 <b>Учитель:</b> {teacher}\n"
                
                # Проверяем замены
                today_str = now.strftime('%Y-%m-%d')
                subs = db.get_substitutions_for_class_date(class_name, today_str)
                substitution = next((s for s in subs if s[3] == next_num), None)
                if substitution:
                    text += f"⚠️ <b>ЗАМЕНА:</b> {substitution[5]} ({substitution[7]})\n"
        text += f"<i>Обновлено: {now.strftime('%H:%M:%S')}</i>"
    else:  # status == 'lesson'
        lesson_num = current_info['number']
        time_left = current_info['time_left']
        text = f"🔔 <b>СЕЙЧАС ИДЁТ УРОК №{lesson_num}</b>\n"
        text += f"📅 Сегодня: <b>{day_name}</b>\n"
        text += f"⏰ Урок закончится через <b>{time_left} мин</b>\n"
        text += f"🕐 Время урока: {current_info['start_time']}–{current_info['end_time']}\n"
        
        # Показываем расписание текущего урока
        if class_name in SCHEDULE_STRUCTURED and day_name in SCHEDULE_STRUCTURED[class_name]:
            lessons = SCHEDULE_STRUCTURED[class_name][day_name]
            current_lesson = next((l for l in lessons if l[0] == lesson_num), None)
            if current_lesson:
                subject, teacher = current_lesson[1], current_lesson[2]
                text += f"📚 <b>Предмет:</b> {subject}\n"
                text += f"👨‍🏫 <b>Учитель:</b> {teacher}\n"
                
                # Проверяем замены
                today_str = now.strftime('%Y-%m-%d')
                subs = db.get_substitutions_for_class_date(class_name, today_str)
                substitution = next((s for s in subs if s[3] == lesson_num), None)
                if substitution:
                    text += f"⚠️ <b>ЗАМЕНА:</b> {substitution[5]} ({substitution[7]})\n"
        text += f"<i>Обновлено: {now.strftime('%H:%M:%S')}</i>"
    
    # Кнопки навигации
    keyboard = [
        [InlineKeyboardButton("🔄 Обновить", callback_data=f'now_class_{class_name}')],
        [InlineKeyboardButton("↩️ Назад к выбору класса", callback_data='menu_now')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    # Защита от ошибки "message is not modified"
    try:
        await query.edit_message_text(
            text,
            reply_markup=reply_markup,
            parse_mode='HTML'
        )
    except BadRequest as e:
        if "message is not modified" in str(e).lower():
            await query.answer("✅ Информация актуальна", show_alert=False)
        else:
            raise

#================== УПРОЩЕННОЕ ДОБАВЛЕНИЕ ЗАМЕНЫ (4 ШАГА) ==================
async def start_adding_substitution(query, context):
    """Начинает процесс добавления замены (упрощенный - 4 шага)."""
    if query.from_user.id not in ADMIN_IDS:
        return
    
    context.user_data['adding_substitution'] = True
    context.user_data['step'] = 'date'
    await show_date_selection(query, context)

async def show_class_lesson_selection(query, context):
    """Показывает выбор класса и урока в одном окне (ШАГ 2)."""
    date_str = context.user_data.get('date')
    day_name = context.user_data.get('day')
    
    if not date_str or not day_name:
        await query.edit_message_text(
            "<b>❌ Ошибка: дата не выбрана</b>",
            parse_mode='HTML'
        )
        return
    
    # Создаем клавиатуру с классами
    keyboard = []
    for class_name in ALL_CLASSES:
        # Проверяем, есть ли уроки у этого класса в выбранный день
        if class_name in SCHEDULE_STRUCTURED and day_name in SCHEDULE_STRUCTURED[class_name]:
            lessons = SCHEDULE_STRUCTURED[class_name][day_name]
            if lessons:
                button_text = f"{class_name.upper()} ({len(lessons)} уроков)"
                callback_data = f'sub_class_{class_name}'
                keyboard.append([InlineKeyboardButton(button_text, callback_data=callback_data)])
    
    keyboard.append([
        InlineKeyboardButton("◀️ Назад к дате", callback_data='back_to_date'),
        InlineKeyboardButton("❌ Отмена", callback_data='cancel_adding')
    ])
    keyboard.append([InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')])
    
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "<b>➕ ДОБАВЛЕНИЕ ЗАМЕНЫ (ШАГ 2/4)</b>\n"
        f"<b>📅 Дата:</b> {date_str} ({day_name})\n"
        "<b>🏫 Выберите класс:</b>\n"
        "<i>Показаны только классы с уроками в этот день</i>",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def show_lesson_selection(query, context):
    """Показывает выбор урока (ШАГ 3)."""
    class_name = context.user_data.get('class_name')
    date_str = context.user_data.get('date')
    day_name = context.user_data.get('day')
    
    if not class_name or not date_str or not day_name:
        await query.edit_message_text(
            "<b>❌ Ошибка: данные не выбраны</b>",
            parse_mode='HTML'
        )
        return
    
    # Получаем уроки для выбранного класса и дня
    if class_name not in SCHEDULE_STRUCTURED or day_name not in SCHEDULE_STRUCTURED[class_name]:
        await query.edit_message_text(
            f"<b>❌ Ошибка: у класса {class_name.upper()} нет уроков в {day_name}</b>",
            parse_mode='HTML'
        )
        return
    
    lessons = SCHEDULE_STRUCTURED[class_name][day_name]
    
    # Создаем клавиатуру с уроками
    keyboard = []
    for lesson_num, subject, teacher in lessons:
        lesson_time = get_lesson_time(lesson_num)
        button_text = f"{lesson_num}. {lesson_time} - {subject} ({teacher})"
        callback_data = f'lesson_{lesson_num}'
        keyboard.append([InlineKeyboardButton(button_text, callback_data=callback_data)])
    
    keyboard.append([
        InlineKeyboardButton("◀️ Назад к классу", callback_data='back_to_class'),
        InlineKeyboardButton("❌ Отмена", callback_data='cancel_adding')
    ])
    keyboard.append([InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')])
    
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "<b>➕ ДОБАВЛЕНИЕ ЗАМЕНЫ (ШАГ 3/4)</b>\n"
        f"<b>📅 Дата:</b> {date_str} ({day_name})\n"
        f"<b>🏫 Класс:</b> {class_name.upper()}\n"
        "<b>🔢 Выберите урок для замены:</b>",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def show_teacher_selection(query, context):
    """Показывает выбор нового учителя (ШАГ 4)."""
    lesson_num = context.user_data.get('lesson')
    class_name = context.user_data.get('class_name')
    date_str = context.user_data.get('date')
    day_name = context.user_data.get('day')
    
    if not all([lesson_num, class_name, date_str, day_name]):
        await query.edit_message_text(
            "<b>❌ Ошибка: данные не выбраны</b>",
            parse_mode='HTML'
        )
        return
    
    # Получаем текущий урок из расписания
    if class_name not in SCHEDULE_STRUCTURED or day_name not in SCHEDULE_STRUCTURED[class_name]:
        await query.edit_message_text(
            f"<b>❌ Ошибка: у класса {class_name.upper()} нет уроков в {day_name}</b>",
            parse_mode='HTML'
        )
        return
    
    lessons = SCHEDULE_STRUCTURED[class_name][day_name]
    current_lesson = next((l for l in lessons if l[0] == lesson_num), None)
    
    if not current_lesson:
        await query.edit_message_text(
            f"<b>❌ Ошибка: урока №{lesson_num} нет у класса {class_name.upper()} в {day_name}</b>",
            parse_mode='HTML'
        )
        return
    
    old_subject, old_teacher = current_lesson[1], current_lesson[2]
    # Сохраняем старые данные
    context.user_data['old_subject'] = old_subject
    context.user_data['old_teacher'] = old_teacher
    
    # Получаем всех учителей для выбора
    all_teachers = get_all_teachers()
    
    # Создаем клавиатуру с учителями (максимум 10 на страницу)
    keyboard = []
    teachers_per_page = 10
    page = context.user_data.get('teacher_page', 0)
    
    # Фильтруем учителей - исключаем текущего учителя из списка
    filtered_teachers = [t for t in all_teachers if t != old_teacher]
    
    start_idx = page * teachers_per_page
    end_idx = start_idx + teachers_per_page
    teachers_page = filtered_teachers[start_idx:end_idx]
    
    for teacher in teachers_page:
        button_text = teacher
        if len(teacher) > 25:
            button_text = teacher[:23] + "..."
        keyboard.append([InlineKeyboardButton(button_text, callback_data=f'select_teacher_{teacher}')])
    
    # Добавляем навигацию по страницам
    nav_row = []
    if page > 0:
        nav_row.append(InlineKeyboardButton("◀️ Назад", callback_data='teacher_page_prev'))
    if end_idx < len(filtered_teachers):
        nav_row.append(InlineKeyboardButton("Вперёд ▶️", callback_data='teacher_page_next'))
    
    if nav_row:
        keyboard.append(nav_row)
    
    keyboard.append([
        InlineKeyboardButton("✏️ Ввести вручную", callback_data='manual_teacher_input'),
        InlineKeyboardButton("🔄 Оставить без изменений", callback_data='keep_old_teacher')
    ])
    keyboard.append([
        InlineKeyboardButton("◀️ Назад к уроку", callback_data='back_to_lesson'),
        InlineKeyboardButton("❌ Отмена", callback_data='cancel_adding')
    ])
    keyboard.append([InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')])
    
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        "<b>➕ ДОБАВЛЕНИЕ ЗАМЕНЫ (ШАГ 4/4)</b>\n"
        f"<b>📅 Дата:</b> {date_str} ({day_name})\n"
        f"<b>🏫 Класс:</b> {class_name.upper()}\n"
        f"<b>🔢 Урок:</b> {lesson_num} ({get_lesson_time(lesson_num)})\n"
        f"<b>📚 Текущий предмет:</b> {old_subject}\n"
        f"<b>👨‍🏫 Текущий учитель:</b> {old_teacher}\n"
        "<b>👩‍🏫 Выберите НОВОГО учителя:</b>\n"
        "<i>Или введите фамилию вручную</i>",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def handle_teacher_selection(query, context):
    """Обрабатывает выбор учителя из списка."""
    if query.data.startswith('select_teacher_'):
        teacher_name = query.data.replace('select_teacher_', '')
        context.user_data['new_teacher'] = teacher_name
        
        # Переходим к подтверждению замены
        await confirm_substitution(query, context)
        return
    
    elif query.data == 'teacher_page_prev':
        page = context.user_data.get('teacher_page', 0)
        if page > 0:
            context.user_data['teacher_page'] = page - 1
        await show_teacher_selection(query, context)
        return
    
    elif query.data == 'teacher_page_next':
        context.user_data['teacher_page'] = context.user_data.get('teacher_page', 0) + 1
        await show_teacher_selection(query, context)
        return
    
    elif query.data == 'manual_teacher_input':
        context.user_data['step'] = 'manual_teacher'
        keyboard = [
            [InlineKeyboardButton("◀️ Вернуться к выбору", callback_data='back_to_teacher_selection')],
            [InlineKeyboardButton("❌ Отмена", callback_data='cancel_adding')],
            [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
        ]
        reply_markup = InlineKeyboardMarkup(keyboard)
        await query.edit_message_text(
            "<b>✏️ ВВОД УЧИТЕЛЯ ВРУЧНУЮ</b>\n"
            "Введите фамилию и инициалы учителя (например: 'Коротчикова Л.В.'):\n"
            "<i>Учитель должен существовать в системе для отправки уведомлений</i>",
            reply_markup=reply_markup,
            parse_mode='HTML'
        )
        return
    
    elif query.data == 'keep_old_teacher':
        # Оставляем старого учителя без изменений
        old_teacher = context.user_data.get('old_teacher')
        context.user_data['new_teacher'] = old_teacher
        await confirm_substitution(query, context)
        return
    
    elif query.data == 'back_to_teacher_selection':
        context.user_data['step'] = 'teacher_selection'
        await show_teacher_selection(query, context)
        return

async def handle_manual_teacher_input(update: Update, context: CallbackContext):
    """Обрабатывает ручной ввод фамилии учителя."""
    if not context.user_data.get('adding_substitution') or context.user_data.get('step') != 'manual_teacher':
        return
    
    teacher_input = update.message.text.strip()
    
    # Проверяем, существует ли такой учитель в системе
    all_teachers = get_all_teachers()
    matching_teachers = [t for t in all_teachers if teacher_input.lower() in t.lower()]
    
    if not matching_teachers:
        # Предлагаем выбрать из похожих или ввести снова
        keyboard = [
            [InlineKeyboardButton("🔄 Ввести снова", callback_data='manual_teacher_input')],
            [InlineKeyboardButton("◀️ Вернуться к списку", callback_data='back_to_teacher_selection')],
            [InlineKeyboardButton("❌ Отмена", callback_data='cancel_adding')]
        ]
        reply_markup = InlineKeyboardMarkup(keyboard)
        await update.message.reply_text(
            f"<b>❌ Учитель '{teacher_input}' не найден в системе.</b>\n"
            "Проверьте правильность написания фамилии и инициалов.\n"
            "Пример правильного формата: <code>Коротчикова Л.В.</code>",
            reply_markup=reply_markup,
            parse_mode='HTML'
        )
        return
    
    # Если найден один учитель - используем его
    if len(matching_teachers) == 1:
        context.user_data['new_teacher'] = matching_teachers[0]
        await confirm_substitution_from_message(update, context)
        return
    
    # Если найдено несколько учителей - предлагаем выбрать
    keyboard = []
    for teacher in matching_teachers[:10]:
        keyboard.append([InlineKeyboardButton(teacher, callback_data=f'select_teacher_{teacher}')])
    
    keyboard.append([InlineKeyboardButton("🔄 Ввести снова", callback_data='manual_teacher_input')])
    keyboard.append([InlineKeyboardButton("❌ Отмена", callback_data='cancel_adding')])
    
    reply_markup = InlineKeyboardMarkup(keyboard)
    await update.message.reply_text(
        f"<b>🔍 Найдено {len(matching_teachers)} учителей:</b>\n"
        "Выберите нужного учителя из списка:",
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def confirm_substitution(query, context):
    """Подтверждает добавление замены (финальный шаг)."""
    # Собираем все данные
    date = context.user_data.get('date')
    day = context.user_data.get('day')
    class_name = context.user_data.get('class_name')
    lesson = context.user_data.get('lesson')
    old_subject = context.user_data.get('old_subject')
    old_teacher = context.user_data.get('old_teacher')
    new_teacher = context.user_data.get('new_teacher', old_teacher)
    new_subject = context.user_data.get('new_subject', old_subject)
    
    if not all([date, day, class_name, lesson, old_subject, old_teacher]):
        await query.edit_message_text(
            "<b>❌ Ошибка: недостаточно данных для добавления замены.</b>",
            parse_mode='HTML'
        )
        return
    
    # Формируем сообщение подтверждения
    confirmation_text = (
        "<b>✅ ПОДТВЕРЖДЕНИЕ ЗАМЕНЫ</b>\n\n"
        f"<b>📅 Дата:</b> {date} ({day})\n"
        f"<b>🏫 Класс:</b> {class_name.upper()}\n"
        f"<b>🔢 Урок:</b> {lesson} ({get_lesson_time(lesson)})\n"
        f"<b>📚 Предмет:</b> {old_subject} → <b>{new_subject}</b>\n"
        f"<b>👨‍🏫 Учитель:</b> {old_teacher} → <b>{new_teacher}</b>\n\n"
        "<i>⚠️ После подтверждения замена будет добавлена в базу данных "
        "и уведомление отправлено новому учителю.</i>"
    )
    
    keyboard = [
        [
            InlineKeyboardButton("✅ Подтвердить", callback_data='confirm_substitution_final'),
            InlineKeyboardButton("✏️ Изменить", callback_data='edit_substitution')
        ],
        [
            InlineKeyboardButton("❌ Отмена", callback_data='cancel_adding')
        ],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await query.edit_message_text(
        confirmation_text,
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def confirm_substitution_from_message(update: Update, context: CallbackContext):
    """Подтверждает замену после ручного ввода учителя."""
    # Собираем все данные
    date = context.user_data.get('date')
    day = context.user_data.get('day')
    class_name = context.user_data.get('class_name')
    lesson = context.user_data.get('lesson')
    old_subject = context.user_data.get('old_subject')
    old_teacher = context.user_data.get('old_teacher')
    new_teacher = context.user_data.get('new_teacher', old_teacher)
    new_subject = context.user_data.get('new_subject', old_subject)
    
    if not all([date, day, class_name, lesson, old_subject, old_teacher]):
        await update.message.reply_text(
            "<b>❌ Ошибка: недостаточно данных для добавления замены.</b>",
            parse_mode='HTML'
        )
        return
    
    # Формируем сообщение подтверждения
    confirmation_text = (
        "<b>✅ ПОДТВЕРЖДЕНИЕ ЗАМЕНЫ</b>\n\n"
        f"<b>📅 Дата:</b> {date} ({day})\n"
        f"<b>🏫 Класс:</b> {class_name.upper()}\n"
        f"<b>🔢 Урок:</b> {lesson} ({get_lesson_time(lesson)})\n"
        f"<b>📚 Предмет:</b> {old_subject} → <b>{new_subject}</b>\n"
        f"<b>👨‍🏫 Учитель:</b> {old_teacher} → <b>{new_teacher}</b>\n\n"
        "<i>⚠️ После подтверждения замена будет добавлена в базу данных "
        "и уведомление отправлено новому учителю.</i>"
    )
    
    keyboard = [
        [
            InlineKeyboardButton("✅ Подтвердить", callback_data='confirm_substitution_final'),
            InlineKeyboardButton("✏️ Изменить", callback_data='edit_substitution')
        ],
        [
            InlineKeyboardButton("❌ Отмена", callback_data='cancel_adding')
        ],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await update.message.reply_text(
        confirmation_text,
        reply_markup=reply_markup,
        parse_mode='HTML'
    )

async def finalize_substitution(query, context):
    """Финализирует добавление замены в базу данных."""
    # Собираем все данные
    date = context.user_data.get('date')
    day = context.user_data.get('day')
    class_name = context.user_data.get('class_name')
    lesson = context.user_data.get('lesson')
    old_subject = context.user_data.get('old_subject')
    old_teacher = context.user_data.get('old_teacher')
    new_teacher = context.user_data.get('new_teacher', old_teacher)
    new_subject = context.user_data.get('new_subject', old_subject)
    
    if not all([date, day, class_name, lesson, old_subject, old_teacher, new_teacher, new_subject]):
        await query.edit_message_text(
            "<b>❌ Ошибка: недостаточно данных для добавления замены.</b>",
            parse_mode='HTML'
        )
        return
    
    try:
        # Сохраняем замену в базу
        db.add_substitution(
            date, day, lesson,
            old_subject, new_subject,
            old_teacher, new_teacher,
            class_name
        )
        
        # Подготавливаем данные для уведомления
        substitution_data = {
            'date': date,
            'day': day,
            'class_name': class_name,
            'lesson': lesson,
            'old_subject': old_subject,
            'new_subject': new_subject,
            'old_teacher': old_teacher,
            'new_teacher': new_teacher
        }
        
        # Отправляем уведомление новому учителю
        await send_substitution_notification(context, new_teacher, substitution_data)
        
        # Очищаем данные пользователя
        context.user_data.clear()
        
        # Формируем сообщение об успехе
        success_message = (
            "✅ <b>ЗАМЕНА УСПЕШНО ДОБАВЛЕНА!</b>\n\n"
            f"📅 <b>Дата:</b> {date} ({day})\n"
            f"🏫 <b>Класс:</b> {class_name.upper()}\n"
            f"🔢 <b>Урок:</b> {lesson} ({get_lesson_time(lesson)})\n"
            f"📚 <b>Предмет:</b> {old_subject} → {new_subject}\n"
            f"👨‍🏫 <b>Учитель:</b> {old_teacher} → {new_teacher}\n\n"
            "🔔 <i>Уведомление отправлено новому учителю.</i>"
        )
        
        keyboard = [
            [InlineKeyboardButton("➕ Добавить ещё замену", callback_data='admin_add_sub')],
            [InlineKeyboardButton("📋 Посмотреть все замены", callback_data='admin_view_subs')],
            [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
        ]
        reply_markup = InlineKeyboardMarkup(keyboard)
        
        await query.edit_message_text(
            success_message,
            reply_markup=reply_markup,
            parse_mode='HTML'
        )
        
        logger.info(f"✅ Замена добавлена: {date} {class_name} урок {lesson} | {old_teacher} → {new_teacher}")
        
    except Exception as e:
        logger.error(f"Ошибка при сохранении замены: {e}")
        keyboard = [
            [InlineKeyboardButton("↩️ Вернуться в админ-панель", callback_data='admin_panel')],
            [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
        ]
        reply_markup = InlineKeyboardMarkup(keyboard)
        await query.edit_message_text(
            f"<b>❌ Ошибка при сохранении замены:</b>\n{str(e)[:200]}",
            reply_markup=reply_markup,
            parse_mode='HTML'
        )

async def edit_substitution(query, context):
    """Возвращает к редактированию замены."""
    # Возвращаемся к выбору урока
    context.user_data['step'] = 'lesson'
    await show_lesson_selection(query, context)

#================== ОБРАБОТЧИК ВСЕХ КНОПОК (ОБНОВЛЕННЫЙ) ==================
async def button_handler(update: Update, context: CallbackContext):
    """Обработка нажатий на inline-кнопки."""
    query = update.callback_query
    if not isinstance(context.user_data, dict):
        context.user_data = {}
    
    # Сохраняем пользователя при нажатии кнопок
    user = query.from_user
    db.add_user(
        user_id=user.id,
        username=user.username,
        first_name=user.first_name,
        last_name=user.last_name,
        language_code=user.language_code
    )
    
    # Проверяем техрежим ДО обработки любого действия
    if await check_maintenance_mode(update, context):
        return
    
    try:
        await query.answer()
    except TimedOut:
        logger.warning("Timeout при ответе на callback query")
        return
    except Exception as e:
        logger.error(f"Ошибка при ответе на callback: {e}")
        return
    
    # Обработка добавления замены (4-шаговый процесс)
    if context.user_data.get('adding_substitution'):
        try:
            step = context.user_data.get('step')
            
            # Обработка отмены
            if query.data == 'cancel_adding':
                context.user_data.clear()
                keyboard = add_start_button()
                reply_markup = InlineKeyboardMarkup(keyboard)
                await query.edit_message_text(
                    "<b>❌ Добавление замены отменено.</b>",
                    reply_markup=reply_markup,
                    parse_mode='HTML'
                )
                return
            
            # Навигация "назад"
            if query.data == 'back_to_date':
                context.user_data['step'] = 'date'
                await show_date_selection(query, context)
                return
            elif query.data == 'back_to_class':
                context.user_data['step'] = 'class'
                await show_class_lesson_selection(query, context)
                return
            elif query.data == 'back_to_lesson':
                context.user_data['step'] = 'lesson'
                await show_lesson_selection(query, context)
                return
            elif query.data == 'back_to_teacher_selection':
                context.user_data['step'] = 'teacher_selection'
                await show_teacher_selection(query, context)
                return
            
            # ШАГ 1: Выбор даты
            if step == 'date' and query.data.startswith('date_'):
                date_str = query.data.split('_', 1)[1]
                context.user_data['date'] = date_str
                
                # Определяем день недели
                try:
                    date_obj = datetime.strptime(date_str, '%Y-%m-%d')
                    weekday = date_obj.weekday()
                    if weekday < 5:
                        day_name = DAYS_OF_WEEK[weekday]
                    else:
                        await query.answer("⚠️ Выходные дни недоступны для выбора", show_alert=True)
                        return
                    context.user_data['day'] = day_name
                except Exception as e:
                    logger.error(f"Ошибка при определении дня недели: {e}")
                    context.user_data['day'] = "Неизвестно"
                
                context.user_data['step'] = 'class'
                await show_class_lesson_selection(query, context)
                return
            
            # ШАГ 2: Выбор класса
            if step == 'class' and query.data.startswith('sub_class_'):
                class_name = query.data.split('_', 2)[2]
                context.user_data['class_name'] = class_name
                context.user_data['step'] = 'lesson'
                await show_lesson_selection(query, context)
                return
            
            # ШАГ 3: Выбор урока
            if step == 'lesson' and query.data.startswith('lesson_'):
                lesson_num = int(query.data.split('_')[1])
                context.user_data['lesson'] = lesson_num
                context.user_data['step'] = 'teacher_selection'
                await show_teacher_selection(query, context)
                return
            
            # ШАГ 4: Выбор учителя
            if step == 'teacher_selection':
                await handle_teacher_selection(query, context)
                return
            
            # Финальное подтверждение
            if query.data == 'confirm_substitution_final':
                await finalize_substitution(query, context)
                return
            elif query.data == 'edit_substitution':
                await edit_substitution(query, context)
                return
            
            # Если шаг не распознан
            await query.answer("⚠️ Неизвестное действие. Используйте кнопки навигации.", show_alert=True)
            
        except Exception as e:
            logger.error(f"Ошибка при добавлении замены: {e}")
            await query.edit_message_text(
                f"❌ Произошла ошибка: {str(e)[:100]}",
                parse_mode='HTML'
            )
        return
    
    # Обработка рассылки технических уведомлений
    if context.user_data.get('broadcasting'):
        if query.data == 'cancel_broadcast':
            await cancel_broadcast(query, context)
            return
        elif query.data == 'confirm_broadcast':
            await confirm_broadcast(query, context)
            return
        elif query.data == 'edit_broadcast_time':
            await edit_broadcast_time(query, context)
            return
    
    # Обработка управления техрежимом
    if context.user_data.get('setting_maintenance'):
        if query.data in ['maintenance_1_hour', 'maintenance_3_hours', 'maintenance_5_hours',
                          'maintenance_until_tomorrow', 'maintenance_indefinite', 'maintenance_custom']:
            await handle_maintenance_duration(query, context)
            return
        elif query.data == 'skip_maintenance_message':
            await skip_maintenance_message(query, context)
            return
    
    # Основные обработчики меню
    if query.data == 'back_to_main':
        await show_main_menu(query)
        return
    elif query.data == 'check_maintenance_status':
        await check_maintenance_status(query, context)
        return
    elif query.data == 'menu_my_class':
        await show_my_class_menu(query, context)
        return
    elif query.data.startswith('myclass_set_'):
        await set_my_class(query, context)
        return
    elif query.data == 'myclass_change':
        await show_class_selection_for_my_class(query, context)
        return
    elif query.data.startswith('myclass_schedule_'):
        await show_my_class_schedule(query, context)
        return
    elif query.data.startswith('myclass_weekly_'):
        await show_my_class_weekly(query, context)
        return
    elif query.data == 'my_class_teacher':
        await show_my_class_menu(query, context)
        return
    elif query.data == 'menu_now':
        await show_now_class_selection(query, context)
        return
    elif query.data.startswith('now_class_'):
        await show_current_lesson(query, context)
        return
    elif query.data == 'menu_teacher':
        await show_teacher_menu(query, context)
        return
    elif query.data.startswith('teacher_') and not query.data.startswith('teacher_search_'):
        await show_teacher_schedule(query, context)
        return
    elif query.data == 'menu_search_teacher':
        await query.edit_message_text(
            "<b>🔍 ПОИСК УЧИТЕЛЯ</b>\n"
            "Введите фамилию или часть фамилии учителя для поиска:\n"
            "<i>Пример: 'Коротчикова' или 'Гуд'</i>",
            parse_mode='HTML'
        )
        context.user_data['searching_teacher'] = True
        return
    elif query.data == 'menu_bells':
        await show_bells_schedule(query)
        return
    elif query.data == 'menu_schedule':
        await show_class_selection(query)
        return
    elif query.data.startswith('class_'):
        await show_day_selection_for_class(query, context)
        return
    elif query.data.startswith('schedule_'):
        await show_daily_schedule(query, context)
        return
    elif query.data.startswith('weekly_'):
        await show_weekly_schedule(query, context)
        return
    elif query.data == 'menu_substitutions':
        await show_substitutions_menu(query)
        return
    elif query.data in ['subs_yesterday', 'subs_today', 'subs_tomorrow']:
        await show_substitutions_for_date(query)
        return
    elif query.data == 'subs_all':
        await show_all_substitutions(query)
        return
    elif query.data == 'menu_help':
        await show_help(query)
        return
    elif query.data == 'admin_panel':
        await show_admin_panel(query)
        return
    elif query.data == 'admin_add_sub':
        await start_adding_substitution(query, context)
        return
    elif query.data == 'admin_view_subs':
        await show_admin_substitutions(query)
        return
    elif query.data == 'admin_delete_sub':
        await request_substitution_deletion(query)
        return
    elif query.data == 'admin_clear_subs':
        await confirm_clear_substitutions(query)
        return
    elif query.data == 'admin_clear_confirm':
        await clear_all_substitutions(query)
        return
    elif query.data == 'admin_broadcast':
        await start_technical_broadcast(query, context)
        return
    elif query.data == 'admin_users':
        await show_users_stats(query, context)
        return
    elif query.data == 'admin_analytics':
        await show_admin_analytics(query, context)
        return
    elif query.data == 'admin_enable_maintenance':
        await enable_maintenance_mode(query, context)
        return
    elif query.data == 'admin_disable_maintenance':
        await disable_maintenance_mode(query, context)
        return
    elif query.data.startswith('teacher_search_'):
        await show_searched_teacher_schedule(query, context)
        return
    
    # Обработка ошибок
    except TimedOut:
        logger.error("Timeout при обработке кнопки")
        await query.edit_message_text(
            "⏳ Превышено время ожидания. Попробуйте снова.",
            parse_mode='HTML'
        )
    except NetworkError as e:
        logger.error(f"Сетевая ошибка: {e}")
        await query.edit_message_text(
            "🌐 Проблемы с сетью. Проверьте соединение и попробуйте снова.",
            parse_mode='HTML'
        )
    except BadRequest as e:
        if "message is not modified" in str(e).lower():
            await query.answer("✅ Информация актуальна", show_alert=False)
        else:
            logger.error(f"Ошибка BadRequest: {e}")
            await query.edit_message_text(
                f"❌ Ошибка: {str(e)[:100]}",
                parse_mode='HTML'
            )
    except Exception as e:
        logger.error(f"Неизвестная ошибка в button_handler: {e}")
        await query.edit_message_text(
            f"❌ Произошла ошибка: {str(e)[:100]}",
            parse_mode='HTML'
        )

#================== ОБРАБОТКА СООБЩЕНИЙ (ОБНОВЛЕННАЯ) ==================
async def handle_message(update: Update, context: CallbackContext):
    """Обработка текстовых сообщений."""
    if not update.message or not update.message.text:
        return
    
    # Сохраняем пользователя
    user = update.effective_user
    db.add_user(
        user_id=user.id,
        username=user.username,
        first_name=user.first_name,
        last_name=user.last_name,
        language_code=user.language_code
    )
    
    # Проверяем техрежим
    if await check_maintenance_mode(update, context):
        return
    
    if not isinstance(context.user_data, dict):
        context.user_data = {}
    
    # Обработка ввода времени для рассылки
    if context.user_data.get('broadcasting') and context.user_data.get('broadcast_step') == 'time':
        await handle_broadcast_time(update, context)
        return
    
    # Обработка ввода времени для техрежима
    if context.user_data.get('setting_maintenance') and context.user_data.get('maintenance_step') == 'input_until':
        await handle_maintenance_input(update, context)
        return
    
    # Обработка ручного ввода учителя
    if (context.user_data.get('adding_substitution') and 
        context.user_data.get('step') == 'manual_teacher'):
        await handle_manual_teacher_input(update, context)
        return
    
    # Обработка поиска учителя
    if context.user_data.get('searching_teacher'):
        search_query = update.message.text.strip()
        if not search_query:
            await update.message.reply_text(
                "<b>❌ Пустой запрос.</b>\nПожалуйста, введите фамилию учителя для поиска.",
                parse_mode='HTML'
            )
            return
        
        all_teachers = get_all_teachers()
        found_teachers = [t for t in all_teachers if search_query.lower() in t.lower()]
        
        if found_teachers:
            text = f"<b>🔍 Результаты поиска по запросу '{search_query}':</b>\n"
            keyboard = []
            for i, teacher in enumerate(found_teachers[:10]):
                button_text = teacher[:25] + "..." if len(teacher) > 25 else teacher
                keyboard.append([InlineKeyboardButton(button_text, callback_data=f'teacher_search_{i}')])
            
            context.user_data['found_teachers'] = found_teachers
            keyboard.append([InlineKeyboardButton("🔍 Новый поиск", callback_data='menu_search_teacher')])
            keyboard.append([InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')])
            reply_markup = InlineKeyboardMarkup(keyboard)
            
            text += f"Найдено учителей: <b>{len(found_teachers)}</b>\nВыберите учителя из списка:"
            await update.message.reply_text(text, reply_markup=reply_markup, parse_mode='HTML')
        else:
            keyboard = [
                [InlineKeyboardButton("🔍 Попробовать снова", callback_data='menu_search_teacher')],
                [InlineKeyboardButton("👨‍🏫 Все учителя", callback_data='menu_teacher')],
                [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
            ]
            reply_markup = InlineKeyboardMarkup(keyboard)
            await update.message.reply_text(
                f"<b>❌ Учителя по запросу '{search_query}' не найдены.</b>\n"
                "Попробуйте указать только фамилию или проверить написание.",
                reply_markup=reply_markup,
                parse_mode='HTML'
            )
        context.user_data['searching_teacher'] = False
        return
    
    # Обработка упоминаний учителей
    await handle_teacher_mentions(update, context)

#================== КОМАНДЫ ==================
async def test_notification(update: Update, context: CallbackContext):
    """Тестовая команда для проверки уведомлений."""
    if update.effective_user.id not in ADMIN_IDS:
        await update.message.reply_text("❌ Только администраторы могут тестировать уведомления.", parse_mode='HTML')
        return
    
    if not context.args:
        teachers_list = "\n".join([f"• {teacher}" for teacher in TEACHER_IDS.keys()])
        await update.message.reply_text(
            f"Использование: /test [фамилия учителя]\n"
            f"<b>Доступные учителя:</b>\n{teachers_list}",
            parse_mode='HTML'
        )
        return
    
    teacher_name = context.args[0]
    found_teacher = None
    for teacher in TEACHER_IDS.keys():
        if teacher_name.lower() in teacher.lower():
            found_teacher = teacher
            break
    
    if not found_teacher:
        await update.message.reply_text(f"❌ Учитель '{teacher_name}' не найден в списке.", parse_mode='HTML')
        return
    
    teacher_id = TEACHER_IDS[found_teacher]
    if not teacher_id or teacher_id == 0:
        await update.message.reply_text(f"❌ Для учителя '{found_teacher}' не установлен Telegram ID.", parse_mode='HTML')
        return
    
    try:
        test_message = (
            f"<b>🔔 ТЕСТОВОЕ УВЕДОМЛЕНИЕ</b>\n"
            f"<b>👨‍💻 От:</b> Администратор бота\n"
            f"<b>🕐 Время:</b> {datetime.now().strftime('%H:%M %d.%m.%Y')}\n"
            f"✅ Система уведомлений работает корректно!\n"
            f"<i>Это тестовое сообщение для проверки работы бота.</i>"
        )
        await context.bot.send_message(chat_id=teacher_id, text=test_message, parse_mode='HTML')
        
        keyboard = add_start_button()
        reply_markup = InlineKeyboardMarkup(keyboard)
        await update.message.reply_text(
            f"✅ Тестовое уведомление отправлено учителю <b>{found_teacher}</b> (ID: {teacher_id})",
            reply_markup=reply_markup,
            parse_mode='HTML'
        )
    except Exception as e:
        error_msg = str(e)
        logger.error(f"Ошибка тестовой отправки: {error_msg}")
        keyboard = add_start_button()
        reply_markup = InlineKeyboardMarkup(keyboard)
        if "chat not found" in error_msg.lower():
            await update.message.reply_text(
                f"❌ Учитель <b>{found_teacher}</b> не начал диалог с ботом.\n"
                "Попросите его отправить команду /start боту.",
                reply_markup=reply_markup,
                parse_mode='HTML'
            )
        else:
            await update.message.reply_text(
                f"❌ Ошибка: {error_msg[:100]}",
                reply_markup=reply_markup,
                parse_mode='HTML'
            )

async def teachers_list(update: Update, context: CallbackContext):
    """Команда для отображения списка всех учителей."""
    all_teachers = get_all_teachers()
    if not all_teachers:
        await update.message.reply_text("<b>❌ Список учителей пуст.</b>", parse_mode='HTML')
        return
    
    teachers_text = "<b>👨‍🏫 СПИСОК УЧИТЕЛЕЙ:</b>\n"
    for i, teacher in enumerate(all_teachers, 1):
        teachers_text += f"{i}. {teacher}\n"
    
    keyboard = [
        [InlineKeyboardButton("🔍 Поиск учителя", callback_data='menu_search_teacher')],
        [InlineKeyboardButton("📋 Расписание учителей", callback_data='menu_teacher')],
        [InlineKeyboardButton("🏠 Старт / Главное меню", callback_data='back_to_main')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    await update.message.reply_text(teachers_text, reply_markup=reply_markup, parse_mode='HTML')

#================== ГЛАВНАЯ ФУНКЦИЯ ==================
def main():
    """Запуск бота."""
    try:
        db.init_db()
        db.update_database_structure()
    except Exception as e:
        logger.error(f"Ошибка инициализации БД: {e}")
        print(f"❌ Ошибка БД: {e}")
        exit(1)
    
    application = (
        Application.builder()
        .token(TOKEN)
        .read_timeout(REQUEST_TIMEOUT)
        .write_timeout(REQUEST_TIMEOUT)
        .connect_timeout(REQUEST_TIMEOUT)
        .pool_timeout(REQUEST_TIMEOUT)
        .build()
    )
    
    # Обработчики
    application.add_handler(CommandHandler("start", start))
    application.add_handler(CommandHandler("test", test_notification))
    application.add_handler(CommandHandler("teachers", teachers_list))
    application.add_handler(CallbackQueryHandler(button_handler))
    application.add_handler(
        MessageHandler(
            filters.TEXT & ~filters.COMMAND,
            handle_message
        )
    )
    
    print("🤖 Бот запущен...")
    print(f"📊 Всего учителей в расписании: {len(get_all_teachers())}")
    print(f"👑 Администраторы: {ADMIN_IDS}")
    print(f"⏱️ Таймауты установлены на: {REQUEST_TIMEOUT} сек")
    print(f"🌍 Часовой пояс: Europe/Minsk (UTC+3)")
    print(f"👥 Пользователей в базе: {db.get_user_count()}")
    
    try:
        application.run_polling(
            allowed_updates=Update.ALL_TYPES,
            drop_pending_updates=True
        )
    except KeyboardInterrupt:
        print("\n🛑 Бот остановлен пользователем")
    except Exception as e:
        print(f"❌ Критическая ошибка: {e}")
        logger.critical(f"Критическая ошибка: {e}")

if __name__ == '__main__':
    main()
